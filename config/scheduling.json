{
  "trigger": {
    "name": "second-brain-daily-briefing",
    "id": "trig_01KvxeDfYDAEwAzw9zw9DKKB",
    "schedule": "45 11 * * 1-5",
    "timezone_note": "Cron is UTC. 11:45 UTC = 6:45 AM CDT (America/Chicago). Adjust for DST: CDT=UTC-5, CST=UTC-6.",
    "model": "claude-sonnet-4-6",
    "environment_id": "env_01TjBJLSRwHfpUPcNVUK99Kb",
    "sources": ["https://github.com/UsernameTron/second-brain"],
    "mcp_connections": [
      {
        "connector_uuid": "f94e7416-4e60-44bc-9ed2-45fa49a68665",
        "name": "Google-Calendar",
        "url": "https://calendarmcp.googleapis.com/mcp/v1"
      }
    ],
    "persist_session": false,
    "enabled": false,
    "prompt": "You are running the second-brain daily briefing. Read CLAUDE.md first. Then run the /today command by executing: node -e \"require('./src/today-command').runToday({ mode: 'scheduled' }).then(r => console.log(r.briefing || r.error))\". Report the output."
  },
  "dst_ops": {
    "description": "RemoteTrigger uses UTC cron. Adjust schedule twice yearly for US Central time.",
    "cdt_schedule": "45 11 * * 1-5",
    "cdt_note": "CDT (UTC-5): 11:45 UTC = 6:45 AM CDT. Active ~Mar second Sun to Nov first Sun.",
    "cst_schedule": "45 12 * * 1-5",
    "cst_note": "CST (UTC-6): 12:45 UTC = 6:45 AM CST. Active ~Nov first Sun to Mar second Sun.",
    "action": "Update trigger.schedule value and re-create the RemoteTrigger with the new cron expression. Use: RemoteTrigger update trig_01KvxeDfYDAEwAzw9zw9DKKB with new cron_expression."
  },
  "fallback_chain": [
    "macOS launchd (PRIMARY — com.secondbrain.today, weekday 06:45 local, only path that reaches the local vault)",
    "CronCreate (secondary — .claude/scheduled_tasks.json, 7-day auto-expiry)",
    "RemoteTrigger (disabled — cloud env cannot reach local vault; see notes.remote_trigger_disabled)"
  ],
  "notes": {
    "test_trigger_deleted": "Test trigger trig_01NHx3Uz8ni8eZSfsMph4miY deleted 2026-04-22.",
    "remote_trigger_disabled": "RemoteTrigger runs in cloud environment env_01TjBJLSRwHfpUPcNVUK99Kb which cannot reach local VAULT_ROOT (~/Claude Cowork); its /today writes go into the void. Disabled by design (trigger.enabled=false), not a config bug. v1.7 Phase 29 STATS-PIPE-03.",
    "local_scheduler": "Primary scheduler is macOS launchd job com.secondbrain.today (~/Library/LaunchAgents/com.secondbrain.today.plist), weekday 06:45 America/Chicago local, running /today scheduled mode with WorkingDirectory=/Users/cpconnor/projects/second-brain. Added v1.7 Phase 29.",
    "dream_scheduler": "Monthly dream-consolidation propose pass: launchd job com.secondbrain.dream, StartCalendarInterval Day 1 / Hour 7 / Minute 15 America/Chicago local, running `node scripts/dream.js --propose` ONLY — /dream-apply is human-invoked and never scheduled. Plist source committed at config/com.secondbrain.dream.plist (not loaded by automation). Install manually: `cp config/com.secondbrain.dream.plist ~/Library/LaunchAgents/com.secondbrain.dream.plist && launchctl load ~/Library/LaunchAgents/com.secondbrain.dream.plist`. Added v1.8 Phase 34 Plan 06."
  }
}
