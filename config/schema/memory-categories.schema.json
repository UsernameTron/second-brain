{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "memory-categories",
  "title": "Memory Categories Configuration",
  "description": "Validates config/memory-categories.json — category taxonomy for memory extraction per D-23",
  "type": "object",
  "minProperties": 1,
  "additionalProperties": {
    "type": "object",
    "required": ["description", "example", "exclusions"],
    "properties": {
      "description": {
        "type": "string",
        "minLength": 1,
        "description": "One-line inclusion shape — what belongs in this category"
      },
      "example": {
        "type": "string",
        "minLength": 1,
        "description": "Concrete example of a memory entry in this category"
      },
      "exclusions": {
        "type": "string",
        "minLength": 1,
        "description": "What does NOT belong in this category"
      }
    },
    "additionalProperties": false
  }
}
