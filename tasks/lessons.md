# Lessons

## Active Rules

### Seed Rules
- [2026-04-21] [Config]: Never modify shared config files without checking downstream consumers.
- [2026-04-21] [Scope]: If a "quick fix" requires 3+ files, it is not quick. Re-plan.
- [2026-04-21] [Testing]: Run the full test suite, not just tests for the changed module.
- [2026-04-21] [Dependencies]: Never add dependencies without explicit user approval.
- [2026-04-21] [Data]: Never delete production data, migrations, or seed data without approval.
- [2026-04-21] [Vault]: Never surface ISPN, Genesys, or Asana content in memory promotion — hard exclusion.

### Learned Rules
- [2026-04-23] [Testing]: Never spawn multiple test/coverage processes in parallel. One invocation at a time, always with `--forceExit`. If a test command is backgrounded by the user, wait for completion notification — do not retry. Triggered by: 5 duplicate jest processes hung simultaneously, had to be killed manually, wasted 30+ minutes.
- [2026-04-23] [Review]: 3-reviewer cross-audit catches findings single-reviewer misses. Gemini caught dead-letter queue gap in memory promotion; Opus caught config-crash paths in loadPipelineConfig. Always request independent review from a second model when shipping milestone-boundary work.
- [2026-04-23] [Hygiene]: Config overlay pattern (pipeline.local.json) solves dev-workflow reversion. Ship hygiene changes ahead of feature milestones when they unblock local development — don't gate infrastructure fixes behind feature scope.
- [2026-04-23] [Hooks]: Stop hook excluded tasks/todo.md from clean-tree check, which allowed session-48 handoff state to survive uncommitted into the next session. Pattern works for rolling ledgers but surfaces ambiguity at milestone close. Future: have the stop hook prompt for commit at session end when milestone-boundary files are dirty.
- [2026-04-23] [Testing]: Do not run the full test suite as a gate on markdown-only commits (planning files, lessons, todo). Tests gate source code changes. Running Jest on a 4-file planning commit is ceremony, not discipline — and when tests are slow, it blocks the user repeatedly for zero value. Check the diff scope first; skip tests when no source/config files changed.
- [2026-04-24] [Verification]: Plan-text verification (gsd-verifier on PLAN.md) validates what the plan *promises*, not what the implementation actually ships. When a plan's must_haves include live-system behavior ("direct push rejected", "CI enforced"), the Phase-level verifier must run commands against the deployed system — not just grep plan files. Triggered by: Task 1 in Plan 17-03 shipped a `gh api` payload that omitted `required_pull_request_reviews`, but plan verification passed because the plan text was correct. Only Pete's post-merge UI audit caught the gap.
- [2026-04-24] [GitHub]: Classic branch protection's `required_status_checks` only gates PR merges — it does NOT block direct pushes. To force the PR path, `required_pull_request_reviews` must be non-null (set `required_approving_review_count: 0` for solo devs). Without the PR requirement, direct push with red CI still lands. Triggered by: BRANCH-PROT-01 shipped without the PR-required bit; direct pushes were still allowed until the fix.
- [2026-04-24] [Git workflow]: GitHub web UI edits bypass all local discipline and can introduce issues that fly past CI and review. Two direct-to-master commits via web UI during this session violated P11 (secret at workflow-level in ci.yml) and disabled CI triggers entirely. Prevention: keep `required_pull_request_reviews` on master (force PR path for UI edits too), review every commit to master in the PR it arrives via. Triggered by: b33ca14 and e4c080c on 2026-04-24, both reverted via PR #32.
- [2026-04-24] [Cache invalidation]: Before recommending a cache-bust / full-re-embed trigger, ask whether the changed value actually affects the cached artifact or only the downstream consumption. Embeddings are frozen vectors — threshold and scoring-decay factors are applied at query time to cosine results, so changing them does NOT require new embeddings. Defensive "hash everything into the version" logic creates unnecessary compute. Triggered by: Phase 19 discuss-phase — recommended composite schema_version (model + dim + decay + threshold), user corrected that threshold/decay are query-time math and re-embed on those is waste. Final lock: `schema_version = hash(model || dimension)` only.
- [2026-04-24] [Discuss-phase]: Don't lock decisions based on offhand operator comments before checking REQUIREMENTS.md / ROADMAP.md / prior phase context. Treat passing remarks as hypotheses to discuss, not as documented overrides. Why: operator-stated preferences from a different chat context are sometimes "casual reads" rather than reviewed architectural calls — locking them silently bypasses the documented spec. How to apply: when an operator preference contradicts a written REQ, surface the conflict before treating either as locked. Triggered by: Phase 20 file-path discussion — locked `RIGHT/metrics/daily-stats.md` from a casual operator read without checking that REQUIREMENTS.md said `RIGHT/daily-stats.md`. Operator caught it and asked to unlock for proper discussion.
- [2026-04-25] [Verification] LESSON-RECIPE-VERIFY-01: Recipes that prescribe additive changes ("add X to file Y") must include a Step 0 that reads the target file and confirms X isn't already present. Why: twice in one session (v1.4 Phase 21 pre-flight), proposed fixes were no-ops because the change already existed — Scenario-6 hooks already at lines 372–381 of semantic-search.test.js, and describeFn skip guard already at lines 149–151 of uat-classification.test.js. How to apply: any Edit/Write step in a recipe must be preceded by a Read step that proves the proposed change isn't redundant.
- [2026-04-25] [Verification] LESSON-TRIP-WIRE-RELATIVE-01: Recipe verification steps that compare test counts must specify pre-fix baseline counts explicitly OR use relative conditions ("any test that was passing before is now failing") instead of absolute ones ("0 failed total"). Why: pre-flight CHECK 2 fired on UAT-01 (a pre-existing local-only failure) during the dotenv-fix verification; the fix itself was sound but the over-strict trip-wire forced a STOP-and-stash detour. How to apply: trip-wires baseline against the most recent known state, not against a Platonic 0-failure ideal.
- [2026-04-25] [Git workflow] LESSON-SQUASH-CONTENT-CHECK-01: After a PR is squash-merged, `git log origin/master..branch` shows the original branch commits as "unique to branch" because squash creates a new SHA with no ancestry link. Use `git cherry-pick --no-commit <orig-sha>` then `git status --porcelain` to verify the diff is empty — this proves content-equivalence regardless of SHA. Why: phantom-branch recovery during this session hit a false-positive STOP because SHA comparison alone didn't see the squash content. How to apply: when verifying whether a branch's content is in master before deletion, use cherry-pick dry-run; bake this verification into recovery recipes.
- [2026-04-25] [Testing] LESSON-PREFLIGHT-CI-MODE-01: Second Brain pre-flight test checks must use `CI=true npm test`, not bare `npm test`. UAT tests use `describeFn = (!skipInCI && HAS_API_KEY) ? describe : describe.skip`; without `CI=true`, local runs hit live API calls and results depend on `.env` key validity. Why: pre-flight CHECK 2 conflated developer-environment behavior with production correctness by requiring local `npm test` to return 0 failures, forcing multiple unnecessary detours. How to apply: green/red signal for this project is `CI=true npm test`; local-mode UAT runs are developer-optional debugging, not pre-flight gates.
- [2026-04-25] [Testing] LESSON-UAT-CORPUS-DRIFT-01: Any classifier-side change (prompt, threshold, excluded terms, content-policy) must include a UAT-corpus-revalidation step. UAT corpora encode classifier-behavior contracts at the time they're written; CI guards mask drift because UAT tests skip in CI. Why: UAT-01's accuracy-aggregate test failed locally because the live classifier returned BLOCKED on cases the corpus expected to receive LEFT/RIGHT — most likely cause: excluded-terms expansion in v1.1 (15 terms, substring matching) shifted classifier decision boundaries without a corresponding corpus rebaseline. How to apply: classifier-side PRs going forward must run `unset CI; npx jest test/uat/uat-classification.test.js` and refresh fixtures if the score drops; v1.5 phase UAT-CORPUS-REFRESH-01 (see tasks/todo.md) will rebaseline the existing corpus.
- [2026-04-25] [Process] LESSON-MANIFEST-FIRST-VALIDATED-01: For any scoped governance work (categorizing N sites, applying labels to N items), produce the categorization manifest first, validate it, then apply. Drift between planning estimates and live code is the default state, not the exception. Why: D-LOCK-2-AMEND-A's manifest-first protocol on Plan 21-03 caught 5 categorization corrections from CONTEXT estimates (~16% of the 32-site corpus). Most consequential: 3 sites in memory-proposals.js (lines 319/329/336) were about to be tagged as last-resort-error but turned out to be always-emit JSON audit logs — diagnostic category, same shape as vault-gateway:159. Without the manifest gate, those 5 sites would have been wrongly tagged. How to apply: any plan whose scope is "apply label X to N items" gets a Task 0 that builds the full N-row manifest and pauses for review before any application step. Triggered by: Plan 21-03 (HYG-CONSOLE-01) execution — manifest-first protocol prevented 5 categorization errors from shipping.
- [2026-04-25] [Hygiene] LESSON-OPTION-A-SCOPE-CORRECTION-01: When an audit gate fails on pre-existing artifacts in the same Lock-class as primary work, the cleanest fix is scope correction (Option A — bring artifacts into compliance) rather than scope limitation (Option B — exempt them). Pattern 9 spirit is uniformity; partial coverage creates a worse audit surface than uniform coverage. Why: Plan 21-03's A3 audit grep flagged 3 pre-existing bare `eslint-disable-next-line no-console` directives outside the primary 9-file scope (in promote-memories.js and semantic-index.js). Bringing them into scope as a "corollary" cost ~30 seconds and brought src/ to fully uniform Pattern 9 governance state; the alternative (scope-limit A3) would have left 3 grandfathered disables that fail any future global audit. How to apply: if a discovered out-of-scope artifact is (a) same work-class as in-scope items and (b) Lock-fence permits, bring it into scope as a corollary with explicit framing in SUMMARY (e.g., "32 primary + 3 corollary"). Reserve Option B (scope-limit) or Option C (defer) for when work-class genuinely differs. Triggered by: Plan 21-03 Part 2 — Connor selected Option A; closeout PR shipped 35 uniformly category-tagged disables instead of 32 + 3 grandfathered.
- [2026-04-25] [Hygiene] LESSON-BONUS-REQ-FLIP-01: D-LOCK-4's "verifiable v1.4-fact error" rule legitimately permits adjacent-scope corrections beyond the named target rows. When a fact-error rule is the gate, apply it broadly within the file; document scope expansions explicitly in SUMMARY so future audits can trace why bonus flips landed in a non-bonus PR. Why: Plan 21-04 was scoped to flip 4 Phase 21 REQ rows; execution caught and flipped 4 Phase 20 rows (STATS-LATENCY-01, STATS-GROWTH-01, +2) that were stale Pending despite Phase 20 shipping via PR #35. Both groups satisfy "verifiable v1.4-fact error" — the rule is broader than the initial framing suggested, and leaving Phase 20 stale would have rolled forward as drift into v1.5. How to apply: when scoped to a "fact-error correction" within a single file, scan the whole file for the same class of error and fix all matches; document the scope expansion in SUMMARY §3 (or equivalent) for reviewer visibility. Triggered by: Plan 21-04 Task 4 — bonus 4 Phase 20 REQ flips landed in the v1.4 close PR alongside the 4 Phase 21 in-scope flips.
- [2026-04-25] [Process] LESSON-LIVE-RECOUNT-AT-EXECUTE-01: Any plan that updates numerical doc figures (test counts, coverage, warning counts, dependency counts) should make "re-count from live state" its first task. Downstream tasks reference the captured value, never stale estimates from CONTEXT or earlier session output. Why: numerical figures in plan briefs go stale through 3 shifts: CONTEXT-time → plan-write-time → execution-time → ship-time. Plan 21-04's Task 1 design (re-count at execution start, reference downstream) prevented all 3 shifts from creating phantom drift. Test count shifted across the session from 1082 (CONTEXT estimate) → 1066 (Plan 21-02 local check) → 1127 (Plan 21-04 execution); only the last is correct for v1.4 ship. How to apply: Task 1 of any doc-refresh plan runs the live commands (`CI=true npm test`, `find test/ -name "*.test.js" | wc -l`, `npm audit`, coverage extract) and captures values for downstream tasks; subsequent tasks must NOT re-derive numbers, NOT pull from CONTEXT, and NOT use figures from prior session output. Triggered by: Plan 21-04 — Task 1 captured 1127/1044/55/coverage-block; all 5 narrative-doc edits referenced that capture and not the stale 982/944 figures sitting in CONTEXT and the plan brief.
- [2026-04-25] [Hygiene] LESSON-PRE-EXISTING-DEBT-ABSORPTION-01: Closeout phases are the natural place to absorb pre-existing technical debt that surfaces during scoped hygiene work. Treating closeout phases as "strict-scope only" misses opportunities to ship hygiene completions that cost under 30 seconds and prevent the debt from rolling forward into the next milestone. Why: Plan 21-03 absorbed 3 bare disables (corollary); Plan 21-04 absorbed 4 stale REQ rows (bonus). Both expansions stayed within Lock-fence and matched the spirit of their respective rules. Both items would otherwise have rolled into v1.5 as inherited debt. How to apply: during a closeout phase, when an audit reveals adjacent debt that is (a) same work-class as primary scope and (b) Lock-fence permits, default to absorption with explicit framing rather than deferral. Reserve deferral for work that genuinely belongs to a future milestone (e.g., HYG-UNICODE-02 deferred to v1.5 because the work-class — full Unicode-variant matching — is genuinely a v1.5 feature, not a v1.4 hygiene fix). Triggered by: Phase 21 v1.4 close — corollary disables (Plan 21-03 PR #44) and bonus REQ flips (Plan 21-04 PR #45) absorbed during scoped hygiene work.
- [2026-07-12] [Audit-scope] LESSON-AUDIT-QUICKWIN-TEST-IMPACT-01: A read-only audit's "Effort-1 Quick Win" rating for a source change is a hypothesis, not a fact — it never ran the suite, so it cannot see test-contract impact. Any finding that changes a function's behavior or its data source must be assumed to touch the tests that encode the old contract; run the full suite immediately after applying and budget for mock/spy updates. Why: F-01 was rated Effort-1 but required updating 4 test files (3 unit mocks lacked `loadExcludedTerms` → TypeError cascade that also failed unrelated selfHeal tests; 1 integration scenario injected terms via the now-dead pipelineConfig path). How to apply: treat behavior/contract-changing findings as ≥Effort-2 regardless of diff size, and gate them on a green full suite before committing.
- [2026-07-12] [Audit-verification] LESSON-ORPHAN-DELETE-DYNAMIC-CONSUMER-01: Before deleting a file flagged as an "orphan" by literal-name grep, check for DYNAMIC consumers (directory scans, registry lookups, filename-convention matching), not just string references. Why: F-02 proposed deleting config/schema/daily-stats-frontmatter.schema.json on a zero-grep-hits basis, but the pre-commit schema-validate hook discovers schemas by scanning config/schema/*.schema.json and matches daily-stats.md frontmatter by filename convention — 3 tests depend on it. Deleting it broke those tests and removed real validation. How to apply: for any "delete unused X" finding, grep the containing dir/glob patterns AND run the suite before trusting the orphan verdict; a read-only grep cannot see convention-based dynamic linkage.
- [2026-07-12] [Verification] LESSON-OPERATION-SUCCEEDED-VS-DID-ITS-JOB-01: "Did the operation report success?" and "did the operation do what it was for?" are different questions — assert the second. Why: the 70-entry promotion loop reported promoted=70 with every guard green, but wrote ZERO embeddings; the driver script was an entry point and never called `require('dotenv').config()`, so VOYAGE_API_KEY was absent and embed-on-promotion degraded non-fatally by design. The corpus would have been invisible to semantic search. How to apply: any script calling into `src/` is an entry point and must load dotenv itself (dotenv lives in `src/pipeline-infra.js`, not in the modules that consume the keys); and verify the artifact the operation exists to produce (sidecar vector count), not just its exit status. Recover embedding gaps with `selfHealIfNeeded()` from `src/semantic-index.js`.
- [2026-07-12] [Planning] LESSON-REVERIFY-PLANNING-ARTIFACT-01: A planning artifact describes the world at authoring time, not at consumption time — re-verify it against live code before consuming it. Why: the Fable 5 audit's §5 shipped a "ready-to-paste v1.6 milestone block," but by the time it was pasted its ENTIRE Phase 1 (B-01, B-03, F-01) plus REQ-CTX-02 and REQ-SURF-03 had already shipped in the very remediation being merged, and F-02 had been refuted. Pasting verbatim would have opened a milestone of already-done work. How to apply: before consuming any pre-written block (milestone scope, backlog, recipe), grep each item against the current code and drop what already shipped; the more authoritative the artifact looks, the more it invites this mistake.
- [2026-07-12] [Git workflow] LESSON-ASSERT-MERGE-ANCESTRY-01: After any merge, assert ancestry in BOTH directions (`git merge-base --is-ancestor origin/master HEAD` and `... <branch> HEAD`) — a merge reporting "Merge made by the 'ort' strategy" is not proof it included what you think. Why: local master had 1 unpushed commit while origin had gained 2 (a true divergence, not "behind"), `--ff-only` correctly refused, but the script continued and merged onto the STALE base, silently excluding both origin commits. The rebuild on the correct base then surfaced a real STATE.md conflict the bad merge had hidden. How to apply: `set -e` does not protect a compound shell script from a failed guard mid-pipeline; verify the post-condition explicitly rather than trusting the command's own success report.
- [2026-07-12] [Data safety] LESSON-ARCHIVE-BEFORE-IRREVERSIBLE-DELETE-01: When asked to delete a target, inspect it first — if what you find contradicts how it was described, stop and surface it before acting. Why: `~/projects/memory-governance` was slated for `rm -rf` after copying 2 named reference files, but it was NOT a git repo (no history, no remote — deletion irreversible) and held 15 more files: a ROADMAP with an M01-M03 milestone sequence and 5 full phase plans, i.e. the reasoning behind the 2 conclusions being preserved. How to apply: for any irreversible delete, check (a) is it recoverable from git/remote, (b) does its actual content match the description; archive with byte-level hash verification, then delete. The description was written from memory and memory is lossy.

- [2026-07-13] [Audit-verification] LESSON-REFUTATION-CASCADES-01: When a finding is refuted, grep for every artifact that *cited* it and re-verify each — a refutation propagates as far as the original finding did. Why: refuting F-02 (the "orphan" daily-stats schema) looked like a one-file correction to the audit doc, but the D-01 edit shipped in the same branch had already consumed the deletion premise: it rewrote config-validator SKILL.md as "8 schemas, each with a backing config," dropping the very schema F-02 wanted deleted. Correcting only the audit would have left the skill doc asserting 8 schemas against a directory of 9 — and silently hiding the one that emits a WARNING on every run, which is exactly the confusion D-01 existed to fix. How to apply: after refuting finding X, `grep -rn "X"` across docs, skills, plans, and requirements; every hit is a candidate for the same correction. Cross-refs and "see also" lines are where the stale premise hides.
- [2026-07-13] [CI] LESSON-RED-CI-NOT-MINE-01: Before fixing a red CI check, confirm the failure is actually caused by the branch. Why: PR #59's `test (20)`/`test (22)` were red, but the jest suite passed — the job died on the `npm audit --audit-level=high` step (ci.yml:43) over two newly-published HIGH advisories against transitive `fast-uri`, which fail identically on master. Reading it as a code regression would have sent me hunting a bug that did not exist. How to apply: pull the failing job's log and find the actual failing *step* (`##[error]Process completed with exit code 1` and what preceded it) before touching source; then check whether the same failure reproduces on the base branch. A time-triggered failure (new CVE, expired token, upstream deprecation) is not a diff-triggered one.
- [2026-07-13] [Git workflow] LESSON-GH-ACTIVE-ACCOUNT-01: A 403 on push is an identity problem, not a permissions problem to escalate. Two GitHub accounts are logged into `gh` on this machine (`peteconnorCTG` and `UsernameTron`); pushes to `UsernameTron/second-brain` fail with "Permission ... denied to peteconnorCTG" whenever the wrong one is active. Fix: `gh auth switch --user UsernameTron`. How to apply: on any push 403, run `gh auth status` first and check which account is Active before touching remotes, tokens, or repo settings.

## Session Exemptions
- [2026-04-22] Session 11: `/prime` boot-only session. No user corrections occurred — stop hook likely triggered on BLOCK gate language from prior session context. No lesson to capture.

## Archived
<!-- Rules that no longer apply -->

<!-- session harvest 2026-07-14 -->
- [2026-07-14] [Verification] LESSON-READ-THE-DEPLOYMENT-NOT-THE-REPO-01: Source code describes intent; the running system is the truth, and they diverge. Why: describing repo source repeatedly produced wrong claims (real quarter/rock/issue counts, whether a decision reader existed, whether RAG existed) — every one resolved only when the LIVE system was inspected directly (browser audit of the deployed app + a filesystem REALITY.md). How to apply: before planning against any system, audit what is actually deployed — the live UI, the deployed function list, real data counts — not the checked-in code or the docs.
- [2026-07-14] [Verification] LESSON-OPERATION-SUCCEEDED-VS-DID-ITS-JOB-02 (reinforces -01): A deploy that reports success can change nothing. Why: `firebase deploy --only firestore:rules` printed success while silently skipping the upload because the file on disk was unchanged; separately, three functions deployed successfully yet were headless (no reader, no caller). How to apply: verify the artifact the operation exists to produce (rule uploaded, function reachable, vector written), not the exit status.
- [2026-07-14] [Governance] LESSON-DOCS-DRIFT-VALIDATE-BEFORE-REPEAT-01: Living docs drift and a prior session had presented incomplete work as complete. Why: docs claimed Q2/7 rocks/222 issues/6 functions/on main; live was Q3/9 rocks/26 open/9 functions/on a chore branch. How to apply: no doc says "done" until validated against the live system; treat any doc claim as provisional until confirmed against deployment.
- [2026-07-14] [Architecture] LESSON-LEVERAGE-NOT-JUDGMENT-SCAFFOLDING-01: Prefer deterministic gates over model-judgment scaffolding; don't reach for multi-agent complexity when a function suffices. Why: decision-capture safety was made robust structurally — Firestore rules (server-write-only), a static authority map (not an LLM classifier), schema-required citations (not a prompt asking for them). How to apply: ask "is this leverage (deterministic gate / verification loop) or judgment (router / classifier the model runs)?" Build the first; a model should never decide whether a model behaved.

<!-- weekly digest harvest 2026-07-17 -->
- [2026-07-17] [Testing] LESSON-WORKTREE-JEST-NULL-RUN-01: A jest run from inside `.claude/worktrees/` silently matches zero tests — `testPathIgnorePatterns` excludes that tree, so the run exits green against nothing. Why: during Phase 31-02 execution (2026-07-16) an executor stalled three times because its in-worktree test runs matched nothing; the real failure (per-suite doMock registry leakage into call-time lazy requires) was only diagnosable after running from the canonical checkout. How to apply: before trusting any test run from a non-canonical checkout, confirm jest actually collected suites/tests; a run that matched nothing is a configuration artifact, not a pass.


<!-- session harvest 2026-07-18 -->
- [2026-07-18] [Verification] LESSON-FIX-THE-CONDITION-NOT-THE-NAMED-FILES-01: When a finding names specific files, fix the condition it describes and re-sweep for every instance — the named list is a sample, not the set. Why: hit twice in one day on the CTG portfolio. (1) PR #63 corrected an exec-facing safety claim ("destructive verbs refused", false since ADR-0041 Rev A) in the two files the audit cited; four other files kept the identical false claim, including the README a reader hits first. (2) A secret-scan hardening propagated to the five repos whose CI branches were unmerged and silently skipped the two whose gates had already landed, leaving them blind to `pat-na1-`/`AIza` for a day. Both were caught only by grepping every merged instance, never by re-reading the worklist. How to apply: before declaring any propagation or multi-file correction done, run a state sweep over all candidate instances; a propagation list always looks complete from inside itself.
- [2026-07-18] [Verification] LESSON-OPERATION-SUCCEEDED-VS-DID-ITS-JOB-03 (reinforces -01, -02): "Merged" is not "deployed," and an audit must score the deployed state. Why: two CTG projects had security fixes merged to main while production served the pre-fix code — ctg-signal-radar's fail-closed auth needed a Cloud Run redeploy (verified live rev 00042-p9v -> 00043-dnv only by `gcloud run services describe`), and ctg-content-engine's Chat token gate is merged but inert until an operator runs a config-before-code rollout. The distinction is invisible in git and decisive for risk. How to apply: before crediting a fix as live, read the running artifact (revision name, deployment id), not the merge commit.
- [2026-07-18] [Process] LESSON-KILLED-FLEET-LEAVES-AN-UPLOADED-BRANCH-WITH-NO-PR-01: An interrupted agent fan-out has a specific signature — committed work on the remote with nothing pointing at it. Why: two background workflow runs died at session boundaries on 2026-07-18; both times the process ended between the upload step and `gh pr create`, stranding real commits on `ctg-gtm-automation` and `claude-agent-harness` that were invisible until `git ls-remote` per repo was diffed against open PRs. How to apply: after any interrupted fan-out, enumerate remote branches against open PRs before concluding work was lost or never started; and prefer committing as you go, which survives session turnover, over long-running workflows, which do not.
- [2026-07-18] [Governance] LESSON-AGENT-MUST-NOT-CONVERT-PROPOSALS-INTO-DISPOSITIONS-01: A subagent may not resolve items sitting under an explicit human-approval heading, even when the reasoning is sound. Why: a doc-sync agent marked audit items F7 DROPPED and F8 DEFERRED in `ctg-hs-exec-tool/tasks/todo.md` under a section literally titled "Pending Pete's approval"; the audit rows were proposals, and converting them to dispositions silently consumed the operator's decision. Both were restored to open with the reasoning preserved as a recommendation. How to apply: when an agent's worklist row lands in a human-gated section, it may add a recommendation and must leave the checkbox state alone.
- [2026-07-18] [Tooling] LESSON-ZSH-SET-DASHDASH-DOES-NOT-WORD-SPLIT-01: `set -- $x` splits in bash but not in zsh, so a loop over "name value" pairs assigns the whole string to `$1` and leaves `$2` empty. Why: a 20-repo `gh pr merge` loop turned into 20 `cd` failures because zsh does not word-split unquoted parameter expansion; nothing was lost only because the malformed `cd` short-circuited before `gh` ran. How to apply: in zsh use `${=x}` to force splitting, or drop the cleverness and write explicit lines — for a small fixed list, explicit lines are the safer trade.
- [2026-07-18] [Tooling] LESSON-PER-REPO-CREDENTIAL-PIN-BEATS-ACCOUNT-SWITCHING-01: In an estate spanning two GitHub identities, pin credentials per repo rather than switching the active account. Why: four CTG repos live under a second account; embedding the username in the remote URL does not help because the gh credential helper serves the active account's token regardless of URL (verified: a repo with `UsernameTron@` in its URL still 404'd). A repo-local helper calling `gh auth token --user <acct>`, preceded by an empty-string entry to clear the inherited chain, fixed all four permanently. How to apply: set the repo-local helper (the empty-string reset is required — `--local` appends, and the inherited helper answers first); note this fixes `git` only, `gh` API calls still need `GH_TOKEN=$(gh auth token --user <acct>)` per command; and read any remote 404 in a two-account estate as a wrong-active-account symptom, not a deleted repo.


<!-- session harvest 2026-07-19 -->
- [2026-07-19] [Tooling] LESSON-CURL-X-POST-FORCES-METHOD-THROUGH-REDIRECTS-01: Probing an Apps Script web app with `curl -L -X POST` returns 405 + Google's "Page Not Found" page even when the app is healthy — explicit `-X POST` forces POST onto the 302 redirect hop to `script.googleusercontent.com/macros/echo`, which only serves GET. Why: the 2026-07-19 content-engine refuse-half probe first read as endpoint-down; a no-follow probe showed POST→302 with a valid `user_content_key`, and re-running with `-d` (implicit POST; curl switches to GET on 302) returned the app's real refusal card at HTTP 200. Server-side, doPost had executed on every attempt — the failure was only in retrieving the result. How to apply: probe Apps Script `/exec` with `curl -sS -L -d '<body>'`, never `-X POST`; treat a 405/"Page Not Found" from script.google.com as a client redirect-method artifact until the executions dashboard confirms doPost didn't run.
- [2026-07-19] [Verification] LESSON-CHAT-APP-FAILURES-SPLIT-AT-THE-EXECUTIONS-DASHBOARD-01: For a Google Chat HTTP-endpoint app, the Apps Script executions dashboard separates "token mismatch" from "delivery dead" in one look — and dashboard currency is provable from a later time-driven row. Why: a live `/status` in the CTG Content Engine space (2026-07-19 ~02:42) produced no reply; executions showed ZERO doPost rows from Chat while the 02:44:59 poller row proved the view was current past the send — so Chat never delivered (broken config URL). A stale token would instead deliver → doPost → AUTH_FAIL refusal card (and this session's own curls appeared as doPost rows, confirming row-level visibility). How to apply: on Chat-app silence, read executions before touching tokens — doPost + AUTH_FAIL log = token side (rotate/re-match); zero web-app rows with a later row proving currency = Chat config/delivery side (re-save the endpoint URL); different owners, different fixes.
  **CORRECTION 2026-07-19 (later same day): this lesson's worked example was a misdiagnosis and its prescribed fix was never needed.** The zero-doPost result came from sending `/status` in a Chat *space* — documented content-engine tech debt F1: slash commands in a space never reach the app; DM or @mention required. Field-by-field inspection of the live Chat config found every value correct (right deployment URL, matching token, LIVE, commands registered); nothing was re-saved, and a DM `/status` replied immediately. The diagnostic *split* above (doPost+AUTH_FAIL = token side; zero rows = delivery side) remains valid, but "delivery side" has a third branch it missed: **the message was sent on a surface that never delivers**. On zero doPost rows, check the sending surface against the app's supported surfaces BEFORE touching the config — an unsupported-surface test manufactures a false outage, and an unnecessary config re-save is itself a risk when the save step is known-flaky.
- [2026-07-19] [Verification] LESSON-DELEGATED-VERIFICATION-DIES-SILENTLY-01 (reinforces LESSON-OPERATION-SUCCEEDED-VS-DID-ITS-JOB): A check delegated to another session is unverified until its evidence artifact is findable somewhere; when the artifact is absent, re-run the check instead of propagating the claim. Why: the content-engine rotation-#2 refuse/succeed verify pair was delegated to session `e805525d…`, which left no report anywhere (CCD transcript search, session list, repo commits, second-brain recall — all empty); re-running it found the succeed half actually FAILS (Chat→endpoint delivery dead). Same session, same shape: WAITING-ON-ME item 12's "remaining: delete the origin branch" had never run — `git ls-remote` still showed `feat/mcp-research-agent` live despite a boot summary over-reading the strike-through as fully closed. How to apply: at reconciliation, demand the artifact (report, log row, remote state) for every delegated or struck-through item; absence of artifact = still open; for cheap checks, re-run rather than chase.

## [2026-07-19] [Cross-session claims revert to unverified]

A claim that crosses a session boundary reverts to unverified — the receiving
session re-grounds it against disk before acting on it.

Two-layer failure on 2026-07-19: a triage session matched archived memories
against live ones by *wording similarity* and never followed the
`superseded_by::` pointers sitting in the archive entries themselves,
producing a false "3 stranded memories, restore them" finding. The
orchestrating session then relayed that finding downstream as established
fact, and an approved 3-item remediation was scoped on top of it. The
receiving session's pointer-following disk check (three `grep content_hash`
runs, ~seconds) killed the premise before any wrong restore was executed.
Rule: provenance is not verification — however trusted the sender, a factual
claim about disk state is re-checked against disk by whoever acts on it.

Triggered by: second-brain archive remediation 2026-07-19 — Item 1 of the
approved plan reversed from "restore three entries" to "correct the false
record" after the hash-pair check.

## [2026-07-26] [Identifier-grep inventories are ceilings, not counts]

A code-identifier grep for a capability names the ceiling of where it might
live; resolve every hit to a verdict (real implementation / domain-specific
keeper / false positive) before planning any consolidation on the count.

The CTG enrichment scan flagged 19 project directories; a full-portfolio
inventory resolved that to exactly 3 real implementations (enrichment-dispatch,
signal-radar's Apollo steps, hubspot-gender-enrichment), 7 enrichment-adjacent
keepers, and 10+ false positives (the word "enrich" in IT-ticket triage, Veo
spend comments, MCP catalog metadata, admin-login geo notes, Drive folder
names). Planning fold-ins from the raw 19 would have scoped moves for projects
with nothing to move and re-litigated settled ADRs (0039/0044).
How to apply: consolidation plans get a verdict table first — per hit: the
exact triggering line, what the code actually does, and one of the three
verdicts — and only verdict-(a) rows become migration steps.

Triggered by: 2026-07-26 enrichment consolidation planning run
(_audit/2026-07-26-enrichment-consolidation/ in CTG-Workspace-Build).

## [2026-07-26] [A writer that has never fired live hides contract bugs]

Dry-run-by-default write paths accumulate payload-contract bugs that no test
catches, because the mock accepts what the real API would reject — verify the
payload contract against the real schema BEFORE any dormant write gate opens.

Both dormant HubSpot writers in the CTG portfolio carry exactly this class of
bug: enrichment-dispatch's writer sends internal field names verbatim
(title/employer where HubSpot expects jobtitle/company — first live write
400s; app/writeback/hubspot.py:128, no mapping table exists anywhere), and
signal-radar's writer sends a company NAME as the idProperty=domain upsert key
(src/backend/writeback/hubspot.py:98 — silent mis-upserts the day a token
lands). Years of green suites, both broken on contact with reality.
How to apply: before flipping any long-dormant dry_run flag, add a contract
test that validates the exact outbound payload against the real target schema
(property names, idProperty semantics), plus one sandbox round-trip.

Triggered by: 2026-07-26 enrichment inventory — both gaps found by reading the
writers, not by any failing test.

## [2026-07-26] [CTG has two ADR namespaces — check both]

"ADR-001"/"ADR-003" in CTG briefs usually mean the per-project consolidated
log at ~/Claude Cowork/ABOUT ME/architecture-decisions.md (ADR-001 DWD,
ADR-003 never-write-then-search, ADR-006 httpx-only, ADR-007 allowed_api_domains),
NOT the workspace set at decisions/adr/ (0001–0044, zero-padded). A search for
ADR-001 under decisions/ returns nothing and the binding rule gets missed.
How to apply: unpadded ADR-NNN → Cowork log; zero-padded ADR-00NN → workspace
decisions/adr/. Note ADR-007's attribute is named settings.allowed_api_domains
in the log but implemented as settings.allowlist in enrichment-dispatch
(app/config.py:41) — same rule, different name; don't flag as a violation.

Triggered by: 2026-07-26 consolidation brief citing ADR-001/ADR-003; find
across projects/*/decisions returned nothing.

## [2026-07-26] [enrichment-dispatch: origin/feat/explorium-provider is a rebase remnant]

The dispatch repo's only non-main remote branch (origin/feat/explorium-provider,
4 commits "unmerged") is a pre-rebase remnant — the same changes are already on
main under different SHAs (ccf4500→9f1aea7 etc.) and the branch is BEHIND main
(missing the ruff <0.16 pin, the calibrated-threshold test, the Explorium
secret mount). Delete it; NEVER merge it — merging would regress three fixes.
How to apply: cherry-pick --no-commit + git status to prove content-equivalence
before trusting any "N commits unmerged" report (same rule as
LESSON-SQUASH-CONTENT-CHECK-01, which this re-confirms for rebase flows).

Triggered by: 2026-07-26 enrichment-dispatch git-state sweep.

- **[GCP-identity]**: Never cite a gcloud/bq 403 as evidence about one identity
  without `tokeninfo`-confirming the token's actual `email` first. On this Mac,
  `gcloud auth print-access-token --account pete@cloudtechgurus.com` mints a
  token that resolves to `cpeteconnor@gmail.com` — the `--account` flag does NOT
  guarantee the minted identity, because only the gmail bootstrap account has a
  usable stored credential. `bq` via ADC does the same. Confirm with
  `curl .../oauth2/v3/tokeninfo?access_token=$T` → `email`.
  How to apply: any "pete@ is denied X" conclusion drawn from a local CLI 403 is
  invalid unless tokeninfo showed pete@; otherwise it is gmail's denial, and
  gmail holds nothing on the org projects (ctg-hs-exec-tool, ctg-workspace-dev).
  Triggered by: 2026-08-14 — PR #130 wrongly diagnosed a "VPC-SC perimeter" from
  gmail-identity 403s; the same trap earlier produced a wrong "bq denies pete@"
  read. Both were the two-identity trap HANDOFF.md already warns about.
