# Quick Task 260721-tx7: Pete-gated audit fixes

**Created:** 2026-07-22
**Mode:** quick (inline execution — see Deviations)
**Source:** tasks/todo.md item 2 (a/b/c), approved by Pete via /gsd:do dispatch 2026-07-21

## Tasks

### Task 1: daily-stats schema type fix
- **Files:** `config/schema/daily-stats-frontmatter.schema.json`
- **Action:** `schema_version` property `"type": "string"` → `"type": "integer"`. Live frontmatter in `RIGHT/daily-stats.md` carries `schema_version: 1` (YAML integer); the schema wrongly requires string. Protected-file guard's "manual review" satisfied by Pete's explicit dispatch approval — the exact diff was filed in the audit and in tasks/todo.md.
- **Verify:** AJV compiles the schema and validates `{schema_version: 1, ...}`; `npx jest daily-stats` green.
- **Done:** Schema accepts integer schema_version; tests pass.

### Task 2: dependency majors
- **Files:** `package.json`, `package-lock.json`
- **Action:** Take the three majors from `.planning/todos.md`: `@anthropic-ai/sdk` ^0.91.1→^0.112.5, `voyageai` 0.2.1→0.4.0 (keep exact pin), `eslint-plugin-n` ^17→^18 (dev). chokidar stays 3.6 (decided, PROJECT.md).
- **Verify (gates from the todo itself):** full jest suite green; `npm run lint` 0 errors; `npm run eval:recall` at-or-above baseline; `npm run test:integration:voyage` live pass (VOYAGE_API_KEY present). Any package failing its gate reverts individually; the rest land.
- **Done:** Gated majors installed or evidence-backed reverted; todo updated with outcome.

### Task 3: branch protection restore (repo settings, no commit)
- **Action:** PUT `repos/UsernameTron/second-brain/branches/master/protection` as UsernameTron (per-invocation `gh auth token --user UsernameTron` — avoids the machine-wide switch that keeps reverting). Config per audit recommendation (STATE.md:64) + v1.4 Phase 17 verified shape: `required_status_checks {strict, contexts: [test (20), test (22)]}`, `required_pull_request_reviews {required_approving_review_count: 0}`, `enforce_admins: false`, force-push and deletion blocked. `Analyze` intentionally omitted per the audit's spelled-out recommendation.
- **Verify:** GET protection returns the config (404 today).
- **Done:** Protection live; evidence in SUMMARY.

## Deviations from stock quick workflow
- Planner/executor subagents skipped; executed inline. The plan is fully dictated by tasks/todo.md item 2, and the recorded lesson `feedback_gsd_executor_worktree_jest_blind` says worktree executors stall on jest self-verify in this repo — "finish/verify inline" is the documented remedy.
