- [x] Dependency majors decision — resolved 2026-07-21 (quick 260721-tx7, Pete-approved via /gsd:do): @anthropic-ai/sdk 0.112.5 landed (suite/lint/license/eval green; required lazy-init fix in content-policy.js — 0.112 constructors start an async credential chain at module scope); eslint-plugin-n 18 landed (0 errors); voyageai 0.4.0 REJECTED on evidence — breaks the embed API against `src/semantic-index.js` (0 embedded, instant failure) and ships `fast-sha256` under Unlicense — stays 0.2.1 exact pin. `Unlicense` added to license allowlist for the SDK's `standardwebhooks→fast-sha256` transitive (public-domain family, same as already-allowed CC0-1.0). chokidar stays 3.6 (v5 ESM-only — rejected, see PROJECT.md decision).
- [ ] 9 `jest/expect-expect` warnings surfaced by eslint-plugin-jest 29.15.5 (assertion-less tests: classifier 511, calendar 415/421/486/492, github 112, memory-extractor 552/571, pipeline-infra 781) — review whether each asserts via helpers or genuinely asserts nothing; fix or annotate per site. 0 errors, gate green meanwhile.
- [ ] `checkContent()` fails OPEN on Unicode-variant excluded terms (src/content-policy.js) — the exact bypass class `normalizeForMatch()` exists to stop. Stage 1 scans NFKD-normalized text so full-width `ＩＳＰＮ` matches; Stage 2 (`classifyWithHaiku`) then extracts context windows from the RAW `content` via `termRegex.exec(content)` (~line 205), finds nothing, and sends Haiku a prompt with zero excerpts — `"Classify these excerpts around the term 'ISPN':\n\n"` — to which it replies ALLOW. `checkContent` returns PASS and the content writes unredacted. Pre-existing, NOT caused by the whole-token matcher change (PR: Route the /today briefing write through vault-gateway). Never caught because all 45 HYG-UNICODE-02 tests exercise `sanitizeContent()` directly, never the gate. **Repro:** temp `test/_probe.test.js` mocking `@anthropic-ai/sdk` to return `ALLOW`, then `await checkContent('Migration notes for ＩＳＰＮ queue handling.', ['ISPN'])` → `{"decision":"PASS"}`. **Fix (recommended):** fail closed — if `contextWindows.length === 0` after a Stage 1 match, return BLOCK without calling Haiku, per the module's stated D-08 policy ("Haiku timeout/unavailability triggers BLOCK, never silent bypass"). Smallest blast radius: post-matcher-change, only a genuine Unicode variant can reach empty windows. **Alternative:** extract windows from `normalizeForMatch(content)` — always finds a window, but lowercases/NFKD-folds every excerpt sent for classification and may shift decisions on normal content. Add `checkContent`-level tests for one variant per bypass category (full-width, soft-hyphen, zero-width).
- [ ] Restore dev-dependency advisory signal in CI. The `Dependency audit` step was scoped to `--omit=dev` on 2026-07-27 (PR #93) after GHSA-mh99-v99m-4gvg (brace-expansion DoS) turned the unscoped audit red with no change to `package.json`/`package-lock.json` — `npm audit` queries the live advisory DB, so a green run on 2026-07-22 blocked every PR five days later. All 22 findings were dev tooling (eslint, jest, license-checker, typescript-estree) and `npm audit --omit=dev` reported 0, so production was never exposed. **Gap:** a genuinely dangerous dev-tool advisory (build-time code execution, compromised plugin) now surfaces nowhere. **Intended fix:** add a second non-blocking step — `npm audit --audit-level=high` with `continue-on-error: true` — so dev advisories stay visible without gating merges. **Also revisit:** `brace-expansion` is installed at 1.1.16 / 2.1.2 / 5.0.7; 5.0.8 is patched, but the 1.x and 2.x lines are pulled by `minimatch@3` and cannot take 5.x without an `overrides` pin that may break eslint/jest. `npm audit fix` (non-force) resolves nothing; `--force` downgrades license-checker to 5.1.1 (breaking). Best resolved by upstream bumps.
