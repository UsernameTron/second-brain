---
phase: 05-integration-wiring
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified: []
autonomous: false
requirements: [INTEG-02]
must_haves:
  truths:
    - "Test trigger trig_01NHx3Uz8ni8eZSfsMph4miY is deleted from claude.ai/code/scheduled"
    - "Real RemoteTrigger 'second-brain-daily-briefing' exists and is enabled"
    - "RemoteTrigger fires on schedule and produces a daily briefing in the vault"
  artifacts:
    - path: "config/scheduling.json"
      provides: "RemoteTrigger configuration reference"
      contains: "second-brain-daily-briefing"
  key_links:
    - from: "RemoteTrigger (Anthropic cloud)"
      to: "src/today-command.js"
      via: "git checkout + node execution"
      pattern: "runToday"
---

<objective>
Delete test trigger, create and enable the real RemoteTrigger for daily briefing.

Purpose: INTEG-02 requires the system to run unattended before 7 AM weekdays. The RemoteTrigger API creates a server-side cron job in Anthropic's cloud that checks out the repo, runs /today, and writes a daily briefing. Per D-11, Gmail section will degrade gracefully (RemoteTrigger cannot reach local gmail-mcp-pete).

Output: A live, firing RemoteTrigger that produces daily briefings on a real cron schedule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-wiring/05-CONTEXT.md

<interfaces>
<!-- RemoteTrigger config from config/scheduling.json: -->
<!--
{
  "trigger": {
    "name": "second-brain-daily-briefing",
    "schedule": "45 11 * * 1-5",
    "model": "claude-sonnet-4-6",
    "environment_id": "env_01TjBJLSRwHfpUPcNVUK99Kb",
    "sources": ["https://github.com/UsernameTron/second-brain"],
    "mcp_connections": [{"connector_uuid": "f94e7416-4e60-44bc-9ed2-45fa49a68665", "name": "Google Calendar"}],
    "persist_session": false,
    "enabled": false,
    "prompt": "You are running the second-brain daily briefing. Read CLAUDE.md first. Then run the /today command by executing: node -e \"require('./src/today-command').runToday({ mode: 'scheduled' }).then(r => console.log(r.briefing || r.error))\". Report the output."
  }
}
-->

<!-- RemoteTrigger runs in Anthropic cloud: -->
<!-- - Has access to: git repo (second-brain), Google Calendar MCP connector -->
<!-- - Does NOT have access to: local stdio MCP servers (gmail-mcp-pete, filesystem) -->
<!-- - Gmail section will show degraded (null mcpClient -> {success: false}) — by design per D-11 -->

<!-- Test trigger to delete: trig_01NHx3Uz8ni8eZSfsMph4miY -->
<!-- Delete at: https://claude.ai/code/scheduled -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Delete test trigger from claude.ai</name>
  <files></files>
  <action>
    User must delete the test trigger from the claude.ai scheduled tasks UI (no API available for deletion).

    Steps:
    1. Open https://claude.ai/code/scheduled in browser
    2. Find trigger with ID trig_01NHx3Uz8ni8eZSfsMph4miY (or name containing "test")
    3. Delete it
    4. Confirm it no longer appears in the list

    Resume with "test-trigger-deleted" or "already-deleted".
  </action>
  <verify>Test trigger trig_01NHx3Uz8ni8eZSfsMph4miY no longer visible at https://claude.ai/code/scheduled</verify>
  <done>Test trigger deleted. Clean slate for creating the real trigger.</done>
</task>

<task type="auto">
  <name>Task 2: Create real RemoteTrigger via API</name>
  <files></files>
  <read_first>
    - config/scheduling.json (full trigger spec — name, schedule, model, environment_id, sources, mcp_connections, prompt)
  </read_first>
  <action>
    Create the real RemoteTrigger using the RemoteTrigger API. The exact API call depends on the available interface — use whichever of these works:

    **Option A: Claude Code RemoteTrigger command** (preferred if available):
    Use the Claude Code scheduling interface to create a trigger with these exact parameters from `config/scheduling.json`:
    - Name: `second-brain-daily-briefing`
    - Schedule: `45 11 * * 1-5` (UTC — 6:45 AM CDT weekdays)
    - Model: `claude-sonnet-4-6`
    - Environment ID: `env_01TjBJLSRwHfpUPcNVUK99Kb`
    - Sources: `https://github.com/UsernameTron/second-brain`
    - MCP connections: Google Calendar connector UUID `f94e7416-4e60-44bc-9ed2-45fa49a68665`
    - Persist session: false
    - Enabled: **false** initially (per D-12 — verify config before enabling)
    - Prompt: exact string from config/scheduling.json `trigger.prompt` field

    **Option B: REST API call**:
    If a direct API endpoint is available, use curl to POST the trigger creation.

    **Option C: Manual creation guidance**:
    If neither programmatic option works, provide exact step-by-step instructions for creating the trigger at https://claude.ai/code/scheduled with all parameter values spelled out.

    After creation, record the new trigger ID. Verify the trigger exists with `enabled: false`.
  </action>
  <verify>Verify at https://claude.ai/code/scheduled that 'second-brain-daily-briefing' trigger exists with schedule '45 11 * * 1-5' and enabled=false</verify>
  <acceptance_criteria>
    - A trigger named "second-brain-daily-briefing" exists at claude.ai/code/scheduled
    - Schedule is "45 11 * * 1-5" (UTC)
    - Trigger is initially disabled (enabled: false)
    - MCP connection includes Google Calendar connector
    - Source points to https://github.com/UsernameTron/second-brain
  </acceptance_criteria>
  <done>Real trigger created and visible at claude.ai/code/scheduled, initially disabled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Enable trigger and verify first scheduled fire</name>
  <files></files>
  <action>
    User enables the trigger and verifies it produces a daily briefing on the next scheduled fire.

    Steps:
    1. Open https://claude.ai/code/scheduled
    2. Confirm trigger "second-brain-daily-briefing" exists with schedule "45 11 * * 1-5" (UTC)
    3. Verify MCP connections include Google Calendar
    4. Verify source is https://github.com/UsernameTron/second-brain
    5. Enable the trigger (flip enabled to true)
    6. Wait for next scheduled fire (or manually trigger if available)
    7. After it fires, check ~/Claude Cowork/RIGHT/daily/ for today's briefing note
    8. Confirm the briefing has section headings (## Meetings, ## Slippage, etc.)
    9. Confirm Gmail section shows degraded warning (expected per D-11 — RemoteTrigger cannot reach local gmail-mcp-pete)

    Resume with "trigger-verified" or describe issues.
  </action>
  <verify>ls ~/Claude\ Cowork/RIGHT/daily/ | tail -1</verify>
  <done>RemoteTrigger fired on schedule and produced a complete daily briefing (with expected Gmail degradation).</done>
</task>

</tasks>

<verification>
- Test trigger trig_01NHx3Uz8ni8eZSfsMph4miY no longer exists at claude.ai/code/scheduled
- Trigger "second-brain-daily-briefing" exists and is enabled
- Trigger has fired at least once and produced a daily note in ~/Claude Cowork/RIGHT/daily/
- Daily note contains section headings (## Meetings, ## Slippage, etc.)
- Gmail section shows degraded state (expected behavior per D-11)
</verification>

<success_criteria>
- INTEG-02 satisfied: RemoteTrigger fires automatically before 7 AM on a real cron schedule
- Briefing is complete except Gmail (which degrades gracefully per design)
- No stale test triggers remain
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-wiring/05-03-SUMMARY.md`
</output>
