---
phase: 04-daily-briefing-and-scheduling
plan: 02
type: tdd
wave: 2
depends_on:
  - 04-01
files_modified:
  - test/today-command.test.js
autonomous: true
requirements:
  - TODAY-01
  - TODAY-02
  - TODAY-03
  - TODAY-04
  - SCHED-02

must_haves:
  truths:
    - "Contract tests prove degradation renders warning format per D-10"
    - "Contract tests prove total failure renders diagnostic checklist per D-11"
    - "Contract tests prove 6 sections appear in correct order"
    - "Contract tests prove slippage scanner handles malformed STATE.md per D-14"
    - "Contract tests prove frog identification falls back to heuristic on LLM failure"
    - "Contract tests prove source health map correctly reports ok/degraded"
  artifacts:
    - path: "test/today-command.test.js"
      provides: "Unit and contract tests for /today command"
      min_lines: 150
  key_links:
    - from: "test/today-command.test.js"
      to: "src/today-command.js"
      via: "require('../src/today-command')"
      pattern: "require.*today-command"
---

<objective>
Create comprehensive tests for the /today command module. These tests validate the contract: degradation format, section ordering, slippage scanning, frog identification, source health, and template rendering.

Purpose: The /today command is the capstone of the entire project. Every prior phase feeds into it. Contract tests ensure the briefing degrades correctly, renders correctly, and composes data from all sources faithfully.
Output: `test/today-command.test.js` with passing test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-daily-briefing-and-scheduling/04-CONTEXT.md
@.planning/phases/04-daily-briefing-and-scheduling/04-01-SUMMARY.md

<interfaces>
From src/today-command.js (created in Plan 01):
```javascript
async function runToday(options = {})
// options: { mcpClient, mode, projectsDir, vaultRoot, date, haikuClient }
// returns: { path, briefing, sourceHealth } or { path: null, briefing: null, error }
```

From src/connectors/types.js:
```javascript
function makeResult(source, data) // { success: true, data, error: null, source, fetchedAt }
function makeError(source, errorMessage) // { success: false, data: null, error, source, fetchedAt }
const SOURCE = { CALENDAR: 'calendar', GMAIL: 'gmail', GITHUB: 'github' }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write /today contract tests</name>
  <read_first>
    - src/today-command.js
    - src/connectors/types.js
    - test/connectors/github.test.js
    - .planning/phases/04-daily-briefing-and-scheduling/04-CONTEXT.md
  </read_first>
  <files>test/today-command.test.js</files>
  <behavior>
    - Test: runToday with all connectors returning success produces briefing with 6 sections in order: Meetings, VIP Emails, Slippage, Frog, GitHub, Pipeline
    - Test: runToday with one connector failing renders that section with D-10 warning format matching /\[.+\] [A-Z_]+: .+/
    - Test: runToday with ALL connectors failing renders diagnostic checklist per D-11 (contains "All data sources unavailable" or "Docker Desktop running")
    - Test: source health map has calendar/gmail/github/pipeline keys with values 'ok' or 'degraded'
    - Test: slippage scanner skips malformed STATE.md files and includes them in warnings
    - Test: slippage scanner respects excludeProjects config
    - Test: slippage scanner flags projects as stalled when daysSinceActivity > staleDays
    - Test: frog identification returns frog+reasoning on LLM success
    - Test: frog identification falls back to heuristic (most stalled project) on LLM failure
    - Test: dry-run mode writes to _dry-run- prefixed path
    - Test: frontmatter contains date, sources map, degraded count, generated timestamp, mode
    - Test: briefing contains horizontal rule after synthesis blockquote before first section
  </behavior>
  <action>
Create `test/today-command.test.js` following the project's existing test patterns (see `test/connectors/github.test.js` for mock patterns).

**Test setup:**
- Use `jest.doMock` for mocking connectors and pipeline-infra (per project convention — jest.doMock preferred over jest.mock when mock factories need dynamic configuration)
- Create a `mockMcpClient` object (can be empty — connectors are mocked)
- Create a `mockHaikuClient` with `classify` that returns `{ success: true, data: { frog: 'Test frog', reasoning: 'Test reasoning' } }`
- Use `tmp` directories for `projectsDir` and `vaultRoot` via `fs.mkdtempSync`
- Create fake `STATE.md` files in temp projectsDir for slippage scanner tests

**Mock strategy:**
- Mock `./connectors/calendar` to return `makeResult(SOURCE.CALENDAR, [{ start: '2026-04-23T09:00:00', summary: 'Standup', attendees: ['alice@example.com'] }])`
- Mock `./connectors/gmail` to return `makeResult(SOURCE.GMAIL, [{ subject: 'Urgent', from: 'vip@example.com', snippet: 'Need review' }])`
- Mock `./connectors/github` to return `makeResult(SOURCE.GITHUB, { repos: [{ name: 'second-brain', commits: 2, prs: 1, issues: 0 }], warnings: [] })`
- Mock `./briefing-helpers` to return `getProposalsPendingCount: () => 3`, `getDeadLetterSummary: () => ({ pending: 1, frozen: 0, total: 1, warning: false })`, `formatBriefingSection` returning appropriate strings
- Mock `./pipeline-infra` to return real `loadPipelineConfig` (reads from test config via CONFIG_DIR_OVERRIDE) and mock `createHaikuClient`

**Test groups:**

1. **"briefing sections"** — verify all 6 sections present in order, horizontal rule after synthesis
2. **"degradation"** — one source fails (check warning format), all sources fail (check diagnostic checklist)
3. **"source health"** — verify frontmatter sources map and degraded count
4. **"slippage scanner"** — test with temp STATE.md files: stale project, active project, malformed file, excluded project
5. **"frog identification"** — LLM success path, LLM failure fallback, no candidates case
6. **"output modes"** — dry-run path prefix, interactive stdout output, scheduled no-stdout
7. **"frontmatter"** — parse output with gray-matter, verify all D-08 fields present

**Cleanup:** `afterAll` removes temp directories.

Follow RED-GREEN-REFACTOR: write tests first, run them (expect failures since mocks may need adjustment), then fix any issues in tests to get them passing against the Plan 01 implementation.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/second-brain && npx jest test/today-command.test.js --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test/today-command.test.js exists with >= 10 test cases
    - Tests cover: section order, degradation warning format, total failure checklist, source health map, slippage scanning, frog identification, dry-run path, frontmatter fields
    - Degradation test asserts error rendering matches `/\[.+\] [A-Z_]+: .+/` regex
    - Total failure test asserts briefing contains "All data sources unavailable" or "Docker Desktop"
    - Slippage test creates temp STATE.md files and verifies stale detection
    - Frog test verifies heuristic fallback when classify returns success===false
    - `npx jest test/today-command.test.js` exits 0
  </acceptance_criteria>
  <done>All /today contract tests pass, covering degradation, section ordering, slippage scanning, frog identification, source health, and template rendering</done>
</task>

</tasks>

<verification>
- `npx jest test/today-command.test.js --no-coverage` exits 0
- `npx jest test/today-command.test.js --no-coverage 2>&1 | grep -c 'PASS'` returns 1
- Test count >= 10
</verification>

<success_criteria>
- All contract tests pass
- Degradation format is verified via regex assertion
- Slippage scanner is tested with real temp files (not just mocks)
- Frog identification tested for both LLM success and failure paths
- No tests depend on external services or network
</success_criteria>

<output>
After completion, create `.planning/phases/04-daily-briefing-and-scheduling/04-02-SUMMARY.md`
</output>
