---
phase: 01-vault-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/vault-gateway.js
  - test/vault-gateway.test.js
  - config/excluded-terms.json
autonomous: false
requirements:
  - VAULT-03
  - VAULT-04

must_haves:
  truths:
    - "Content mentioning ISPN, Genesys, or Asana is blocked or quarantined before reaching disk"
    - "Neutral references (career narrative, tool mentions) pass the filter per D-07"
    - "Agent-generated vault content is rejected if it contains banned words from the style guide"
    - "Haiku timeout or unavailability causes quarantine, never silent bypass per D-08"
    - "Excluded terms list is expanded to 15-20 entries per D-10"
  artifacts:
    - path: "src/vault-gateway.js"
      provides: "Complete three-gate enforcement: path guard + content filter + style lint"
      contains: "classifyWithHaiku"
    - path: "src/vault-gateway.js"
      provides: "Style lint with banned word extraction from style guide"
      contains: "checkStyle"
    - path: "src/vault-gateway.js"
      provides: "Style guide file watcher for banned word hot-reload"
      contains: "anti-ai-writing-style"
    - path: "test/vault-gateway.test.js"
      provides: "Tests for content filter, style lint, and full three-gate pipeline"
      min_lines: 150
    - path: "config/excluded-terms.json"
      provides: "Expanded keyword list with 15-20 entries per D-10"
      min_lines: 1
  key_links:
    - from: "src/vault-gateway.js"
      to: "@anthropic-ai/sdk"
      via: "Haiku classification call in checkContent Stage 2"
      pattern: "anthropic"
    - from: "src/vault-gateway.js"
      to: "~/Claude Cowork/ABOUT ME/anti-ai-writing-style.md"
      via: "fs.readFileSync at init + fs.watch for hot-reload"
      pattern: "anti-ai-writing-style"
    - from: "src/vault-gateway.js"
      to: "config/excluded-terms.json"
      via: "JSON.parse at init + fs.watch hot-reload"
      pattern: "excluded-terms"
---

<objective>
Add content filtering (two-stage with Haiku classification) and anti-AI style linting to the vault-gateway, completing the three-gate enforcement pipeline. Expand the excluded terms list to 15-20 entries.

Purpose: Without content filtering, excluded employer/client content can reach the vault. Without style linting, agent-generated prose can contain banned AI-sounding words. These two guards complete the write-permission boundary that makes subsequent phases safe to build on.

Output: Complete `src/vault-gateway.js` with all three guards operational, expanded keyword list, and comprehensive tests including Haiku mock scenarios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-vault-foundation/01-CONTEXT.md
@.planning/phases/01-vault-foundation/01-RESEARCH.md
@.planning/phases/01-vault-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — the executor must understand these exports -->

From src/vault-gateway.js (created in Plan 01):
```javascript
// Exports available from Plan 01:
module.exports = {
  vaultWrite,       // async (relativePath, content, options?) => { decision, path } | throws VaultWriteError
  vaultRead,        // async (relativePath) => string | throws VaultWriteError
  toWikilink,       // (noteName, displayText?) => string
  bootstrapVault,   // async () => void
  initGateway,      // async () => config
  getConfig,        // () => { left: string[], right: string[], excludedTerms: string[] }
  normalizePath,    // (inputPath) => string | throws VaultWriteError
  checkPath,        // (normalizedPath, config) => { decision: 'PASS' | 'BLOCK', reason? }
  quarantine,       // async (originalPath, content, reason) => { decision: 'QUARANTINED', quarantinePath }
  VaultWriteError,  // Error subclass with .code property
  VAULT_ROOT        // string — path.join(HOME, 'Claude Cowork')
};
```

From config/vault-paths.json:
```json
{
  "left": ["ABOUT ME", "Daily", "Relationships", "Drafts"],
  "right": ["memory", "briefings", "ctg", "job-hunt", "interview-prep", "content", "research", "ideas", "proposals"]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add content filter and style lint guards to vault-gateway</name>
  <files>src/vault-gateway.js, test/vault-gateway.test.js</files>
  <read_first>
    src/vault-gateway.js
    test/vault-gateway.test.js
    config/excluded-terms.json
    ~/Claude Cowork/ABOUT ME/anti-ai-writing-style.md
    .planning/phases/01-vault-foundation/01-CONTEXT.md
    .planning/phases/01-vault-foundation/01-RESEARCH.md
  </read_first>
  <action>
    Modify `src/vault-gateway.js` to add Guard 2 (content filter) and Guard 3 (style lint) to the `vaultWrite` pipeline. The existing path guard (Guard 1) from Plan 01 remains unchanged.

    **Guard 2 — Content Filter (per D-06 through D-11):**

    Add `checkContent(content, config)` async function:
    - Stage 1: Keyword scan. For each term in `config.excludedTerms`, build regex `new RegExp('\\b' + escapeRegex(term) + '\\b', 'i')`. Test against content. No match = return `{ decision: 'PASS' }` immediately (zero-latency common case per D-06).
    - Regex escape: `term.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')` before regex construction (research anti-pattern: regex injection).
    - Stage 2 (fires only on Stage 1 hit): Call `classifyWithHaiku(content, excludedTerms)`.

    Add `classifyWithHaiku(content, excludedTerms)` async function:
    - Uses `@anthropic-ai/sdk`: `const Anthropic = require('@anthropic-ai/sdk'); const client = new Anthropic();`
    - Calls `client.messages.create()` with:
      - `model: 'claude-haiku-4-5'`
      - `max_tokens: 10`
      - `timeout: 2000` (2s per D-08)
      - System prompt per research Pitfall 3 — must include BLOCK and ALLOW examples:
        ```
        You are a content filter. Classify the following content as BLOCK or ALLOW.
        BLOCK: content that is about or from these organizations — their architecture, internal processes, client data, confidential strategy, or internal people: ${excludedTerms.join(', ')}
        ALLOW: neutral tool references ("tracked in Asana"), career narrative ("led a team at Genesys"), generic industry mentions.

        BLOCK examples:
        - "The Genesys Cloud routing configuration for ISPN queue handling..." (architecture/operations)
        - "Asana project ID 12345 with client Acme..." (client data)

        ALLOW examples:
        - "I led a 12-person contact center team at Genesys." (career narrative)
        - "Tracked in Asana" (neutral tool reference)
        - "Genesys is a common CCaaS vendor in the enterprise market." (generic industry mention)

        Respond with only the word BLOCK or ALLOW.
        ```
      - User message: `Classify:\n\n${content.slice(0, 500)}` (truncate per research)
    - Parse response: if `response.content[0].text.trim().toUpperCase() === 'BLOCK'` return `'BLOCK'`, else return `'ALLOW'`.
    - On any error (timeout, API down): throw the error. The caller (`checkContent`) catches it and returns QUARANTINE per D-08.

    `checkContent` return values:
    - No keyword match: `{ decision: 'PASS' }`
    - Keyword match + Haiku says BLOCK: `{ decision: 'QUARANTINE', reason: 'Excluded content detected (term: X)' }`
    - Keyword match + Haiku says ALLOW: `{ decision: 'PASS' }`
    - Keyword match + Haiku error/timeout: `{ decision: 'QUARANTINE', reason: 'Haiku unavailable, queued for review (error message)' }`

    **Guard 3 — Style Lint (per D-12 through D-15):**

    Add `extractBannedWords(styleGuideContent)` function:
    - Parse the `## Banned Words` section table from the style guide markdown.
    - Extract words from the first column of each table row (skip header row and separator row).
    - Return lowercase array. Expected words from the current style guide: `genuinely`, `honestly`, `straightforward`, `dive in`, `let's dive deep`, `game-changer`, `leverage`, `synergy`, `synergies`, `innovative`, `cutting-edge`, `seamlessly`, `empower`, `empowering`, `unlock`, `robust`, `at the end of the day`, `it goes without saying`, `in today's fast-paced world`, `i'd be happy to`, `great question`, `certainly`, `absolutely`, `of course`.

    Add `loadStyleGuide()` function:
    - Reads `path.join(VAULT_ROOT, 'ABOUT ME', 'anti-ai-writing-style.md')` via `fs.readFileSync`.
    - Returns full content (for prompt injection per D-12) and extracted banned words (for lint per D-13).
    - Cache result. Watch file with `fs.watch` and reload on change (per D-13 "reloaded when style guide file changes"). Use same debounce pattern as config watcher.

    Add `checkStyle(content, bannedWords, attemptCount)` function:
    - For each banned word, build regex `new RegExp('\\b' + escapeRegex(word) + '\\b', 'i')` and test against content. (Latency target <10ms per D-13 — regex-only, no semantic analysis.)
    - No match: return `{ decision: 'PASS' }`.
    - Match + attemptCount === 0: return `{ decision: 'REJECT', reason: "Banned word detected: 'X' -- regenerate without it" }` (per D-14 first violation).
    - Match + attemptCount > 0: return `{ decision: 'QUARANTINE', reason: "Repeated style violation: 'X'" }` (per D-14 second violation).

    Add `getStyleGuideForPrompt()` function:
    - Returns the full text of `anti-ai-writing-style.md` for system prompt injection (per D-12). Consumers in future phases will call this when generating vault-destined content.

    **Update vaultWrite pipeline:**

    Modify `vaultWrite(relativePath, content, options = {})` to run all three guards in sequence:
    1. Path guard (existing from Plan 01) — BLOCK throws VaultWriteError
    2. Content filter (new) — QUARANTINE calls quarantine()
    3. Style lint (new) — REJECT throws VaultWriteError with code STYLE_VIOLATION, QUARANTINE calls quarantine()
    4. All PASS — write to vault (existing from Plan 01)

    The `options` parameter accepts `{ attemptCount: number }` for style lint retry tracking per D-14.

    **Update exports:**

    Add to module.exports: `checkContent`, `checkStyle`, `extractBannedWords`, `getStyleGuideForPrompt`, `loadStyleGuide`.

    **Add tests to test/vault-gateway.test.js:**

    9. **Content filter Stage 1 — no match:** Content "Hello world" with excludedTerms ["ISPN"] returns `{ decision: 'PASS' }`. No Haiku call made.

    10. **Content filter Stage 1 — keyword hit, Stage 2 Haiku BLOCK:** Content "The ISPN queue routing architecture" with mocked Haiku returning "BLOCK" returns `{ decision: 'QUARANTINE', reason: /Excluded content/ }`.

    11. **Content filter Stage 1 — keyword hit, Stage 2 Haiku ALLOW:** Content "I led a team at Genesys" with mocked Haiku returning "ALLOW" returns `{ decision: 'PASS' }`.

    12. **Content filter — Haiku timeout/error:** Mock Haiku to throw. Returns `{ decision: 'QUARANTINE', reason: /Haiku unavailable/ }`. Verify content is NEVER silently passed.

    13. **Style lint — clean content:** Content "This is a test note" returns `{ decision: 'PASS' }`.

    14. **Style lint — first violation:** Content "This is genuinely great" with attemptCount=0 returns `{ decision: 'REJECT', reason: /genuinely/ }`.

    15. **Style lint — repeated violation:** Content "This is genuinely great" with attemptCount=1 returns `{ decision: 'QUARANTINE', reason: /genuinely/ }`.

    16. **Banned word extraction:** `extractBannedWords(styleGuideContent)` returns array containing `genuinely`, `honestly`, `robust`, `of course` (at minimum).

    17. **Full pipeline — clean write to RIGHT:** vaultWrite('memory/test.md', 'Clean content') succeeds with `{ decision: 'WRITTEN' }`. (Mock fs operations.)

    18. **Full pipeline — write blocked by content filter:** vaultWrite('memory/test.md', 'The ISPN architecture...') with mocked Haiku BLOCK returns `{ decision: 'QUARANTINED' }`.

    19. **Full pipeline — write rejected by style lint:** vaultWrite('memory/test.md', 'This is genuinely innovative') throws VaultWriteError with code STYLE_VIOLATION.

    Mock `@anthropic-ai/sdk` for all Haiku tests — do not make real API calls. Mock `fs.promises` and `fs.readFileSync` for vault operations. For `extractBannedWords` test, use the actual content of `~/Claude Cowork/ABOUT ME/anti-ai-writing-style.md` loaded as a test fixture string (hardcode the relevant table section, do not read the real file in tests).
  </action>
  <behavior>
    - Test: Clean content passes content filter without Haiku call
    - Test: Keyword-hit content triggers Haiku; BLOCK result quarantines
    - Test: Career narrative with keyword passes after Haiku ALLOW
    - Test: Haiku failure quarantines (never silently passes)
    - Test: Clean content passes style lint
    - Test: First banned word hit rejects with specific word callout
    - Test: Second banned word hit quarantines
    - Test: Full pipeline runs all three guards in sequence
  </behavior>
  <verify>
    <automated>cd ~/projects/second-brain && npx jest --verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - src/vault-gateway.js contains `async function checkContent(`
    - src/vault-gateway.js contains `async function classifyWithHaiku(`
    - src/vault-gateway.js contains `function checkStyle(`
    - src/vault-gateway.js contains `function extractBannedWords(`
    - src/vault-gateway.js contains `function getStyleGuideForPrompt(`
    - src/vault-gateway.js contains `claude-haiku-4-5` (model name for Stage 2)
    - src/vault-gateway.js contains `timeout: 2000` (D-08 timeout)
    - src/vault-gateway.js contains `anti-ai-writing-style.md` (style guide path)
    - src/vault-gateway.js contains `STYLE_VIOLATION` (error code)
    - vaultWrite function body contains calls to checkPath, checkContent, and checkStyle in that order
    - test/vault-gateway.test.js contains at least 19 test cases total (8 from Plan 01 + 11 new)
    - `npx jest` exits 0 with all tests passing
  </acceptance_criteria>
  <done>Three-gate enforcement pipeline complete: path guard + content filter (two-stage with Haiku) + style lint (banned word regex). All tests pass. Haiku timeout triggers quarantine, never bypass. Style guide hot-reloads.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Expand excluded terms list and verify gateway</name>
  <files>config/excluded-terms.json</files>
  <action>
    Present the user with the keyword expansion request per D-10. The excluded-terms list currently has 3 seed entries (ISPN, Genesys, Asana). D-10 requires expanding to 15-20 terms before v1 go-live.

    Ask the user for additional terms in these categories:
    - Former employer product names (Genesys Cloud, PureCloud, etc.)
    - Project codenames from ISPN/Genesys/Asana work
    - Client names from those engagements
    - Internal tool names
    - Senior leadership names to exclude

    After receiving terms, update `config/excluded-terms.json` with the full list. The gateway will hot-reload the new terms automatically.

    Also run these verification steps and present results to the user:
    1. `cd ~/projects/second-brain && npx jest --verbose` — show test results
    2. `node -e "const gw = require('./src/vault-gateway.js'); gw.bootstrapVault().then(() => console.log('Vault directories created'))"` — create RIGHT-side vault directories
    3. `ls ~/Claude\ Cowork/ | head -20` — show vault directory listing
    4. `node -e "const gw = require('./src/vault-gateway.js'); gw.vaultWrite('ABOUT ME/test.md', 'test').catch(e => console.log('Blocked:', e.code))"` — demonstrate LEFT-side write protection
  </action>
  <verify>
    <automated>node -e "const et = require('./config/excluded-terms.json'); console.assert(et.length >= 15, 'Need 15+ terms, got ' + et.length); console.log('Terms:', et.length)"</automated>
  </verify>
  <acceptance_criteria>
    - config/excluded-terms.json contains at least 15 entries
    - config/excluded-terms.json contains "ISPN", "Genesys", "Asana" (original seed terms)
    - All RIGHT-side directories exist under ~/Claude Cowork/
    - `npx jest` exits 0
  </acceptance_criteria>
  <done>Excluded terms list expanded to 15-20 entries per D-10. All tests pass. Vault directories bootstrapped. User has verified LEFT-side write protection.</done>
</task>

</tasks>

<verification>
1. `npx jest --verbose` — all tests pass (19+ tests)
2. `node -e "const gw = require('./src/vault-gateway.js'); console.log(Object.keys(gw))"` — exports include vaultWrite, vaultRead, checkContent, checkStyle, extractBannedWords, getStyleGuideForPrompt, toWikilink, bootstrapVault
3. `node -e "const gw = require('./src/vault-gateway.js'); gw.checkPath('ABOUT ME/x.md', gw.getConfig()).decision"` — returns BLOCK
4. `node -e "const gw = require('./src/vault-gateway.js'); gw.checkPath('memory/x.md', gw.getConfig()).decision"` — returns PASS
5. `node -e "const gw = require('./src/vault-gateway.js'); gw.checkStyle('This is genuinely great', gw.extractBannedWords(require('fs').readFileSync(gw.VAULT_ROOT + '/ABOUT ME/anti-ai-writing-style.md', 'utf8')), 0).decision"` — returns REJECT
6. Config files at `config/vault-paths.json` and `config/excluded-terms.json` are valid JSON with expanded terms
7. All 9 RIGHT-side directories exist under `~/Claude Cowork/`
</verification>

<success_criteria>
- Content filter blocks ISPN/Genesys/Asana architectural content via two-stage pipeline
- Content filter allows career narratives and neutral tool references per D-07
- Haiku timeout/unavailability triggers quarantine, never silent bypass per D-08
- Style lint extracts banned words from the real style guide file
- Style lint rejects first violation, quarantines repeated violation per D-14
- Style guide hot-reloads when file changes per D-13
- Excluded terms list expanded to 15-20 entries per D-10
- Full three-gate pipeline executes in order: path -> content -> style
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-vault-foundation/01-02-SUMMARY.md`
</output>
