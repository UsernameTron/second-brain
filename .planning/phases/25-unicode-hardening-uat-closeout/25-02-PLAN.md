---
phase: 25-unicode-hardening-uat-closeout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/uat/uat-classification.test.js
autonomous: true
requirements:
  - UAT-REFRESH-01
  - UAT-SMOKE-01

must_haves:
  truths:
    - "UAT classification test produces a meaningful accuracy score locally when ANTHROPIC_API_KEY is set"
    - "No stale or ambiguous fixture data remains in the deterministic corpus"
    - "gh workflow run uat.yml fires and completes successfully with an artifact uploaded"
  artifacts:
    - path: "test/uat/uat-classification.test.js"
      provides: "Rebaselined UAT classification corpus with current-behavior expectations"
      contains: "DETERMINISTIC_CASES"
    - path: ".github/workflows/uat.yml"
      provides: "UAT workflow definition (already exists — confirmed executable)"
  key_links:
    - from: "test/uat/uat-classification.test.js"
      to: "src/classifier.js"
      via: "classifyInput() called with corpus inputs"
      pattern: "classifyInput"
    - from: ".github/workflows/uat.yml"
      to: "test/uat/"
      via: "npm run test:uat:ci"
      pattern: "test:uat:ci"
---

<objective>
Rebaseline the UAT classification corpus so it reflects current classifier behavior (no stale expectations), then trigger the Phase 17 UAT workflow on GitHub Actions and confirm it produces an artifact.

Purpose: Close two carry-forward items — the UAT corpus was written in Phase 17 and may contain stale expectations given classifier improvements since then, and the UAT workflow has never been smoke-tested end-to-end via `gh workflow run`.

Output: Updated `test/uat/uat-classification.test.js` with confirmed-accurate corpus, and a successful GitHub Actions run with artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebaseline UAT classification corpus</name>
  <files>test/uat/uat-classification.test.js</files>
  <read_first>
    - test/uat/uat-classification.test.js (full file — understand current corpus structure, 12 deterministic + 4 edge cases)
    - src/classifier.js (understand classifyInput signature, how it determines LEFT vs RIGHT)
    - config/pipeline.json (classifier configuration the test depends on)
  </read_first>
  <action>
    1. Run the UAT classification test locally with the API key to see current accuracy:
       ```bash
       ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY npx jest test/uat/uat-classification.test.js --verbose --no-coverage 2>&1
       ```
       Record which cases pass and which fail.

    2. For any deterministic case that consistently classifies differently from its `expected` field (i.e., the classifier has legitimately changed behavior since the corpus was written), update the `expected` value to match current behavior. Document the change with a comment explaining why.

       IMPORTANT: Only update expectations that are genuinely stale (classifier behavior has shifted). Do NOT weaken expectations just to make tests pass — if the classifier is wrong, that is a classifier bug, not a corpus bug. The threshold is: if the classifier's answer is reasonable given the input, update the corpus. If the classifier's answer is clearly wrong, leave the expectation and flag it.

    3. If all 12 deterministic cases already pass at >=80% accuracy, the corpus is fresh — no changes needed. In that case, add a comment at the top of DETERMINISTIC_CASES confirming rebaseline date:
       ```javascript
       // Rebaselined: 2026-04-26 — all 12 cases confirmed against current classifier behavior
       ```

    4. Ensure the edge cases (EDGE-01 through EDGE-04) do not throw and handle gracefully. These are not accuracy-counted but must not crash.

    5. Run the test again to confirm >=80% accuracy passes:
       ```bash
       ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY npx jest test/uat/uat-classification.test.js --verbose --no-coverage 2>&1
       ```
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/second-brain/.claude/worktrees/agent-afdcb0d4 && ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-skip} npx jest test/uat/uat-classification.test.js --no-coverage 2>&1 | grep -E "accuracy|Tests:|passed|failed" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - test/uat/uat-classification.test.js contains "Rebaselined:" comment with date
    - If run with ANTHROPIC_API_KEY set: overall accuracy assertion (>=80%) passes
    - If run without ANTHROPIC_API_KEY: test skips cleanly with "skipped" message (no crash)
    - No deterministic case has a stale expectation that was identified but left unfixed
    - Edge cases (EDGE-01 through EDGE-04) still present and test graceful handling
  </acceptance_criteria>
  <done>UAT classification corpus is confirmed accurate against current classifier behavior, test produces meaningful accuracy score locally, no stale fixtures remain</done>
</task>

<task type="auto">
  <name>Task 2: Trigger UAT workflow and confirm artifact</name>
  <files></files>
  <read_first>
    - .github/workflows/uat.yml (understand workflow structure, trigger mechanism, artifact upload step)
  </read_first>
  <action>
    1. Trigger the UAT workflow manually via GitHub CLI:
       ```bash
       gh workflow run uat.yml
       ```

    2. Wait for the workflow to start (poll for up to 30 seconds):
       ```bash
       sleep 10 && gh run list --workflow=uat.yml --limit=1 --json databaseId,status,conclusion
       ```

    3. Get the run ID and wait for completion (timeout 5 minutes):
       ```bash
       RUN_ID=$(gh run list --workflow=uat.yml --limit=1 --json databaseId --jq '.[0].databaseId')
       gh run watch $RUN_ID --exit-status
       ```

    4. After completion, verify the artifact was uploaded:
       ```bash
       gh run view $RUN_ID --json artifacts --jq '.artifacts[].name'
       ```
       Expected output should contain "uat-accuracy-report-" prefix.

    5. If the workflow fails due to missing ANTHROPIC_API_KEY secret: that is expected if the secret is not configured in the repo. In that case, verify:
       - The workflow DID fire (status is not "skipped" or "not found")
       - The failure reason is specifically the API key absence (not a code error)
       - Document the finding: "UAT workflow fires but requires ANTHROPIC_API_KEY secret to be configured in repo settings"
       
       This still satisfies UAT-SMOKE-01 — the requirement is that the workflow "fires, executes to completion" which means the infrastructure works. If the secret is missing, that is a configuration gap the user must resolve, not a code defect.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/second-brain/.claude/worktrees/agent-afdcb0d4 && gh run list --workflow=uat.yml --limit=1 --json status,conclusion,databaseId 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `gh run list --workflow=uat.yml --limit=1` returns at least one run (not empty)
    - The most recent run has status "completed" (not "queued" or "in_progress" indefinitely)
    - Either: conclusion is "success" and artifact named "uat-accuracy-report-*" exists, OR conclusion is "failure" and the failure log contains "ANTHROPIC_API_KEY" (secret not configured — documented as user config gap)
    - The workflow was triggered by workflow_dispatch (manual trigger), confirming the trigger mechanism works
  </acceptance_criteria>
  <done>Phase 17 carry-forward closed: UAT workflow confirmed to fire via gh workflow run, execute its steps, and either produce an artifact (if secret configured) or fail gracefully with clear error (if secret missing)</done>
</task>

</tasks>

<verification>
- UAT classification test produces >=80% accuracy locally (with API key)
- UAT classification test skips cleanly without API key (CI guard works)
- `gh workflow run uat.yml` triggers successfully
- GitHub Actions shows a completed UAT run
- No stale expectations remain in the corpus
</verification>

<success_criteria>
- test/uat/uat-classification.test.js has rebaselined corpus with confirmed expectations
- `gh workflow run uat.yml` fires and the run reaches completion (success or documented failure)
- Phase 17 UAT carry-forward is closed — the infrastructure works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/25-unicode-hardening-uat-closeout/25-02-SUMMARY.md`
</output>
