---
phase: 03-external-integrations
task: pre-execution patching (cross-AI review feedback)
total_tasks: 8 patch items
status: in_progress
last_updated: 2026-04-22T20:00:31.565Z
---

<current_state>
Cross-AI review complete (Gemini + Codex). Both returned PATCH-THEN-PROCEED. Currently applying patches to all 4 plan files on branch `docs/phase3-cross-ai-review`. 3 of 5 patches applied and committed. 2 remaining patches are small (calendar edge cases, defensive input validation). After patches, merge to master and execute.
</current_state>

<completed_work>

- Cross-AI review: Gemini (LOW risk, PATCH-THEN-PROCEED) + Codex (MEDIUM risk, PATCH-THEN-PROCEED). 03-REVIEWS.md written with consensus summary.
- Patch 03-04 (HIGH): Rewrote GitHub connector to use actual MCP_DOCKER tools (list_commits + list_issues + list_pull_requests per repo) instead of fictional list_user_events. Added owner + repos config normalization, partial failure handling, Promise.allSettled composition.
- Patch 03-01 config: Updated connectors.json and schema to add `owner` field, remove `eventTypes` (no longer needed).
- Patch 03-03 (HIGH): Wired `allowedSenders` parameter to `list_recent_messages` MCP call per D-09. Added defensive guards (mcpClient, messageId, to/subject required checks). Updated behavior specs.
- Patch 03-01 validation (in progress): Updated loadConnectorsConfig for full JSON schema validation (not just top-level key checks). Added getConnectorsConfig() memoized lazy wrapper. Behavior specs updated. File modified but not yet committed.
</completed_work>

<remaining_work>

- Patch 03-02: Add calendar edge-case specs (all-day events with no start hour, timezone normalization, self-attendee email matching)
- Patch all plans: Add defensive input validation behavior specs for missing mcpClient parameter across 03-02 and 03-04 (03-03 already done)
- Commit all uncommitted patches
- Update 03-REVIEWS.md consensus section: change gate recommendation from PATCH-THEN-PROCEED to PASS (patches applied)
- Squash-merge docs/phase3-cross-ai-review to master
- Then: `/gsd:execute-phase 3`
</remaining_work>

<decisions_made>

- GitHub connector: list_commits + list_issues + list_pull_requests per repo (verified MCP_DOCKER tool surface — no list_user_events exists)
- GitHub config: owner + repos instead of eventTypes (activity types implicit in tool selection)
- Gmail: pass allowedSenders to server-side MCP call (D-09 trust boundary)
- All connectors: memoized lazy config loader instead of module-level constant (Phase 4 graceful degradation)
- Schema validation: full JSON schema validation, not just top-level key presence check (D-23)
- Dismissed Gemini's credential concern for Calendar/GitHub (managed by Cowork/Docker MCP auth)
- Deferred pagination concern (MCP tools handle pagination at their layer)
- Integration tests remain manual pre-ship per CONTEXT.md design (not in execution plans)
</decisions_made>

<blockers>
- None blocking patches or execution
</blockers>

<human_actions>
- Create gmail-mcp-pete repo skeleton (UsernameTron/gmail-mcp-pete) before 03-03 integration tests — not blocking patches but needed before execute-phase completes
</human_actions>

<context>
Branch `docs/phase3-cross-ai-review` has 5 commits ahead of master (review file + 4 patch commits). 03-01-PLAN.md has uncommitted changes (schema validation + lazy loader patch). The remaining work is mechanical — 2 small patches, then merge. After merge, Phase 3 is ready for /gsd:execute-phase 3 with 4 plans in 2 waves.
</context>

<next_action>
1. Finish patch 03-02 (calendar edge cases: all-day events, timezone, attendee matching)
2. Add mcpClient guard specs to 03-02 and 03-04 behavior sections
3. Commit all patches
4. Update REVIEWS.md gate recommendation to PASS
5. Squash-merge branch to master, delete branch
6. Run `/gsd:execute-phase 3`
</next_action>
