---
phase: 03-external-integrations
task: discuss-phase (area 4 of 4 in progress)
total_tasks: 4 discussion areas + context file generation
status: in_progress
last_updated: 2026-04-22T18:16:29.747Z
---

<current_state>
Mid-flight in `/gsd:discuss-phase 3`. Area 1 (connector approach) fully resolved with 5 probes. Area 4 (composition model) presented — awaiting Pete's pick between sub-options A1 and A2. Areas 2 and 3 not yet started.

Discussion order (Pete-specified): 1 -> 4 -> 2 -> 3.
Areas 2+3 will be discussed in same pass per Pete's instruction.
</current_state>

<completed_work>

- Area 1: Connector Approach — DONE (9 decisions locked)
  - Probe 1a: Cowork native Calendar tools found via ToolSearch
  - Probe 1b: Scheduling = Hybrid (manual/session-start, no headless cron v1)
  - Probe 1c: Calendar connector confirmed reachable from Claude Code terminal (list_calendars returned Pete's calendars)
  - Probe 1d: Docker gmail-mcp found but uses IMAP/SMTP (send capable) — rejected for zero-trust
  - Decision: Calendar = Cowork native, Gmail = custom Node OAuth, GitHub = Docker MCP (already wired)
  - Gmail server: standalone repo, 3 tools only, OAuth gmail.readonly + gmail.compose
  - OAuth: personal Google account, Testing mode consent screen, plaintext credentials chmod 600
</completed_work>

<remaining_work>

- Area 4: Composition model — IN PROGRESS
  - Two sub-options presented:
    - A1: Extend briefing-helpers.js directly (one file, mixed concerns)
    - A2: New src/connectors/ module (calendar.js, gmail.js, github.js) imported by briefing-helpers
  - Recommendation: A2 (clean separation, connector swap = one file change)
  - AWAITING: Pete's pick

- Area 2: Data shaping and filtering — NOT STARTED
  - VIP sender list for Gmail, event filtering for Calendar, repo scoping for GitHub
  - What format do consumers (briefing-helpers, /today) expect?

- Area 3: Error handling and degradation — NOT STARTED
  - Check whether Phase 2 dead-letter patterns (D-35 through D-39) fit one-shot connector reads
  - Or whether SCHED-02's "degraded briefing with warnings" is a separate contract
  - Pete said: don't force-fit

- Write 03-CONTEXT.md and 03-DISCUSSION-LOG.md
- Cross-AI review with Gemini before planning (Gemini CLI needs re-auth: `gemini auth login`)
</remaining_work>

<decisions_made>

- Calendar connector: Cowork native (mcp__claude_ai_Google_Calendar__*) — live in Claude Code
- Calendar write-tool risk: Application-layer read-only policy module required (connector exposes create/update/delete)
- Gmail connector: Custom Node.js MCP server with OAuth (gmail.readonly + gmail.compose scopes)
- Gmail server: standalone repo (not inside second-brain), decoupled lifecycle
- Gmail tool surface: exactly 3 tools (list_recent_messages, get_message_body, create_draft)
- Gmail VIP filter: baked into server logic, not caller-toggleable
- OAuth credentials: ~/.config/gmail-mcp/credentials.json, chmod 600, no Keychain v1
- OAuth client: Pete's personal Google account, Testing mode consent screen
- GitHub connector: Docker MCP already wired, no Phase 3 wiring needed
- Scheduling: Hybrid — manual/session-start primary, headless cron deferred to Phase 4
- Failure mode: single attempt, log on failure, degraded briefing with warning line (SCHED-02)
- ccdScheduledTasksEnabled recurrence syntax: deferred to Phase 4 research, not Phase 3
</decisions_made>

<blockers>
- Gemini CLI needs re-auth (`gemini auth login`) — flagged in sessions 4, 10, 11, 12, 13, 14. Required for cross-AI review.
</blockers>

<context>
Pete is running this discuss-phase with explicit probe-first discipline. He's treating PROJECT.md's "Cowork native preferred" as a hypothesis, not a default — validated Calendar through live probes, rejected Docker Gmail on zero-trust grounds despite it being available in the catalog.

His composition model bias: normalized adapter layer because connector uncertainty exists (Calendar may swap for headless, Gmail may swap for native). The adapter isolates Phase 4 from Phase 3's connector decisions.

For Areas 2+3, he wants them in one pass and explicitly said to check whether Phase 2 dead-letter patterns fit connector reads (they likely don't — dead-letter is for preserving user-captured content, not for read-only data fetches from external services).
</context>

<next_action>
Start with: Resume `/gsd:discuss-phase 3`. Pete picks Area 4 sub-option (A1 vs A2). Then discuss Areas 2+3 together. Then write 03-CONTEXT.md + 03-DISCUSSION-LOG.md. Then cross-AI review with Gemini (needs re-auth first).
</next_action>
