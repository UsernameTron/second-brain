---
phase: 19-semantic-memory-search
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - CLAUDE.md
  - README.md
  - docs/DEVOPS-HANDOFF.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "CLAUDE.md commands table includes /recall --semantic and /recall --hybrid with one-line descriptions"
    - "CLAUDE.md lists VOYAGE_API_KEY under environment variables with a link to Voyage's API docs for key acquisition"
    - "CLAUDE.md test count reflects the post-Phase-19 actual count (run npx jest --listTests | wc -l or parse jest output)"
    - "CLAUDE.md coverage numbers reflect the post-Phase-19 actual values from a fresh `npm run test -- --coverage` invocation"
    - "README.md commands table includes /recall --semantic and /recall --hybrid"
    - "README.md dependencies / tech stack section mentions voyageai@0.2.1"
    - "docs/DEVOPS-HANDOFF.md documents VOYAGE_API_KEY as a required env var for semantic memory features (with acquisition steps)"
    - "docs/DEVOPS-HANDOFF.md documents the ~/.cache/second-brain/ cache path — purpose, files, permissions, retention"
    - "docs/DEVOPS-HANDOFF.md documents memory.semantic.* config keys with defaults and ranges"
    - "Every Phase 19 REQ-ID (MEM-EMBED-01, MEM-SEMANTIC-01, MEM-INDEX-REFRESH-01, MEM-DEGRADE-01) is referenced in at least one of the three docs"
    - "Running the docs-sync agent does NOT invent features, file paths, or test numbers that do not exist — all claims must be verifiable from the live codebase"
  artifacts:
    - path: CLAUDE.md
      provides: "Project governance doc reflecting Phase 19 command surface and test/coverage numbers"
      contains: "--semantic"
    - path: README.md
      provides: "Public-facing doc with new commands and dep"
      contains: "voyageai"
    - path: docs/DEVOPS-HANDOFF.md
      provides: "DevOps reference covering env vars, config, cache path, and degradation behavior"
      contains: "VOYAGE_API_KEY"
  key_links: []
---

<objective>
Update the three living documents (CLAUDE.md, README.md, docs/DEVOPS-HANDOFF.md) to reflect the Phase 19 delivery. This is the day-one documentation surface for the new semantic memory commands — NOT the comprehensive Phase 21 DOCS-FINAL-01 rewrite, which covers all of v1.4.

Purpose: Second Brain's code standards (CLAUDE.md §Documentation Standards) require the three living documents to be updated on every commit that changes the command surface or dependencies. /recall --semantic and /recall --hybrid are new commands, VOYAGE_API_KEY is a new env var, and the cache path ~/.cache/second-brain/ is a new artifact DevOps needs to know about.
Output: Edits to three existing files. No new docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-semantic-memory-search/19-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@README.md
@docs/DEVOPS-HANDOFF.md
@src/semantic-index.js
@src/utils/voyage-health.js
@config/pipeline.json
@package.json

<interfaces>
**Previous session lesson** (tasks/lessons.md 2026-04-24 around Phase 16): the repo-doc-architect agent once invented 3 file paths (src/config/, src/hooks/, src/memory-pipeline.js) and misreported test count (776 vs 799 actual). Do NOT repeat. Every doc change must be grounded in a verifiable fact — `ls` the path, `grep` the symbol, `npx jest --listTests | wc -l` the count.

**Docs-sync agent** (`.claude/agents/docs-sync.md`) is already deployed. This plan invokes it but cross-checks all output against live artifacts before committing.

**Phase 21 DOCS-FINAL-01** is a separate deferred obligation that will do a comprehensive v1.4 docs rewrite covering all 5 phases. Plan 05 does NOT try to solve DOCS-FINAL-01 — only the day-one deltas for Phase 19.

**Test count measurement**:
- Total test files: `npx jest --listTests 2>/dev/null | wc -l`
- Tests run in CI: `CI=true npx jest --listTests 2>/dev/null | wc -l`
- Test cases count: from jest output tail
Prior to Phase 19, CLAUDE.md reports: 799 total across 39 test files (775 passing, 24 UAT skipped in CI).
After Plan 04, ≈ 50 new tests added across 4 new test files.

**Coverage measurement**:
- `npm test -- --coverage 2>&1 | tail -30` shows the summary table. Extract Statements / Branches / Functions / Lines percentages.
- Report branch coverage specifically — that's the reported headline metric.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Compile Phase 19 docs delta from live artifacts</name>
  <files>.planning/phases/19-semantic-memory-search/19-05-DELTA.md</files>
  <read_first>
    - /Users/cpconnor/projects/second-brain/CLAUDE.md (full — see what already exists)
    - /Users/cpconnor/projects/second-brain/README.md (full)
    - /Users/cpconnor/projects/second-brain/docs/DEVOPS-HANDOFF.md (full)
    - /Users/cpconnor/projects/second-brain/src/semantic-index.js (confirm exports actually exist as claimed)
    - /Users/cpconnor/projects/second-brain/config/pipeline.json (confirm memory.semantic.* keys are real)
    - /Users/cpconnor/projects/second-brain/package.json (confirm voyageai@0.2.1 dep)
    - /Users/cpconnor/projects/second-brain/tasks/lessons.md (the 2026-04-24 lesson on inventing paths)
  </read_first>
  <action>
    1. Before editing any doc, build a DELTA file at `.planning/phases/19-semantic-memory-search/19-05-DELTA.md` that lists every factual claim the docs will include, with a verification command next to each. This forces grounding in reality.

       Structure:
       ```markdown
       # Phase 19 Docs Delta

       ## New commands (add to CLAUDE.md + README.md tables)
       | Command | Description | Source |
       |---|---|---|
       | `/recall --semantic <query>` | Semantic search via Voyage AI embeddings | verified: `grep -n "'--semantic'" src/recall-command.js` |
       | `/recall --hybrid <query>` | RRF fusion of keyword + semantic results | verified: `grep -n "'--hybrid'" src/recall-command.js` |

       ## New environment variables (DEVOPS-HANDOFF.md)
       | Var | Required? | Default | Purpose | Source |
       |---|---|---|---|---|
       | `VOYAGE_API_KEY` | Optional (needed for --semantic/--hybrid) | unset | Voyage AI API key for embedding generation | verified: `grep -n "VOYAGE_API_KEY" src/semantic-index.js src/utils/voyage-health.js` |

       ## New config keys (DEVOPS-HANDOFF.md)
       | Key | Default | Range | Purpose | Source |
       |---|---|---|---|---|
       | memory.semantic.model | "voyage-4-lite" | enum | Embedding model | config/pipeline.json:<line> |
       | memory.semantic.threshold | 0.72 | 0-1 | Similarity cutoff | config/pipeline.json:<line> |
       | memory.semantic.recencyDecay | 0.2 | 0-1 | Temporal boost factor | config/pipeline.json:<line> |
       | memory.semantic.rrfK | 60 | ≥1 | RRF k constant | config/pipeline.json:<line> |
       | memory.semantic.candidatesPerSource | 20 | 1-200 | Top-N per source for fusion | config/pipeline.json:<line> |
       | memory.semantic.embedBatchSize | 128 | 1-128 | Voyage batch max | config/pipeline.json:<line> |
       | memory.semantic.timeoutMs | 3000 | ≥100 | Voyage request timeout | config/pipeline.json:<line> |
       | memory.semantic.degradedModeMinutes | 15 | ≥1 | Degraded window length | config/pipeline.json:<line> |
       | memory.semantic.embeddingDim | 1024 | enum | Vector dimension | config/pipeline.json:<line> |

       ## New artifacts (DEVOPS-HANDOFF.md)
       - `~/.cache/second-brain/embeddings.jsonl` — one line per entry, `{hash, embedding, addedAt, category}`. chmod 0600. Permanent (reclaimed only on schema_version change).
       - `~/.cache/second-brain/index-metadata.json` — `{schema_version, updatedAt}`.
       - `~/.cache/second-brain/voyage-health.json` — `{consecutive_failures, last_failure, last_failure_code, degraded_until}`. chmod 0600. Ephemeral.

       ## New dependency
       - voyageai@0.2.1 (production) — verified: `grep -A 1 '"voyageai"' package.json`

       ## Test count (post-Phase-19)
       - Total test files: <run `find test -name '*.test.js' | wc -l`>
       - Total tests: <run `npx jest --listTests 2>/dev/null | wc -l` — no, that lists files; use `npm test -- --passWithNoTests 2>&1 | grep -E "Tests:.*passed" | tail -1` for the case count>
       - CI-skipped: <grep -r "describeMaybe = process.env.CI" test | wc -l> UAT files, each with one describe block

       ## Coverage (post-Phase-19)
       - Branch: <from `npm test -- --coverage 2>&1 | grep -A 1 "All files" | tail -1`>
       - Statements: <same source>
       - Functions: <same source>
       - Lines: <same source>

       ## REQ-ID coverage
       | REQ-ID | Mentioned in | Source |
       |---|---|---|
       | MEM-EMBED-01 | CLAUDE.md or DEVOPS-HANDOFF.md | (will verify during doc edit) |
       | MEM-SEMANTIC-01 | CLAUDE.md or DEVOPS-HANDOFF.md | (will verify during doc edit) |
       | MEM-INDEX-REFRESH-01 | CLAUDE.md or DEVOPS-HANDOFF.md | (will verify during doc edit) |
       | MEM-DEGRADE-01 | CLAUDE.md or DEVOPS-HANDOFF.md | (will verify during doc edit) |
       ```

    2. Actually run each verification command and fill in the `<line>` / count / percentage placeholders with real values. Do NOT proceed to Task 2 until the delta file is fully grounded.

    3. If any expected artifact is missing (e.g., voyage-health.json's key is different from claimed), STOP and report. Do not paper over discrepancies.
  </action>
  <verify>
    <automated>test -f .planning/phases/19-semantic-memory-search/19-05-DELTA.md && grep -c "verified:" .planning/phases/19-semantic-memory-search/19-05-DELTA.md | awk '$1 >= 5 { print "OK" } $1 < 5 { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/19-semantic-memory-search/19-05-DELTA.md` exists
    - At least 5 `verified:` entries with grep/ls commands that actually ran
    - No placeholder like `<line>` or `<from ...>` remains — all values are concrete
    - Any discrepancy between plan-claimed behavior and live code is flagged, not hidden
  </acceptance_criteria>
  <done>
    DELTA.md is a grounded, verifiable source of truth for the three doc edits.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    - /Users/cpconnor/projects/second-brain/CLAUDE.md (full)
    - /Users/cpconnor/projects/second-brain/.planning/phases/19-semantic-memory-search/19-05-DELTA.md (from Task 1)
    - /Users/cpconnor/projects/second-brain/tasks/lessons.md (docs-sync lesson about invented paths)
  </read_first>
  <action>
    Apply ONLY these edits, strictly from DELTA.md:

    1. **Commands table** (currently has /today, /new, /wrap, /promote-memories, /reroute, /promote-unrouted, /recall):
       - Extend the `/recall` entry to mention --semantic and --hybrid flags, OR add separate rows for `/recall --semantic` and `/recall --hybrid`.
       - Keep descriptions one-line and consistent with existing entries' style.

    2. **Tech Stack** section:
       - Add `voyageai 0.2.1` to Key dependencies line near `@anthropic-ai/sdk 0.90+`.

    3. **Project Status** section (test count + coverage lines):
       - Update `Test count:` line with the post-Phase-19 numbers from DELTA.md (total tests, passing, UAT-skipped).
       - Update `Coverage:` line with the post-Phase-19 branch/statements/functions/lines percentages from DELTA.md.
       - Update `Latest Release:` line to add a mention of Phase 19 delivery but keep the v1.3.0 tag line accurate — do NOT invent a v1.4 tag if it hasn't been cut yet. If v1.3.0 is still the latest released tag, keep it and add a separate line like `Phase 19 complete: Semantic Memory Search (YYYY-MM-DD)`.

    4. **Architecture** section (if there's one describing modules) or create a short note: `src/semantic-index.js` is the new Phase 19 module and `src/utils/voyage-health.js` is the Pattern 7 denial tracker. Keep this to 1-2 lines.

    5. Do NOT:
       - Invent files that don't exist (lesson from Phase 16)
       - Modify the `## GSD Workflow Enforcement` section (unchanged)
       - Touch the Developer Profile placeholder
       - Rename or renumber existing milestones
       - Add REQ-IDs that aren't in REQUIREMENTS.md

    6. Verify every line of the edit against DELTA.md. If you can't source a claim, delete it.

    7. Run a grep audit:
       ```
       grep -n "recall" CLAUDE.md | head -10
       grep -n "semantic" CLAUDE.md | head -5
       grep -n "VOYAGE" CLAUDE.md
       grep -n "voyageai" CLAUDE.md
       ```
       All expected terms must appear at least once.
  </action>
  <verify>
    <automated>grep -q "semantic" CLAUDE.md && grep -q "voyageai" CLAUDE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md mentions `/recall --semantic` and `/recall --hybrid` (or a combined row explaining both flags)
    - Test count and coverage numbers match current-from-CI values (not rounded, not stale)
    - voyageai dependency mentioned in tech stack
    - No invented file paths (src/hooks/, src/config/ etc. that don't exist) — verified by `ls` of every path cited
    - Existing sections (GSD workflow, developer profile, etc.) untouched
  </acceptance_criteria>
  <done>
    CLAUDE.md reflects Phase 19 delivery with zero invented claims.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update README.md</name>
  <files>README.md</files>
  <read_first>
    - /Users/cpconnor/projects/second-brain/README.md (full)
    - /Users/cpconnor/projects/second-brain/.planning/phases/19-semantic-memory-search/19-05-DELTA.md
  </read_first>
  <action>
    1. Add /recall --semantic and /recall --hybrid to the commands table (public-facing style — can be more casual than CLAUDE.md but still accurate).

    2. If README has a "What's New" or version section, append a one-liner about Phase 19 (semantic retrieval + hybrid fusion + graceful degradation).

    3. If README has a "Setup" / "Install" section, add a line about the optional `VOYAGE_API_KEY` env var with a pointer to the DEVOPS-HANDOFF for details.

    4. If README has an "Installation" or "Dependencies" block listing major deps, add voyageai 0.2.1.

    5. Do NOT restructure the README or add new sections that weren't already there. Minimal additive edits only.

    6. Verify with grep:
       ```
       grep -n "semantic\\|hybrid\\|VOYAGE" README.md
       ```
       Must find matches.
  </action>
  <verify>
    <automated>grep -q "semantic" README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - README.md mentions `--semantic` and `--hybrid` flags in the commands section
    - Mentions `VOYAGE_API_KEY` (or points at DEVOPS-HANDOFF for env var setup)
    - No structural changes — only additive edits to existing sections
    - No invented file paths or commands
  </acceptance_criteria>
  <done>
    Public-facing README reflects Phase 19.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update docs/DEVOPS-HANDOFF.md</name>
  <files>docs/DEVOPS-HANDOFF.md</files>
  <read_first>
    - /Users/cpconnor/projects/second-brain/docs/DEVOPS-HANDOFF.md (full)
    - /Users/cpconnor/projects/second-brain/.planning/phases/19-semantic-memory-search/19-05-DELTA.md
  </read_first>
  <action>
    This is the doc DevOps reads. It gets the most detail:

    1. **Environment variables** section:
       - Add `VOYAGE_API_KEY` with: purpose, acquisition steps (sign up at voyage.ai, copy key from dashboard), scope (required for --semantic/--hybrid, optional otherwise), rotation guidance (document how to rotate — new key in .env, restart any long-running process).

    2. **Configuration** section (if exists) or new subsection:
       - Document the memory.semantic.* block with all 9 keys, their defaults, ranges, and what they tune. Include a note that `pipeline.local.json` overlays production values for dev tuning.

    3. **Cache / Disk** section (create if missing):
       - Document `~/.cache/second-brain/` directory:
         - Path: `~/.cache/second-brain/` (or `$CACHE_DIR_OVERRIDE` for tests)
         - Files: embeddings.jsonl, index-metadata.json, voyage-health.json
         - Permissions: 0700 dir, 0600 files
         - Size estimate: ~4KB per entry × N entries (e.g., 500-entry vault ≈ 2 MB)
         - Backup: NOT required — regeneratable from memory.md via self-heal; sensitive data not stored here
         - Cleanup: deletion triggers full re-embed on next /recall --semantic

    4. **Degradation behavior** subsection:
       - Document the 3-failure threshold and 15-minute degraded-mode window
       - What operators see: `(semantic unavailable — using keyword only)` banner in /recall output; stderr warning on 401; ~/.cache/second-brain/voyage-health.json has the state
       - Recovery: one successful Voyage call resets the counter

    5. **Known tech debt / deferred** section:
       - Note that Phase 21 will handle HYG-JSDOC-01, HYG-CONSOLE-01 including the Phase 19 modules
       - FUT-HNSW-01 flagged for when vault exceeds ~20K entries
       - FUT-RERANK-01 deferred; RRF is adequate for v1.4

    6. **REQ-ID coverage**: ensure MEM-EMBED-01, MEM-SEMANTIC-01, MEM-INDEX-REFRESH-01, MEM-DEGRADE-01 each appear in the relevant subsection.

    7. If the doc has a "Deployment maturity" or "Release checklist" section, add a post-Phase-19 checkbox item: "VOYAGE_API_KEY provisioned in deployment environment (if semantic features are enabled)".

    8. Verify with grep:
       ```
       grep -c "VOYAGE_API_KEY\\|memory.semantic\\|embeddings.jsonl\\|MEM-" docs/DEVOPS-HANDOFF.md
       ```
       Must find multiple matches.
  </action>
  <verify>
    <automated>grep -c "VOYAGE_API_KEY" docs/DEVOPS-HANDOFF.md | awk '$1 >= 2 { print "OK" } $1 < 2 { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - docs/DEVOPS-HANDOFF.md contains sections or subsections covering: VOYAGE_API_KEY, memory.semantic.* config keys, ~/.cache/second-brain/ path, degradation behavior, deferred tech debt
    - All 4 Phase 19 REQ-IDs (MEM-EMBED-01, MEM-SEMANTIC-01, MEM-INDEX-REFRESH-01, MEM-DEGRADE-01) appear at least once
    - No claim contradicts the actual code (e.g. says threshold default is 0.8 when code says 0.72)
    - File permissions documented correctly: dir 0700, files 0600
    - Cache path is correctly noted as regeneratable (no backup required)
  </acceptance_criteria>
  <done>
    DevOps has everything they need to deploy and operate Phase 19 features.
  </done>
</task>

</tasks>

<verification>
- `grep -l "semantic" CLAUDE.md README.md docs/DEVOPS-HANDOFF.md` lists all three files
- `grep -l "VOYAGE_API_KEY" docs/DEVOPS-HANDOFF.md` matches
- `grep -c "MEM-" docs/DEVOPS-HANDOFF.md` ≥ 4 (all four Phase 19 REQ-IDs)
- Every file path cited in the three docs exists (run `ls` on each one from the diff)
- Every command cited in the three docs resolves (run `grep -l '<command>' src/` for each)
- Test count and coverage numbers in CLAUDE.md match a fresh `npm test -- --coverage` invocation
</verification>

<success_criteria>
- DevOps can deploy Phase 19 using DEVOPS-HANDOFF.md alone
- A new contributor can find the --semantic flag documented in README.md
- CLAUDE.md is accurate — no invented paths, no stale numbers
- Lesson captured in 2026-04-24 ("docs-sync agent invented 3 file paths") is honored throughout
</success_criteria>

<output>
After completion, create `.planning/phases/19-semantic-memory-search/19-05-SUMMARY.md` noting which doc sections were edited, what the test count/coverage deltas were, and confirming every claim was grounded in DELTA.md.
</output>
