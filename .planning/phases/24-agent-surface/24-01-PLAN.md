---
phase: 24-agent-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/test-verifier.md
  - test/agents/test-verifier-mode.test.js
autonomous: true
requirements:
  - AGENT-VERIFY-01

must_haves:
  truths:
    - "test-verifier agent accepts 'phase-close N' or 'verify requirements: REQ-ID1, REQ-ID2' invocation and returns per-REQ-ID verdicts"
    - "Each REQ-ID gets a verdict of PASS, FAIL, or UNTESTED based on targeted Jest runs against *.test.js files"
    - "The agent still functions in its original full-suite mode when invoked without phase-close phrasing"
  artifacts:
    - path: ".claude/agents/test-verifier.md"
      provides: "Phase-Closure Verification Mode instructions"
      contains: "Phase-Closure Verification Mode"
    - path: "test/agents/test-verifier-mode.test.js"
      provides: "Tests confirming the agent instruction contract is parseable and complete"
      contains: "AGENT-VERIFY-01"
  key_links:
    - from: ".claude/agents/test-verifier.md"
      to: "PLAN.md files in .planning/phases/"
      via: "REQ-ID grep against test/ --include=*.test.js"
      pattern: "grep -rl .* --include=.*.test.js"
---

<objective>
Expand the test-verifier agent with a Phase-Closure Verification Mode that reports per-REQ-ID pass/fail verdicts.

Purpose: At phase-close time, the orchestrator (human or GSD) needs actionable signal per requirement — not just "all tests pass." This mode maps each REQ-ID to its test file(s) via grep, runs them in isolation, and emits a structured verdict table.

Output: Updated `.claude/agents/test-verifier.md` with dual-mode instructions and a lightweight contract test confirming the mode's instruction set is complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-agent-surface/24-RESEARCH.md

<interfaces>
Current test-verifier agent at .claude/agents/test-verifier.md:
  Frontmatter: name=test-verifier, tools=Read/Grep/Glob/Bash, model=haiku
  Output: "Test Verification Report" with Suite/Tests/Verdict lines
  Constraint: read-only, never modifies files, runs full suite every time

Precedent: docs-sync.md dual-mode agent pattern (Phase 23):
  Has a "Phase-Closure Audit Mode" section triggered by specific invocation phrasing
  Same agent, two modes, mode triggered by invocation phrasing
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase-Closure Verification Mode to test-verifier agent</name>
  <files>.claude/agents/test-verifier.md, test/agents/test-verifier-mode.test.js</files>
  <read_first>
    - .claude/agents/test-verifier.md (current agent file — preserve existing full-suite mode)
    - .claude/agents/docs-sync.md (dual-mode pattern precedent from Phase 23)
    - .planning/phases/24-agent-surface/24-RESEARCH.md (Pattern 1: Agent Instruction-Set Extension)
  </read_first>
  <behavior>
    - Test 1: test-verifier.md contains the string "Phase-Closure Verification Mode"
    - Test 2: test-verifier.md contains invocation trigger patterns "phase-close" and "verify requirements:"
    - Test 3: test-verifier.md contains the grep scope restriction "--include=*.test.js"
    - Test 4: test-verifier.md contains all three verdict values: "PASS", "FAIL", "UNTESTED"
    - Test 5: test-verifier.md retains the original "Test Verification Report" output format (backward compat)
    - Test 6: test-verifier.md contains "AGENT-VERIFY-01" as a self-referencing requirement tag
  </behavior>
  <action>
    1. Create `test/agents/test-verifier-mode.test.js` with the 6 contract tests above. Each test reads `.claude/agents/test-verifier.md` via `fs.readFileSync` and asserts the presence of required strings. Tag the file header with `// Requirement: AGENT-VERIFY-01`.

    2. Run tests — they MUST fail (RED) because the agent file does not yet have the new section.

    3. Add a new `## Phase-Closure Verification Mode` section to `.claude/agents/test-verifier.md` AFTER the existing `## Output Format` section. The section must contain:

    **Trigger:** Activated when invoked with phrasing containing `"phase-close <N>"` or `"verify requirements: REQ-ID1, REQ-ID2"`. If neither pattern is present, use the standard full-suite mode above.

    **Steps (in agent instruction language):**
    ```
    1. Parse REQ-IDs from the invocation. Accept comma-separated IDs after "verify requirements:" or extract from `.planning/phases/NN-*/NN-*-PLAN.md` frontmatter `requirements:` fields when given a phase number.
    2. For each REQ-ID:
       a. Run: grep -rl "$REQ_ID" test/ --include="*.test.js"
       b. If no files found: verdict = UNTESTED
       c. If files found: run `npx jest <files> --no-coverage --silent`
       d. verdict = PASS if exit code 0, FAIL otherwise
    3. Emit the Phase-Close Verification Report (see output format below).
    ```

    **Output format for Phase-Closure mode:**
    ```
    Phase-Close Verification Report
    ================================
    Phase: <N> -- <phase-name>
    REQ-IDs checked: <count>

    | REQ-ID | Test File(s) | Verdict |
    |--------|-------------|---------|
    | <ID>   | <files>     | PASS/FAIL/UNTESTED |

    Overall: PASS (<N>/<N> requirements verified) | FAIL (<N> failures, <N> untested)
    ```

    **Constraints for this mode:**
    - Scope grep to `--include="*.test.js"` to avoid false positives from fixture/snapshot/planning files
    - UNTESTED is a distinct verdict (not PASS) — it signals a coverage gap, not a success
    - Never modify test files; read-only observation only
    - Report UNTESTED count separately in the Overall line

    Also add `// Requirement: AGENT-VERIFY-01` as a comment line at the very bottom of the agent file (after the closing section).

    4. Run tests — they MUST pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/second-brain && npx jest test/agents/test-verifier-mode.test.js --no-coverage --verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/test-verifier.md contains "## Phase-Closure Verification Mode"
    - .claude/agents/test-verifier.md contains 'grep -rl "$REQ_ID" test/ --include="*.test.js"'
    - .claude/agents/test-verifier.md contains "UNTESTED"
    - .claude/agents/test-verifier.md contains "Phase-Close Verification Report"
    - .claude/agents/test-verifier.md retains "Test Verification Report" (original mode preserved)
    - .claude/agents/test-verifier.md contains "AGENT-VERIFY-01"
    - test/agents/test-verifier-mode.test.js contains "AGENT-VERIFY-01"
    - test/agents/test-verifier-mode.test.js exits 0
  </acceptance_criteria>
  <done>test-verifier agent has dual-mode capability: original full-suite mode is unchanged, new Phase-Closure mode accepts REQ-ID lists and returns per-ID verdicts with PASS/FAIL/UNTESTED. All 6 contract tests pass.</done>
</task>

</tasks>

<verification>
- `npx jest test/agents/test-verifier-mode.test.js --no-coverage` exits 0
- `grep "Phase-Closure Verification Mode" .claude/agents/test-verifier.md` returns a match
- `grep "Test Verification Report" .claude/agents/test-verifier.md` returns a match (original mode preserved)
- `grep "AGENT-VERIFY-01" .claude/agents/test-verifier.md test/agents/test-verifier-mode.test.js` returns matches in both files
</verification>

<success_criteria>
The test-verifier agent can be invoked with "verify requirements: AGENT-VERIFY-01, AGENT-MEMORY-01" and its instructions describe how to produce a per-REQ-ID verdict table. The original full-suite mode is untouched. Contract tests confirm the instruction set is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/24-agent-surface/24-01-SUMMARY.md`
</output>
