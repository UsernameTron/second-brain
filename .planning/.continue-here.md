---
phase: v1.6-audit-remediation
task: null
total_tasks: 19
status: paused
mode: autonomous-on-resume
last_updated: 2026-07-12T17:14:41.852Z
---

<current_state>
Fable 5 full-depth audit is COMPLETE and committed. Report at
`.planning/research/FABLE5-AUDIT-2026-07-12.md` (commit fd3946c) on branch
`docs/fable5-audit-2026-07-12`. No remediation has started — that is the next
session's autonomous work. 19 verified findings (16 Quick Wins, 3 v1.6 candidates).
The report's section 4 (Quick Wins) and section 5 (v1.6 milestone block) ARE the plan to execute.
</current_state>

<completed_work>

- Fable 5 audit across 6 dimensions (config, hooks, agents/commands, skills, context economy, pipeline/CI)
- Orchestrated via Workflow tool: 44 agents (6 finders + 21 skeptics + 17 diff-checks), 0 errors
- 21 findings proposed -> 2 refuted by adversarial verification (A-02, B-02) -> 19 confirmed
- Report written + committed (fd3946c); all 4 verification gates passed (single-file write, exclusion grep clean, 5 sections, all finding detail present)
</completed_work>

<remaining_work>
Execute the audit remediation. Full dependency-ordered command sequence is in the report section 4.

Quick Wins (16, paste-ready, apply in this order):
1. B-01 — re-arm git hooks FIRST (cp pre-push into hooks/, `git config core.hooksPath hooks`)
2. F-01 — restore semantic excluded-terms gate (highest ROI; confirm loadExcludedTerms exported first)
3. F-03, F-04, B-03 — code cleanups + post-merge coverage guard
4. F-02 — git rm orphan schema
5. E-01, A-03, C-05, E-03, E-04 — CLAUDE.md edits (one pass)
6. D-01, D-02 — config-validator / pipeline-health SKILL.md hygiene
7. C-03 — docs-sync scope guard
8. C-02 — rm empty agent-memory dir
9. A-01 — prune allowlist

v1.6 candidates (3, via /gsd:new-milestone using report section 5):
- C-01 — ship /reroute wrapper (use r.to, NOT r.target — diff-check caught the bug)
- E-02 — SessionStart staleness hook (decision: threshold N days)
- C-04 — wire docs-sync audit mode to a trigger, or mark manual-only (decision: which event)
</remaining_work>

<decisions_made>

- Audit orchestrated with parallel fan-out + adversarial verification (ultracode opt-in)
- Report committed on `docs/fable5-audit-2026-07-12`, NOT master (branch-first rule + never-commit-master memory)
- A-02 (auto-test friction) and B-02 (log rotation) dropped — refuted against actual file bytes; auto-test.sh already path-scopes to src/*.js, and the log is gitignored/immaterial
</decisions_made>

<blockers>
None. Note: the audit's own top finding F-01 is a live security gap (semantic /recall
ships queries to the external Voyage API with the excluded-terms gate silently disabled).
</blockers>

<context>
The report IS the plan — no separate planning artifact needed. B-01 must run first because
it re-arms the local pre-commit hook (schema-validate + vault-boundary) that has been silently
no-op'ing; every commit AFTER B-01 will actually be gated, so config/vault edits must pass
those gates. Two unpushed items exist: b948d79 on master (pre-existing, flagged at session
start) and fd3946c on the docs branch. Neither is pushed. GSD is the authoritative workflow:
Quick Wins can batch through /gsd:quick; the 3 v1.6 candidates go through /gsd:new-milestone
then plan/execute. E-02 and C-04 carry genuine design decisions — surface the approach choice
at that point rather than guessing.
</context>

<next_action>
Resume on branch `docs/fable5-audit-2026-07-12`. Open
`.planning/research/FABLE5-AUDIT-2026-07-12.md`, then apply section 4 Quick Wins in dependency
order — start with B-01 (re-arm hooks), then F-01 (semantic gate). Run `CI=true npx jest
--forceExit` after code changes to confirm green; ship as a PR. Then `/gsd:new-milestone` from
section 5 for C-01, E-02, C-04. Never commit to master — branch first.
</next_action>
