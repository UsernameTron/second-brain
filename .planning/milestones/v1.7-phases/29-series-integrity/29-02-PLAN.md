---
phase: 29-series-integrity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config/scheduling.json
  - ~/Library/LaunchAgents/com.secondbrain.today.plist
autonomous: true
requirements: [STATS-PIPE-03]

must_haves:
  truths:
    - "launchctl list shows com.secondbrain.today loaded"
    - "The launch agent runs /today scheduled mode weekday 06:45 America/Chicago"
    - "config/scheduling.json documents RemoteTrigger as vault-unreachable and stays disabled by design"
  artifacts:
    - path: "~/Library/LaunchAgents/com.secondbrain.today.plist"
      provides: "weekday 06:45 launchd trigger for /today scheduled mode"
      contains: "com.secondbrain.today"
    - path: "config/scheduling.json"
      provides: "notes documenting RemoteTrigger disabled-by-design + launchd as primary"
      contains: "remote_trigger_disabled"
  key_links:
    - from: "~/Library/LaunchAgents/com.secondbrain.today.plist"
      to: "src/today-command.js runToday({ mode: 'scheduled' })"
      via: "ProgramArguments node -e invocation"
      pattern: "runToday"
---

<objective>
Land a daily-stats row every weekday without manual action by scheduling /today
locally via macOS launchd, and record in config why the cloud RemoteTrigger is
permanently disabled.

RemoteTrigger runs in a cloud environment (env_01TjBJLSRwHfpUPcNVUK99Kb) that
cannot reach the local vault (VAULT_ROOT=~/Claude Cowork), so its writes go into
the void — an architecture mismatch, not a config bug. A local launchd agent
replaces it as the primary scheduler.

Purpose: satisfies STATS-PIPE-03.
Output: ~/Library/LaunchAgents/com.secondbrain.today.plist (loaded), and
config/scheduling.json notes documenting the disabled RemoteTrigger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@config/scheduling.json
@config/schema/scheduling.schema.json

<interfaces>
<!-- Facts the executor needs. Do not explore. -->

Canonical repo path (NOT this worktree): /Users/cpconnor/projects/second-brain
Node binary (from `which node`): /opt/homebrew/opt/node@22/bin/node
Vault root default: /Users/cpconnor/Claude Cowork  (space in path — quote it)

Scheduled-run entry point (already used by the RemoteTrigger prompt in scheduling.json):
```
node -e "require('./src/today-command').runToday({ mode: 'scheduled' }).then(r => console.log(r.briefing || r.error))"
```
runToday accepts mode 'interactive' | 'scheduled' | 'dry-run'; scheduled writes the
daily note and records stats (non-dry-run path).

scheduling.schema.json constraints (draft-07, additionalProperties:false):
- `trigger` object: additionalProperties=false — CANNOT add new keys there.
- `notes` object: additionalProperties = { type: string } — arbitrary string keys ARE allowed.
- `fallback_chain`: array of strings.
So documentation must live under `notes` (or fallback_chain), never as a new trigger key.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and load the com.secondbrain.today launchd agent</name>
  <files>~/Library/LaunchAgents/com.secondbrain.today.plist</files>
  <read_first>
    - config/scheduling.json (the existing RemoteTrigger prompt shows the exact runToday invocation to mirror)
    - src/today-command.js (lines 41-46: VAULT_ROOT default; confirm runToday scheduled mode is the non-dry-run path)
  </read_first>
  <action>
    First confirm the node path and repo path on this machine:
    ```bash
    which node          # expect /opt/homebrew/opt/node@22/bin/node
    ls -d /Users/cpconnor/projects/second-brain   # canonical repo (NOT the .claude/worktrees copy)
    ```
    Use the actual `which node` output if it differs.

    Write ~/Library/LaunchAgents/com.secondbrain.today.plist with:
    - Label: com.secondbrain.today
    - ProgramArguments: [ <node-abs-path>, "-e",
      "require('./src/today-command').runToday({ mode: 'scheduled' }).then(r => console.log(r.briefing || r.error)).catch(e => { console.error(e); process.exit(1); })" ]
    - WorkingDirectory: /Users/cpconnor/projects/second-brain
    - StartCalendarInterval: an ARRAY of 5 dicts, one per weekday (Weekday 1,2,3,4,5), each with Hour 6 and Minute 45 (local time — launchd uses the machine's local tz, America/Chicago)
    - EnvironmentVariables: { PATH = "/opt/homebrew/opt/node@22/bin:/usr/local/bin:/usr/bin:/bin",
      VAULT_ROOT = "/Users/cpconnor/Claude Cowork" }
    - StandardOutPath: /Users/cpconnor/Library/Logs/com.secondbrain.today.out.log
    - StandardErrorPath: /Users/cpconnor/Library/Logs/com.secondbrain.today.err.log
    - RunAtLoad: false

    Exact plist body:
    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
    <plist version="1.0">
    <dict>
      <key>Label</key>
      <string>com.secondbrain.today</string>
      <key>ProgramArguments</key>
      <array>
        <string>/opt/homebrew/opt/node@22/bin/node</string>
        <string>-e</string>
        <string>require('./src/today-command').runToday({ mode: 'scheduled' }).then(r =&gt; console.log(r.briefing || r.error)).catch(e =&gt; { console.error(e); process.exit(1); })</string>
      </array>
      <key>WorkingDirectory</key>
      <string>/Users/cpconnor/projects/second-brain</string>
      <key>EnvironmentVariables</key>
      <dict>
        <key>PATH</key>
        <string>/opt/homebrew/opt/node@22/bin:/usr/local/bin:/usr/bin:/bin</string>
        <key>VAULT_ROOT</key>
        <string>/Users/cpconnor/Claude Cowork</string>
      </dict>
      <key>StartCalendarInterval</key>
      <array>
        <dict><key>Weekday</key><integer>1</integer><key>Hour</key><integer>6</integer><key>Minute</key><integer>45</integer></dict>
        <dict><key>Weekday</key><integer>2</integer><key>Hour</key><integer>6</integer><key>Minute</key><integer>45</integer></dict>
        <dict><key>Weekday</key><integer>3</integer><key>Hour</key><integer>6</integer><key>Minute</key><integer>45</integer></dict>
        <dict><key>Weekday</key><integer>4</integer><key>Hour</key><integer>6</integer><key>Minute</key><integer>45</integer></dict>
        <dict><key>Weekday</key><integer>5</integer><key>Hour</key><integer>6</integer><key>Minute</key><integer>45</integer></dict>
      </array>
      <key>StandardOutPath</key>
      <string>/Users/cpconnor/Library/Logs/com.secondbrain.today.out.log</string>
      <key>StandardErrorPath</key>
      <string>/Users/cpconnor/Library/Logs/com.secondbrain.today.err.log</string>
      <key>RunAtLoad</key>
      <false/>
    </dict>
    </plist>
    ```

    Validate, then load and verify:
    ```bash
    plutil -lint ~/Library/LaunchAgents/com.secondbrain.today.plist
    launchctl unload ~/Library/LaunchAgents/com.secondbrain.today.plist 2>/dev/null || true
    launchctl load -w ~/Library/LaunchAgents/com.secondbrain.today.plist
    launchctl list | grep secondbrain
    ```
    If `launchctl load` errors on this macOS version, fall back to:
    `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.secondbrain.today.plist`
    then re-run `launchctl list | grep secondbrain`.

    Do NOT trigger the job now (`launchctl kickstart`) — it would write a real row into the
    live vault mid-plan. Loading is sufficient; it fires at the next weekday 06:45.
  </action>
  <verify>
    <automated>plutil -lint ~/Library/LaunchAgents/com.secondbrain.today.plist && launchctl list | grep secondbrain</automated>
  </verify>
  <acceptance_criteria>
    - `plutil -lint ~/Library/LaunchAgents/com.secondbrain.today.plist` prints "OK".
    - `launchctl list | grep secondbrain` prints a line containing `com.secondbrain.today`.
    - `/usr/libexec/PlistBuddy -c "Print :StartCalendarInterval:0:Minute" ~/Library/LaunchAgents/com.secondbrain.today.plist` returns `45`.
    - `/usr/libexec/PlistBuddy -c "Print :StartCalendarInterval:0:Hour" ~/Library/LaunchAgents/com.secondbrain.today.plist` returns `6`.
    - `grep -c "runToday" ~/Library/LaunchAgents/com.secondbrain.today.plist` >= 1.
    - `/usr/libexec/PlistBuddy -c "Print :WorkingDirectory" ~/Library/LaunchAgents/com.secondbrain.today.plist` returns `/Users/cpconnor/projects/second-brain`.
  </acceptance_criteria>
  <done>The com.secondbrain.today agent is a lint-clean plist, loaded in launchctl, scheduled for weekday 06:45 local, and will run /today scheduled mode without manual action.</done>
</task>

<task type="auto">
  <name>Task 2: Document RemoteTrigger disabled-by-design in config/scheduling.json</name>
  <files>config/scheduling.json</files>
  <read_first>
    - config/scheduling.json (current structure — trigger.enabled is false; notes object at bottom)
    - config/schema/scheduling.schema.json (notes allows arbitrary string keys; trigger is additionalProperties:false — do NOT add trigger keys)
  </read_first>
  <action>
    Edit config/scheduling.json. Keep `trigger.enabled` = false (do not enable it). Add two
    keys to the existing top-level `notes` object (which the schema permits as arbitrary
    strings) — preserve the existing `test_trigger_deleted` key:

    ```json
    "notes": {
      "test_trigger_deleted": "Test trigger trig_01NHx3Uz8ni8eZSfsMph4miY deleted 2026-04-22.",
      "remote_trigger_disabled": "RemoteTrigger runs in cloud environment env_01TjBJLSRwHfpUPcNVUK99Kb which cannot reach local VAULT_ROOT (~/Claude Cowork); its /today writes go into the void. Disabled by design (trigger.enabled=false), not a config bug. v1.7 Phase 29 STATS-PIPE-03.",
      "local_scheduler": "Primary scheduler is macOS launchd job com.secondbrain.today (~/Library/LaunchAgents/com.secondbrain.today.plist), weekday 06:45 America/Chicago local, running /today scheduled mode with WorkingDirectory=/Users/cpconnor/projects/second-brain. Added v1.7 Phase 29."
    }
    ```

    Also reorder `fallback_chain` so launchd is documented as primary (string edits only,
    stays a string array — schema-safe):
    ```json
    "fallback_chain": [
      "macOS launchd (PRIMARY — com.secondbrain.today, weekday 06:45 local, only path that reaches the local vault)",
      "CronCreate (secondary — .claude/scheduled_tasks.json, 7-day auto-expiry)",
      "RemoteTrigger (disabled — cloud env cannot reach local vault; see notes.remote_trigger_disabled)"
    ]
    ```

    Do not add, remove, or rename any `trigger` object keys (schema additionalProperties:false).
    After editing, confirm the JSON still validates against the schema.
  </action>
  <verify>
    <automated>node -e "const s=require('./config/scheduling.json'); if(s.trigger.enabled!==false) throw new Error('trigger must stay disabled'); if(!s.notes.remote_trigger_disabled||!s.notes.local_scheduler) throw new Error('notes missing'); console.log('scheduling.json OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('config/scheduling.json','utf8'))"` exits 0 (valid JSON).
    - `grep -c "remote_trigger_disabled" config/scheduling.json` == 1.
    - `grep -c "local_scheduler" config/scheduling.json` == 1.
    - `grep "vault-unreachable\|cannot reach local" config/scheduling.json` returns a match.
    - `node -e "const s=require('./config/scheduling.json'); process.exit(s.trigger.enabled===false?0:1)"` exits 0 (trigger stays disabled).
    - The config-validator skill (or the project AJV validation of scheduling against scheduling.schema.json) reports the file valid — no additionalProperties violation.
  </acceptance_criteria>
  <done>config/scheduling.json documents RemoteTrigger as vault-unreachable and disabled-by-design, names launchd as primary, and still validates against scheduling.schema.json with trigger.enabled=false.</done>
</task>

</tasks>

<verification>
- `plutil -lint` on the plist passes and `launchctl list | grep secondbrain` shows the job loaded.
- The plist runs runToday scheduled mode at weekday 06:45 local with the correct WorkingDirectory and node path.
- config/scheduling.json still valid JSON, still schema-valid, trigger.enabled stays false, and documents the disabled RemoteTrigger + local launchd primary.
</verification>

<success_criteria>
- STATS-PIPE-03: a local launchd job runs /today scheduled mode weekday 06:45 local so a daily-stats row lands without manual action, and config/scheduling.json documents RemoteTrigger as vault-unreachable and permanently disabled.
</success_criteria>

<output>
After completion, create `.planning/phases/29-series-integrity/29-02-SUMMARY.md`
</output>
