---
phase: 30-outcome-instrumentation
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified: []
autonomous: false
requirements: [STATS-OUTCOME-01, STATS-OUTCOME-02]
must_haves:
  truths:
    - "A real non-dry-run /today produces a daily-stats.md row with 11 columns"
    - "recall_hits <= recall_count in the live row"
    - "echo_shown is 0 or 1 and echo_score is a number or the em dash"
    - "No query text appears in daily-stats.md or any counter JSON"
  artifacts:
    - path: "RIGHT/daily-stats.md"
      provides: "live 11-column daily-stats row (vault surface — not created in CI)"
      contains: "recall_hits"
  key_links:
    - from: "live /today"
      to: "RIGHT/daily-stats.md"
      via: "recordDailyStats writes 11-column row with echo + recall_hits"
      pattern: "recall_hits"
---

<objective>
Confirm the retrieval-outcome instrumentation works end-to-end against the LIVE vault — the one thing CI cannot prove, because RIGHT/daily-stats.md does not exist yet and the vault is unreachable from the isolated worktree/cloud env. A real /recall must record a hit, and a real non-dry-run /today must write an 11-column daily-stats row with valid echo and recall-hit values and zero query-text leakage.

Purpose: Phase 30 success criterion 3 ("A real /today run produces a daily-stats.md row with 11 columns") is intrinsically a live check. This gate closes it.

Output: human-confirmed live daily-stats row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-outcome-instrumentation/30-01-SUMMARY.md
@.planning/phases/30-outcome-instrumentation/30-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live 11-column daily-stats row verification</name>
  <action>Pause for the operator to run a live /recall and non-dry-run /today against the real vault, then inspect RIGHT/daily-stats.md. This is a manual verification gate — no code changes. See how-to-verify for exact steps.</action>
  <what-built>End-to-end retrieval-outcome instrumentation from Plans 01-02: /recall records hit/miss after results, and non-dry-run /today records Memory Echo shown/score and writes an 11-column daily-stats row.</what-built>
  <how-to-verify>
    Run from the REAL checkout /Users/cpconnor/projects/second-brain (NOT the isolated worktree — the live vault at ~/Claude Cowork must be reachable):
    1. Run one real recall: `node scripts/recall.js "compounding memory" --hybrid` — confirm it returns results (or a clean no-results line, no crash).
    2. Run one real non-dry-run /today against the live vault (scheduled or interactive mode — the mode that writes RIGHT/daily-stats.md).
    3. Open `RIGHT/daily-stats.md` in the vault and confirm:
       - Header row has 11 columns ending in `recall_hits | echo_shown | echo_score`.
       - Today's data row has 11 cells.
       - `recall_hits` <= `recall_count`.
       - `echo_shown` is 0 or 1; `echo_score` is a number (e.g. 0.72) or the em dash.
    4. Confirm NO query text (none of the words from your recall query) appears anywhere in `RIGHT/daily-stats.md` or in `~/.cache/second-brain/daily-counters-*.json`.
  </how-to-verify>
  <resume-signal>Type "approved", or paste the row / describe the mismatch if any column, value, or query-text check fails.</resume-signal>
</task>

</tasks>

<verification>
- Human confirms an 11-column live row exists with recall_hits <= recall_count and valid echo_shown/echo_score.
- Human confirms no query text in daily-stats.md or counter JSONs.
</verification>

<success_criteria>
- RIGHT/daily-stats.md has an 11-column header and a today row with 11 cells.
- recall_hits <= recall_count; echo_shown in {0,1}; echo_score numeric or em dash.
- No query text on the vault surface or in counter files.
</success_criteria>

<output>
After completion, create `.planning/phases/30-outcome-instrumentation/30-03-SUMMARY.md`
</output>
