---
phase: 30-outcome-instrumentation
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/recall-command.js
  - src/today-command.js
  - test/recall-command.test.js
  - test/today-command.test.js
autonomous: true
requirements: [STATS-OUTCOME-01, STATS-OUTCOME-02]
must_haves:
  truths:
    - "A /recall that returns >=1 result and is not blocked records a hit; a blocked or empty /recall records the invocation but not a hit"
    - "recordRecallInvocation is called AFTER results are computed so hit/resultCount are known"
    - "Every non-dry-run /today calls recordEchoShown with whether Memory Echo had entries and its top score"
    - "A non-dry-run /today passes recallHits, echoShown, echoScore from readDailyCounters into recordDailyStats, producing an 11-column row"
    - "Internal callers (_internal: true) still suppress the recall invocation counter"
    - "No query text is passed to any stats function"
  artifacts:
    - path: "src/recall-command.js"
      provides: "hit-aware recordRecallInvocation call placed after topN/blocked are known"
      contains: "recordRecallInvocation"
    - path: "src/today-command.js"
      provides: "recordEchoShown call after getMemoryEcho; echo fields passed to recordDailyStats"
      contains: "recordEchoShown"
  key_links:
    - from: "src/recall-command.js runRecall"
      to: "daily-stats.recordRecallInvocation"
      via: "call after results with { hit: topN.length > 0 && !blocked, resultCount: topN.length }"
      pattern: "recordRecallInvocation\\(\\{"
    - from: "src/today-command.js"
      to: "daily-stats.recordEchoShown"
      via: "call right after getMemoryEcho with memoryEcho.entries.length > 0 and memoryEcho.score"
      pattern: "recordEchoShown\\("
    - from: "src/today-command.js recordDailyStats block"
      to: "readDailyCounters recallHits/echoShown/echoScore"
      via: "pass counters.recallHits/echoShown/echoScore into recordDailyStats stats object"
      pattern: "recallHits: counters"
---

<objective>
Wire the /recall and /today commands to the outcome-instrumentation contracts created in Plan 01. /recall records a hit/miss after results are computed; /today records whether Memory Echo was shown and its top score, then flows those counters into the daily-stats row. Result: a real /today produces an 11-column daily-stats row capturing retrieval value.

Purpose: the counters and columns from Plan 01 are inert until the commands populate them. This plan connects the user-visible surfaces.

Output: src/recall-command.js and src/today-command.js wired to daily-stats, with integration tests proving hit/miss accounting and the 11-column echo-bearing row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-outcome-instrumentation/30-01-SUMMARY.md

<interfaces>
<!-- Contracts delivered by Plan 01 (src/daily-stats.js). Do not re-derive — use these. -->

recordRecallInvocation(opts = {}) — opts: { hit?: boolean, resultCount?: number, now?: Date }
  Always increments recallCount; increments recallHits only when opts.hit is truthy. Never throws.

recordEchoShown(shown, score, opts = {}) — shown: boolean, score: number, opts: { now?: Date }
  Persists echoShown (0|1) and echoScore to the day's counter file. Never throws.

readDailyCounters(opts = {}) returns:
  { proposals, promotions, recallCount, recallHits, echoShown, echoScore, avgConfidence }

recordDailyStats(stats, opts?) — stats now also reads:
  { recallHits, echoShown, echoScore } (in addition to proposals, promotions, totalEntries, memoryKb, recallCount, avgLatencyMs, avgConfidence)

<!-- Current /recall counter call (recall-command.js ~lines 69-74) — placed BEFORE results, must move: -->
  if (!options._internal) {
    try {
      const { recordRecallInvocation } = require('./daily-stats');
      recordRecallInvocation();
    } catch (_) { /* briefing-is-the-product */ }
  }

<!-- Where results become known in runRecall (~lines 124-125): -->
  const topN = hits.slice(0, flags.top);
  const empty = topN.length === 0 && !blocked;
  // ... `blocked` is set earlier in the try block for hybrid/semantic blocked paths.

<!-- /today Memory Echo (today-command.js ~lines 230-237) — getMemoryEcho returns { entries: [...], score }: -->
  let memoryEcho;
  const memEchoStart = Date.now();
  try {
    memoryEcho = await getMemoryEcho(connectorResults, { threshold: echoThreshold });
  } catch (_err) {
    memoryEcho = { entries: [], score: 0, skipped: true };
  }
  latencies.memoryEcho = Date.now() - memEchoStart;

<!-- /today stats block (today-command.js ~lines 312-350, mode !== 'dry-run'): -->
  let counters = { proposals: 0, promotions: 0, recallCount: 0, avgConfidence: null };
  try { counters = readDailyCounters(); } catch (_) {}
  ...
  recordDailyStats({ proposals, promotions, totalEntries, memoryKb, recallCount, avgLatencyMs, avgConfidence });
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /recall records hit/miss after results are known</name>
  <files>src/recall-command.js, test/recall-command.test.js</files>
  <read_first>
    - src/recall-command.js (full file — runRecall, the early recordRecallInvocation call ~lines 69-74, and where topN/empty/blocked are computed ~lines 124-125)
    - test/recall-command.test.js (mock setup ~lines 32/62/107 and the _internal + blocked cases ~lines 136-156)
  </read_first>
  <action>
    In src/recall-command.js, move the recall-counter call so hit/miss is known:

    1. DELETE the current early block (~lines 69-74) that calls `recordRecallInvocation()` before the search runs.

    2. After `topN` and `empty` are computed and `blocked` is final (immediately after the line `const empty = topN.length === 0 && !blocked;`, ~line 125), add:
    ```js
    // STATS-OUTCOME-01: record hit/miss AFTER results are known. Internal callers
    // (Memory Echo morning hit) still suppress the counter via _internal.
    // Query text is NEVER passed — content policy (daily-stats.md is exportable).
    if (!options._internal) {
      try {
        const { recordRecallInvocation } = require('./daily-stats');
        recordRecallInvocation({ hit: topN.length > 0 && !blocked, resultCount: topN.length });
      } catch (_) { /* briefing-is-the-product: never break recall on stats failure */ }
    }
    ```
    Placement note: this sits after the main try/catch (which sets hits=[] on crash) so a crashed search records a non-hit invocation. `blocked` is in scope (declared at top of runRecall). Do not pass `query` or any snippet text.

    In test/recall-command.test.js:
    - Add a test: a keyword search returning >=1 result calls recordRecallInvocation with an object whose `hit === true` and `resultCount` equals the number of results.
    - Add a test: a blocked hybrid/semantic recall (res.blocked = true) calls recordRecallInvocation with `hit === false` (invocation counted, hit not).
    - Add a test: an empty result set calls recordRecallInvocation with `hit === false`.
    - Update the existing _internal test (~line 136) to confirm recordRecallInvocation is NOT called when `options._internal` is true.
    - Update mock assertions that expected `recordRecallInvocation()` with no args to expect the new object argument.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/second-brain && CI=true npx jest test/recall-command.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "recordRecallInvocation({ hit: topN.length > 0 && !blocked, resultCount: topN.length })" src/recall-command.js` matches
    - `grep -c "recordRecallInvocation()" src/recall-command.js` returns 0 (old no-arg call removed)
    - The recordRecallInvocation call appears AFTER `const empty = topN.length === 0` in file order: `grep -n "const empty = topN.length" src/recall-command.js` line number < `grep -n "recordRecallInvocation({ hit" src/recall-command.js` line number
    - `grep -n "query" src/recall-command.js` shows no query passed into any recordRecallInvocation call
    - `CI=true npx jest test/recall-command.test.js` passes with new hit/blocked/empty/_internal assertions
  </acceptance_criteria>
  <done>recordRecallInvocation fires once per non-internal /recall, after results, with hit = (>=1 result AND not blocked); tests cover hit, blocked, empty, and _internal suppression; no query text passed.</done>
</task>

<task type="auto">
  <name>Task 2: /today records Memory Echo outcome and writes the 11-column row</name>
  <files>src/today-command.js, test/today-command.test.js</files>
  <read_first>
    - src/today-command.js (Memory Echo block ~lines 222-237, and the non-dry-run stats block ~lines 312-355)
    - test/today-command.test.js (setupRecordStatsMocks ~lines 1258-1318 — mocks daily-stats with recordDailyStats + readDailyCounters)
    - .planning/phases/30-outcome-instrumentation/30-01-SUMMARY.md (confirms recordEchoShown + readDailyCounters new fields)
  </read_first>
  <action>
    In src/today-command.js:

    1. Right after `latencies.memoryEcho = Date.now() - memEchoStart;` (~line 237), record the echo outcome (non-fatal, mode-agnostic — the counter is only read into a row on non-dry-run runs; echo happens the same either way, but guard the WRITE so dry-run practice does not pollute counters). Add:
    ```js
    // STATS-OUTCOME-02: record whether Memory Echo was shown and its top score.
    // Skip in dry-run so practice runs don't pollute the day's counters. Non-fatal.
    if (mode !== 'dry-run') {
      try {
        const { recordEchoShown } = require('./daily-stats');
        recordEchoShown(memoryEcho.entries.length > 0, memoryEcho.score);
      } catch (_) { /* briefing-is-the-product */ }
    }
    ```

    2. In the non-dry-run stats block, extend the `counters` default object and the `recordDailyStats` call to carry the three new fields. Change the default init to:
    ```js
    let counters = { proposals: 0, promotions: 0, recallCount: 0, recallHits: 0, echoShown: 0, echoScore: 0, avgConfidence: null };
    ```
    and extend the recordDailyStats stats object with:
    ```js
    recallHits: counters.recallHits,
    echoShown: counters.echoShown,
    echoScore: counters.echoScore,
    ```
    (append these alongside the existing recallCount/avgLatencyMs/avgConfidence fields). Do not pass any query text.

    In test/today-command.test.js:
    - Update `setupRecordStatsMocks` (~line 1310) to add `recordEchoShown: jest.fn()` to the daily-stats mock, and return it so tests can assert on it. Also add `flushMissedDays: jest.fn()` if not present (currently required by today-command; adding it prevents the silent-catch masking).
    - Update the `readDailyCountersImpl` default (~line 1268) to include `recallHits: 1, echoShown: 1, echoScore: 0.72` so the passed-through row can be asserted.
    - Add a test: a non-dry-run (mode 'scheduled') run calls recordEchoShown once, and calls recordDailyStats with a stats object containing recallHits, echoShown, and echoScore equal to the mocked counter values.
    - Add a test: a dry-run does NOT call recordEchoShown.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/second-brain && CI=true npx jest test/today-command.test.js 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "recordEchoShown(memoryEcho.entries.length > 0, memoryEcho.score)" src/today-command.js` matches
    - The recordEchoShown call is guarded by `mode !== 'dry-run'`: within ~5 lines above the call, `grep -n "mode !== 'dry-run'" src/today-command.js` appears
    - `grep -n "recallHits: counters.recallHits" src/today-command.js` and `grep -n "echoShown: counters.echoShown" src/today-command.js` and `grep -n "echoScore: counters.echoScore" src/today-command.js` all match
    - `grep -n "query" src/today-command.js` shows no query text passed to recordEchoShown or recordDailyStats
    - `CI=true npx jest test/today-command.test.js` passes with a test asserting recordDailyStats received recallHits/echoShown/echoScore and a test asserting dry-run skips recordEchoShown
  </acceptance_criteria>
  <done>Non-dry-run /today records echo shown/score and passes recallHits/echoShown/echoScore into recordDailyStats; dry-run skips the echo write; integration tests green.</done>
</task>

</tasks>

<verification>
- `CI=true npx jest test/recall-command.test.js test/today-command.test.js` passes.
- `grep -n "query" src/recall-command.js src/today-command.js` confirms no query text reaches any stats function.
- Live end-to-end verification (11-column row against the real vault) is Plan 03's blocking human-verify checkpoint.
</verification>

<success_criteria>
- src/recall-command.js calls recordRecallInvocation({ hit, resultCount }) after results, gated on !_internal.
- src/today-command.js calls recordEchoShown after getMemoryEcho (non-dry-run) and passes recallHits/echoShown/echoScore into recordDailyStats.
- A real /today produces an 11-column daily-stats row.
- No query text is written to any counter or stats file.
</success_criteria>

<output>
After completion, create `.planning/phases/30-outcome-instrumentation/30-02-SUMMARY.md`
</output>
