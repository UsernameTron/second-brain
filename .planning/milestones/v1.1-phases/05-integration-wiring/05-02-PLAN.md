---
phase: 05-integration-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config/excluded-terms.json
  - src/content-policy.js
  - src/classifier.js
autonomous: false
requirements: [INTEG-03]
must_haves:
  truths:
    - "A capture containing 'genesys' (any case, anywhere in text) is blocked by the ingress filter"
    - "config/excluded-terms.json contains 15-20 entries covering former employers, project codenames, client names, internal tools"
    - "Substring matching works — 'mygenesysproject' triggers the filter (no word-boundary requirement)"
    - "classifier.js runStage0 reads excluded terms correctly from bare-array JSON (no silent empty list)"
  artifacts:
    - path: "config/excluded-terms.json"
      provides: "Expanded excluded terms blocklist"
      min_entries: 15
    - path: "src/content-policy.js"
      provides: "Updated matching logic — substring + case-insensitive, content lowercased once"
      contains: "contentLower"
    - path: "src/classifier.js"
      provides: "Fixed config-shape parsing for excluded terms"
      contains: "Array.isArray"
  key_links:
    - from: "src/content-policy.js"
      to: "config/excluded-terms.json"
      via: "fs.readFileSync + JSON.parse"
      pattern: "excluded-terms\\.json"
    - from: "src/vault-gateway.js"
      to: "src/content-policy.js"
      via: "checkContent(content, config.excludedTerms)"
      pattern: "checkContent"
    - from: "src/classifier.js"
      to: "config/excluded-terms.json"
      via: "fs.readFileSync + JSON.parse"
      pattern: "excluded-terms\\.json"
---

<!-- REVISION (reviews mode): Changes from original 05-02-PLAN.md
  1. [HIGH] classifier.js schema mismatch: Added Task 1a to fix the `parsed.terms || []`
     bug. Verified classifier.js IS in active ingress path (imported by new-command.js,
     reroute.js, promote-unrouted.js). Bare array requires `Array.isArray` check.
  2. [MEDIUM] toLowerCase() performance: Task 1 now lowercases content once outside the
     loop (`const contentLower = content.toLowerCase()`) instead of per-term.
  3. Added classifier.js to files_modified and must_haves.
-->

<objective>
Expand excluded terms to 15-20 entries, update matching from word-boundary to substring + case-insensitive, and fix classifier.js config-shape bug.

Purpose: INTEG-03 requires former-employer and project-codename captures to be silently dropped. The current blocklist has only 3 entries and uses word-boundary regex matching (which misses substrings like "mygenesysproject"). Per D-06, matching must be substring + case-insensitive. Additionally, classifier.js has a bug where it expects `{terms: [...]}` but the file is a bare array, causing exclusion to silently not work through the classifier code path (used by new-command.js, reroute.js, promote-unrouted.js).

Output: An expanded blocklist, updated content-policy.js with optimized substring matching, and fixed classifier.js that correctly reads the bare-array format.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-wiring/05-CONTEXT.md

<interfaces>
<!-- Current content-policy.js matching logic (lines 155-158, 195-197): -->
<!-- Uses word-boundary regex: new RegExp('\\b' + escapeRegex(term) + '\\b', 'i') -->
<!-- Must change to substring with content lowercased once outside loop -->

<!-- Current excluded-terms.json format: bare JSON array -->
<!-- ["ISPN", "Genesys", "Asana"] -->

<!-- Three consumers of excluded-terms.json: -->
<!-- 1. vault-gateway.js loadConfig() — reads as bare array, passes to checkContent -->
<!-- 2. classifier.js runStage0() — reads and does parsed.terms || [] -->
<!--    BUG: classifier expects {terms: [...]} wrapper but file is bare array -->
<!--    This means classifier currently gets [] (empty) — exclusion not working there -->
<!-- 3. content-policy.js — receives terms from caller, does not read file directly -->

<!-- classifier.js is in ACTIVE ingress path: -->
<!-- - new-command.js line 40: require('./classifier') -->
<!-- - reroute.js line 96: require('./classifier') -->
<!-- - promote-unrouted.js line 103: require('./classifier') -->

From src/content-policy.js:
  - checkContent(content, excludedTerms, contextChars) — Stage 1 keyword scan, Stage 2 Haiku classification
  - sanitizeContent(content, excludedTerms) — removes paragraphs containing excluded terms
  - Both use: new RegExp('\\b' + escapeRegex(term) + '\\b', 'i')
  - Must change to substring matching per D-06

From src/classifier.js (line 88-93):
  - runStage0() reads excluded-terms.json:
    const raw = fs.readFileSync(termsPath, 'utf8');
    const parsed = JSON.parse(raw);
    excludedTerms = parsed.terms || [];
  - BUG: parsed is an array, parsed.terms is undefined, excludedTerms becomes []
  - Fix: use Array.isArray(parsed) ? parsed : (parsed.terms || [])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update content-policy.js matching and fix classifier.js config-shape bug</name>
  <files>src/content-policy.js, src/classifier.js</files>
  <read_first>
    - src/content-policy.js (full file — understand both checkContent and sanitizeContent matching logic)
    - src/classifier.js (lines 85-100 — runStage0 excluded terms loading)
    - config/excluded-terms.json (current 3 entries — verify format is bare array)
    - src/vault-gateway.js (lines 425-440 — how it calls checkContent and sanitizeContent)
  </read_first>
  <action>
    **Change 1: content-policy.js checkContent function (around line 196)**

    Replace:
    ```js
    const regex = new RegExp('\\b' + escapeRegex(term) + '\\b', 'i');
    if (regex.test(content)) {
    ```
    With (lowercase content ONCE outside the loop, per review feedback on toLowerCase() performance):
    ```js
    const contentLower = content.toLowerCase();
    ```
    Place `contentLower` BEFORE the `for (const term of excludedTerms)` loop. Then inside the loop:
    ```js
    if (contentLower.includes(term.toLowerCase())) {
    ```

    **Change 2: content-policy.js sanitizeContent function (around line 155-158)**

    Replace:
    ```js
    const hasExcludedTerm = excludedTerms.some(term => {
      const regex = new RegExp('\\b' + escapeRegex(term) + '\\b', 'i');
      return regex.test(paragraph);
    });
    ```
    With:
    ```js
    const paragraphLower = paragraph.toLowerCase();
    const hasExcludedTerm = excludedTerms.some(term =>
      paragraphLower.includes(term.toLowerCase())
    );
    ```

    Update JSDoc comments near these lines to reflect "case-insensitive substring" instead of "word-boundary".

    **Change 3: classifier.js runStage0 (around line 93)**

    Replace:
    ```js
    excludedTerms = parsed.terms || [];
    ```
    With:
    ```js
    excludedTerms = Array.isArray(parsed) ? parsed : (parsed.terms || []);
    ```

    This handles both the current bare-array format AND any future wrapped format.

    Do NOT modify the `classifyWithHaiku` function or the `escapeRegex` helper in content-policy.js.
    Do NOT modify `src/vault-gateway.js`.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/second-brain && node -e "
      const cp = require('./src/content-policy');
      // Test substring match (no word boundary)
      cp.checkContent('working on mygenesysproject today', ['Genesys']).then(r => {
        console.log('substring match:', r.decision);
        if (r.decision !== 'BLOCK') throw new Error('Expected BLOCK, got ' + r.decision);
      });
    " && node -e "
      // Verify classifier.js reads bare array correctly
      const fs = require('fs');
      const path = require('path');
      const raw = fs.readFileSync('./config/excluded-terms.json', 'utf8');
      const parsed = JSON.parse(raw);
      const terms = Array.isArray(parsed) ? parsed : (parsed.terms || []);
      console.log('classifier terms count:', terms.length);
      if (terms.length < 3) throw new Error('Expected >= 3 terms, got ' + terms.length);
    " && echo "All checks pass"</automated>
  </verify>
  <acceptance_criteria>
    - src/content-policy.js checkContent function does NOT contain `new RegExp('\\b'` in the keyword scan loop
    - src/content-policy.js sanitizeContent function does NOT contain `new RegExp('\\b'` in the excludedTerms.some callback
    - src/content-policy.js checkContent contains `contentLower = content.toLowerCase()` BEFORE the for loop (lowercased once)
    - src/content-policy.js checkContent contains `contentLower.includes(term.toLowerCase())`
    - src/content-policy.js sanitizeContent contains `paragraphLower = paragraph.toLowerCase()` BEFORE the some() call
    - src/content-policy.js sanitizeContent contains `paragraphLower.includes(term.toLowerCase())`
    - src/classifier.js line ~93 contains `Array.isArray(parsed)` (handles bare array)
    - src/classifier.js does NOT contain the exact string `parsed.terms || []` without the Array.isArray guard
    - `escapeRegex` function still exists in content-policy.js (used by classifyWithHaiku)
    - Existing tests pass: `cd /Users/cpconnor/projects/second-brain && npx jest test/content-policy --passWithNoTests` exits 0
  </acceptance_criteria>
  <done>Matching logic is substring + case-insensitive with content lowercased once per call. classifier.js correctly reads bare-array format. "mygenesysproject" triggers the filter through both code paths.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Collect excluded terms from user</name>
  <files></files>
  <action>
    Pause and collect the list of 12-17 additional excluded terms from the user. Per D-07, categories are: former employer product names, former employer company names, former project codenames, former client names, former internal tool names. Current entries: ISPN, Genesys, Asana. User provides the remaining terms.

    Present the request: "Please provide the list of additional excluded terms (12-17 entries). Categories: former employer product/company names, project codenames, client names, internal tool names. Current list has: ISPN, Genesys, Asana."
  </action>
  <verify>User has provided a list of 12-17 additional terms</verify>
  <done>User-provided terms collected, ready to write to config/excluded-terms.json in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Expand config/excluded-terms.json with user-provided terms</name>
  <files>config/excluded-terms.json</files>
  <read_first>
    - config/excluded-terms.json (current content — bare JSON array with 3 entries)
  </read_first>
  <action>
    Replace the contents of `config/excluded-terms.json` with the expanded list combining:
    - The existing 3 entries: "ISPN", "Genesys", "Asana"
    - The user-provided terms from Task 2

    Format: bare JSON array (matching current format — do NOT wrap in `{terms: [...]}`). Sort alphabetically for maintainability. Deduplicate (case-insensitive). Example:
    ```json
    ["Asana", "Genesys", "ISPN", "TermA", "TermB", ...]
    ```

    Verify the total count is between 15 and 20 inclusive.

    After writing the file, run the existing test suite to confirm nothing breaks with the expanded list.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/second-brain && node -e "const terms = require('./config/excluded-terms.json'); console.log('count:', terms.length); console.log('has ISPN:', terms.includes('ISPN')); console.log('has Genesys:', terms.includes('Genesys')); console.log('has Asana:', terms.includes('Asana')); if (terms.length < 15 || terms.length > 20) throw new Error('Expected 15-20 terms, got ' + terms.length)"</automated>
  </verify>
  <acceptance_criteria>
    - config/excluded-terms.json is a valid JSON array (bare array, not wrapped in {terms: [...]})
    - Array length is between 15 and 20 inclusive
    - Array contains "ISPN", "Genesys", "Asana" (original entries preserved)
    - Array is sorted alphabetically
    - No duplicate entries (case-insensitive)
    - `npx jest --passWithNoTests` exits 0 (no regressions)
  </acceptance_criteria>
  <done>Excluded terms expanded to 15-20 entries. Filter catches all former-employer and project-codename references via substring matching through both content-policy and classifier code paths.</done>
</task>

</tasks>

<verification>
- `node -e "const t = require('./config/excluded-terms.json'); console.log(t.length >= 15 && t.length <= 20)"` prints `true`
- Submitting a capture containing "genesys" (lowercase, embedded in another word) through vault-gateway results in BLOCK decision
- `grep 'Array.isArray' src/classifier.js` confirms classifier.js config-shape fix
- `grep 'contentLower' src/content-policy.js` confirms content is lowercased once (not per-term)
- `npx jest` passes with no regressions
- content-policy.js does not use word-boundary regex for excluded term matching
</verification>

<success_criteria>
- INTEG-03 satisfied: 15-20 excluded terms active, substring + case-insensitive matching in place
- Former employer names, project codenames, client names, and internal tool names are all covered
- Existing ingress filter (vault-gateway -> content-policy) correctly blocks captures containing any excluded term as a substring
- classifier.js code path (new-command, reroute, promote-unrouted) also correctly loads and applies excluded terms
- Content is lowercased once per call, not once per term (performance fix)
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-wiring/05-02-SUMMARY.md`
</output>
