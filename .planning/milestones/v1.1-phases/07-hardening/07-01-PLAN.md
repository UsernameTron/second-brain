---
phase: 07-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .nvmrc
  - README.md
autonomous: true
requirements:
  - CI-01
must_haves:
  truths:
    - "A push to master triggers a GitHub Actions workflow that runs the full test suite on Node 20 and 22"
    - "A PR against master triggers the same workflow"
    - "A failing test causes the CI run to report failure in the PR checks panel"
    - "A green CI badge is visible at the top of README.md"
    - "Branch protection on master requires CI to pass before merging"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow definition"
      contains: "npm test"
    - path: ".nvmrc"
      provides: "Local Node version pin"
      contains: "22"
    - path: "README.md"
      provides: "CI status badge"
      contains: "actions/workflows/ci.yml/badge.svg"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json scripts.test"
      via: "npm test step"
      pattern: "run: npm test"
    - from: "README.md"
      to: ".github/workflows/ci.yml"
      via: "badge URL referencing workflow name"
      pattern: "badge.svg"
---

<objective>
Create a GitHub Actions CI pipeline that runs the full test suite (502+ tests) on every push to master and every PR, with Node 20+22 matrix, README badge, and branch protection.

Purpose: Prevent broken code from reaching master. CI-01 requirement — final hardening gate for v1.1 go-live.
Output: Working CI pipeline, visible badge, enforced branch protection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hardening/07-CONTEXT.md
@package.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CI workflow and .nvmrc</name>
  <files>.github/workflows/ci.yml, .nvmrc</files>
  <read_first>
    - package.json (confirm test script is `jest --verbose`)
    - .planning/phases/07-hardening/07-CONTEXT.md (decisions D-01 through D-03)
  </read_first>
  <action>
Create `.nvmrc` containing exactly `22` (per D-02).

Create `.github/workflows/ci.yml` with this exact structure:

```yaml
name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
      - run: npm ci
      - run: npm test
```

Per D-01: Node 20 and 22 LTS matrix.
Per D-03: Triggers on push to master and PRs only — no feature branch pushes, no cron.
Cache strategy uses setup-node built-in npm cache (Claude's discretion per CONTEXT.md).
  </action>
  <verify>
    <automated>cat .github/workflows/ci.yml && cat .nvmrc && echo "---" && grep -c "node-version:" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml exists and contains `name: CI`
    - .github/workflows/ci.yml contains `push:` with `branches: [master]`
    - .github/workflows/ci.yml contains `pull_request:` with `branches: [master]`
    - .github/workflows/ci.yml contains `node-version: [20, 22]`
    - .github/workflows/ci.yml contains `run: npm ci`
    - .github/workflows/ci.yml contains `run: npm test`
    - .github/workflows/ci.yml contains `cache: 'npm'`
    - .nvmrc exists and contains `22`
  </acceptance_criteria>
  <done>CI workflow file is committed and .nvmrc pins Node 22 for local dev</done>
</task>

<task type="auto">
  <name>Task 2: Add README badge and enable branch protection</name>
  <files>README.md</files>
  <read_first>
    - README.md (see current first line for badge insertion point)
    - .planning/phases/07-hardening/07-CONTEXT.md (decisions D-04, D-05)
  </read_first>
  <action>
**Badge (per D-04):** Insert a CI status badge as the very first line of README.md, before the `# Second Brain` heading:

```
[![CI](https://github.com/UsernameTron/second-brain/actions/workflows/ci.yml/badge.svg)](https://github.com/UsernameTron/second-brain/actions/workflows/ci.yml)
```

Then a blank line, then the existing `# Second Brain` heading.

**Branch protection (per D-05):** Run `gh api` to enable branch protection on master requiring the CI checks to pass:

```bash
gh api repos/UsernameTron/second-brain/branches/master/protection \
  --method PUT \
  --field required_status_checks='{"strict":true,"contexts":["test (20)","test (22)"]}' \
  --field enforce_admins=false \
  --field required_pull_request_reviews=null \
  --field restrictions=null
```

The check context names `test (20)` and `test (22)` match the GitHub Actions job name `test` with the matrix value appended. If the `gh api` call fails due to auth or permissions, log the error and note it for manual setup — do not block the plan on this.
  </action>
  <verify>
    <automated>head -3 README.md && echo "---" && gh api repos/UsernameTron/second-brain/branches/master/protection --jq '.required_status_checks.contexts' 2>/dev/null || echo "Branch protection check requires auth — verify manually in GitHub Settings > Branches"</automated>
  </verify>
  <acceptance_criteria>
    - README.md first line contains `[![CI](https://github.com/UsernameTron/second-brain/actions/workflows/ci.yml/badge.svg)]`
    - README.md second line is blank
    - README.md third line is `# Second Brain`
    - Branch protection API call attempted (success or documented failure)
  </acceptance_criteria>
  <done>README shows CI badge at top, branch protection requires CI pass before merge to master</done>
</task>

</tasks>

<verification>
1. `.github/workflows/ci.yml` exists with correct triggers, matrix, and test step
2. `.nvmrc` contains `22`
3. README.md has CI badge as first line
4. Branch protection enabled on master (or documented as needing manual setup)
5. Push the branch and verify the workflow runs in GitHub Actions
</verification>

<success_criteria>
- CI workflow triggers on next push to master or PR
- Test suite (502+ tests) runs on both Node 20 and 22
- Failing test causes red X in PR checks
- Green badge visible on README.md in GitHub
- Master branch protected — PRs cannot merge without passing CI
</success_criteria>

<output>
After completion, create `.planning/phases/07-hardening/07-01-SUMMARY.md`
</output>
