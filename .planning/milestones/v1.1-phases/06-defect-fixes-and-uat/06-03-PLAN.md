---
phase: 06-defect-fixes-and-uat
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - test/uat/uat-classification.test.js
  - test/uat/uat-wikilinks.test.js
autonomous: false
requirements:
  - UAT-01
  - UAT-02
  - UAT-03

must_haves:
  truths:
    - "10+ diverse synthetic captures routed through classifier produce correct left/right classification at >80% accuracy"
    - "Promoted memories produce at least one meaningful wikilink back-reference to existing vault content"
    - "The full capture-to-classify-to-propose-to-promote flow works end-to-end in Obsidian vault UI"
  artifacts:
    - path: "test/uat/uat-classification.test.js"
      provides: "Classification accuracy validation against 10+ synthetic inputs"
      contains: "accuracy"
    - path: "test/uat/uat-wikilinks.test.js"
      provides: "Wikilink relevance validation against real vault content"
      contains: "wikilink"
  key_links:
    - from: "test/uat/uat-classification.test.js"
      to: "src/classifier.js"
      via: "Imports and invokes classify() with synthetic inputs"
      pattern: "classify"
    - from: "test/uat/uat-wikilinks.test.js"
      to: "src/wikilink-engine.js"
      via: "Imports and invokes wikilink generation with test content"
      pattern: "wikilink"
---

<objective>
Validate the system against real-world conditions: classification accuracy (UAT-01), wikilink relevance (UAT-02), and end-to-end Obsidian UX (UAT-03).

Purpose: Prove the capture-to-memory flow works correctly with diverse inputs before declaring go-live.
Output: UAT test scripts with results, human-verified end-to-end walkthrough.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-defect-fixes-and-uat/06-CONTEXT.md

<interfaces>
From src/classifier.js:
```javascript
const { createHaikuClient, loadPipelineConfig } = require('./pipeline-infra');

// Stage 1 classification — returns {side: 'LEFT'|'RIGHT', confidence, reasoning}
async function classifyContent(content, options = {}) { ... }

// Stage 2 escalation for low-confidence results
async function escalateClassification(content, stage1Result, options = {}) { ... }
```

From src/wikilink-engine.js:
```javascript
// Generate wikilinks for content against vault index
async function generateWikilinks(content, options = {}) { ... }
// Returns array of {target, relevance, context} objects
```

From src/new-command.js:
```javascript
// Full /new pipeline: classify -> route -> propose
async function executeNew(input, options = {}) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classification accuracy validation (UAT-01)</name>
  <files>test/uat/uat-classification.test.js</files>
  <read_first>
    - src/classifier.js (full file — understand classifyContent signature, return shape, options)
    - src/content-policy.js (excluded terms list — captures containing these should be filtered)
    - config/pipeline.json (classification config: confidence thresholds, escalation rules)
    - test/classifier.test.js (existing test patterns, mock setup for LLM calls)
  </read_first>
  <action>
Per D-06: Create synthetic test inputs covering each domain. This is a test-harness script, NOT a unit test — it calls the real classifier (requires ANTHROPIC_API_KEY) against synthetic inputs and reports accuracy.

Create `test/uat/uat-classification.test.js` with:

1. **Test corpus** — 12+ synthetic captures with expected classification:
   - LEFT (voice/reflections): "I've been thinking about how my career has shifted since leaving enterprise software. The move to AI feels right." (expected: LEFT)
   - LEFT (personal draft): "Dear Mom, wanted to let you know we're planning to visit in June." (expected: LEFT)
   - LEFT (journal): "Today was rough. Three meetings back to back and I couldn't focus on what matters." (expected: LEFT)
   - RIGHT (technical note): "Claude Code hooks fire on PreToolUse and PostToolUse events. The matcher pattern uses regex." (expected: RIGHT)
   - RIGHT (meeting summary): "Meeting with the team: decided to use Docker MCP gateway for all external integrations." (expected: RIGHT)
   - RIGHT (project note): "Second Brain Phase 5 complete. RemoteTrigger ID: trig_01KvxeDfYDAEwAzw9zw9DKKB." (expected: RIGHT)
   - RIGHT (code snippet): "function createHaikuClient(options = {}) { return createLlmClient({...options, model: 'claude-haiku-4-5'}); }" (expected: RIGHT)
   - RIGHT (process doc): "The memory promotion flow: capture -> classify -> propose -> review -> promote." (expected: RIGHT)
   - EDGE (ambiguous): "I think the architecture we chose for the vault was the right call — it feels natural." (could be either, classify should not crash)
   - EDGE (excluded term): "The ISPN project had some interesting patterns we could reuse." (should be filtered by content policy before classification)
   - EDGE (very short): "Quick note: buy milk" (should classify without crashing)
   - EDGE (empty-ish): "..." (should handle gracefully)

2. **Test harness:**
   - Skip entire suite if `ANTHROPIC_API_KEY` is not set (use `describe.skip` or conditional)
   - For each input, call `classifyContent(input)` and record the result
   - Compare result.side against expected side
   - Calculate accuracy: correct / total (excluding edge cases from accuracy denominator)
   - Assert accuracy >= 0.8 (80%)
   - Log each result for manual review: input snippet, expected, actual, confidence

3. **Jest config:** Add `testPathPattern` or tag so UAT tests can be run separately: `npx jest test/uat/ --verbose`

Create `test/uat/` directory if it doesn't exist.
  </action>
  <verify>
    <automated>ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY npx jest test/uat/uat-classification.test.js --verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - test/uat/uat-classification.test.js exists
    - File contains at least 10 test inputs with expected LEFT/RIGHT classification
    - File contains accuracy calculation with `>= 0.8` or `>= 80` threshold assertion
    - File contains `describe.skip` or `process.env.ANTHROPIC_API_KEY` guard for environments without API key
    - `npx jest test/uat/uat-classification.test.js --verbose` exits 0 (either runs and passes, or skips cleanly)
  </acceptance_criteria>
  <done>12+ synthetic captures classified with >80% accuracy; results logged for review; test passes or skips cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Wikilink relevance validation (UAT-02)</name>
  <files>test/uat/uat-wikilinks.test.js</files>
  <read_first>
    - src/wikilink-engine.js (full file — understand generateWikilinks signature, vault index scanning, return shape)
    - test/wikilink-engine.test.js (existing test patterns, mock setup)
    - config/pipeline.json (wikilink config if any)
  </read_first>
  <action>
Per D-07: Validate that promoted memories produce meaningful cross-references against existing vault content at `~/Claude Cowork/`.

Create `test/uat/uat-wikilinks.test.js` with:

1. **Test inputs** — 5+ memory content samples that should link to existing vault notes:
   - A memory about "daily briefing" — should link to notes about /today, daily prep, or similar
   - A memory about "memory promotion" — should link to notes about proposals, memory pipeline
   - A memory about "left/right architecture" — should link to vault structure notes
   - A memory about "Claude Code hooks" — should link to any hook-related notes
   - A memory about "Gmail integration" — should link to connector or integration notes

2. **Test harness:**
   - Skip if VAULT_ROOT is not set or `~/Claude Cowork/` doesn't exist
   - Skip if ANTHROPIC_API_KEY is not set (wikilink engine may use LLM)
   - For each input, call the wikilink generation function
   - Assert: at least one wikilink returned per input (non-empty array)
   - Assert: wikilink targets are actual files that exist in the vault (spot check)
   - Log results for manual review: input snippet, generated wikilinks, target existence

3. **Relevance check:** For each generated wikilink, verify the target file exists. If the wikilink engine returns links to non-existent notes, flag as a concern but don't fail the test (the engine may generate aspirational links).
  </action>
  <verify>
    <automated>ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY npx jest test/uat/uat-wikilinks.test.js --verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - test/uat/uat-wikilinks.test.js exists
    - File contains at least 5 test memory content samples
    - File contains vault existence check (fs.existsSync or similar) for wikilink targets
    - File contains skip guard for missing VAULT_ROOT or ANTHROPIC_API_KEY
    - `npx jest test/uat/uat-wikilinks.test.js --verbose` exits 0 (either runs and passes, or skips cleanly)
  </acceptance_criteria>
  <done>Promoted memories produce wikilinks; at least some link to existing vault notes; results logged for human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end Obsidian UX walkthrough (UAT-03)</name>
  <files>n/a — human verification only</files>
  <action>
    Present the following verification steps to the user. All defects are fixed (FIX-01, FIX-03, FIX-04, FIX-05) and classification/wikilink UAT passed. The full capture-to-memory pipeline is ready for end-to-end validation in the Obsidian vault UI.

    Steps for user:
    1. Open Obsidian with the vault at ~/Claude Cowork/
    2. Trigger a capture by running `/new` with a test input (e.g., "Meeting notes: decided to use Docker MCP gateway for calendar integration. Action items: verify remote trigger, update config.")
    3. Verify the input is classified (check terminal output for LEFT/RIGHT classification)
    4. Check that a proposal appears in ~/Claude Cowork/proposals/memory-proposals.md
    5. In the proposals file, mark the proposal as accepted (check the checkbox)
    6. Run /promote-memories (or the promote command)
    7. Verify the promoted memory appears in ~/Claude Cowork/memory/memory.md with: a content_hash field, at least one wikilink back-reference (e.g., [[some-note]]), correct category and metadata
    8. Verify no duplicate entry exists if you run promote again
    9. Confirm: the full flow completed without manual workarounds or errors
  </action>
  <verify>User confirms "approved" or describes issues.</verify>
  <done>User confirms the full capture-to-classify-to-propose-to-promote flow works end-to-end in Obsidian vault UI without manual workarounds.</done>
</task>

</tasks>

<verification>
- UAT-01: Classification accuracy >= 80% on 10+ diverse synthetic inputs
- UAT-02: Wikilinks generated and at least some point to existing vault notes
- UAT-03: Human-verified end-to-end flow in Obsidian vault UI
- All existing unit tests continue to pass: `npx jest --verbose`
</verification>

<success_criteria>
- UAT-01: 10+ captures classified, >80% accuracy, results logged
- UAT-02: Wikilinks generated for test memories, vault existence verified
- UAT-03: Human confirms full capture-to-memory flow works in Obsidian without workarounds
</success_criteria>

<output>
After completion, create `.planning/phases/06-defect-fixes-and-uat/06-03-SUMMARY.md`
</output>
