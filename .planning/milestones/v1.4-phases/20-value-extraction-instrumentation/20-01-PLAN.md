---
phase: 20-value-extraction-instrumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/schema/pipeline.schema.json
  - config/pipeline.json
  - test/config-schemas.test.js
autonomous: true
requirements:
  - STATS-DAILY-01
must_haves:
  truths:
    - "loadConfigWithOverlay('pipeline') validates a config containing the new stats sub-object without error"
    - "loadConfigWithOverlay('pipeline') still validates a pipeline.json that omits the stats block (backward-compat additive change)"
    - "Defaults applied when stats keys are missing match the values in CONTEXT.md D-01/D-02/D-08"
  artifacts:
    - path: "config/schema/pipeline.schema.json"
      provides: "stats sub-object schema definition with 5 keys (enabled, path, timezone, summaryLineEnabled, schemaVersion)"
      contains: '"stats"'
    - path: "config/pipeline.json"
      provides: "stats block defaults"
      contains: '"stats"'
    - path: "test/config-schemas.test.js"
      provides: "validation test for stats sub-object presence and absence"
  key_links:
    - from: "config/pipeline.json"
      to: "config/schema/pipeline.schema.json"
      via: "AJV validation in loadConfigWithOverlay('pipeline', { validate: true })"
      pattern: "loadConfigWithOverlay.*pipeline"
---

<objective>
Extend the pipeline config schema with a `stats` sub-object so downstream Phase 20 plans (daily-stats writer, /today integration, summary-line render) can read tunables through the existing `loadConfigWithOverlay('pipeline')` path. Strictly additive — backward compatible.

Purpose: Foundation for all later Phase 20 work. PLAN-02 (daily-stats core), PLAN-04 (today-command integration), and PLAN-05 (summary-line render) all read these config keys.
Output: Schema sub-object + default values + validation test confirming both presence and absence paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-value-extraction-instrumentation/20-CONTEXT.md

@config/schema/pipeline.schema.json
@config/pipeline.json
@src/pipeline-infra.js

<interfaces>
<!-- Existing schema convention from Phase 19 (memory.semantic block) -->
<!-- pipeline.schema.json uses additionalProperties:false at the root, but accepts new top-level keys when added to "properties" -->

From src/pipeline-infra.js (existing function — do not modify):
```javascript
function loadConfigWithOverlay(name, opts = {})
// name = 'pipeline' loads config/pipeline.json + config/pipeline.local.json overlay,
// validates against config/schema/pipeline.schema.json when opts.validate === true
```

Schema convention (from Phase 19 memory.semantic block — same shape to follow):
- Sub-object at root level
- All keys optional with explicit defaults
- additionalProperties:false on the sub-object
- Existing top-level additionalProperties:false at root must be updated to include "stats" in "properties"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add stats sub-object to pipeline.schema.json with 5 keys and defaults</name>
  <files>config/schema/pipeline.schema.json</files>
  <read_first>
    - config/schema/pipeline.schema.json (current schema — confirm root shape, locate memory.semantic sub-object as the convention reference, locate root "properties" object so "stats" is added at the same level)
    - .planning/phases/20-value-extraction-instrumentation/20-CONTEXT.md (D-01, D-02, D-08, "Schemas to extend" section — exact key list and defaults)
    - config/pipeline.json (current shape — confirm stats key not yet present)
  </read_first>
  <behavior>
    - When pipeline config includes a complete stats block with all 5 keys, AJV validates without error
    - When pipeline config omits the stats key entirely, AJV validates without error (sub-object is optional)
    - When pipeline config includes stats with an unknown key (e.g. `stats.bogus`), AJV rejects (additionalProperties:false on the sub-object)
    - When pipeline config includes stats.timezone as a non-string, AJV rejects
  </behavior>
  <action>
    Add a `stats` sub-object to `config/schema/pipeline.schema.json` under the root `properties` object (sibling to existing `memory`, `connectors`, etc.).

    The stats sub-object MUST have exactly these 5 keys with these defaults and types (verbatim from CONTEXT.md "Schemas to extend"):

    ```json
    "stats": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": { "type": "boolean", "default": true },
        "path": { "type": "string", "default": "RIGHT/daily-stats.md" },
        "timezone": { "type": "string", "default": "America/Chicago" },
        "summaryLineEnabled": { "type": "boolean", "default": true },
        "schemaVersion": { "type": "integer", "default": 1 }
      }
    }
    ```

    Notes on the values:
    - `path` is **vault-relative** (per CONTEXT.md "Caveats / Pitfalls": vault-gateway expects vault-relative paths; absolute resolved via VAULT_ROOT). Do NOT use `~/Claude Cowork/RIGHT/daily-stats.md` here — the vault root is resolved at runtime.
    - `timezone` is `America/Chicago` per D-08 (operator is in Fort Worth, Central time). Do NOT use `America/Los_Angeles` even though REQUIREMENTS.md STATS-GROWTH-01 says so — that REQ has a known bug (REQ-AMEND-01). Per CONTEXT.md, REQ-AMEND-01 is corrected in the same commit as CONTEXT.md (already shipped 4a9bb8d on 2026-04-24); this plan must use `America/Chicago`.
    - The whole `stats` sub-object is optional (NOT in `required`), so existing pipeline.json without a stats block still validates.

    Do NOT modify the root-level `additionalProperties` setting; just add `stats` under `properties`.
  </action>
  <verify>
    <automated>node -e "const Ajv = require('ajv'); const schema = require('./config/schema/pipeline.schema.json'); const ajv = new Ajv({useDefaults:true,strict:false}); const validate = ajv.compile(schema); const ok1 = validate({stats:{enabled:true,path:'RIGHT/daily-stats.md',timezone:'America/Chicago',summaryLineEnabled:true,schemaVersion:1}}); const ok2 = validate({}); const bad = validate({stats:{bogus:1}}); if(!ok1) throw new Error('full stats block rejected: '+JSON.stringify(validate.errors)); if(!ok2) throw new Error('empty config rejected: '+JSON.stringify(validate.errors)); if(bad) throw new Error('additionalProperties not enforced'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"stats"' config/schema/pipeline.schema.json` exits 0
    - `grep -q '"timezone"' config/schema/pipeline.schema.json` exits 0
    - `grep -q '"America/Chicago"' config/schema/pipeline.schema.json` exits 0
    - `grep -q '"summaryLineEnabled"' config/schema/pipeline.schema.json` exits 0
    - `grep -q '"schemaVersion"' config/schema/pipeline.schema.json` exits 0
    - `grep -q '"path"' config/schema/pipeline.schema.json` exits 0
    - The 1-liner under `<automated>` above exits 0 and prints `OK`
    - `grep -c "America/Los_Angeles" config/schema/pipeline.schema.json` returns 0 (no leakage of the REQ bug)
  </acceptance_criteria>
  <done>
    Schema validates a full stats block, validates an empty config, rejects unknown stats keys, and rejects wrong types.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add stats defaults to config/pipeline.json and add schema validation tests</name>
  <files>
    config/pipeline.json
    test/config-schemas.test.js
  </files>
  <read_first>
    - config/pipeline.json (current contents — append stats block alongside existing memory block)
    - test/config-schemas.test.js (existing patterns — find Phase 19 memory.semantic test as the structural template)
    - config/schema/pipeline.schema.json (the schema authored in Task 1 — values must mirror)
  </read_first>
  <behavior>
    - pipeline.json has a stats block with exactly the 5 keys at the values declared in the schema defaults
    - config-schemas.test.js has a positive test: full stats block validates
    - config-schemas.test.js has a positive test: pipeline.json without stats validates (backward compat)
    - config-schemas.test.js has a negative test: stats with an unknown key fails validation
    - config-schemas.test.js has a negative test: stats.timezone as non-string fails validation
  </behavior>
  <action>
    1. In `config/pipeline.json`, add a top-level `stats` block with these EXACT values:
    ```json
    "stats": {
      "enabled": true,
      "path": "RIGHT/daily-stats.md",
      "timezone": "America/Chicago",
      "summaryLineEnabled": true,
      "schemaVersion": 1
    }
    ```
    Position it after the existing `memory` block. JSON syntax: ensure the trailing comma on the previous block and no trailing comma on this block (or wherever it sits in source order).

    2. In `test/config-schemas.test.js`, add a `describe('pipeline.schema.json — stats sub-object')` block with these 4 cases:
       - `it('validates a full stats block with all 5 keys')` — passes the JSON above into the AJV validator, expects `valid === true`.
       - `it('validates pipeline.json without a stats block (backward compat)')` — passes `{}` into validator, expects `valid === true`.
       - `it('rejects stats with an unknown key')` — passes `{stats:{bogus:1}}`, expects `valid === false`.
       - `it('rejects stats.timezone as a non-string')` — passes `{stats:{timezone:42}}`, expects `valid === false`.

    Use the same AJV setup pattern as the existing `memory.semantic` tests in this file (Phase 19 convention). Do NOT add a new AJV instance if one is already loaded at the top of the file.
  </action>
  <verify>
    <automated>npx jest test/config-schemas.test.js --forceExit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"stats"' config/pipeline.json` exits 0
    - `grep -q '"America/Chicago"' config/pipeline.json` exits 0
    - `grep -q '"RIGHT/daily-stats.md"' config/pipeline.json` exits 0
    - `node -e "JSON.parse(require('fs').readFileSync('config/pipeline.json','utf8'))"` exits 0 (valid JSON)
    - `npx jest test/config-schemas.test.js --forceExit` exits 0
    - `grep -c "stats" test/config-schemas.test.js` returns at least 4 (the 4 new it-blocks reference stats)
    - `node -e "const {loadConfigWithOverlay} = require('./src/pipeline-infra'); const c = loadConfigWithOverlay('pipeline', {validate: true}); if (c.stats.timezone !== 'America/Chicago') throw new Error('default not applied'); console.log('OK')"` exits 0 and prints `OK`
  </acceptance_criteria>
  <done>
    pipeline.json contains the stats block, config-schemas.test.js has 4 new passing assertions, and `loadConfigWithOverlay('pipeline', { validate: true })` returns a config with `stats.timezone === 'America/Chicago'`.
  </done>
</task>

</tasks>

<verification>
- AJV validation passes for both populated and empty stats config
- `npx jest test/config-schemas.test.js --forceExit` exits 0
- `npx jest test/pipeline-infra.test.js --forceExit` still exits 0 (no regression)
- `npm run lint` exits 0 (no new ESLint errors)
- `grep -c "America/Los_Angeles" config/schema/pipeline.schema.json config/pipeline.json` returns 0
</verification>

<success_criteria>
1. `config/schema/pipeline.schema.json` contains a `stats` sub-object with the 5 keys and types specified in CONTEXT.md
2. `config/pipeline.json` contains stats defaults that match the schema
3. `test/config-schemas.test.js` has 4 new passing test cases (full block, missing block, unknown key, wrong type)
4. `loadConfigWithOverlay('pipeline')` returns a config with `stats.timezone === 'America/Chicago'`
5. No regression in existing config tests (`npx jest test/config-schemas.test.js test/pipeline-infra.test.js --forceExit` exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/20-value-extraction-instrumentation/20-01-SUMMARY.md`
</output>
