---
phase: 18-memory-retrieval-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - config/pipeline.json
  - config/schema/pipeline.schema.json
  - test/unit/pipeline-schema.memory.test.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "minisearch@^7.2.0 is a production dependency"
    - "config/pipeline.json contains memory.echoThreshold = 0.65"
    - "config/schema/pipeline.schema.json allows the memory.echoThreshold key under strict additionalProperties:false validation"
    - "safeLoadPipelineConfig() loads pipeline.json without schema warnings"
  artifacts:
    - path: package.json
      provides: "minisearch dependency declaration"
      contains: '"minisearch":'
    - path: config/pipeline.json
      provides: "memory.echoThreshold config key"
      contains: "echoThreshold"
    - path: config/schema/pipeline.schema.json
      provides: "Schema permitting memory.echoThreshold"
      contains: "memory"
  key_links:
    - from: config/pipeline.json
      to: config/schema/pipeline.schema.json
      via: "AJV validation in pipeline-infra.safeLoadPipelineConfig"
      pattern: "memory"
---

<objective>
Add the `minisearch` npm dependency and a new `memory.echoThreshold` config key (with schema update) so Wave 2 memory-reader.js can import minisearch and Wave 3 today-command can read the threshold without the existing AJV `additionalProperties: false` check rejecting it.

Purpose: Unblocks Wave 2 (memory-reader.js needs minisearch) and Wave 3 (today-command reads `memory.echoThreshold` per TODAY-ECHO-01). Research document marks this as HIGH-confidence prerequisite work.
Output: Updated `package.json` / `package-lock.json`, extended pipeline schema, new config entry, schema regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-memory-retrieval-foundation/18-RESEARCH.md
@package.json
@config/pipeline.json
@config/schema/pipeline.schema.json

<interfaces>
Current pipeline schema uses `additionalProperties: false` at the root (line 277). Adding a top-level `memory` key REQUIRES schema extension — otherwise AJV will reject the config.

Existing `thresholds` block (lines 260-275) is the model for adding an object with numeric bounded properties.

Current pipeline.json has no `memory` block — we are adding one.

minisearch from npm (confirmed by research): `minisearch@7.2.0`, MIT license, zero runtime dependencies. License check script (`npm run license-check`) allows MIT.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install minisearch@^7.2.0 as production dependency</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current dependencies block)
    - .planning/phases/18-memory-retrieval-foundation/18-RESEARCH.md (Search Algorithm section, lines 88-109)
    - CLAUDE.md (Do Not Touch List — lock files; adding approved deps is allowed)
  </read_first>
  <action>
    1. Run `npm install minisearch@^7.2.0 --save` from the repo root `/Users/cpconnor/projects/second-brain`. This adds the dep to `dependencies` (not devDependencies) and updates package-lock.json.

    2. Verify the resulting entry in `package.json` dependencies block looks like:
       ```json
       "minisearch": "^7.2.0"
       ```

    3. Run `npm run license-check` — MUST pass. Minisearch is MIT so the allowlist (`MIT;ISC;Apache-2.0;BSD-2-Clause;BSD-3-Clause;CC0-1.0`) already permits it.

    4. Run `npm test -- --listTests 2>&1 | head -5` (or `npm test 2>&1 | tail -20`) to confirm the dep install did not break module resolution anywhere.

    5. Do NOT hand-edit package-lock.json. Let npm manage it.
  </action>
  <verify>
    <automated>node -e "const ms = require('minisearch'); console.log(typeof ms === 'function' ? 'OK' : 'FAIL'); process.exit(typeof ms === 'function' ? 0 : 1);"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies block contains `"minisearch":` with a `^7.2.` or `^7.` prefix
    - `package-lock.json` contains `"minisearch"` and a resolved version ≥ 7.2.0
    - `npm run license-check` exits 0
    - `node -e "require('minisearch')"` exits 0 (module resolves)
    - No modification to existing `dependencies` entries beyond the addition
  </acceptance_criteria>
  <done>
    minisearch resolvable via require, license check green, no drift in existing deps.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend pipeline.schema.json to permit memory.echoThreshold and add config entry</name>
  <files>config/schema/pipeline.schema.json, config/pipeline.json, test/unit/pipeline-schema.memory.test.js</files>
  <read_first>
    - config/schema/pipeline.schema.json (full file — note root `additionalProperties: false` at the bottom)
    - config/pipeline.json (full file)
    - src/pipeline-infra.js (find `safeLoadPipelineConfig` to confirm it enforces the schema)
    - .planning/phases/18-memory-retrieval-foundation/18-RESEARCH.md (Threshold Calibration, lines 161-170)
  </read_first>
  <behavior>
    - Test 1: Valid config with `memory.echoThreshold: 0.65` validates against the schema (AJV returns true).
    - Test 2: Config with `memory.echoThreshold: 1.5` fails validation (exceeds maximum 1).
    - Test 3: Config with `memory.echoThreshold: "high"` fails validation (wrong type).
    - Test 4: Config with `memory: {}` (empty object, no echoThreshold) validates (echoThreshold is optional with default applied at read time).
    - Test 5: Config with an unknown key under `memory` (e.g. `memory.foo: 1`) fails validation (additionalProperties: false on the memory block).
    - Test 6: The live `config/pipeline.json` file validates successfully against the current schema.
  </behavior>
  <action>
    1. Add a new `memory` property block to `config/schema/pipeline.schema.json` properties object (insert alongside `thresholds`, before the final closing brace):

       ```json
       "memory": {
         "type": "object",
         "properties": {
           "echoThreshold": {
             "type": "number",
             "minimum": 0,
             "maximum": 1
           }
         },
         "additionalProperties": false
       }
       ```

       Do NOT add `memory` to the top-level `required` array — it remains optional with a default applied at read time.

    2. Add the config block to `config/pipeline.json` (keep file valid JSON; append before the closing brace):

       ```json
         "memory": {
           "echoThreshold": 0.65
         }
       ```

       Ensure the preceding `thresholds` block gets a trailing comma.

    3. Create `test/unit/pipeline-schema.memory.test.js` covering the 6 behaviors above. Load the schema with `const schema = require('../../config/schema/pipeline.schema.json')` and use AJV directly:

       ```javascript
       const Ajv = require('ajv');
       const schema = require('../../config/schema/pipeline.schema.json');
       const ajv = new Ajv({ allErrors: true });
       const validate = ajv.compile(schema);
       ```

       For each test, construct a minimal config by cloning the real `config/pipeline.json` via `JSON.parse(JSON.stringify(require('../../config/pipeline.json')))` and mutating the `memory` block. This ensures the tests exercise the schema against realistic structure, not isolated fragments.

    4. Run `npx jest --forceExit test/unit/pipeline-schema.memory.test.js` — all 6 tests MUST pass.

    5. Run the full test suite `npx jest --forceExit` — MUST still pass (the new schema property must not break any existing config-loading test).
  </action>
  <verify>
    <automated>npx jest --forceExit test/unit/pipeline-schema.memory.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `config/schema/pipeline.schema.json` contains the string `"memory"` as a property name at the root properties level
    - `config/schema/pipeline.schema.json` contains `"echoThreshold"` with `"minimum": 0` and `"maximum": 1`
    - `config/pipeline.json` contains `"echoThreshold": 0.65`
    - `config/pipeline.json` remains valid JSON: `node -e "JSON.parse(require('fs').readFileSync('config/pipeline.json', 'utf8'))"` exits 0
    - `test/unit/pipeline-schema.memory.test.js` exists with at least 6 tests, all passing
    - `npx jest --forceExit` full suite exits 0
  </acceptance_criteria>
  <done>
    Schema accepts echoThreshold, config has default 0.65, AJV validation tests pass, full suite green.
  </done>
</task>

</tasks>

<verification>
- `npx jest --forceExit` exits 0 with no regressions from the pre-phase-18 baseline
- `node -e "require('minisearch')"` resolves
- `node -e "const c = require('./config/pipeline.json'); console.log(c.memory.echoThreshold);"` prints `0.65`
- `npm run license-check` exits 0
- `npm run lint` exits 0
</verification>

<success_criteria>
- Wave 2 plans can `require('minisearch')` from source modules
- Wave 3 plans can read `config.memory.echoThreshold` with a documented default
- Schema validation catches bad values at config load
</success_criteria>

<output>
After completion, create `.planning/phases/18-memory-retrieval-foundation/18-02-SUMMARY.md`
</output>
