---
phase: 21-closeout-hygiene
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - src/memory-proposals.js
  - src/promote-memories.js
  - src/recall-command.js
  - src/today-command.js
  - src/memory-reader.js
  - src/classifier.js
  - src/memory-extractor.js
  - src/daily-stats.js
  - src/semantic-index.js
  - src/vault-gateway.js
autonomous: true
requirements:
  - HYG-JSDOC-01
must_haves:
  truths:
    - "Every public symbol on `module.exports` across the 10 named files has a JSDoc block with @param, @returns, and a one-line description (per D-LOCK-3)"
    - "module.exports._testOnly objects are explicitly excluded with the documented rationale comment at the export site (per D-LOCK-3-AMEND-A)"
    - "Worklist orders src/memory-proposals.js and src/promote-memories.js first as the largest construction tasks (zero JSDoc baseline) per D-LOCK-1-AMEND-B"
    - "No logic changes; JSDoc-only edits — Lock 5 fence holds (src/*.js permitted edit type is JSDoc + lint-disable)"
    - "All existing tests continue to pass with no behavioral regression"
  artifacts:
    - path: "src/memory-proposals.js"
      provides: "@param/@returns/description JSDoc on 4 public exports; _testOnly excluded with rationale comment"
      contains: "@param"
    - path: "src/promote-memories.js"
      provides: "@param/@returns/description JSDoc on 1 public export (promoteMemories)"
      contains: "@param"
    - path: "src/semantic-index.js"
      provides: "JSDoc completion on 9 public exports; _testOnly excluded with rationale comment"
      contains: "Test-only seam"
  key_links:
    - from: "src/memory-proposals.js"
      to: "test/memory-proposals.test.js"
      via: "_testOnly seam preserved; JSDoc rationale documents the carve-out"
      pattern: "_testOnly"
---

<objective>
Add JSDoc (@param, @returns, one-line description) to every symbol on `module.exports` across 10 named source files. Apply the D-LOCK-3 narrow definition of "public" — every key on the top-level `module.exports` object literal qualifies. Apply the D-LOCK-3-AMEND-A carve-out — `module.exports._testOnly` exports (in `memory-proposals.js` and `semantic-index.js`) are excluded with a documented rationale comment at the export site:

```
// Test-only seam — not public API. JSDoc not required per Phase 21 D-LOCK-3.
```

Worklist ordering follows D-LOCK-1-AMEND-B: prioritize zero-baseline files (`memory-proposals.js`, `promote-memories.js`) as the largest construction tasks, then move through partial-coverage files, then finalize strong-baseline files.

Per Lock 5: `src/*.js` edits in Phase 21 are JSDoc-only (plus the lint-disable comments handled in Plan 21-03). No logic changes. No refactors.

Purpose: Closeout hygiene that makes the public surface of the second-brain codebase legible for the v1.5 contributor (and AI agents). Sets a documentation contract that future phases extend.
Output: 10 source files with complete JSDoc on every public export and explicit carve-outs for test-only seams.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-closeout-hygiene/21-CONTEXT.md
@.planning/REQUIREMENTS.md

@src/memory-proposals.js
@src/promote-memories.js
@src/recall-command.js
@src/today-command.js
@src/memory-reader.js
@src/classifier.js
@src/memory-extractor.js
@src/daily-stats.js
@src/semantic-index.js
@src/vault-gateway.js

<interfaces>
<!-- Public export inventory (verified 2026-04-25 against module.exports object literals) -->

<!-- Wave 1 (zero JSDoc baseline — largest construction tasks per D-LOCK-1-AMEND-B): -->
<!--   src/memory-proposals.js  (4 exports + _testOnly):                                    -->
<!--     generateCandidateId, writeCandidate, readProposals, flushPendingBuffer            -->
<!--     _testOnly: { acquireLock, releaseLock }   ← excluded                              -->
<!--   src/promote-memories.js  (1 export):                                                -->
<!--     promoteMemories                                                                   -->

<!-- Wave 2 (partial-coverage files): -->
<!--   src/recall-command.js     (2): runRecall, parseRecallArgs                           -->
<!--   src/today-command.js      (1): runToday                                             -->
<!--   src/memory-reader.js      (3): readMemory, searchMemoryKeyword, getMemoryEcho       -->
<!--   src/classifier.js         (4): classifyInput, runStage0, runStage1, runStage2       -->

<!-- Wave 3 (strong-baseline files — fill JSDoc gaps only on bare exports): -->
<!--   src/memory-extractor.js   (4): extractFromTranscript, extractFromFile,              -->
<!--                                  extractFromDirectory, extractMemories                -->
<!--   src/daily-stats.js        (9): recordDailyStats, dateKey, readDailyStats,           -->
<!--                                  recordRecallInvocation, recordProposalsBatch,        -->
<!--                                  recordPromotion, recordTopCosine, recordTopRrf,      -->
<!--                                  readDailyCounters                                    -->
<!--   src/semantic-index.js     (9 + _testOnly):                                          -->
<!--     indexNewEntries, semanticSearch, hybridSearch, selfHealIfNeeded,                  -->
<!--     computeSchemaVersion, createVoyageClient, getEmbeddingsPath, getMetadataPath      -->
<!--     _testOnly: { _cosine, _adjustedScore, _daysSince, classifyVoyageError,            -->
<!--                  extractRetryAfterSec, readAllEmbeddings, readMetadata } ← excluded   -->
<!--   src/vault-gateway.js     (17): vaultWrite, vaultRead, vaultWriteAtomic, quarantine, -->
<!--                                  toWikilink, toQualifiedWikilink, bootstrapVault,     -->
<!--                                  initGateway, getConfig, loadConfig, validateConfig,  -->
<!--                                  normalizePath, checkPath, logDecision,               -->
<!--                                  VaultWriteError, VAULT_ROOT, configEvents            -->

<!-- Total public symbols requiring JSDoc: 4+1+2+1+3+4+4+9+9+17 = 54 -->
<!-- Plus 2 _testOnly carve-outs (memory-proposals.js, semantic-index.js) -->

<!-- JSDoc shape required (D-LOCK-3): -->
<!--   /** -->
<!--    * One-line description of what the function does and why a caller would use it. -->
<!--    * @param {Type} name - Short purpose. -->
<!--    * @param {Type} [optional] - Short purpose; note default if relevant. -->
<!--    * @returns {Type} Short description of the return value. -->
<!--    */ -->

<!-- For non-function exports (constants, error classes, event emitters), document with: -->
<!--   /** -->
<!--    * One-line description. -->
<!--    * @type {Type}   (or @class for classes) -->
<!--    */ -->

<!-- _testOnly carve-out comment (verbatim per D-LOCK-3-AMEND-A): -->
<!--   // Test-only seam — not public API. JSDoc not required per Phase 21 D-LOCK-3. -->
<!--   module.exports._testOnly = { ... }; -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 (Wave 1, zero baseline): Add JSDoc to src/memory-proposals.js (4 exports) and add _testOnly carve-out comment</name>
  <files>src/memory-proposals.js</files>
  <read_first>
    - src/memory-proposals.js (whole file — note function signatures and call sites for each export)
    - test/memory-proposals.test.js (confirm parameter shapes and return shapes used in tests, to validate JSDoc accuracy)
    - .planning/phases/21-closeout-hygiene/21-CONTEXT.md (D-LOCK-3 and D-LOCK-3-AMEND-A wording)
  </read_first>
  <behavior>
    - Every public export (generateCandidateId, writeCandidate, readProposals, flushPendingBuffer) has a JSDoc block with @param, @returns, one-line description
    - _testOnly object has the carve-out comment immediately above its export line, verbatim
    - Existing acquireLock / releaseLock implementations unchanged
    - No logic changes anywhere in the file
  </behavior>
  <action>
    1. Add JSDoc above each of the 4 public exports' implementations:
       - `generateCandidateId(content, sourceRef, sessionId)` → @param all three (string), @returns {string} (the deterministic candidate ID hash)
       - `writeCandidate({ content, category, sourceRef, confidence, ... })` → document the options object shape, @returns {Promise<{candidateId, written, buffered}>}
       - `readProposals()` → @returns {Promise<Array<Proposal>>}
       - `flushPendingBuffer()` → @returns {Promise<{flushed: number}>}

    2. Immediately above the existing `module.exports._testOnly = { ... }` line (currently at line 397), insert this exact comment:

    ```javascript
    // Test-only seam — not public API. JSDoc not required per Phase 21 D-LOCK-3.
    module.exports._testOnly = {
      acquireLock,
      releaseLock,
    };
    ```

    3. Do NOT modify any function bodies. JSDoc-only edits.
  </action>
  <verify>
    <automated>node -e "const m = require('./src/memory-proposals'); ['generateCandidateId','writeCandidate','readProposals','flushPendingBuffer'].forEach(k => { if (typeof m[k] !== 'function') throw new Error('missing export: '+k); }); if (!m._testOnly || typeof m._testOnly.acquireLock !== 'function') throw new Error('_testOnly missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\\s*\\* @param" src/memory-proposals.js` is at least 8 (4 exports × ~2 params each)
    - `grep -c "^\\s*\\* @returns" src/memory-proposals.js` is at least 4
    - `grep -F "Test-only seam — not public API. JSDoc not required per Phase 21 D-LOCK-3." src/memory-proposals.js` returns 1 match
    - `git diff master -- src/memory-proposals.js | grep -E "^[+-][^+-]" | grep -vE "^[+-]\\s*(\\*|//|/\\*\\*|\\*/)"` returns empty (only comment-line changes)
    - `CI=true npx jest test/memory-proposals.test.js --forceExit` exits 0
  </acceptance_criteria>
  <done>
    src/memory-proposals.js has JSDoc on all 4 public exports and the verbatim _testOnly carve-out comment; tests pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 (Wave 1, zero baseline): Add JSDoc to src/promote-memories.js (1 export)</name>
  <files>src/promote-memories.js</files>
  <read_first>
    - src/promote-memories.js (whole file — focus on promoteMemories function signature, options shape, and return shape)
    - test/promote-memories.test.js (confirm parameter and return contracts)
  </read_first>
  <behavior>
    - promoteMemories has a JSDoc block with @param (options object), @returns (the promotion-result object with promoted/deferred/duplicates/rejected/skipped/archived counts), and a one-line description
    - No logic changes
  </behavior>
  <action>
    Add JSDoc above the `promoteMemories` function definition. Cover (at minimum):
    - One-line description: "Promote candidate memories from staging to memory.md based on threshold, deduplication, and content/style policy gates."
    - @param {Object} [options] — including {since, until, dryRun, threshold, archiveAfter} as known options
    - @returns {Promise<{promoted: number, deferred: number, duplicates: number, rejected: number, skipped: number, archived: number}>}
  </action>
  <verify>
    <automated>node -e "const m = require('./src/promote-memories'); if (typeof m.promoteMemories !== 'function') throw new Error('missing export'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\\s*\\* @param" src/promote-memories.js` is at least 1
    - `grep -c "^\\s*\\* @returns" src/promote-memories.js` is at least 1
    - `git diff master -- src/promote-memories.js | grep -vE "^(\\+\\+\\+|---|@@|[+-]\\s*(\\*|//|/\\*\\*|\\*/))"` returns empty (only comment-line changes)
    - `CI=true npx jest test/promote-memories.test.js --forceExit` exits 0
  </acceptance_criteria>
  <done>
    src/promote-memories.js has JSDoc on promoteMemories; tests pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 (Wave 2, partial baseline): Fill JSDoc gaps in recall-command.js (2), today-command.js (1), memory-reader.js (3), classifier.js (4)</name>
  <files>
    src/recall-command.js
    src/today-command.js
    src/memory-reader.js
    src/classifier.js
  </files>
  <read_first>
    - src/recall-command.js (look at runRecall and parseRecallArgs signatures)
    - src/today-command.js (look at runToday signature and option shape)
    - src/memory-reader.js (look at readMemory, searchMemoryKeyword, getMemoryEcho signatures)
    - src/classifier.js (look at classifyInput, runStage0, runStage1, runStage2 signatures and the result-object shapes)
    - associated test files (confirm parameter and return shapes)
  </read_first>
  <behavior>
    - All 10 public exports across these 4 files have JSDoc with @param, @returns, one-line description
    - No logic changes
  </behavior>
  <action>
    For each file, add JSDoc above any export that lacks it. Match the JSDoc to the actual function signature and return shape.

    Per-export coverage:
    - **recall-command.js**: runRecall(args), parseRecallArgs(argv) — both need JSDoc; parseRecallArgs returns the parsed shape with mode/query/since/category/top.
    - **today-command.js**: runToday(opts) — opts includes {date, mode, force, ...}; returns the rendered briefing object or string.
    - **memory-reader.js**: readMemory(opts), searchMemoryKeyword(query, opts), getMemoryEcho(opts) — document each.
    - **classifier.js**: classifyInput(input, opts), runStage0(input, opts), runStage1(input, opts), runStage2(input, opts) — document each; result objects vary per stage (see existing return statements at lines ~157, 267, 296).

    Re-use existing partial JSDoc where present; only ADD missing tags. Do NOT modify any working function bodies.
  </action>
  <verify>
    <automated>CI=true npx jest test/recall-command.test.js test/today-command.test.js test/memory-reader.test.js test/classifier.test.js --forceExit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\\s*\\* @param" src/recall-command.js` is at least 2
    - `grep -c "^\\s*\\* @returns" src/recall-command.js` is at least 2
    - `grep -c "^\\s*\\* @param" src/today-command.js` is at least 1
    - `grep -c "^\\s*\\* @returns" src/today-command.js` is at least 1
    - `grep -c "^\\s*\\* @param" src/memory-reader.js` is at least 3
    - `grep -c "^\\s*\\* @returns" src/memory-reader.js` is at least 3
    - `grep -c "^\\s*\\* @param" src/classifier.js` is at least 4
    - `grep -c "^\\s*\\* @returns" src/classifier.js` is at least 4
    - `CI=true npx jest test/recall-command.test.js test/today-command.test.js test/memory-reader.test.js test/classifier.test.js --forceExit` exits 0
  </acceptance_criteria>
  <done>
    All 10 public exports across these 4 files have @param, @returns, and a one-line description.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4 (Wave 3, strong baseline): Fill JSDoc gaps in memory-extractor.js (4), daily-stats.js (9), semantic-index.js (9 + _testOnly carve-out), vault-gateway.js (17)</name>
  <files>
    src/memory-extractor.js
    src/daily-stats.js
    src/semantic-index.js
    src/vault-gateway.js
  </files>
  <read_first>
    - src/memory-extractor.js (already strong baseline — confirm any export missing JSDoc, fill those only)
    - src/daily-stats.js (already strong baseline — same)
    - src/semantic-index.js (already strong baseline — confirm carve-out shape and any missing export JSDoc)
    - src/vault-gateway.js (already strong baseline — 17 exports including 2 non-function constants and configEvents EventEmitter; @type for non-functions)
  </read_first>
  <behavior>
    - Every export in each file has at least @param, @returns, and a one-line description (or @type / @class for non-function constants)
    - semantic-index.js _testOnly carve-out has the verbatim rationale comment immediately above its line (which is the comment at the closing `_testOnly: { ... }` block in the existing module.exports literal — adapt placement to match the file's structure: place the comment above the `_testOnly:` key inside the object literal, OR refactor to a separate `module.exports._testOnly = { ... }` line if cleaner; verify equivalence)
    - vault-gateway.js: VAULT_ROOT and VaultWriteError get @type / @class respectively; configEvents gets @type {EventEmitter}
    - No logic changes
  </behavior>
  <action>
    For each file, identify any public export that lacks JSDoc, and add the @param/@returns/description. For non-function exports (VAULT_ROOT, VaultWriteError, configEvents in vault-gateway.js), use:
    ```
    /**
     * One-line description.
     * @type {string|EventEmitter|...}
     */
    ```
    or for the error class:
    ```
    /**
     * Thrown when [...].
     * @class
     * @extends Error
     */
    ```

    For semantic-index.js, the existing `module.exports = { ..., _testOnly: { ... } }` structure embeds _testOnly inline. Add the verbatim D-LOCK-3-AMEND-A rationale comment immediately above the `_testOnly:` key:

    ```javascript
    module.exports = {
      indexNewEntries,
      semanticSearch,
      // ... other public exports
      // Test-only seam — not public API. JSDoc not required per Phase 21 D-LOCK-3.
      _testOnly: {
        _cosine,
        _adjustedScore,
        // ...
      },
    };
    ```
  </action>
  <verify>
    <automated>CI=true npx jest test/memory-extractor.test.js test/daily-stats.test.js test/semantic-index.test.js test/vault-gateway.test.js --forceExit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Test-only seam — not public API. JSDoc not required per Phase 21 D-LOCK-3." src/semantic-index.js` returns 1 match
    - `grep -c "^\\s*\\* @param" src/vault-gateway.js` is at least 14 (function exports)
    - `grep -c "^\\s*\\* @returns" src/vault-gateway.js` is at least 14
    - `grep -c "^\\s*\\* @type\\|^\\s*\\* @class" src/vault-gateway.js` is at least 3 (VAULT_ROOT, VaultWriteError, configEvents)
    - `grep -c "^\\s*\\* @param" src/semantic-index.js` is at least 9
    - `grep -c "^\\s*\\* @param" src/daily-stats.js` is at least 9
    - `grep -c "^\\s*\\* @param" src/memory-extractor.js` is at least 4
    - `CI=true npx jest test/memory-extractor.test.js test/daily-stats.test.js test/semantic-index.test.js test/vault-gateway.test.js --forceExit` exits 0
  </acceptance_criteria>
  <done>
    All 4 strong-baseline files have JSDoc completion on every public export; semantic-index.js carries the _testOnly rationale comment.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Final coverage audit — confirm all 54 public exports across all 10 files have JSDoc</name>
  <files>(read-only audit; no file modifications expected — task fails if any export was missed)</files>
  <read_first>
    - all 10 files modified above
  </read_first>
  <behavior>
    - Every key on `module.exports = { ... }` (excluding `_testOnly`) across 10 files has at least @param, @returns, and a one-line description
    - 2 `_testOnly` exports (memory-proposals.js, semantic-index.js) carry the verbatim D-LOCK-3-AMEND-A rationale comment
  </behavior>
  <action>
    Run a comprehensive audit script (one-liner using node + grep) that:
    1. For each of the 10 files, parses out the `module.exports` object literal and lists every key (excluding `_testOnly`).
    2. For each key, locates the function declaration in the file and confirms a JSDoc block exists immediately above it.
    3. Confirms the JSDoc contains at least one `@param` line (or `@type`/`@class` for non-function constants) and one `@returns` line (for functions).
    4. Reports any missing entries with file path and symbol name.

    If audit finds gaps, add the missing JSDoc and re-run. Audit passing is the gate to plan completion.
  </action>
  <verify>
    <automated>bash -c 'set -e; for f in src/memory-proposals.js src/promote-memories.js src/recall-command.js src/today-command.js src/memory-reader.js src/classifier.js src/memory-extractor.js src/daily-stats.js src/semantic-index.js src/vault-gateway.js; do P=$(grep -c "^\\s*\\* @param" "$f" || true); R=$(grep -c "^\\s*\\* @returns" "$f" || true); echo "$f: @param=$P @returns=$R"; done'</automated>
  </verify>
  <acceptance_criteria>
    - All 10 files report non-zero @param and @returns counts (vault-gateway.js has @type/@class supplements for 3 non-function exports — counted separately)
    - `CI=true npm test --forceExit` exits 0 across the full suite (no regression)
    - `npm run lint` exits 0 (no new ESLint errors introduced by JSDoc edits — confirms no inadvertent code drift)
    - `git diff master -- src/ | grep -E "^[+-]" | grep -vE "^(\\+\\+\\+|---|@@|[+-]\\s*(\\*|//|/\\*\\*|\\*/))"` returns empty (logic unchanged everywhere — only comment lines added)
  </acceptance_criteria>
  <done>
    All 54 public exports across 10 files have JSDoc; both _testOnly carve-outs carry the rationale comment; full test suite green; logic unchanged.
  </done>
</task>

</tasks>

<verification>
- `git diff master -- src/ | grep -vE "^(\\+\\+\\+|---|@@|[+-]\\s*(\\*|//|/\\*\\*|\\*/))"` returns empty (no logic touched)
- `CI=true npm test --forceExit` exits 0
- `npm run lint` exits 0
- `grep -F "Test-only seam — not public API. JSDoc not required per Phase 21 D-LOCK-3." src/memory-proposals.js src/semantic-index.js | wc -l` returns 2
- For each of the 10 files, @param and @returns counts are at least the export count
</verification>

<success_criteria>
1. All 4 public exports of `src/memory-proposals.js` have @param, @returns, one-line description; _testOnly carries the verbatim D-LOCK-3-AMEND-A rationale comment
2. `src/promote-memories.js`'s sole export has @param, @returns, one-line description
3. All exports in `src/recall-command.js` (2), `src/today-command.js` (1), `src/memory-reader.js` (3), `src/classifier.js` (4) have JSDoc
4. All exports in `src/memory-extractor.js` (4), `src/daily-stats.js` (9), `src/semantic-index.js` (9 + carve-out), `src/vault-gateway.js` (17) have JSDoc; non-function exports use @type / @class
5. Total public exports documented: 54. Total _testOnly carve-outs: 2.
6. No logic changes anywhere; full test suite passes; lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/21-closeout-hygiene/21-02-SUMMARY.md` documenting:
- Per-file count of @param / @returns lines added (delta from baseline)
- Confirmation of _testOnly carve-out comment in memory-proposals.js and semantic-index.js
- Citation of D-LOCK-3 (narrow public definition) and D-LOCK-3-AMEND-A (_testOnly exclusion) as the framing decisions
- Verification that `git diff master -- src/` shows only comment-line changes
</output>
