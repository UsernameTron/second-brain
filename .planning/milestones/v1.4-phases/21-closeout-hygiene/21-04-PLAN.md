---
phase: 21-closeout-hygiene
plan: 04
type: execute
wave: 4
depends_on:
  - 21-01
  - 21-02
  - 21-03
files_modified:
  - CLAUDE.md
  - README.md
  - docs/DEVOPS-HANDOFF.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - tasks/todo.md
autonomous: true
requirements:
  - DOCS-FINAL-01
must_haves:
  truths:
    - "All 5 docs (CLAUDE.md, README.md, docs/DEVOPS-HANDOFF.md, .planning/PROJECT.md, .planning/ROADMAP.md) reference live test counts (re-counted at execution time, NOT hardcoded from CONTEXT.md or this plan) and the corrected '32' console-site figure (NOT '41')"
    - "Edits are bounded by the 'verifiable v1.4-fact error' rule per D-LOCK-4 — no rewriting beyond drift fixes"
    - "REQUIREMENTS.md HYG-UNICODE-01 row is reframed per Path B: documents the ASCII-only matcher as the v1.4 contract and references HYG-UNICODE-02 as the v1.5 successor"
    - "All 4 Phase 21 REQ checkboxes (HYG-UNICODE-01, HYG-JSDOC-01, HYG-CONSOLE-01, DOCS-FINAL-01) are flipped from `[ ]` to `[x]` (D-LOCK-4-SUB-i)"
    - "npm audit, license-checker, and GitGuardian secrets scan all confirmed green; results recorded in 21-04-SUMMARY.md (D-LOCK-4-SUB-ii)"
    - "19-HUMAN-UAT.md is cited in v1.4 close documentation; status:complete confirmed unchanged (D-LOCK-4-SUB-iii — no-op verify)"
    - ".planning/PROJECT.md Phase 20 status corrected from '(next)' to shipped with PR reference"
    - ".planning/STATE.md flipped to milestone-close ready state"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 21 REQ rows updated to [x] complete; HYG-UNICODE-01 reframed per Path B"
      contains: "[x] HYG-UNICODE-01"
    - path: ".planning/phases/21-closeout-hygiene/21-04-SUMMARY.md"
      provides: "Final v1.4 close ledger — npm audit / license-check / GitGuardian results, live test count, 32-vs-41 correction record, doc-by-doc change log"
      contains: "npm audit"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/19-semantic-memory-search/19-HUMAN-UAT.md"
      via: "v1.4 close narrative cites the UAT artifact (D-LOCK-4-SUB-iii)"
      pattern: "19-HUMAN-UAT.md"
    - from: ".planning/REQUIREMENTS.md"
      to: "tasks/todo.md"
      via: "HYG-UNICODE-01 reframing references HYG-UNICODE-02 in v1.5 backlog (Path B per D-LOCK-5-AMEND-A)"
      pattern: "HYG-UNICODE-02"
---

<objective>
Refresh the 5 named v1.4 documents and the 3 supporting planning artifacts so that every "verifiable v1.4-fact error" is corrected. Per D-LOCK-4, edits are bounded — only drift fixes, no narrative rewrites, no scope expansion.

Three corrections drive most of the work:
1. **Stale test counts** ("982 total", "944 passing", "1,082", "1,066", "48 suites", etc.). Plan 21-04 must use the **live test count at the moment 21-04 executes**. Do NOT hardcode any number from CONTEXT.md, this plan brief, or earlier session output — those are stale by definition. Re-count via `CI=true npm test 2>&1 | tail -20` (or `npm test --listFiles 2>&1` for file count) at execution start.
2. **Stale "41 no-console" figure**. Actual count after Plan 21-03 ships is **0** (all disabled), but the historical corpus size is **32**. Update REQUIREMENTS.md, ROADMAP.md, CLAUDE.md, README.md, and docs/DEVOPS-HANDOFF.md so wherever "41" appeared, it now reads "32" (with appropriate fix-narrative wording where the doc is describing historical scope, e.g. "32 ESLint no-console warnings (originally tracked as 41 in v1.3 backlog drift; corrected in Phase 21)").
3. **Path B reframing of HYG-UNICODE-01** per D-LOCK-5-AMEND-A. REQUIREMENTS.md row text must explicitly state: "ASCII-only substring matching is the v1.4 contract; Unicode-variant catching is deferred to v1.5 HYG-UNICODE-02."

Plus 6 sub-tasks per D-LOCK-4: REQUIREMENTS.md checkbox sync, quality-gate confirmations (npm audit / license-checker / GitGuardian), 19-HUMAN-UAT.md citation, Phase 20 status correction in PROJECT.md, HYG-UNICODE-02 v1.5 backlog confirmation, STATE.md milestone-close.

Per Lock 5: only the 5 named docs + REQUIREMENTS.md + STATE.md + tasks/todo.md are touched. No code edits.

Purpose: Last plan in v1.4. Closes the milestone with documentation that matches reality.
Output: 8 files updated; 21-04-SUMMARY.md ledger documenting every change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-closeout-hygiene/21-CONTEXT.md
@.planning/phases/19-semantic-memory-search/19-HUMAN-UAT.md

@CLAUDE.md
@README.md
@docs/DEVOPS-HANDOFF.md
@tasks/todo.md

<interfaces>
<!-- Doc-by-doc drift inventory (verified 2026-04-25; lines may have shifted by execution time — re-grep before editing) -->

<!-- CLAUDE.md: -->
<!--   Line ~26: "982 total tests across 52 test files (944 passing, 38 skipped ...)" — UPDATE to live numbers -->
<!--   Line ~26: "41 no-console warnings" (if present) — UPDATE to 32 with fix-narrative -->
<!--   Phase 21 status — UPDATE to shipped after this plan completes -->

<!-- README.md: -->
<!--   "982 total tests" / "944 pass, 38 skipped" — appears at multiple lines (~27, 76, 117, 199) — UPDATE all -->
<!--   "41 no-console warnings" (line ~199 per Explore audit) — UPDATE to 32 -->
<!--   Phase 19 listed complete (2026-04-24) but Phase 20 / 21 may be missing — ADD them -->

<!-- docs/DEVOPS-HANDOFF.md: -->
<!--   "982 tests" / "944 passing" repeated 3+ times (lines ~24, 165, 179, 188) — UPDATE all -->
<!--   "41 no-console warnings" (line ~188) — UPDATE to 32 -->
<!--   Phase 21 backlog references Phase 19 numbers — UPDATE -->
<!--   No mention of Phase 20 daily-stats artifacts — ADD -->

<!-- .planning/PROJECT.md: -->
<!--   Line ~9 / ~79–80: "982 tests across 48 suites" — UPDATE to live numbers -->
<!--   Phase 20 listed as "(next)" but it's shipped (PR #39) — UPDATE to shipped -->
<!--   Phase 21 wording matches Lock 4 scope — confirm; UPDATE status when this plan ships -->

<!-- .planning/ROADMAP.md: -->
<!--   Line ~241: "41 no-console" in v1.4 progress table — UPDATE to 32 -->
<!--   Phase 21 unchecked in v1.4 progress table — flip to checked (`[x]`) when this plan ships -->

<!-- .planning/REQUIREMENTS.md (verified line ranges 91–94): -->
<!--   | HYG-UNICODE-01 | 21 — Closeout Hygiene | Pending | TBD | -->
<!--   | HYG-JSDOC-01   | 21 — Closeout Hygiene | Pending | TBD | -->
<!--   | HYG-CONSOLE-01 | 21 — Closeout Hygiene | Pending | TBD | -->
<!--   | DOCS-FINAL-01  | 21 — Closeout Hygiene | Pending | TBD | -->
<!--   All four flip Pending → Complete (or `[ ]` → `[x]` if checkbox column exists; match existing column shape). -->
<!--   The detailed HYG-UNICODE-01 description (elsewhere in the file) — REWRITE per Path B. -->
<!--   Stale "41" references inside the HYG-CONSOLE-01 row description — UPDATE to 32. -->

<!-- .planning/STATE.md: -->
<!--   status: executing → milestone-close-ready (or whatever the project's standard close state is) -->
<!--   stopped_at: ... → "Phase 21 complete; v1.4 ready for milestone close ceremony" -->
<!--   progress block: completed_phases bumps from 7 → 9 (Phases 20 + 21) when 21-04 lands -->

<!-- tasks/todo.md: -->
<!--   Confirm HYG-UNICODE-02 v1.5 entry exists (introduced by Plan 21-01); if not present (e.g. the Plan 21-01 entry was lost), add it. -->

<!-- LIVE TEST COUNT — re-count at execution time, do NOT hardcode: -->
<!--   `CI=true npm test 2>&1 | tail -10` → look for "Tests:" summary line -->
<!--   `find test/ -name "*.test.js" | wc -l` → file count -->
<!-- Sample shape from prior run (illustrative only — actual values come from live re-count): -->
<!--   Tests:       NN failed, NN skipped, NN passed, NN total -->
<!--   Test Suites: NN passed, NN total -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-count live test/coverage numbers and capture as the source-of-truth for all doc updates</name>
  <files>(read-only / no file modifications — produces session-internal values for downstream tasks)</files>
  <read_first>
    - none — this task is a fresh data-gathering step
  </read_first>
  <behavior>
    - Captures live test count, test file count, test suite count, skipped count, and (separately) live ESLint no-console count
    - Captures live coverage numbers (branch / statements / functions / lines) from the most recent passing CI=true npm test run
  </behavior>
  <action>
    Run, in order:
    1. `find test/ -name "*.test.js" | wc -l` — file count
    2. `CI=true npm test --forceExit 2>&1 | tee /tmp/21-04-tests.log | tail -30` — capture Tests:/Test Suites:/Coverage summary
    3. `grep -E "^Tests:|^Test Suites:" /tmp/21-04-tests.log` — extract the canonical lines
    4. `npx eslint src/ 2>&1 | grep -c "no-console"` — confirm no-console warnings (expected 0 after Plan 21-03 lands; if Plan 21-04 runs before 21-03 lands, this returns >0 — execution order is enforced by `depends_on`)
    5. Record results in a session note (or in the Task 6 SUMMARY scratch file). Use these values everywhere downstream — do not re-derive.
  </action>
  <verify>
    <automated>grep -E "^Tests:" /tmp/21-04-tests.log</automated>
  </verify>
  <acceptance_criteria>
    - Tests: line shows expected v1.4 final count (e.g. "Tests: ~38 skipped, ~1100 passed, ~1138 total" — actual numbers from live re-count)
    - Coverage block present in /tmp/21-04-tests.log
    - All four numbers (test file count, test count, suite count, skipped count) recorded for downstream use
    - ESLint no-console count is 0 (Plan 21-03 already shipped per `depends_on`)
  </acceptance_criteria>
  <done>
    Live numbers captured; downstream tasks reference these values, not the stale ones in CONTEXT or this plan brief.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refresh CLAUDE.md, README.md, docs/DEVOPS-HANDOFF.md with live test counts and 32-not-41 corrections</name>
  <files>
    CLAUDE.md
    README.md
    docs/DEVOPS-HANDOFF.md
  </files>
  <read_first>
    - CLAUDE.md (search for "982", "944", "41", "Phase 19", "Phase 20", "Phase 21" — confirm every drift hit)
    - README.md (same searches)
    - docs/DEVOPS-HANDOFF.md (same searches)
    - Task 1 captured numbers (use these — do NOT re-grep CONTEXT for stale figures)
  </read_first>
  <behavior>
    - Every mention of "982", "944", or test-count numbers in these 3 docs is updated to the live numbers from Task 1
    - Every mention of "41 no-console" in these 3 docs is updated to "32" with fix-narrative (e.g. "32 ESLint no-console warnings, all eliminated in Phase 21 via category-tagged disables")
    - Every Phase status reference reflects current state (Phase 20 shipped; Phase 21 in-flight or shipped depending on execution order — use "shipped" once 21-01/02/03 are merged and 21-04 is being shipped)
    - No narrative rewriting beyond drift fixes (Lock 5 fence: only "verifiable v1.4-fact errors" change)
  </behavior>
  <action>
    For each of the 3 docs:
    1. `grep -nE "982|944|\\b41\\b|Phase 19|Phase 20|Phase 21" <file>` — produces the full hit list.
    2. For each hit, decide: update to live number (from Task 1), update from "41" to "32" with fix-narrative, or leave as-is (e.g. if "41" appears in a context unrelated to no-console, like a phase number or page reference, leave it).
    3. Apply Edit operations one at a time, verifying each change. Do NOT do bulk sed replacements that might overwrite intended content.

    For "41" → "32" specifically: in fix-narrative passages, prefer wording like "32 ESLint no-console warnings" or "32 console sites (corrected from earlier 41 figure that reflected v1.3 backlog drift)" depending on context. The reviewer should be able to tell from the wording why "41" appears at all.

    For Phase 20 / Phase 21: ensure both are listed in any "shipped phases" / "milestone progress" tables in CLAUDE.md and README.md. Add Phase 20 "Value Extraction Instrumentation" with PR reference; add Phase 21 "Closeout Hygiene" with status (in-flight while this plan executes; shipped after 21-04 PR merges).
  </action>
  <verify>
    <automated>bash -c 'for f in CLAUDE.md README.md docs/DEVOPS-HANDOFF.md; do echo "=== $f ==="; grep -nE "982 total|944 passing|\\b41 (no-console|console)" "$f" || echo "NO STALE HITS"; done'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "982 total|944 passing" CLAUDE.md README.md docs/DEVOPS-HANDOFF.md` returns no matches
    - `grep -E "41 no-console|41 console" CLAUDE.md README.md docs/DEVOPS-HANDOFF.md` returns no matches (or only inside fix-narrative wording per acceptance review)
    - Each of the 3 docs mentions Phase 20 (shipped) and Phase 21 (in-flight or shipped)
    - `git diff master -- CLAUDE.md README.md docs/DEVOPS-HANDOFF.md` shows narrowly-targeted edits — no large block rewrites
  </acceptance_criteria>
  <done>
    All 3 docs reference live test counts and 32-console-sites; Phase 20 / 21 visible in milestone tables.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Refresh .planning/PROJECT.md and .planning/ROADMAP.md (planning artifacts)</name>
  <files>
    .planning/PROJECT.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/PROJECT.md (find "982 tests", "48 suites", "Phase 20", "Phase 21" hits)
    - .planning/ROADMAP.md (find "41 no-console" in v1.4 progress table; find Phase 21 row checkbox)
  </read_first>
  <behavior>
    - PROJECT.md test counts updated to live numbers from Task 1
    - PROJECT.md Phase 20 status changed from "(next)" to shipped with PR reference (PR #39 or whatever the actual merge-PR number is)
    - PROJECT.md Phase 21 status reflects current state
    - ROADMAP.md "41 no-console" → "32" with fix-narrative
    - ROADMAP.md v1.4 progress table — Phase 21 row checkbox flipped from `[ ]` to `[x]` (since this plan IS the Phase 21 close)
  </behavior>
  <action>
    1. PROJECT.md:
       - Update test count line to live numbers (from Task 1).
       - Find Phase 20 status line (currently "(next)"). Change to shipped state (e.g. "Phase 20 — Value Extraction Instrumentation (shipped 2026-04-NN, PR #NN)" — use the actual PR number from `git log` or PROJECT.md history).
       - Update Phase 21 status to reflect current state.

    2. ROADMAP.md:
       - Find "41" / "no-console" hits in v1.4 progress table.
       - Update to "32" with fix-narrative if there's room, or simply "32" if the table is tight.
       - Find the Phase 21 row in the v1.4 progress table. Flip checkbox `[ ]` → `[x]` (and update any "in progress" / "pending" status text to "complete" or whatever the existing convention shows for closed phases).

    3. Run `git diff master -- .planning/PROJECT.md .planning/ROADMAP.md` and review — confirm only drift fixes.
  </action>
  <verify>
    <automated>bash -c 'echo "=== PROJECT.md ==="; grep -nE "982 tests|48 suites|Phase 20|Phase 21" .planning/PROJECT.md; echo "=== ROADMAP.md ==="; grep -nE "\\b41\\b|Phase 21" .planning/ROADMAP.md'</automated>
  </verify>
  <acceptance_criteria>
    - PROJECT.md no longer says "982 tests across 48 suites" with stale numbers
    - PROJECT.md Phase 20 status is shipped with PR reference (no longer "(next)")
    - ROADMAP.md "41" inside HYG-CONSOLE-01 / no-console context is replaced with "32"
    - ROADMAP.md Phase 21 v1.4 progress row is `[x]` checked
    - `git diff master -- .planning/PROJECT.md .planning/ROADMAP.md` shows narrowly-targeted edits
  </acceptance_criteria>
  <done>
    Both planning artifacts match live state; Phase 21 progress row checked on ROADMAP.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update REQUIREMENTS.md — checkbox sync, HYG-UNICODE-01 Path B reframing, "41" → "32" correction</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md whole file (locate the 4 Phase 21 REQ rows around lines 91–94, find the detailed HYG-UNICODE-01 description elsewhere in the file, find any HYG-CONSOLE-01 description with "41")
    - .planning/phases/21-closeout-hygiene/21-CONTEXT.md (D-LOCK-5-AMEND-A wording for the HYG-UNICODE-01 reframing)
  </read_first>
  <behavior>
    - All 4 Phase 21 REQ rows show Complete/`[x]` status (sync per D-LOCK-4-SUB-i)
    - HYG-UNICODE-01 detailed description includes the Path B explicit-limitation language: "ASCII-only substring matching is the v1.4 contract; Unicode-variant catching is deferred to v1.5 HYG-UNICODE-02."
    - HYG-UNICODE-01 description references the 45 test.todo() entries from Plan 21-01 as the gap-documentation artifact
    - HYG-CONSOLE-01 description uses "32" (with optional fix-narrative wording where context allows)
    - No narrative rewriting beyond these specific corrections
  </behavior>
  <action>
    1. Locate the 4 Phase 21 REQ rows. For each, change "Pending" → "Complete" (or `[ ]` → `[x]` if that's the column shape). Match the exact column format used by other complete rows in the file.

    2. Locate the detailed HYG-UNICODE-01 description (separate from the table row). Edit its requirement text to add the Path B framing:

    ```
    HYG-UNICODE-01: Document the Unicode-variant gap for excluded-term matching by adding test.todo() entries
    in test/content-policy.test.js (one per term × variant type: full-width Latin, soft-hyphen-injected,
    non-ASCII whitespace; 15 terms × 3 = 45 entries). ASCII-only substring matching is the v1.4 contract;
    Unicode-variant catching is deferred to v1.5 HYG-UNICODE-02.
    ```
    (Adapt to fit the existing requirement format — header, status, acceptance — but the Path B sentence must appear verbatim or near-verbatim.)

    3. Locate any HYG-CONSOLE-01 description that says "41" and update to "32" with fix-narrative if appropriate.

    4. Confirm REQUIREMENTS.md still parses as the existing format (no broken table cells, no stray bullets).
  </action>
  <verify>
    <automated>bash -c 'echo "=== REQ rows ==="; grep -E "HYG-UNICODE-01|HYG-JSDOC-01|HYG-CONSOLE-01|DOCS-FINAL-01" .planning/REQUIREMENTS.md; echo "=== Path B ==="; grep -E "ASCII-only|HYG-UNICODE-02" .planning/REQUIREMENTS.md'</automated>
  </verify>
  <acceptance_criteria>
    - All 4 Phase 21 REQ rows show Complete (or `[x]`) status
    - REQUIREMENTS.md contains the phrase "ASCII-only substring matching" or near-equivalent Path B wording
    - REQUIREMENTS.md references "HYG-UNICODE-02" as the v1.5 successor
    - REQUIREMENTS.md no longer says "41 no-console" or "41 console" outside fix-narrative context
    - File still parses as valid markdown with intact tables
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md reflects Phase 21 completion + Path B reframing + 32-corpus correction.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Confirm HYG-UNICODE-02 in tasks/todo.md; cite 19-HUMAN-UAT.md in v1.4 close narrative</name>
  <files>
    tasks/todo.md
    .planning/REQUIREMENTS.md (or wherever the v1.4 close narrative is being authored — likely REQUIREMENTS.md or PROJECT.md)
  </files>
  <read_first>
    - tasks/todo.md (confirm HYG-UNICODE-02 entry from Plan 21-01 is present)
    - .planning/phases/19-semantic-memory-search/19-HUMAN-UAT.md (confirm `status: complete` is unchanged)
  </read_first>
  <behavior>
    - tasks/todo.md HYG-UNICODE-02 entry exists with the linkage back to Plan 21-01 (added by Plan 21-01 Task 2; this task verifies, does not duplicate)
    - 19-HUMAN-UAT.md status:complete confirmed unchanged (no-op verify per D-LOCK-4-SUB-iii)
    - The v1.4 close narrative (in REQUIREMENTS.md or PROJECT.md) cites 19-HUMAN-UAT.md as evidence of Phase 19 UAT closure
  </behavior>
  <action>
    1. `grep -F "HYG-UNICODE-02" tasks/todo.md` — confirm the entry exists. If missing (i.e. Plan 21-01 didn't ship it for some reason), add the wording from Plan 21-01 Task 2 here as a fallback.

    2. `grep -E "^status:" .planning/phases/19-semantic-memory-search/19-HUMAN-UAT.md` — confirm `status: complete` is the current frontmatter value. Do NOT modify the file.

    3. In REQUIREMENTS.md or PROJECT.md (wherever the v1.4 close narrative summarizes shipped phases), add a single citation line near the Phase 19 entry, e.g.:
    ```
    Phase 19 UAT closure: see .planning/phases/19-semantic-memory-search/19-HUMAN-UAT.md (status: complete; 4/4 UAT items pass with detailed notes including Test 1 calibration fix 0.72 → 0.55).
    ```
    Match the existing milestone-narrative format. One line, no rewriting elsewhere.
  </action>
  <verify>
    <automated>grep -E "HYG-UNICODE-02|19-HUMAN-UAT" tasks/todo.md .planning/REQUIREMENTS.md .planning/PROJECT.md 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "HYG-UNICODE-02" tasks/todo.md` returns at least 1
    - `grep -F "status: complete" .planning/phases/19-semantic-memory-search/19-HUMAN-UAT.md` returns 1 (file unchanged)
    - REQUIREMENTS.md or PROJECT.md contains a reference to "19-HUMAN-UAT.md"
    - No edit to 19-HUMAN-UAT.md itself
  </acceptance_criteria>
  <done>
    HYG-UNICODE-02 backlog confirmed; 19-HUMAN-UAT.md cited; D-LOCK-4-SUB-iii closed as no-op verify.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Run quality-gate trio (npm audit, license-checker, GitGuardian); record results in 21-04-SUMMARY.md</name>
  <files>.planning/phases/21-closeout-hygiene/21-04-SUMMARY.md</files>
  <read_first>
    - none (fresh quality-gate execution)
  </read_first>
  <behavior>
    - npm audit reports 0 high/critical vulnerabilities (or documented exceptions)
    - license-checker confirms all dependency licenses pass the project's allowlist
    - GitGuardian secrets scan reports 0 secrets in the working tree
    - Results recorded in 21-04-SUMMARY.md
  </behavior>
  <action>
    Run each gate and capture the summary line:
    1. `npm audit --json | jq '.metadata.vulnerabilities'` — expect zeros for high/critical (or documented exceptions). Save the output line.
    2. `npx license-checker --summary` — confirm all licenses are in the project's allowlist (the existing CI license-check step should already enforce this; this is a confirmation re-run).
    3. GitGuardian scan: if `ggshield` is locally available, run `ggshield secret scan repo`; otherwise rely on the latest CI GitGuardian status (cite the workflow run ID).

    Record each result in `.planning/phases/21-closeout-hygiene/21-04-SUMMARY.md` under a "Quality Gates" section with the date, command, and pass/fail.
  </action>
  <verify>
    <automated>grep -E "npm audit|license-check|GitGuardian|ggshield" .planning/phases/21-closeout-hygiene/21-04-SUMMARY.md | head -10</automated>
  </verify>
  <acceptance_criteria>
    - 21-04-SUMMARY.md has a "Quality Gates" section
    - All 3 gates have a recorded pass/fail with date and command
    - Any failures are documented with mitigation (e.g. accepted exception or follow-up backlog item)
  </acceptance_criteria>
  <done>
    Quality-gate trio confirmed green and recorded in SUMMARY.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Update .planning/STATE.md to milestone-close ready state</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current frontmatter and Current Position section)
    - .planning/phases/21-closeout-hygiene/21-CONTEXT.md (close-state expectations)
  </read_first>
  <behavior>
    - Frontmatter reflects Phase 21 complete (completed_phases bumps from 7 to at least 9 to include Phases 20 and 21)
    - status field shows the milestone-close-ready state (whatever the project's standard wording is)
    - stopped_at reflects "Phase 21 complete; v1.4 ready for milestone close ceremony"
    - Current Position section updated to reflect closed milestone
  </behavior>
  <action>
    Edit STATE.md frontmatter:
    - `completed_phases: 7` → `completed_phases: 9` (or whatever count matches the actual completed-phase total after Phases 20 and 21 ship)
    - `completed_plans: 27` → updated count reflecting all 21-NN plans merged
    - `stopped_at: "Completed 20-05-PLAN.md"` → `stopped_at: "Completed Phase 21; v1.4 milestone close ready"`
    - `last_updated:` → current ISO timestamp
    - `last_activity:` → current date
    - `status: executing` → milestone-close-ready (or matching project convention)

    Edit Current Position section:
    - Phase: 21 → milestone-close
    - Plan: Not started → All 4 plans complete
    - Status: Ready to execute → Ready for `/gsd:complete-milestone v1.4` ceremony
  </action>
  <verify>
    <automated>grep -E "^status:|^completed_phases:|^stopped_at:" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md frontmatter status reflects milestone-close-ready
    - completed_phases count incremented to match actual phase completion
    - stopped_at message names "v1.4" and "milestone close"
    - Current Position section reflects closed state
  </acceptance_criteria>
  <done>
    STATE.md ready for /gsd:complete-milestone v1.4 ceremony.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 8: Author 21-04-SUMMARY.md as the v1.4 close ledger</name>
  <files>.planning/phases/21-closeout-hygiene/21-04-SUMMARY.md</files>
  <read_first>
    - All edits made by Tasks 1–7 (provides the doc-by-doc change log)
    - Task 6 quality-gate output (already captured in this file under "Quality Gates")
  </read_first>
  <behavior>
    - 21-04-SUMMARY.md exists and consolidates all v1.4 close artifacts: live test/coverage numbers, doc-by-doc change log (8 files touched), quality-gate results, REQUIREMENTS.md checkbox sync confirmation, HYG-UNICODE-02 v1.5 backlog confirmation, 19-HUMAN-UAT.md citation
    - Cites D-LOCK-4 and all sub-tasks (D-LOCK-4-SUB-i, ii, iii) as completion evidence
    - Cites D-LOCK-5-AMEND-A as the framing for the HYG-UNICODE-01 reframing in REQUIREMENTS.md
  </behavior>
  <action>
    Build out 21-04-SUMMARY.md (already partially populated by Task 6's Quality Gates section). Add sections:
    1. **Plan metadata** (phase, plan, requirement, completion date)
    2. **Live numbers (Task 1 capture)** — test file count, test count, suites, skipped, coverage block
    3. **Doc-by-doc change log** — for each of CLAUDE.md, README.md, docs/DEVOPS-HANDOFF.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, tasks/todo.md, list the specific drift fixes applied
    4. **REQUIREMENTS.md checkbox sync** — confirmation that all 4 Phase 21 REQ rows are now Complete/`[x]`
    5. **Path B reframing record** — show the before/after wording of HYG-UNICODE-01 description in REQUIREMENTS.md
    6. **Quality Gates** (already populated from Task 6)
    7. **Sub-task closures** — explicit confirmation of D-LOCK-4-SUB-i, ii, iii
    8. **v1.4 close readiness** — pointer to STATE.md milestone-close-ready state and next-step `/gsd:complete-milestone v1.4` ceremony
  </action>
  <verify>
    <automated>wc -l .planning/phases/21-closeout-hygiene/21-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - 21-04-SUMMARY.md exists and contains all 8 sections above
    - Cites D-LOCK-4, D-LOCK-4-SUB-i, ii, iii, D-LOCK-5-AMEND-A
    - References live (Task 1) numbers, not stale ones
    - Lists every file modified by Plan 21-04 with the specific drift fix applied
  </acceptance_criteria>
  <done>
    21-04-SUMMARY.md is the v1.4 close ledger; ready for `/gsd:complete-milestone v1.4` to consume.
  </done>
</task>

</tasks>

<verification>
- `grep -E "982 total|944 passing" CLAUDE.md README.md docs/DEVOPS-HANDOFF.md .planning/PROJECT.md` returns no matches
- `grep -E "\\b41 no-console|\\b41 console" CLAUDE.md README.md docs/DEVOPS-HANDOFF.md .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns no matches outside fix-narrative wording
- `grep -E "ASCII-only|HYG-UNICODE-02" .planning/REQUIREMENTS.md` returns at least 2 matches
- All 4 Phase 21 REQ rows in REQUIREMENTS.md show Complete (or `[x]`)
- `grep -F "status: complete" .planning/phases/19-semantic-memory-search/19-HUMAN-UAT.md` still returns 1 (file untouched)
- `git diff master -- .planning/phases/19-semantic-memory-search/` returns empty
- `.planning/STATE.md` frontmatter status is milestone-close-ready
- `.planning/phases/21-closeout-hygiene/21-04-SUMMARY.md` exists and contains all 8 mandated sections
- `CI=true npm test --forceExit` exits 0 (no regression)
- `npx eslint src/` reports 0 errors / 0 warnings
</verification>

<success_criteria>
1. All 5 named docs (CLAUDE.md, README.md, docs/DEVOPS-HANDOFF.md, .planning/PROJECT.md, .planning/ROADMAP.md) reference live test counts (re-counted at execution time, NOT hardcoded) and the corrected "32" console-site figure
2. REQUIREMENTS.md HYG-UNICODE-01 row is reframed per Path B with explicit ASCII-only-contract language and HYG-UNICODE-02 successor reference
3. All 4 Phase 21 REQ rows in REQUIREMENTS.md show Complete (D-LOCK-4-SUB-i)
4. tasks/todo.md HYG-UNICODE-02 v1.5 backlog entry is confirmed present
5. 19-HUMAN-UAT.md is cited in v1.4 close narrative; file content unchanged (D-LOCK-4-SUB-iii)
6. PROJECT.md Phase 20 status corrected from "(next)" to shipped with PR reference
7. STATE.md flipped to milestone-close-ready
8. Quality gates (npm audit, license-checker, GitGuardian) confirmed green; results in 21-04-SUMMARY.md (D-LOCK-4-SUB-ii)
9. 21-04-SUMMARY.md authored as v1.4 close ledger with 8 mandated sections
10. Lock 5 fence preserved — no code edits in Plan 21-04
</success_criteria>

<output>
21-04-SUMMARY.md is the canonical close ledger. After it lands, the next step is the v1.4 milestone-close ceremony (typically `/gsd:complete-milestone v1.4` or equivalent), which is OUT of Phase 21 scope and runs in a separate session.

Note for the executor: Plan 21-04 deliberately does NOT hardcode the live test count anywhere in this brief. All numerical values come from Task 1's re-count at execution time. This avoids the staleness pattern (CONTEXT-time number → plan-write-time number → execution-time number) that creates phantom drift.
</output>
