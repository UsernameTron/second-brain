---
phase: 21-closeout-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/content-policy.test.js
autonomous: true
requirements:
  - HYG-UNICODE-01
must_haves:
  truths:
    - "test/content-policy.test.js gains exactly 45 test.todo() entries (15 excluded terms × 3 Unicode variant types) covering full-width Latin, soft-hyphen-injected, and non-ASCII whitespace variants"
    - "src/content-policy.js is NOT modified — Lock 5 fence preserved per D-LOCK-5-AMEND-A"
    - "test.todo() blocks document the gap without asserting behavior the ASCII-only matcher cannot deliver"
    - "HYG-UNICODE-02 added to tasks/todo.md as v1.5 backlog (matcher upgrade) — backfills the test.todo() blocks when matcher is upgraded"
    - "Running `CI=true npm test` after this plan reports 0 new failures (test.todo entries are reported as 'todo', not 'failed')"
  artifacts:
    - path: "test/content-policy.test.js"
      provides: "45 test.todo() entries documenting Unicode-variant gap; one per (term × variant) pair"
      contains: "test.todo"
    - path: "tasks/todo.md"
      provides: "HYG-UNICODE-02 v1.5 backlog entry for Unicode-aware matcher upgrade"
      contains: "HYG-UNICODE-02"
  key_links:
    - from: "test/content-policy.test.js"
      to: "src/content-policy.js"
      via: "test.todo() blocks naming the matcher behavior reserved for v1.5 HYG-UNICODE-02"
      pattern: "test\\.todo.*Unicode"
---

<objective>
Document the ASCII-only-matcher Unicode-variant gap by adding 45 `test.todo()` entries to `test/content-policy.test.js` (one per excluded-term × Unicode-variant-type pair). Reserve the test surface; do not assert behavior the matcher cannot deliver. Record a v1.5 backlog entry (HYG-UNICODE-02) for the matcher upgrade that will backfill these entries.

Per D-LOCK-5-AMEND-A (Path B), Phase 21 acknowledges the matcher's ASCII-only behavior as a v1.4 limitation. No `src/` logic changes. Lock 5 fence holds without exception.

Variant inventory is locked to Lock 5's three types: full-width Latin, soft-hyphen-injected, non-ASCII whitespace. (Lock 1's larger inventory of curly quotes / em-dashes / smart apostrophes is a sizing-estimate artifact — those characters do not appear inside ASCII identifiers like ISPN/Genesys/Asana — and is not authoritative for implementation.)

Purpose: Closeout hygiene that documents the gap durably while preserving the Lock 5 fence. Backfill happens in v1.5 HYG-UNICODE-02 when the matcher is upgraded.
Output: 45 `test.todo()` entries + one tasks/todo.md backlog entry for HYG-UNICODE-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-closeout-hygiene/21-CONTEXT.md
@.planning/REQUIREMENTS.md

@config/excluded-terms.json
@src/content-policy.js
@test/content-policy.test.js
@tasks/todo.md

<interfaces>
<!-- Excluded-term corpus (do not modify): config/excluded-terms.json contains 15 ASCII-only terms -->
<!-- Verbatim list (verified 2026-04-25): -->
<!--   Asana, Five9, Fiverr, Genesys, ININ, Interactive Intelligence, ISPN, -->
<!--   Onbe, OpenDoor, PureCloud, PureConnect, Sandler, Stride Care, Totango, UKG -->

<!-- Matcher (do NOT modify per D-LOCK-5-AMEND-A): src/content-policy.js:195-204 -->
<!-- Current behavior: -->
<!--   const contentLower = content.toLowerCase(); -->
<!--   if (contentLower.includes(term.toLowerCase())) { ... } -->
<!-- ASCII-only — cannot match full-width Latin, soft-hyphen-injected, or non-ASCII whitespace variants -->

<!-- Variant generation rules (test surface only): -->
<!--   FULL_WIDTH: replace each ASCII letter A-Z/a-z with the corresponding Unicode full-width Latin code point (U+FF21–U+FF3A / U+FF41–U+FF5A); spaces stay ASCII -->
<!--   SOFT_HYPHEN: insert U+00AD (soft-hyphen, zero-width when not at line break) between every adjacent pair of letters -->
<!--   NON_ASCII_WHITESPACE: replace every ASCII space with U+00A0 (no-break space); for single-word terms, insert one U+00A0 between the third and fourth character -->

<!-- test.todo() syntax (Jest): -->
<!--   test.todo('description string'); -->
<!--   Reports as "todo" in jest output, never executes a body, never fails. Reserves the slot. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 45 test.todo() entries to test/content-policy.test.js</name>
  <files>test/content-policy.test.js</files>
  <read_first>
    - test/content-policy.test.js (locate appropriate describe block and existing test structure)
    - config/excluded-terms.json (confirm 15 terms, exact spelling and case)
    - src/content-policy.js:195-204 (re-read to confirm matcher is unchanged ASCII-only)
    - .planning/phases/21-closeout-hygiene/21-CONTEXT.md (D-LOCK-5-AMEND-A verbatim wording)
  </read_first>
  <behavior>
    - test/content-policy.test.js gains a single new describe block: `describe('HYG-UNICODE-01: Unicode variant coverage (test.todo — gap reserved for v1.5 HYG-UNICODE-02)')`
    - Inside the describe block: 45 test.todo() entries, one per (term × variant) pair
    - Each test.todo description names the excluded term and the variant type, e.g. `test.todo("Genesys — full-width Latin variant: matcher catches Ｇｅｎｅｓｙｓ");`
    - Existing tests in the file continue to pass without modification
    - jest reports 45 new entries under "todo" status, 0 new "failed" entries
  </behavior>
  <action>
    1. Open `test/content-policy.test.js`. Identify the most appropriate location for the new describe block (typically end of file, after the last existing describe).

    2. Add the following block at the end of the file, before any final closing braces:

    ```javascript
    describe('HYG-UNICODE-01: Unicode variant coverage (test.todo — gap reserved for v1.5 HYG-UNICODE-02)', () => {
      // Per D-LOCK-5-AMEND-A (Path B): src/content-policy.js matcher is ASCII-only
      // (.toLowerCase().includes()). The test.todo entries below document the gap
      // and reserve the test surface. They will be activated and asserted when
      // v1.5 HYG-UNICODE-02 ships an Unicode-aware matcher.
      //
      // Variant inventory (Lock 5):
      //   - full-width Latin (U+FF21–U+FF3A / U+FF41–U+FF5A)
      //   - soft-hyphen-injected (U+00AD between letters)
      //   - non-ASCII whitespace (U+00A0 substituted for spaces, or inserted)
      //
      // 15 excluded terms × 3 variants = 45 test.todo entries.

      // ── Asana ───────────────────────────────────────────────────────────────
      test.todo('Asana — full-width Latin variant: matcher catches Ａｓａｎａ');
      test.todo('Asana — soft-hyphen variant: matcher catches A\\u00ADs\\u00ADa\\u00ADn\\u00ADa');
      test.todo('Asana — non-ASCII whitespace variant: matcher catches Asa\\u00A0na');

      // ── Five9 ───────────────────────────────────────────────────────────────
      test.todo('Five9 — full-width Latin variant: matcher catches Ｆｉｖｅ９');
      test.todo('Five9 — soft-hyphen variant: matcher catches F\\u00ADi\\u00ADv\\u00ADe\\u00AD9');
      test.todo('Five9 — non-ASCII whitespace variant: matcher catches Fiv\\u00A0e9');

      // ── Fiverr ──────────────────────────────────────────────────────────────
      test.todo('Fiverr — full-width Latin variant: matcher catches Ｆｉｖｅｒｒ');
      test.todo('Fiverr — soft-hyphen variant: matcher catches F\\u00ADi\\u00ADv\\u00ADe\\u00ADr\\u00ADr');
      test.todo('Fiverr — non-ASCII whitespace variant: matcher catches Fiv\\u00A0err');

      // ── Genesys ─────────────────────────────────────────────────────────────
      test.todo('Genesys — full-width Latin variant: matcher catches Ｇｅｎｅｓｙｓ');
      test.todo('Genesys — soft-hyphen variant: matcher catches G\\u00ADe\\u00ADn\\u00ADe\\u00ADs\\u00ADy\\u00ADs');
      test.todo('Genesys — non-ASCII whitespace variant: matcher catches Gen\\u00A0esys');

      // ── ININ ────────────────────────────────────────────────────────────────
      test.todo('ININ — full-width Latin variant: matcher catches ＩＮＩＮ');
      test.todo('ININ — soft-hyphen variant: matcher catches I\\u00ADN\\u00ADI\\u00ADN');
      test.todo('ININ — non-ASCII whitespace variant: matcher catches INI\\u00A0N');

      // ── Interactive Intelligence ────────────────────────────────────────────
      test.todo('Interactive Intelligence — full-width Latin variant: matcher catches Ｉｎｔｅｒａｃｔｉｖｅ Ｉｎｔｅｌｌｉｇｅｎｃｅ');
      test.todo('Interactive Intelligence — soft-hyphen variant: matcher catches I\\u00ADn\\u00ADt\\u00ADe\\u00ADr\\u00ADa\\u00ADc\\u00ADt\\u00ADi\\u00ADv\\u00ADe I\\u00ADn\\u00ADt\\u00ADe\\u00ADl\\u00ADl\\u00ADi\\u00ADg\\u00ADe\\u00ADn\\u00ADc\\u00ADe');
      test.todo('Interactive Intelligence — non-ASCII whitespace variant: matcher catches Interactive\\u00A0Intelligence');

      // ── ISPN ────────────────────────────────────────────────────────────────
      test.todo('ISPN — full-width Latin variant: matcher catches ＩＳＰＮ');
      test.todo('ISPN — soft-hyphen variant: matcher catches I\\u00ADS\\u00ADP\\u00ADN');
      test.todo('ISPN — non-ASCII whitespace variant: matcher catches ISP\\u00A0N');

      // ── Onbe ────────────────────────────────────────────────────────────────
      test.todo('Onbe — full-width Latin variant: matcher catches Ｏｎｂｅ');
      test.todo('Onbe — soft-hyphen variant: matcher catches O\\u00ADn\\u00ADb\\u00ADe');
      test.todo('Onbe — non-ASCII whitespace variant: matcher catches Onb\\u00A0e');

      // ── OpenDoor ────────────────────────────────────────────────────────────
      test.todo('OpenDoor — full-width Latin variant: matcher catches ＯｐｅｎＤｏｏｒ');
      test.todo('OpenDoor — soft-hyphen variant: matcher catches O\\u00ADp\\u00ADe\\u00ADn\\u00ADD\\u00ADo\\u00ADo\\u00ADr');
      test.todo('OpenDoor — non-ASCII whitespace variant: matcher catches Ope\\u00A0nDoor');

      // ── PureCloud ───────────────────────────────────────────────────────────
      test.todo('PureCloud — full-width Latin variant: matcher catches ＰｕｒｅＣｌｏｕｄ');
      test.todo('PureCloud — soft-hyphen variant: matcher catches P\\u00ADu\\u00ADr\\u00ADe\\u00ADC\\u00ADl\\u00ADo\\u00ADu\\u00ADd');
      test.todo('PureCloud — non-ASCII whitespace variant: matcher catches Pur\\u00A0eCloud');

      // ── PureConnect ─────────────────────────────────────────────────────────
      test.todo('PureConnect — full-width Latin variant: matcher catches ＰｕｒｅＣｏｎｎｅｃｔ');
      test.todo('PureConnect — soft-hyphen variant: matcher catches P\\u00ADu\\u00ADr\\u00ADe\\u00ADC\\u00ADo\\u00ADn\\u00ADn\\u00ADe\\u00ADc\\u00ADt');
      test.todo('PureConnect — non-ASCII whitespace variant: matcher catches Pur\\u00A0eConnect');

      // ── Sandler ─────────────────────────────────────────────────────────────
      test.todo('Sandler — full-width Latin variant: matcher catches Ｓａｎｄｌｅｒ');
      test.todo('Sandler — soft-hyphen variant: matcher catches S\\u00ADa\\u00ADn\\u00ADd\\u00ADl\\u00ADe\\u00ADr');
      test.todo('Sandler — non-ASCII whitespace variant: matcher catches San\\u00A0dler');

      // ── Stride Care ─────────────────────────────────────────────────────────
      test.todo('Stride Care — full-width Latin variant: matcher catches Ｓｔｒｉｄｅ Ｃａｒｅ');
      test.todo('Stride Care — soft-hyphen variant: matcher catches S\\u00ADt\\u00ADr\\u00ADi\\u00ADd\\u00ADe C\\u00ADa\\u00ADr\\u00ADe');
      test.todo('Stride Care — non-ASCII whitespace variant: matcher catches Stride\\u00A0Care');

      // ── Totango ─────────────────────────────────────────────────────────────
      test.todo('Totango — full-width Latin variant: matcher catches Ｔｏｔａｎｇｏ');
      test.todo('Totango — soft-hyphen variant: matcher catches T\\u00ADo\\u00ADt\\u00ADa\\u00ADn\\u00ADg\\u00ADo');
      test.todo('Totango — non-ASCII whitespace variant: matcher catches Tot\\u00A0ango');

      // ── UKG ─────────────────────────────────────────────────────────────────
      test.todo('UKG — full-width Latin variant: matcher catches ＵＫＧ');
      test.todo('UKG — soft-hyphen variant: matcher catches U\\u00ADK\\u00ADG');
      test.todo('UKG — non-ASCII whitespace variant: matcher catches UK\\u00A0G');
    });
    ```

    3. Do NOT modify `src/content-policy.js`. The matcher remains ASCII-only per D-LOCK-5-AMEND-A.

    4. Verify the file still parses as JavaScript and existing tests pass.
  </action>
  <verify>
    <automated>CI=true npx jest test/content-policy.test.js --forceExit 2>&1 | tee /tmp/21-01-jest.log; grep -E "Tests:.*todo" /tmp/21-01-jest.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test\\.todo" test/content-policy.test.js` returns at least 45 (the new entries; baseline is 0)
    - `grep -c "HYG-UNICODE-01" test/content-policy.test.js` returns 1 (the describe block header)
    - `git diff master -- src/content-policy.js` returns empty (matcher unchanged)
    - `CI=true npx jest test/content-policy.test.js --forceExit` exits 0
    - Jest output shows "45 todo" (or higher if other todos pre-existed) and "0 failed" for this file
    - `node -e "JSON.parse(require('fs').readFileSync('config/excluded-terms.json','utf8')).length === 15 || process.exit(1)"` exits 0 (corpus integrity check)
  </acceptance_criteria>
  <done>
    test/content-policy.test.js has 45 new test.todo entries documenting the Unicode-variant gap; matcher unchanged; jest passes without new failures.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Capture HYG-UNICODE-02 in tasks/todo.md v1.5 backlog</name>
  <files>tasks/todo.md</files>
  <read_first>
    - tasks/todo.md (locate v1.5 backlog section; existing format conventions for backlog items)
    - .planning/phases/21-closeout-hygiene/21-CONTEXT.md (D-LOCK-5-AMEND-A wording for the deferred requirement)
  </read_first>
  <behavior>
    - tasks/todo.md contains the HYG-UNICODE-02 v1.5 backlog entry (added by PR #40 discuss-phase commit). This task verifies presence; only adds if grep returns 0 — idempotent against the existing entry.
  </behavior>
  <action>
    Step 0 — Verify presence:

    ```bash
    grep -cF 'HYG-UNICODE-02' tasks/todo.md
    ```

    If the count is >= 1, this task is COMPLETE — HYG-UNICODE-02 was already captured in PR #40 discuss-phase. Skip Step 1 and proceed to verification.

    Step 1 — Add ONLY if Step 0 returned 0. Add to `tasks/todo.md` under the v1.5 backlog section using existing formatting conventions:

    ```markdown
    - **HYG-UNICODE-02** (v1.5): Upgrade `src/content-policy.js` matcher to catch Unicode variants of excluded terms (full-width Latin, soft-hyphen-injected, non-ASCII whitespace). Backfills the 45 `test.todo()` entries in `test/content-policy.test.js` introduced by Phase 21 Plan 21-01 (HYG-UNICODE-01). Approach: NFKC normalization + soft-hyphen stripping + non-ASCII-whitespace folding before `.includes()`. Per Phase 21 D-LOCK-5-AMEND-A.
    ```
  </action>
  <verify>
    <automated>grep -cF 'HYG-UNICODE-02' tasks/todo.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF 'HYG-UNICODE-02' tasks/todo.md` returns EXACTLY 1 (no duplicate from Plan 21-01 re-execution)
    - `grep -q 'v1.5' tasks/todo.md` exits 0 (backlog section exists)
    - The entry references 'test.todo' or 'Plan 21-01' so the backfill linkage is discoverable
  </acceptance_criteria>
  <done>
    HYG-UNICODE-02 captured in v1.5 backlog with explicit linkage to Plan 21-01's test.todo() blocks.
  </done>
</task>

</tasks>

<verification>
- `git diff master -- src/` returns empty (Lock 5 fence preserved)
- `CI=true npx jest test/content-policy.test.js --forceExit` exits 0 with 45 new "todo" entries
- `CI=true npm test --forceExit` exits 0 (no regression elsewhere)
- `npm run lint` exits 0 (no new ESLint errors in the test file)
- `grep -c "HYG-UNICODE-02" tasks/todo.md` returns 1
- `grep -c "test\\.todo" test/content-policy.test.js` returns at least 45
</verification>

<success_criteria>
1. `test/content-policy.test.js` has 45 new `test.todo()` entries — one per (15 excluded terms × 3 variant types: full-width Latin, soft-hyphen, non-ASCII whitespace)
2. `src/content-policy.js` is unchanged from master (Lock 5 fence preserved per D-LOCK-5-AMEND-A)
3. `tasks/todo.md` has a HYG-UNICODE-02 v1.5 backlog entry naming Plan 21-01 as the source of the test.todo() blocks
4. `CI=true npm test --forceExit` exits 0 — todo entries do not fail
5. No regression in existing content-policy tests
</success_criteria>

<output>
After completion, create `.planning/phases/21-closeout-hygiene/21-01-SUMMARY.md` documenting:
- Number of test.todo() entries added (expected: 45)
- Confirmation that src/content-policy.js was not modified
- Citation of D-LOCK-5-AMEND-A (Path B) as the framing decision
- Reference to HYG-UNICODE-02 v1.5 backlog entry as the backfill target
</output>
