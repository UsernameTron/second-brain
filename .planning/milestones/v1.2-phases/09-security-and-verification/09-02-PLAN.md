---
phase: 09-security-and-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/test-verifier.md
autonomous: true
requirements:
  - SEC-02

must_haves:
  truths:
    - "Running 'Use the test-verifier agent' executes the full Jest suite and reports pass/fail counts"
    - "Test verifier never modifies source or test files — read-only reporter"
    - "Failure output includes file path and line number for each failing test"
    - "Agent is distinct from test-runner: verifier reports, runner fixes"
  artifacts:
    - path: ".claude/agents/test-verifier.md"
      provides: "Independent test verification gate agent"
      contains: "name: test-verifier"
  key_links:
    - from: ".claude/agents/test-verifier.md"
      to: "npm test"
      via: "Bash tool execution"
      pattern: "npm test"
---

<objective>
Deploy the independent test verification gate agent (SEC-02) that runs the full Jest suite and reports results without modifying code.

Purpose: Provides an independent verification layer distinct from test-runner. Test-verifier reports pass/fail only (read-only, no Edit tool). Test-runner fixes failures (has Edit). Both coexist for different purposes per D-12.

Output: Test verifier agent file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-security-and-verification/09-CONTEXT.md

<interfaces>
<!-- Existing agent pattern from test-runner.md -->
Frontmatter: name, description, tools, model
Body: role statement, When Invoked steps, Constraints, Output Format

<!-- test-runner.md tools for contrast -->
test-runner: tools: Read, Edit, Bash, Glob, Grep (has Edit — fixes things)
test-verifier: tools: Read, Grep, Glob, Bash (NO Edit — reports only, per D-09)

<!-- test-runner model -->
test-runner: model: haiku
test-verifier: model: haiku (per D-11 — mechanical task, speed matters)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test-verifier agent</name>
  <files>.claude/agents/test-verifier.md</files>
  <read_first>
    - .claude/agents/test-runner.md (existing agent pattern to follow — but note test-verifier is read-only, no Edit tool)
    - .claude/agents/vault-guardian.md (read-only agent pattern with structured output format)
  </read_first>
  <action>
Create `.claude/agents/test-verifier.md` with this exact structure:

Frontmatter:
- name: test-verifier
- description: "Independent test verification gate — runs the full Jest suite and reports pass/fail results without modifying code. Use proactively after GSD execution phases complete, during /gsd:verify-work, or on demand for test health checks. Reports only — never fixes."
- tools: Read, Grep, Glob, Bash
- model: haiku

Body sections:

1. Role statement: "You are an independent test verification gate for a Node.js project using Jest 30. Your role is strictly reporting — you observe and report test outcomes but never modify code."

2. "When Invoked" section with 4 numbered steps:
   - Step 1: Run `cd "$CLAUDE_PROJECT_DIR" && npx jest --verbose --no-coverage 2>&1` to execute the full test suite with verbose output.
   - Step 2: Parse the output to extract: total test count, passed count, failed count, suite count.
   - Step 3: For each failure, extract: test name, file path, line number (from stack trace), and failure message. Use `grep` or `Read` on failing test files if stack traces are ambiguous.
   - Step 4: Produce the structured output report.

3. "Constraints" section:
   - Never modify any file. No Edit tool, no Write tool, no file creation.
   - Never run `npm install`, `npm fix`, or any command that changes project state.
   - Never mark tests as `.skip` or suggest skipping tests.
   - Run the full suite every time — never a subset. Regressions hide in unrelated modules.
   - If Jest itself fails to start (config error, missing dependency), report the error as-is without attempting repair.

4. "Output Format" section:
```
Test Verification Report
========================
Suite: [N] suites, [N] passed, [N] failed
Tests: [N] total, [N] passed, [N] failed
Duration: [N]s

Failures:
  [1] test/module.test.js:42 — "test name"
      Expected: X
      Received: Y

  [2] test/other.test.js:18 — "test name"
      Error: description

Verdict: PASS | FAIL ([N] failures)
```

If all tests pass, the Failures section should read: "Failures: none"
The Verdict line is the critical output — PASS when 0 failures, FAIL with count otherwise.
  </action>
  <verify>
    <automated>test -f .claude/agents/test-verifier.md && grep -q "name: test-verifier" .claude/agents/test-verifier.md && grep -q "tools: Read, Grep, Glob, Bash" .claude/agents/test-verifier.md && grep -q "model: haiku" .claude/agents/test-verifier.md && ! grep -q "Edit" .claude/agents/test-verifier.md && grep -q "Verdict" .claude/agents/test-verifier.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/test-verifier.md exists
    - Frontmatter contains `name: test-verifier`
    - Frontmatter contains `tools: Read, Grep, Glob, Bash` (no Edit, no Write)
    - Frontmatter contains `model: haiku`
    - Description includes "verify-work" trigger language (per D-13)
    - Description includes "independent" or "verification gate" language
    - Body contains "When Invoked" section with `npx jest --verbose`
    - Body contains "Constraints" section with "Never modify" language
    - Body contains "Output Format" section with `Verdict: PASS | FAIL` template
    - File does NOT contain "Edit" anywhere (tools or body)
    - Body does NOT contain "fix" as an action the agent should take
  </acceptance_criteria>
  <done>Test verifier agent exists at .claude/agents/test-verifier.md, is read-only (no Edit tool), reports pass/fail with file:line context, distinct from test-runner</done>
</task>

</tasks>

<verification>
1. `.claude/agents/test-verifier.md` exists with correct frontmatter
2. Agent tools are `Read, Grep, Glob, Bash` — no Edit (per D-09)
3. Agent model is `haiku` (per D-11)
4. Output format includes Verdict line
5. Agent description includes /gsd:verify-work trigger language (per D-13)
6. Agent is distinct from test-runner (no fix capability, no Edit tool)
</verification>

<success_criteria>
- Test verifier agent is invokable and reports test results
- Agent never modifies files — tools list excludes Edit and Write
- Output format provides file:line context for failures
- Agent description triggers on /gsd:verify-work context
</success_criteria>

<output>
After completion, create `.planning/phases/09-security-and-verification/09-02-SUMMARY.md`
</output>
