---
phase: 10-agent-hardening-and-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/docs-sync.md
  - .claude/agents/pipeline-reviewer.md
  - .claude/agents/test-runner.md
  - .claude/agents/vault-guardian.md
  - .mcp.json
autonomous: true
requirements:
  - SKILL-01
  - SKILL-03

must_haves:
  truths:
    - "All four original agents (docs-sync, pipeline-reviewer, test-runner, vault-guardian) have explicit 'Use when...' trigger language in description"
    - "All four original agents specify a model: field in frontmatter"
    - "All four original agents have a structured output format contract with clear success/failure distinction"
    - "context7 MCP server is reachable and resolve-library-id and get-library-docs tools are available"
  artifacts:
    - path: ".claude/agents/docs-sync.md"
      provides: "Hardened docs-sync agent"
      contains: "Use when"
    - path: ".claude/agents/pipeline-reviewer.md"
      provides: "Hardened pipeline-reviewer agent"
      contains: "Use when"
    - path: ".claude/agents/test-runner.md"
      provides: "Hardened test-runner agent"
      contains: "Use when"
    - path: ".claude/agents/vault-guardian.md"
      provides: "Hardened vault-guardian agent"
      contains: "Use when"
    - path: ".mcp.json"
      provides: "context7 MCP server configuration"
      contains: "context7"
  key_links:
    - from: ".mcp.json"
      to: "context7 MCP server"
      via: "stdio transport"
      pattern: "context7"
---

<objective>
Harden the four original agents (docs-sync, pipeline-reviewer, test-runner, vault-guardian) with improved descriptions, tool restrictions, model declarations, and output contracts. Also install context7 MCP server for project-wide library documentation access.

Purpose: Close quality gaps in agent definitions identified by crew analysis. Enable up-to-date library doc lookups via context7.
Output: Four improved agent files + .mcp.json with context7 entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden four original agents</name>
  <files>.claude/agents/docs-sync.md, .claude/agents/pipeline-reviewer.md, .claude/agents/test-runner.md, .claude/agents/vault-guardian.md</files>
  <read_first>
    - .claude/agents/docs-sync.md
    - .claude/agents/pipeline-reviewer.md
    - .claude/agents/test-runner.md
    - .claude/agents/vault-guardian.md
    - .claude/agents/security-scanner.md (reference — well-structured agent to use as quality benchmark)
    - .planning/REQUIREMENTS.md (SKILL-01 acceptance criteria)
  </read_first>
  <action>
Apply four targeted improvements to each of the four original agents (docs-sync, pipeline-reviewer, test-runner, vault-guardian). Do NOT modify security-scanner.md or test-verifier.md — those are Phase 9 agents and already hardened.

For each agent, apply these four improvements:

1. **Description trigger language** — Rewrite `description:` to include explicit "Use when..." clause. Examples of the pattern:
   - docs-sync: "Keeps README.md, CLAUDE.md, and docs/DEVOPS-HANDOFF.md current after code changes. Use when features are completed, merged, or documentation may be stale."
   - pipeline-reviewer: "Reviews changes to content pipeline and connector code for correctness, contract compliance, and pattern consistency. Use when pipeline or connector source files (src/classifier.js, src/memory-extractor.js, src/promote-memories.js, src/wikilink-engine.js, src/note-formatter.js, src/lifecycle.js, src/connectors/*.js) are modified."
   - test-runner: "Runs Jest test suite, diagnoses failures, and applies fixes. Use when code has been modified and tests need to be run, or when test failures need diagnosis and repair."
   - vault-guardian: "Audits LEFT/RIGHT vault write-permission boundary, content policy, and style policy enforcement in vault-gateway code. Use when vault-gateway.js, content-policy.js, or style-policy.js are modified, or for on-demand vault integrity audits."

2. **Model field** — All four already have `model:` fields. Verify each is present. If any is missing, add `model: sonnet` (or `model: haiku` for test-runner which already uses haiku).

3. **Tool restrictions** — Review each agent's `tools:` list for minimal privilege:
   - docs-sync: Currently `Read, Write, Edit, Glob, Grep`. This is correct — it needs Write/Edit to update docs. Keep as-is.
   - pipeline-reviewer: Currently `Read, Grep, Glob, Bash`. Correct for read-only reviewer. Keep as-is.
   - test-runner: Currently `Read, Edit, Bash, Glob, Grep`. Correct — it fixes test failures. Keep as-is.
   - vault-guardian: Currently `Read, Grep, Glob, Bash`. Correct for read-only auditor. Keep as-is.

4. **Output format contract** — Add or tighten the output format section to include:
   - A clear success/failure verdict line (e.g., "Status: COMPLETE | FAILED")
   - For agents that produce structured output, specify the exact fields
   - For docs-sync: Add "Status: COMPLETE (N updates) | NO CHANGES NEEDED" as the first line of output
   - For pipeline-reviewer: Already has "Overall: APPROVE | CHANGES REQUESTED" — keep as-is
   - For test-runner: Add "Status: PASS (N tests) | FAIL (N failures)" as the first line of output
   - For vault-guardian: Already has "Overall: PASS | ATTENTION NEEDED" — keep as-is

Preserve all existing content in each agent file. These are surgical edits to the frontmatter `description:` field and the output format section — do not rewrite the agent logic sections.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/second-brain && grep -l "Use when" .claude/agents/docs-sync.md .claude/agents/pipeline-reviewer.md .claude/agents/test-runner.md .claude/agents/vault-guardian.md | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - grep "Use when" .claude/agents/docs-sync.md returns a match
    - grep "Use when" .claude/agents/pipeline-reviewer.md returns a match
    - grep "Use when" .claude/agents/test-runner.md returns a match
    - grep "Use when" .claude/agents/vault-guardian.md returns a match
    - grep "model:" .claude/agents/docs-sync.md returns a match
    - grep "model:" .claude/agents/pipeline-reviewer.md returns a match
    - grep "model:" .claude/agents/test-runner.md returns a match
    - grep "model:" .claude/agents/vault-guardian.md returns a match
    - grep -c "Status:" .claude/agents/docs-sync.md returns at least 1
    - grep -c "Status:" .claude/agents/test-runner.md returns at least 1
    - security-scanner.md and test-verifier.md are NOT modified (git diff --name-only shows no changes to those files)
  </acceptance_criteria>
  <done>All four original agents have explicit "Use when..." trigger language in description, model: field in frontmatter, verified tool restrictions, and structured output format with clear success/failure verdict line.</done>
</task>

<task type="auto">
  <name>Task 2: Install context7 MCP server</name>
  <files>.mcp.json</files>
  <read_first>
    - .planning/REQUIREMENTS.md (SKILL-03 acceptance criteria)
    - CLAUDE.md (check for any existing MCP references to update)
  </read_first>
  <action>
Create `.mcp.json` at project root with context7 MCP server entry. The context7 server provides up-to-date library documentation lookups via two tools: `resolve-library-id` and `get-library-docs`.

Create the file with this exact content:

```json
{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"]
    }
  }
}
```

After creating the file, verify it is valid JSON by running: `node -e "JSON.parse(require('fs').readFileSync('.mcp.json','utf8')); console.log('valid')"`

Then add a brief note to CLAUDE.md in the Technology Stack or Architecture section documenting context7 availability:

```
## MCP Servers

- **context7**: Library documentation lookups. Configured in `.mcp.json`. Tools: `resolve-library-id`, `get-library-docs`.
```

The `.mcp.json` file must be committed to the repo (verify it is NOT in `.gitignore`).
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/second-brain && node -e "const c=JSON.parse(require('fs').readFileSync('.mcp.json','utf8')); console.log(c.mcpServers.context7 ? 'OK' : 'MISSING')" && ! grep -q "^\.mcp\.json$" .gitignore 2>/dev/null && echo "not-gitignored"</automated>
  </verify>
  <acceptance_criteria>
    - .mcp.json exists at project root
    - node -e "JSON.parse(require('fs').readFileSync('.mcp.json','utf8'))" exits 0 (valid JSON)
    - grep "context7" .mcp.json returns a match
    - grep "resolve-library-id\|get-library-docs\|context7" CLAUDE.md returns a match (documented)
    - .mcp.json is NOT listed in .gitignore
  </acceptance_criteria>
  <done>.mcp.json exists at project root with context7 server entry, is valid JSON, is not gitignored, and context7 is documented in CLAUDE.md.</done>
</task>

</tasks>

<verification>
- All four original agent files contain "Use when" in their description field
- All four original agent files have model: in frontmatter
- All four original agent files have a clear success/failure verdict in output format
- .mcp.json exists, is valid JSON, contains context7 entry
- .mcp.json is not in .gitignore
- context7 documented in CLAUDE.md
- security-scanner.md and test-verifier.md are unmodified
</verification>

<success_criteria>
SKILL-01: Four original agents hardened with improved descriptions (explicit trigger language), verified model fields, reviewed tool restrictions, and structured output contracts.
SKILL-03: context7 MCP installed via .mcp.json, documented in CLAUDE.md, committed to repo.
</success_criteria>

<output>
After completion, create `.planning/phases/10-agent-hardening-and-skills/10-01-SUMMARY.md`
</output>
