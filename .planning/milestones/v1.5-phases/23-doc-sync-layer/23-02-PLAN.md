---
phase: 23-doc-sync-layer
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - .claude/agents/docs-sync.md
autonomous: true
requirements:
  - AGENT-DOCSYNC-01

must_haves:
  truths:
    - "When the docs-sync agent is invoked with 'audit' or 'phase closure check', it runs jest, extracts doc stats, compares them, and emits a structured DOCSYNC-AUDIT: PASS or BLOCK verdict"
    - "The agent uses the block_threshold_pct from config/docsync.json to determine BLOCK vs PASS"
    - "A correctly synced state produces DOCSYNC-AUDIT: PASS with Violations: none"
  artifacts:
    - path: ".claude/agents/docs-sync.md"
      provides: "Extended docs-sync agent with Phase-Closure Audit Mode section"
      contains: "DOCSYNC-AUDIT"
  key_links:
    - from: ".claude/agents/docs-sync.md"
      to: "config/docsync.json"
      via: "agent reads threshold from config"
      pattern: "config/docsync\\.json"
    - from: ".claude/agents/docs-sync.md"
      to: "jest --coverage"
      via: "agent runs jest for live stats"
      pattern: "jest.*coverage"
---

<objective>
Extend the existing docs-sync Claude Code agent with a Phase-Closure Audit Mode that compares living-doc stats against live jest output and emits a structured PASS/BLOCK verdict.

Purpose: Create a blocking gate at phase closure time that prevents shipping phases when documentation has drifted beyond the configured threshold. This pairs with the post-merge hook (Plan 01) which provides non-blocking warnings at merge time.

Output: Updated docs-sync.md agent with audit mode section and structured output contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-doc-sync-layer/23-RESEARCH.md
@.planning/phases/23-doc-sync-layer/23-01-PLAN.md

<interfaces>
<!-- Current docs-sync agent structure -->
The existing agent has:
- Frontmatter: name, description, tools (Read, Write, Edit, Glob, Grep), model (sonnet)
- "When Invoked" section with 5-step procedure
- "Constraints" section
- "Output Format" section with Status: COMPLETE/NO CHANGES pattern

<!-- Threshold config created by Plan 01 -->
config/docsync.json:
```json
{
  "warn_threshold_pct": 1.0,
  "block_threshold_pct": 3.0
}
```

<!-- Agent audit output contract from research -->
```
DOCSYNC-AUDIT: PASS|BLOCK
Checked: CLAUDE.md, README.md
Stats compared: test_count, coverage_statements, coverage_branches
Violations: none | [list of violations]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase-Closure Audit Mode to docs-sync agent</name>
  <files>.claude/agents/docs-sync.md</files>
  <read_first>
    - .claude/agents/docs-sync.md (current agent content — must preserve existing sections exactly)
    - config/docsync.json (threshold config created by Plan 01 — confirm shape)
    - hooks/post-merge-doc-sync.js (Plan 01 artifact — understand the extractDocStats regex patterns so the agent instructions align)
    - CLAUDE.md (understand current stat format the agent will parse)
    - README.md (understand current stat format the agent will parse)
  </read_first>
  <action>
    Edit .claude/agents/docs-sync.md to add Bash to the tools list (agent needs to run jest and git commands) and append a new "Phase-Closure Audit Mode" section AFTER the existing "Output Format" section. Do NOT modify any existing sections — only add new content.

    Update the frontmatter tools line from:
    `tools: Read, Write, Edit, Glob, Grep`
    to:
    `tools: Read, Write, Edit, Glob, Grep, Bash`

    Update the description to include audit trigger terms. Change from:
    `description: Keeps README.md, CLAUDE.md, and docs/DEVOPS-HANDOFF.md current after code changes. Use when features are completed, merged, or documentation may be stale. Use when src/ files, config/, or dependencies change and docs need to reflect the update.`
    to:
    `description: Keeps README.md, CLAUDE.md, and docs/DEVOPS-HANDOFF.md current after code changes. Use when features are completed, merged, or documentation may be stale. Use when src/ files, config/, or dependencies change and docs need to reflect the update. Also performs documentation drift audits at phase closure — invoke with "audit", "DOCSYNC-CHECK", or "phase closure check".`

    Append the following section after the existing "Output Format" section:

    ```markdown
    ## Phase-Closure Audit Mode

    Activated when invoked with the word "audit", "DOCSYNC-CHECK", or "phase closure check".

    In audit mode, do NOT update documents. Instead, compare stated stats against live reality and emit a structured verdict.

    ### Audit Procedure

    1. Read `config/docsync.json` to get `block_threshold_pct` (default: 3.0 if file missing)
    2. Run `npx jest --coverage --json --outputFile=/tmp/jest-docsync.json --silent --forceExit` to get live stats
    3. Read `/tmp/jest-docsync.json` for `numTotalTests` (live test count)
    4. Read `coverage/coverage-summary.json` for `total.statements.pct` and `total.branches.pct`
    5. Read `CLAUDE.md` and `README.md` — extract stated stats using these patterns:
       - Test count: number followed by "total" (e.g., "1127 total across 55 test files")
       - Statement coverage: "Statements" followed by percentage (e.g., "Statements 94.62%")
       - Branch coverage: "Branch" followed by percentage (e.g., "Branch 81.28%")
    6. Compare each extracted stat against live reality:
       - Test count: exact match required (any difference is a violation)
       - Coverage percentages: violation if `abs(doc - live) > block_threshold_pct`
    7. Emit the structured verdict (see Audit Output Format below)
    8. Clean up: `rm -f /tmp/jest-docsync.json`

    ### Audit Output Format

    Always emit this exact structure as the FINAL output block:

    ```
    DOCSYNC-AUDIT: PASS
    Checked: CLAUDE.md, README.md
    Stats compared: test_count, coverage_statements, coverage_branches
    Violations: none
    ```

    Or if violations found:

    ```
    DOCSYNC-AUDIT: BLOCK
    Checked: CLAUDE.md, README.md
    Stats compared: test_count, coverage_statements, coverage_branches
    Violations:
      - test count: doc states 1127, actual is 1146, drift=19
      - statements %: doc states 94.62, actual is 94.32, drift=0.30
    ```

    ### Audit Constraints

    - Do NOT edit any documents in audit mode — read-only comparison only
    - DOCSYNC-AUDIT: BLOCK means phase closure should not proceed until docs are updated
    - DOCSYNC-AUDIT: PASS means documentation is within acceptable drift thresholds
    - If jest fails to run or coverage data is unavailable, emit DOCSYNC-AUDIT: BLOCK with explanation
    ```
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/second-brain && grep -c "DOCSYNC-AUDIT" .claude/agents/docs-sync.md && grep -c "Phase-Closure Audit Mode" .claude/agents/docs-sync.md && grep "tools:" .claude/agents/docs-sync.md | grep -c "Bash"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/docs-sync.md contains "## Phase-Closure Audit Mode"
    - .claude/agents/docs-sync.md contains "DOCSYNC-AUDIT: PASS" and "DOCSYNC-AUDIT: BLOCK"
    - .claude/agents/docs-sync.md frontmatter tools line contains "Bash"
    - .claude/agents/docs-sync.md description contains "audit" and "DOCSYNC-CHECK" and "phase closure check"
    - .claude/agents/docs-sync.md contains "config/docsync.json" (references threshold config)
    - .claude/agents/docs-sync.md contains "block_threshold_pct"
    - .claude/agents/docs-sync.md still contains "## When Invoked" (existing section preserved)
    - .claude/agents/docs-sync.md still contains "## Constraints" (existing section preserved)
    - .claude/agents/docs-sync.md still contains "## Output Format" (existing section preserved)
  </acceptance_criteria>
  <done>The docs-sync agent has a Phase-Closure Audit Mode section that defines the audit procedure, structured output contract (DOCSYNC-AUDIT: PASS/BLOCK), and references the threshold config from config/docsync.json. All existing agent sections are preserved unchanged.</done>
</task>

</tasks>

<verification>
1. `grep "DOCSYNC-AUDIT" .claude/agents/docs-sync.md` — shows both PASS and BLOCK patterns
2. `grep "tools:" .claude/agents/docs-sync.md` — includes Bash
3. `grep "## When Invoked" .claude/agents/docs-sync.md` — existing section preserved
4. `grep "block_threshold_pct" .claude/agents/docs-sync.md` — threshold referenced
5. Agent markdown is valid (frontmatter has name, description, tools, model)
</verification>

<success_criteria>
- docs-sync agent has audit mode that emits structured DOCSYNC-AUDIT verdict
- Agent references config/docsync.json for threshold values
- Agent instructions specify exact jest commands and stat extraction patterns
- Existing write-mode functionality is preserved unchanged
- Output contract matches the format specified in research (DOCSYNC-AUDIT: PASS|BLOCK)
</success_criteria>

<output>
After completion, create `.planning/phases/23-doc-sync-layer/23-02-SUMMARY.md`
</output>
