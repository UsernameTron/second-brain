---
phase: 28-surface-completion
plan: 03
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - CLAUDE.md
  - README.md
autonomous: true
requirements: [REQ-SURF-02]

must_haves:
  truths:
    - "CLAUDE.md status block test count + coverage percentages match a live jest --coverage run at execution time"
    - "README.md test/coverage stats match the same live run"
    - "The pre-push docsync gate from plan 02 exits 0 (no BLOCKED) against the refreshed docs"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Refreshed Project Status test-count + coverage block"
    - path: "README.md"
      provides: "Refreshed test/coverage stats (lines ~27,117-118,149,159)"
  key_links:
    - from: "CLAUDE.md / README.md numbers"
      to: "live jest --coverage output"
      via: "captured at execution time, never copied from a doc or prompt"
      pattern: "total"
---

<objective>
Refresh the living-doc stats from a LIVE re-count so the new pre-push docsync gate (plan 02) passes on the next push (corollary in CONTEXT). The current docs claim `1234` tests / `1205` passing / coverage `92.74`/`81.15`; CONTEXT notes the live suite has since moved (post-27-03). Numbers MUST come from the actual jest run at execution time, not from this plan or the prompt (LESSON-LIVE-RECOUNT-AT-EXECUTE-01).

Purpose: Make the REQ-SURF-02 gate green and the docs honest in one step. Depends on plan 02 so the gate exists to verify against.
Output: updated CLAUDE.md status block + README stats.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-surface-completion/28-CONTEXT.md
@hooks/pre-push-docsync.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-count live and refresh CLAUDE.md + README.md stats</name>
  <files>CLAUDE.md, README.md</files>
  <read_first>CLAUDE.md "Project Status" block (test count + coverage lines), README.md lines 27,117-118,149,159, hooks/post-merge-doc-sync.js regexes (RE_TEST_COUNT / RE_STMT_PCT / RE_BRANCH_PCT — the doc strings must match these)</read_first>
  <action>
    Run a full live count and capture the numbers from the actual output — DO NOT copy any figures
    from CONTEXT or this plan:
      `CI=true npx jest --coverage --forceExit --testPathIgnorePatterns='/node_modules/'`
    (the `--testPathIgnorePatterns` override is required only if executing inside a `.claude/worktrees`
    checkout, which package.json's jest config ignores; harmless otherwise.)

    From the output capture: total tests, passing, skipped, number of test suites/files, and coverage
    Statements / Branches / Functions / Lines percentages.

    Update BOTH docs to the captured values:
    - CLAUDE.md Project Status: the `**Test count:**` line and the `**Coverage:**` line.
    - README.md: line ~27 (`npm test ... N total tests (P pass, S skipped ...)`), lines ~117-118
      (`N total tests across F test files`, `Branch coverage: X%`), line ~149 (`N total`), and line
      ~159 (`Statements X%, Functions X%, Lines X%, Branch X%`).
    Ensure the doc strings still match the extractor regexes: "<N> total", "Statements <pct>%",
    "Branch <pct>%" — the gate keys on exactly these. Refresh the test-file count too if it changed.
    Surgical edits only — do not restructure the docs.
  </action>
  <verify>
    <automated>node -e "const {extractDocStats,getLiveStats}=require('./hooks/post-merge-doc-sync'); const fs=require('fs'); const live=getLiveStats(process.cwd()); if(!live){console.error('no live stats');process.exit(1)} for(const f of ['CLAUDE.md','README.md']){const d=extractDocStats(fs.readFileSync(f,'utf8')); if(d.testCount!==live.testCount){console.error(f+' testCount '+d.testCount+' != live '+live.testCount);process.exit(1)}} console.error('DOCS MATCH LIVE')"</automated>
  </verify>
  <done>Both docs' extracted testCount equals live testCount; coverage strings updated to live values.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the pre-push docsync gate passes on refreshed docs</name>
  <files>CLAUDE.md, README.md</files>
  <read_first>hooks/pre-push-docsync.js</read_first>
  <action>
    Run the plan-02 gate directly and confirm it exits 0 (no BLOCKED) with the refreshed docs — this is
    the corollary's real acceptance test. If it blocks, the doc edits in Task 1 are still off; fix them,
    do not weaken the gate or the thresholds.
  </action>
  <verify>
    <automated>node hooks/pre-push-docsync.js; echo "exit=$?" | grep -q "exit=0" && echo GATE-GREEN</automated>
  </verify>
  <done>`node hooks/pre-push-docsync.js` exits 0; docs no longer trip the block threshold.</done>
</task>

</tasks>

<verification>
- Live jest run drove the numbers; no figure copied from prompt/CONTEXT.
- CLAUDE.md and README.md extracted stats match live output.
- `node hooks/pre-push-docsync.js` exits 0 (gate green).
</verification>

<success_criteria>
Living docs match reality and the new blocking gate passes on the next push.
</success_criteria>

<output>
After completion, create `.planning/phases/28-surface-completion/28-03-SUMMARY.md`.
</output>
