---
phase: 28-surface-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/reroute.md
autonomous: true
requirements: [REQ-SURF-01]

must_haves:
  truths:
    - "Invoking the reroute wrapper against a file calls rerouteFile from src/reroute"
    - "On success the wrapper prints the new location read from r.to (never r.target)"
    - "On failure the wrapper prints r.reason and exits non-zero"
    - "Every command in the second-brain CLAUDE.md command table resolves to a .claude/commands/*.md file or a scripts/ entry point"
  artifacts:
    - path: ".claude/commands/reroute.md"
      provides: "Slash-command wrapper for src/reroute.rerouteFile"
      contains: "rerouteFile"
  key_links:
    - from: ".claude/commands/reroute.md"
      to: "src/reroute.js"
      via: "require('./src/reroute') node -e inline reference impl"
      pattern: "require\\('\\./src/reroute'\\)"
    - from: ".claude/commands/reroute.md"
      to: "rerouteFile result object"
      via: "reads r.to and r.from"
      pattern: "r\\.to"
---

<objective>
Make `/reroute` a real, invocable slash command (REQ-SURF-01). The engine `src/reroute.js` already works; only the wrapper is missing, so the command table advertises a command with no entry point.

Purpose: Close the last unreachable command on the repo's advertised surface (ROADMAP success criteria 1 and 3).
Output: `.claude/commands/reroute.md` following the `promote-unrouted.md` pattern, plus a verification that no documented command is unreachable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-surface-completion/28-CONTEXT.md

<interfaces>
<!-- src/reroute.js — consumed AS-IS, no changes. Verified signature: -->
```
async function rerouteFile(filePath)   // takes ONLY a file path — re-classifies, NO target param
  -> { rerouted: boolean, from?: string, to?: string, reason?: string, failureMode?: string }
```
CRITICAL: the result field is `r.to` (and `r.from`). `r.target` DOES NOT EXIST — the audit's own
proposed diff had exactly this bug. The wrapper MUST read `r.to`.

module.exports = { rerouteFile };
</interfaces>

<!-- Wrapper pattern to copy (frontmatter description + node -e inline reference impl): -->
@.claude/commands/promote-unrouted.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the /reroute slash-command wrapper</name>
  <files>.claude/commands/reroute.md</files>
  <read_first>.claude/commands/promote-unrouted.md (structure), src/reroute.js:74-82,234,238 (return shape + accepted args)</read_first>
  <action>
    Create `.claude/commands/reroute.md` copying the structure of `promote-unrouted.md`:
    frontmatter `description:` line + a prose paragraph + a `node -e` reference-implementation
    fenced block driven by `$ARGUMENTS`.

    Signature reality (per D-19/D-20 in CONTEXT, verified against src/reroute.js): `rerouteFile`
    takes ONLY the file path and RE-CLASSIFIES the file — there is NO `--target` parameter.
    The wrapper therefore accepts just `<file>` and does NOT invent a `--target` flag.
    (ROADMAP success criterion 1 paraphrases usage as `/reroute <file> --target <path>`; the source
    is authoritative — document the real single-arg signature, do not fabricate a target flag.)

    The node -e block MUST:
    - `const { rerouteFile } = require('./src/reroute');`
    - Read the file path from `process.argv.slice(1)`; if missing, write a Usage string to stderr
      and `process.exit(1)`.
    - `await rerouteFile(filePath)`.
    - On `r.rerouted === true`: write `'Rerouted ' + r.from + ' -> ' + r.to + '\n'` to stdout
      (reads `r.to` and `r.from` — NOT `r.target`).
    - On failure: write `'Failed: ' + (r.reason || 'unknown error') + '\n'` to stdout and
      `process.exit(1)`.
    - `.catch` any thrown error to stderr and `process.exit(1)`.
    - Pass `$ARGUMENTS` after `-- ` exactly as promote-unrouted.md does.

    Frontmatter description usage string: `Usage: /reroute <file>` (dead-letter or left-proposal file path).
    Document in the Arguments section that reroute re-runs the full classification pipeline and reports
    the destination it chose; there is no manual target override.
  </action>
  <verify>
    <automated>test -f .claude/commands/reroute.md && grep -q "require('./src/reroute')" .claude/commands/reroute.md && grep -q "r\.to" .claude/commands/reroute.md && ! grep -q "r\.target" .claude/commands/reroute.md && echo OK</automated>
  </verify>
  <done>reroute.md exists, requires ./src/reroute, reads r.to/r.from, contains no r.target reference.</done>
</task>

<task type="auto">
  <name>Task 2: Verify no documented command is unreachable</name>
  <files>.claude/commands/reroute.md</files>
  <read_first>CLAUDE.md lines 43-52 (command table), .claude/commands/ listing, scripts/ listing</read_first>
  <action>
    Confirm ROADMAP success criterion 3: every command in the second-brain `CLAUDE.md` command table
    (lines ~43-52: `/today`, `/new`, `/wrap`, `/promote-memories`, `/reroute`, `/promote-unrouted`,
    the three `/recall` variants, and `node scripts/recall.js`) resolves to a real entry point —
    a matching `.claude/commands/<name>.md` OR a `scripts/*.js` file. The `/recall --semantic` and
    `/recall --hybrid` rows both map to `recall.md`; `node scripts/recall.js` maps to `scripts/recall.js`.
    This is a verification task — do NOT edit CLAUDE.md's table; if a genuine gap remains, report it.
  </action>
  <verify>
    <automated>for c in today new wrap promote-memories reroute promote-unrouted recall; do test -f ".claude/commands/$c.md" || { echo "MISSING $c"; exit 1; }; done; test -f scripts/recall.js && echo "ALL COMMANDS REACHABLE"</automated>
  </verify>
  <done>All command-table entries resolve; command emits "ALL COMMANDS REACHABLE".</done>
</task>

</tasks>

<verification>
- `.claude/commands/reroute.md` exists and wires to `src/reroute.rerouteFile` reading `r.to`.
- No `r.target` anywhere in the wrapper.
- Every command advertised in CLAUDE.md's table has a resolvable entry point.
</verification>

<success_criteria>
`/reroute` is invocable and reports the destination from `r.to`; no advertised command is unreachable.
</success_criteria>

<output>
After completion, create `.planning/phases/28-surface-completion/28-01-SUMMARY.md`.
</output>
