---
phase: 27-context-honesty
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - decisions/ADR-020-authority-hierarchy.md
  - src/reach-exporter.js
  - test/reach-exporter.test.js
  - CLAUDE.md
  - /Users/cpconnor/Claude Cowork/CLAUDE.md
autonomous: true
requirements: [REQ-CTX-03]
must_haves:
  truths:
    - "decisions/ADR-020-authority-hierarchy.md ranks ABOUT ME/ > memory.md > CLAUDE.md > auto-memory blob"
    - "Every pairwise conflict between the four stores has one defined winner in ADR-020"
    - "ADR-020 states Rule-4: canon disagrees with Pete live -> Pete wins, canon is stale, flag in-session, never silently defer"
    - "The reach pointer template (renderReachFile) carries the hierarchy one-liner referencing ADR-020, so every allowlisted surface sees it"
    - "Project CLAUDE.md carries a one-line router pointer to ADR-020 and asserts no identity fact of its own"
    - "~/Claude Cowork/CLAUDE.md carries the hierarchy one-liner"
  artifacts:
    - path: "decisions/ADR-020-authority-hierarchy.md"
      provides: "Normative authority hierarchy (Status/Context/Decision/Rationale/Consequences)"
      min_lines: 40
      contains: "ABOUT ME"
    - path: "src/reach-exporter.js"
      provides: "Hierarchy one-liner in the generated pointer template"
      contains: "ADR-020"
    - path: "test/reach-exporter.test.js"
      provides: "Regression asserting the hierarchy line is rendered"
    - path: "CLAUDE.md"
      provides: "One-line router pointer to ADR-020"
      contains: "ADR-020"
  key_links:
    - from: "src/reach-exporter.js renderReachFile"
      to: "decisions/ADR-020-authority-hierarchy.md"
      via: "hierarchy one-liner in the generated cache"
      pattern: "ADR-020"
    - from: "CLAUDE.md"
      to: "decisions/ADR-020-authority-hierarchy.md"
      via: "router pointer line"
      pattern: "ADR-020"
---

<objective>
Establish one normative source-of-truth hierarchy (REQ-CTX-03) and wire pointers to it so every surface arbitrates conflicts the same way. Today memory.md, CLAUDE.md, ABOUT ME/ canon, and the auto-memory blob all assert facts and none outranks the others — a conflict has no defined winner.

Purpose: give every pairwise conflict one winner, and make CLAUDE.md a router (not a fact store).
Output: ADR-020, a hierarchy one-liner in the reach pointer template, and router pointers in project + Cowork CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/27-context-honesty/27-CONTEXT.md
@.planning/REQUIREMENTS.md

<interfaces>
ADR shape — `decisions/ADR-019-reach-layer-mechanism.md` uses: `# ADR-NNN: Title`, `**Status:** Accepted (date)`, `**Context:** ...`, `## Decision`, `## Constraints honored` / `## Rationale`, `## Alternatives considered`, `## Consequences`, `## Provenance`.

REQUIREMENTS.md REQ-CTX-03 authority table (the four stores + rules) is the source to ADAPT (not redesign): ABOUT ME/ (highest, human-only), memory.md (historical record via /promote-memories gate), CLAUDE.md (router, a fact here is a defect), auto-memory blob (lowest, non-authoritative cache, nobody writes it).

reach-exporter renderReachFile (src/reach-exporter.js ~line 65-102) currently emits a `**Canonical store:**` block then a `**Query it live...**` block. The hierarchy one-liner goes into this returned template string.

`~/Claude Cowork/CLAUDE.md` = absolute path `/Users/cpconnor/Claude Cowork/CLAUDE.md` — OUTSIDE this repo, agent-writable (vault root is not LEFT). Created 2026-07-15.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-020 authority hierarchy</name>
  <files>decisions/ADR-020-authority-hierarchy.md</files>
  <read_first>
    - decisions/ADR-019-reach-layer-mechanism.md (ADR prose shape to mirror)
    - decisions/ADR-018-cross-surface-reach.md (companion ADR the hierarchy pairs with)
    - .planning/REQUIREMENTS.md (REQ-CTX-03 section — the four-store table and the four rules to adapt)
  </read_first>
  <action>
    Create `decisions/ADR-020-authority-hierarchy.md` following the ADR-018/019 shape (Status: Accepted (2026-07-15); Context; Decision; Rationale; Consequences; Provenance).
    Content it MUST contain:
    - The ranking, most to least authoritative: `ABOUT ME/` canon > `memory.md` > `CLAUDE.md` > Anthropic auto-memory blob. Include the four-store table (store / authority / writable-by) adapted from REQUIREMENTS.md REQ-CTX-03.
    - An explicit pairwise-winner statement covering all six pairs so no conflict is "it depends": ABOUT ME/ beats each of the other three; memory.md beats CLAUDE.md and the blob; CLAUDE.md beats the blob.
    - The four carried-over rules: (1) canon wins on any conflict (recency is not authority — fix canon, don't route around it); (2) one fact, one home (identity/company/architecture/exclusions each resolve to exactly one file); (3) provenance and status survive (a suggestion is not a decision); (4) **Rule-4** — when canon disagrees with Pete live in-session, Pete wins and canon is stale: flag it immediately, never silently defer and never silently override.
    - Scope note: this governs the four CURRENT stores only; the `~/memory/` target architecture in `.planning/research/AUTHORITY.md` stays research (separate milestone, per ADR-019 alternatives).
    - Provenance: REQ-CTX-03, accepted by Pete 2026-07-12; adapts `.planning/research/AUTHORITY.md`.
  </action>
  <verify>
    <automated>test -f decisions/ADR-020-authority-hierarchy.md && grep -q "ABOUT ME" decisions/ADR-020-authority-hierarchy.md && grep -qi "Pete wins" decisions/ADR-020-authority-hierarchy.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^# ADR-020" decisions/ADR-020-authority-hierarchy.md`
    - `grep -q "Status:" decisions/ADR-020-authority-hierarchy.md`
    - `grep -q "ABOUT ME" decisions/ADR-020-authority-hierarchy.md && grep -q "memory.md" decisions/ADR-020-authority-hierarchy.md && grep -q "auto-memory" decisions/ADR-020-authority-hierarchy.md`
    - `grep -qi "one fact, one home" decisions/ADR-020-authority-hierarchy.md`
    - `grep -qi "Pete wins" decisions/ADR-020-authority-hierarchy.md` (Rule-4)
  </acceptance_criteria>
  <done>ADR-020 exists, ranks all four stores, defines every pairwise winner, and states Rule-4.</done>
</task>

<task type="auto">
  <name>Task 2: Add hierarchy one-liner to the reach pointer template + regression test</name>
  <files>src/reach-exporter.js, test/reach-exporter.test.js</files>
  <read_first>
    - src/reach-exporter.js (renderReachFile, ~lines 59-102 — the returned template string)
    - test/reach-exporter.test.js (renderReachFile describe block, ~lines 62-90)
  </read_first>
  <action>
    In `src/reach-exporter.js` `renderReachFile`, add a single hierarchy line into the returned template, immediately after the `**Canonical store:** ...` paragraph. Exact line to insert:
    `**Authority:** on any conflict, `ABOUT ME/` canon > `memory.md` > `CLAUDE.md` > this cache — the higher-ranked store wins (see `decisions/ADR-020-authority-hierarchy.md` in the second-brain repo).`
    Do not alter the frontmatter, the query-command block, or the digest section.
    In `test/reach-exporter.test.js`, extend the existing `renderReachFile` "renders frontmatter, canonical pointer, ..." test (or add a sibling test) to assert the rendered output contains both `ADR-020` and the ranking substring `ABOUT ME/` `> ` `memory.md`.
  </action>
  <verify>
    <automated>CI=true npx jest test/reach-exporter.test.js -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "ADR-020" src/reach-exporter.js`
    - `grep -q "Authority:" src/reach-exporter.js`
    - `grep -q "ADR-020" test/reach-exporter.test.js`
    - `! grep -q "console.log" src/reach-exporter.js`
    - `CI=true npx jest test/reach-exporter.test.js` passes (all existing reach tests still green).
  </acceptance_criteria>
  <done>The generated pointer file carries the hierarchy one-liner referencing ADR-020; reach-exporter tests pass including the new assertion.</done>
</task>

<task type="auto">
  <name>Task 3: Router pointer in project CLAUDE.md + hierarchy one-liner in Cowork CLAUDE.md</name>
  <files>CLAUDE.md, /Users/cpconnor/Claude Cowork/CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (project root — the "## Memory" section and the line-23 `> Last verified:` comment)
    - /Users/cpconnor/Claude Cowork/CLAUDE.md (outside repo; read before editing to place the line idempotently)
  </read_first>
  <action>
    (a) In project `CLAUDE.md`, add a one-line router pointer to ADR-020 within the existing "## Memory" section (do NOT restate any identity fact — CLAUDE.md is a router). Exact line to add:
    `> **Source-of-truth hierarchy:** `ABOUT ME/` canon > `memory.md` > this file > auto-memory blob. This file is a router, not a fact store — see [decisions/ADR-020-authority-hierarchy.md](decisions/ADR-020-authority-hierarchy.md).`
    Also update the line-23 status comment from "a SessionStart staleness hook (v1.6 E-02) will read this date" to present tense: "read by the SessionStart staleness hook (`.claude/hooks/staleness-check.js`, v1.6 REQ-CTX-01)" — keeping the comment accurate now that the hook ships this phase (per CONTEXT specifics). This is a comment-text edit only.
    (b) In `/Users/cpconnor/Claude Cowork/CLAUDE.md` (absolute path, outside repo), add the same hierarchy one-liner idempotently (skip if a line containing `ADR-020` already exists). Point at the repo copy: `see `~/projects/second-brain/decisions/ADR-020-authority-hierarchy.md``.
  </action>
  <verify>
    <automated>grep -q "ADR-020" CLAUDE.md && grep -q "staleness-check.js" CLAUDE.md && grep -q "ADR-020" "/Users/cpconnor/Claude Cowork/CLAUDE.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Source-of-truth hierarchy" CLAUDE.md && grep -q "ADR-020" CLAUDE.md`
    - `grep -q "router, not a fact store" CLAUDE.md`
    - Line-23 comment updated: `grep -q "read by the SessionStart staleness hook" CLAUDE.md`
    - `grep -q "ADR-020" "/Users/cpconnor/Claude Cowork/CLAUDE.md"` and exactly one such line: `[ "$(grep -c "ADR-020" "/Users/cpconnor/Claude Cowork/CLAUDE.md")" = "1" ]`
  </acceptance_criteria>
  <done>Project CLAUDE.md points to ADR-020 as a router and its status comment is present-tense-accurate; Cowork CLAUDE.md carries the hierarchy one-liner exactly once.</done>
</task>

</tasks>

<verification>
- `CI=true npx jest test/reach-exporter.test.js` passes.
- ADR-020 exists with the ranking, all pairwise winners, and Rule-4.
- Both CLAUDE.md files reference ADR-020; project CLAUDE.md states no new fact.
</verification>

<success_criteria>
A written authority hierarchy ranks ABOUT ME/ > memory.md > CLAUDE.md > auto-memory blob with one defined winner per conflict; a fact contradicting canon flags in-session (Rule-4); no fact-type has two homes. (ROADMAP Phase 27 success criteria 3, 4, 5.)
</success_criteria>

<output>
After completion, create `.planning/phases/27-context-honesty/27-02-SUMMARY.md`.
</output>
