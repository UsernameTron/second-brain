---
phase: 27-context-honesty
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/semantic-index.js
  - test/semantic-index.test.js
  - test/semantic-index-errors.test.js
  - test/classifier.test.js
  - test/integration/semantic-search.test.js
autonomous: true
requirements: [REQ-CTX-03]
must_haves:
  truths:
    - "semanticSearch returns a blocked/fail-closed result (no Voyage call, no query egress) when excluded terms are empty or unloadable"
    - "hybridSearch propagates the blocked result rather than silently falling back to ungated keyword search"
    - "The full test suite passes under CI with mocks updated to reflect fail-closed behavior"
  artifacts:
    - path: "src/semantic-index.js"
      provides: "Fail-closed guard on empty/unloadable excluded terms in semanticSearch"
      contains: "excludedTerms"
    - path: "test/semantic-index.test.js"
      provides: "Regression: empty excluded terms => blocked; non-empty => normal path"
  key_links:
    - from: "src/semantic-index.js semanticSearch"
      to: "content-policy exclusion gate / Voyage"
      via: "guard on empty excludedTerms BEFORE checkContent/embed"
      pattern: "excludedTerms"
---

<objective>
Close the fail-open exclusion hole in the semantic search path (absorbed debt, CONTEXT.md "Absorbed debt"; STATE.md "Candidate for Phase 27"; per LESSON-PRE-EXISTING-DEBT-ABSORPTION-01). `loadExcludedTerms()` returns `[]` on any read/parse error, and `checkContent(query, [])` returns PASS with no model call — so a missing or corrupt `config/excluded-terms.json` silently ships ungated `/recall --semantic` queries to Voyage, re-opening the exact hole F-01 closed. The ISPN/Genesys/Asana exclusion is a hard rule and must fail closed.

Purpose: make the declared exclusion enforcement actually performed (the milestone theme), mirroring vault-gateway's fail-closed config pattern (REQ-CTX-03 governs exclusions as a one-home fact-type).
Output: a guard in `semanticSearch`, plus updated mocks and a regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-context-honesty/27-CONTEXT.md

<interfaces>
src/semantic-index.js `semanticSearch` (~line 397): after `const excludedTerms = loadExcludedTerms();` (~line 405) it calls `const policy = await checkContent(query, excludedTerms);`. The blocked-return shape already used:
`return { results: [], blocked: true, reason: policy.reason || '...', matchedTerm: policy.matchedTerm };`

`hybridSearch` (~line 478) ALREADY propagates blocked: `if (semRes.blocked) return { results: [], blocked: true, reason: semRes.reason };` (line 498) — so no hybrid change is needed beyond confirming it.

Test mocks that will break (they return empty terms, which the guard now rejects):
- test/semantic-index.test.js:48 and :742 — `loadExcludedTerms: jest.fn(() => [])`
- test/semantic-index-errors.test.js:52 — `loadExcludedTerms: jest.fn(() => [])`
- test/classifier.test.js:31,98,181,285,421 — `loadExcludedTerms: jest.fn(() => [])` (only relevant if a test exercises semanticSearch/hybridSearch; classifier's own ingress gate is unaffected)
- test/integration/semantic-search.test.js:361,378 — already inject `['ISPN','Genesys','Asana']` (these stay green)

DO NOT modify `loadExcludedTerms()` in src/pipeline-infra.js — config-loaders-malformed.test.js:154 asserts it returns `[]` on malformed input. The guard belongs at the consumer (semanticSearch), mirroring vault-gateway.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fail-closed guard to semanticSearch</name>
  <files>src/semantic-index.js</files>
  <read_first>
    - src/semantic-index.js (semanticSearch ~397-460 and hybridSearch ~478-532)
    - src/pipeline-infra.js (loadExcludedTerms ~341-347 — the fail-open being defended against; leave it unchanged)
    - src/vault-gateway.js (the fail-closed config pattern to mirror)
  </read_first>
  <behavior>
    - excludedTerms is `[]` (empty) or not an array: semanticSearch returns `{ results: [], blocked: true, failClosed: true, reason: <exclusion-unavailable message> }` and makes NO Voyage/embed call and NO checkContent call.
    - excludedTerms is non-empty (e.g. ['ISPN','Genesys','Asana']): existing behavior unchanged — checkContent runs, then Voyage.
    - hybridSearch with empty terms surfaces `blocked: true` (via existing line-498 propagation), not a keyword-only downgrade.
  </behavior>
  <action>
    In `src/semantic-index.js` `semanticSearch`, immediately after `const excludedTerms = loadExcludedTerms();` and BEFORE the `checkContent` call, add:
    ```js
    if (!Array.isArray(excludedTerms) || excludedTerms.length === 0) {
      return { results: [], blocked: true, failClosed: true, reason: 'exclusion terms empty or unloadable — failing closed (config/excluded-terms.json)' };
    }
    ```
    This mirrors vault-gateway: an unloadable safety config denies the operation rather than passing it through. No `console.log`. Do not change loadExcludedTerms, checkContent, or hybridSearch logic (confirm hybridSearch line ~498 already returns on `semRes.blocked`).
  </action>
  <verify>
    <automated>grep -q "failClosed" src/semantic-index.js && CI=true npx jest test/semantic-index.test.js -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "failClosed" src/semantic-index.js`
    - `grep -q "failing closed" src/semantic-index.js`
    - `! grep -q "console.log" src/semantic-index.js`
    - The guard appears BEFORE the `checkContent` call in semanticSearch (verify by reading: guard block is between `loadExcludedTerms()` and `await checkContent`).
  </acceptance_criteria>
  <done>semanticSearch returns a fail-closed blocked result on empty/unloadable excluded terms with no Voyage or checkContent call; non-empty terms behave as before.</done>
</task>

<task type="auto">
  <name>Task 2: Update mocks and add regression coverage</name>
  <files>test/semantic-index.test.js, test/semantic-index-errors.test.js, test/classifier.test.js, test/integration/semantic-search.test.js</files>
  <read_first>
    - test/semantic-index.test.js (lines 48, 742 — the `loadExcludedTerms: jest.fn(() => [])` mocks)
    - test/semantic-index-errors.test.js (line 52 mock)
    - test/classifier.test.js (lines 31, 98, 181, 285, 421 mocks)
    - test/integration/semantic-search.test.js (lines 355-378 — already inject real terms; the pattern to copy)
  </read_first>
  <action>
    Update the mocks that feed the semanticSearch/hybridSearch path so they return non-empty terms and existing assertions still hold. Change `loadExcludedTerms: jest.fn(() => [])` to `loadExcludedTerms: jest.fn(() => ['ISPN', 'Genesys', 'Asana'])` in test/semantic-index.test.js (:48, :742) and test/semantic-index-errors.test.js (:52). For test/classifier.test.js, only change the mock in tests that actually invoke semanticSearch/hybridSearch (run the suite to find failures; leave classifier-ingress-only tests untouched).
    Add a regression test in test/semantic-index.test.js asserting: with `loadExcludedTerms` mocked to return `[]`, `semanticSearch('any query', {})` resolves to an object with `blocked === true` and `failClosed === true`, and (spy-assert) the Voyage embed client is never constructed/called.
    Then run the FULL suite under CI and fix any remaining breakage caused by the guard (per LESSON-AUDIT-QUICKWIN-TEST-IMPACT-01, this is a contract change — expect a handful of mock updates, not zero).
  </action>
  <verify>
    <automated>CI=true npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "failClosed" test/semantic-index.test.js` (regression asserts the new field).
    - `! grep -q "jest.fn(() => \[\])" test/semantic-index-errors.test.js` OR the remaining empty-mock is in a test that expects blocked (documented in the test name).
    - `CI=true npm test` exits 0 with the full suite green; coverage thresholds hold (branches 80 / functions 90 / lines 90 / statements 90).
  </acceptance_criteria>
  <done>All mocks feeding semanticSearch return real exclusion terms, a regression test proves empty terms fail closed with no Voyage call, and the full CI suite passes.</done>
</task>

</tasks>

<verification>
- `CI=true npm test` passes (full suite, coverage thresholds hold).
- `grep -q "failClosed" src/semantic-index.js` and the guard precedes checkContent.
- src/pipeline-infra.js loadExcludedTerms is unchanged.
</verification>

<success_criteria>
A missing or corrupt `config/excluded-terms.json` no longer ships ungated queries to Voyage — `/recall --semantic` (and `--hybrid`) fail closed with an explicit blocked result. Declared exclusion enforcement is now actually performed. (CONTEXT.md Absorbed debt; supports REQ-CTX-03 one-home governance of exclusions.)
</success_criteria>

<output>
After completion, create `.planning/phases/27-context-honesty/27-03-SUMMARY.md`.
</output>
