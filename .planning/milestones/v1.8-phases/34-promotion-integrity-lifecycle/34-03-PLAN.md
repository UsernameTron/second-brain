---
phase: 34-promotion-integrity-lifecycle
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-baseline.js
  - eval/baseline-sentinel-hashes.json
  - test/verify-baseline.test.js
  - package.json
  - hooks/pre-push
autonomous: true
requirements: [VERIFY-SENTINEL-01]
must_haves:
  truths:
    - "`node scripts/verify-baseline.js` checks the 27 real content_hash values from memory.md.pre-governance.20260712 and exits 0 only when all 27 resolve (in memory.md ∪ memory-archive/)"
    - "If any sentinel hash is missing, the script prints which hashes failed and exits non-zero"
    - "The 27 sentinel hashes are committed as a repo fixture — the check no longer depends on a fabricated or absent artifact"
    - "No milestone report can cite baseline integrity without a real run: the check is wired into the pre-push docs gate the same way pre-push-docsync is"
  artifacts:
    - path: "scripts/verify-baseline.js"
      provides: "Baseline-integrity check with real pass/fail exit code"
      exports: ["main / exit 0|1"]
    - path: "eval/baseline-sentinel-hashes.json"
      provides: "The 27 content_hash values, committed"
      contains: "content_hash"
  key_links:
    - from: "hooks/pre-push"
      to: "scripts/verify-baseline.js"
      via: "invocation in the pre-push gate"
      pattern: "verify-baseline"
---

<objective>
Replace the fabricated "3/3 sentinel hashes verified" claim (VERIFY-SENTINEL-01) with a real, run-backed check. The 2026-07-12/13 governance session reported a sentinel-hash verification that never existed — no artifact defined it, and the claim propagated across sessions as fact. Remediation per v1.7-REQUIREMENTS.md:85: define the check as the 27 content_hash values in `memory.md.pre-governance.20260712` (audited 2026-07-19: 27/27 resolve — 18 live in memory.md, 9 in memory-archive/), implement it as a script with a real exit code, and wire it into reporting so no report cites baseline integrity without a run artifact.

Purpose: baseline integrity becomes a mechanical, non-forgeable check on the same doc-gate pattern as pre-push-docsync.
Output: committed 27-hash fixture, `scripts/verify-baseline.js` with real exit code, `npm run verify:baseline`, and a pre-push gate hookup.
</objective>

<constraints>
- The 27 hashes must be the REAL values from `memory.md.pre-governance.20260712`. Do NOT invent them. Locate the backup file first (repo, vault `~/Claude Cowork/memory/`, or memory-archive/backups); if it cannot be located, STOP and report a CHECKPOINT — a guessed hash list would recreate exactly the fabricated-check failure this requirement exists to kill.
- Resolution set = live `memory/memory.md` UNION every file in `memory-archive/` (the audit found 18 live + 9 archived). Match on the `content_hash:: <hash>` field text.
- No `console.log` in production script code — this is a CLI, so `console.log` for user output is acceptable here (scripts/ are entry points, not library modules; match scripts/eval-recall.js which uses console.log freely).
</constraints>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.8-phases/34-promotion-integrity-lifecycle/34-CONTEXT.md
@.planning/milestones/v1.7-REQUIREMENTS.md

<interfaces>
Exit-code + CLI precedent — scripts/eval-recall.js: exit 0 ok / 1 regression / 2 preflight; uses `process.exit(code)` and plain console.log.
Pre-push gate precedent — hooks/pre-push:26 invokes `node "$(dirname "$0")/pre-push-docsync.js"` and blocks on non-zero (SKIP_DOCSYNC bypass).
content_hash field format in memory.md: `content_hash:: a1b2c3d4e5f6` (12-hex).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the 27 sentinel hashes into a committed fixture</name>
  <read_first>
    - .planning/milestones/v1.7-REQUIREMENTS.md:85 (VERIFY-SENTINEL-01 — the 27-hash definition and the 2026-07-19 audit result)
    - the backup file `memory.md.pre-governance.20260712` (locate under repo, `~/Claude Cowork/memory/`, or memory-archive/backups)
  </read_first>
  <action>
    Locate `memory.md.pre-governance.20260712`. Extract every `content_hash:: <hash>` value from it — there are 27. Write them to `eval/baseline-sentinel-hashes.json` as `{ "source": "memory.md.pre-governance.20260712", "captured": "<ISO date>", "hashes": ["<h1>", ...] }` (exactly 27 entries). If the backup file cannot be found anywhere, STOP and report a CHECKPOINT rather than fabricating hashes.
  </action>
  <acceptance_criteria>
    - `eval/baseline-sentinel-hashes.json` exists and `node -e "console.log(require('./eval/baseline-sentinel-hashes.json').hashes.length)"` prints `27`
    - every entry matches `/^[0-9a-f]{12}$/`
  </acceptance_criteria>
  <verify>
    <automated>node -e "const h=require('./eval/baseline-sentinel-hashes.json').hashes; if(h.length!==27) process.exit(1); if(!h.every(x=>/^[0-9a-f]{12}$/.test(x))) process.exit(1)"</automated>
  </verify>
  <done>The 27 real sentinel hashes are committed as a repo fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: verify-baseline.js with real exit code + npm script + tests</name>
  <read_first>
    - eval/baseline-sentinel-hashes.json (from Task 1)
    - scripts/eval-recall.js (exit-code + CLI output precedent)
    - src/memory-reader.js:23-24 (VAULT_ROOT / MEMORY_FILE resolution to reuse)
  </read_first>
  <behavior>
    - All 27 hashes present across memory.md ∪ memory-archive/ → prints `27/27 baseline hashes resolve` and exits 0
    - One hash missing → prints the missing hash(es) and exits 1
    - Uses VAULT_ROOT env for the memory root (testable against a temp fixture vault)
  </behavior>
  <action>
    Create scripts/verify-baseline.js: load the 27 hashes from eval/baseline-sentinel-hashes.json; read `$VAULT_ROOT/memory/memory.md` and every `.md` under `$VAULT_ROOT/memory-archive/`; for each sentinel hash check `content_hash:: <hash>` appears in the union; print `N/27 baseline hashes resolve` and, on any miss, the list of missing hashes; `process.exit(missing.length === 0 ? 0 : 1)`. Add `"verify:baseline": "node scripts/verify-baseline.js"` to package.json scripts. Add test/verify-baseline.test.js exercising the 3 behavior bullets against a temp VAULT_ROOT fixture vault (a small memory.md with a subset of hashes + one archive file), asserting exit 0 when all present and exit 1 when one is removed.
  </action>
  <acceptance_criteria>
    - `grep -n "verify:baseline" package.json` matches
    - `grep -n "process.exit" scripts/verify-baseline.js` shows the pass/fail exit
    - `npx jest test/verify-baseline.test.js` exits 0
    - Against the real vault: `VAULT_ROOT=~/Claude\ Cowork node scripts/verify-baseline.js` prints `27/27 ... resolve` and exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx jest test/verify-baseline.test.js</automated>
  </verify>
  <done>A real, exit-coded baseline-integrity check exists and passes against the live vault.</done>
</task>

<task type="auto">
  <name>Task 3: Wire verify-baseline into the pre-push docs gate</name>
  <read_first>
    - hooks/pre-push:20-32 (docsync gate invocation + SKIP bypass pattern)
  </read_first>
  <action>
    In hooks/pre-push, after the docsync gate block, add an invocation of `node scripts/verify-baseline.js` that blocks the push (non-zero) when baseline integrity fails, mirroring the docsync gate structure (respecting a `SKIP_BASELINE=1` bypass for parity with `SKIP_DOCSYNC`). Set `VAULT_ROOT` for the invocation the same way other hook-invoked scripts resolve it, or document that it defaults to `~/Claude Cowork`. Keep the change minimal and matching existing hook style.
  </action>
  <acceptance_criteria>
    - `grep -n "verify-baseline" hooks/pre-push` matches
    - `grep -n "SKIP_BASELINE" hooks/pre-push` matches (bypass parity)
    - `bash -n hooks/pre-push` exits 0 (valid shell)
  </acceptance_criteria>
  <verify>
    <automated>bash -n hooks/pre-push</automated>
  </verify>
  <done>No push can claim baseline integrity without the check actually running; bypass exists for parity with docsync.</done>
</task>

</tasks>

<verification>
- `node -e` hash-count check passes (27 real hashes)
- `npx jest test/verify-baseline.test.js` green
- `bash -n hooks/pre-push` valid; gate invokes verify-baseline
</verification>

<success_criteria>
Baseline integrity is a real, run-backed, exit-coded check gated at pre-push — the fabricated-sentinel failure class is closed.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.8-phases/34-promotion-integrity-lifecycle/34-03-SUMMARY.md`.
</output>
