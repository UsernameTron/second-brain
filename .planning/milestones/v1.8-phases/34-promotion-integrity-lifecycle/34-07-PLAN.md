---
phase: 34-promotion-integrity-lifecycle
plan: 07
type: execute
wave: 5
depends_on: ["34-06"]
files_modified:
  - scripts/dream.js
  - src/dream.js
  - .claude/skills/dream-apply/SKILL.md
  - test/dream-apply.test.js
  - package.json
autonomous: true
requirements: [DREAM-CONSOLIDATION-01]
must_haves:
  truths:
    - "`/dream-apply` snapshots memory.md + embeddings.jsonl + index.db to memory/.snapshots/dream-YYYYMMDD/ BEFORE any edit"
    - "Accepted MERGE ops insert the merged entry and append `superseded-by:: <newhash>` to each source (sources never deleted); accepted STALE ops append their flag line"
    - "The apply sequence ends with `npm run eval:recall`; on exit 1 it AUTO-RESTORES the snapshot, reverts op statuses to unresolved, and exits non-zero with the eval diff"
    - "Apply is human-invoked only — never scheduled — and batch-capped at 5-10 ops"
    - "Checkbox state of the changeset is read via the SHARED parseCheckboxState"
  artifacts:
    - path: "scripts/dream.js"
      provides: "--apply branch: snapshot → apply → reindex → eval gate → restore-on-fail"
      contains: "--apply"
    - path: ".claude/skills/dream-apply/SKILL.md"
      provides: "/dream-apply skill (vault store, human-only)"
  key_links:
    - from: "scripts/dream.js --apply"
      to: "npm run eval:recall"
      via: "mandatory post-apply gate, auto-restore on exit 1"
      pattern: "eval:recall|eval-recall"
    - from: "scripts/dream.js --apply"
      to: "memory/.snapshots/dream-YYYYMMDD/"
      via: "snapshot-first then restore-on-fail"
      pattern: "snapshots"
---

<objective>
Build the dream APPLY path (human-invoked only): snapshot-first, apply accepted MERGE/STALE ops in place (sources superseded, never deleted), reindex, and gate on `npm run eval:recall` with automatic snapshot restore on regression. Full sequence: DREAM-CONSOLIDATION-DESIGN.md:112-125.

Purpose: the only path that mutates memory.md does so reversibly, behind a recall-regression backstop.
Output: `scripts/dream.js --apply`, the apply/restore logic in src/dream.js, and the `/dream-apply` skill.
</objective>

<constraints>
- Apply is HUMAN-INVOKED ONLY — never add it to any plist/schedule.
- SNAPSHOT FIRST: copy memory/memory.md, embeddings.jsonl, index.db to memory/.snapshots/dream-YYYYMMDD/ before touching anything (codifies the .pre-audit.20260719 backup pattern).
- Sources are SUPERSEDED, never deleted — golden hashes stay resolvable throughout. Old embeddings are KEPT (indexNewEntries only adds new merged hashes); the --strict drift check requires sidecar/file parity.
- Acquire the proposals `.lock` (memory-proposals.js lock helpers) around apply — guards concurrent wrap/promote writes.
- Batch cap = promotion.batchCapMin/Max (5-10) ops per invocation.
- Changeset checkbox state read via the SHARED parseCheckboxState (Plan 34-01) — no second parser.
- MANDATORY eval gate: `npm run eval:recall`; exit 1 → auto-restore snapshot, revert op statuses to unresolved, exit non-zero with the eval diff.
</constraints>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.8-phases/34-promotion-integrity-lifecycle/34-CONTEXT.md
@.planning/research/DREAM-CONSOLIDATION-DESIGN.md

<interfaces>
Apply sequence (DREAM-CONSOLIDATION-DESIGN.md:114-124), in order:
1. Snapshot memory.md + embeddings.jsonl + index.db → memory/.snapshots/dream-YYYYMMDD/
2. Acquire proposals lock (memory-proposals.js _testOnly.acquireLock or a shared lock helper)
3. Apply accepted ops: MERGE inserts merged entry into current ## YYYY-MM section + appends superseded-by:: <newhash> to each source; STALE appends its flag line
4. regenerateAutoIndex() (src/promote-memories.js)
5. indexNewEntries() for new merged hashes (src/semantic-index.js) — old embeddings kept
6. SQLite rebuild (scripts/build-index.js), reach export, drift check --strict
7. npm run eval:recall — exit 1 → auto-restore snapshot, revert op statuses, exit non-zero with eval diff
8. Update state/dream-ledger.json; mark applied ops processed in the changeset

Reuse: regenerateAutoIndex + appendToMemoryFile (src/promote-memories.js), indexNewEntries (src/semantic-index.js), parseCheckboxState + lock helpers (src/memory-proposals.js), scripts/eval-recall.js (exit 1 on regression), scripts/build-index.js --strict.
Named design test: apply → eval-fail → snapshot-restore round-trip on the frozen seed vault.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Snapshot + in-place apply of accepted MERGE/STALE ops</name>
  <read_first>
    - .planning/research/DREAM-CONSOLIDATION-DESIGN.md:114-124 (apply sequence steps 1-5)
    - src/promote-memories.js (regenerateAutoIndex, appendToMemoryFile, month-section insertion)
    - src/dream.js (changeset parser from Plan 34-06), src/memory-proposals.js parseCheckboxState + lock
  </read_first>
  <behavior>
    - snapshotStore() copies memory.md + embeddings.jsonl + index.db into memory/.snapshots/dream-YYYYMMDD/ and returns the snapshot path
    - applyOps reads the changeset, selects only accepted ops (via parseCheckboxState), caps at batchCapMax
    - a MERGE op inserts the merged entry and appends `superseded-by:: <newhash>` to BOTH source entries (sources remain in the file)
    - a STALE op appends its `stale:: ...` (or superseded-by::) line to the target
    - restoreSnapshot(path) byte-restores all three files
  </behavior>
  <action>
    In src/dream.js add `snapshotStore()`, `applyOps(changeset, config)`, `restoreSnapshot(snapshotPath)`. snapshotStore copies the three files (fs.copyFileSync) into memory/.snapshots/dream-<YYYYMMDD>/ (mkdir recursive). applyOps: parse the changeset, keep ops whose parseCheckboxState status === 'accepted', slice to config.promotion.batchCapMax; for MERGE insert the merged-entry block into the current `## YYYY-MM` section of memory.md and append `superseded-by:: <newhash>` to each source entry's field block; for STALE append the flag line to the target. Call regenerateAutoIndex() then indexNewEntries() for new merged hashes. restoreSnapshot copies the snapshot files back over the live ones. Add test/dream-apply.test.js against a temp VAULT_ROOT fixture: MERGE supersedes-not-deletes both sources; snapshot then restore is byte-identical.
  </action>
  <acceptance_criteria>
    - `grep -n "snapshotStore" src/dream.js`, `grep -n "restoreSnapshot" src/dream.js`, `grep -n "applyOps" src/dream.js` all match
    - `grep -n "superseded-by" src/dream.js` matches in the MERGE apply path
    - `grep -n "parseCheckboxState" src/dream.js` used to select accepted ops (no new checkbox regex)
    - `npx jest test/dream-apply.test.js -t "supersede"` and `-t "restore"` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npx jest test/dream-apply.test.js && npm run lint</automated>
  </verify>
  <done>Accepted ops apply in place with sources superseded-not-deleted; snapshot/restore round-trips byte-identically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Eval gate with auto-restore on regression</name>
  <read_first>
    - .planning/research/DREAM-CONSOLIDATION-DESIGN.md:122-124 (eval gate + auto-restore)
    - scripts/eval-recall.js (exit codes: 0 ok / 1 regression / 2-3 preflight)
    - scripts/build-index.js (--strict drift check)
  </read_first>
  <behavior>
    - After applyOps, the sequence runs the eval and the --strict drift check
    - eval exit 1 (recall regression) → restoreSnapshot is called, op statuses revert to unresolved, the process exits non-zero and prints the eval diff
    - eval exit 0 → ops marked processed, ledger updated, snapshot retained
  </behavior>
  <action>
    Add `runEvalGate(snapshotPath, changesetPath)` to src/dream.js (or the apply orchestrator): shell out to `npm run eval:recall` (child_process, inherit/capture), and run build-index --strict; if eval exit code === 1, call restoreSnapshot(snapshotPath), rewrite the changeset op boxes back to unresolved (uncheck accept), and cause a non-zero exit surfacing the eval diff. On exit 0, mark applied ops processed and update state/dream-ledger.json opsAccepted. Add tests using the frozen seed vault: force an eval failure (mock the eval runner to return exit 1) and assert restoreSnapshot was called and memory.md matches the pre-apply snapshot (the named design round-trip test); force exit 0 and assert ops marked processed + ledger updated.
  </action>
  <acceptance_criteria>
    - `grep -nE "eval:recall|eval-recall" src/dream.js scripts/dream.js` matches (mandatory gate wired)
    - `grep -n "restoreSnapshot" src/dream.js` is called on the eval-fail branch
    - `npx jest test/dream-apply.test.js -t "eval-fail"` (round-trip restore) exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx jest test/dream-apply.test.js && npm run lint</automated>
  </verify>
  <done>A recall regression auto-restores the snapshot and aborts non-zero; a clean run marks ops processed and updates the ledger.</done>
</task>

<task type="auto">
  <name>Task 3: scripts/dream.js --apply wiring + /dream-apply skill</name>
  <read_first>
    - scripts/dream.js (the --propose dispatch from Plan 34-06, where --apply stubs in)
    - src/dream.js (snapshotStore/applyOps/runEvalGate/restoreSnapshot)
    - .claude/skills/dream-propose/SKILL.md (mirror structure for the apply skill)
  </read_first>
  <action>
    Wire the `--apply` branch in scripts/dream.js: resolve the current proposals/dream-changeset-YYYY-MM.md; snapshotStore(); acquire the proposals lock; applyOps(); regenerate/reindex/build-index --strict/reach-export; runEvalGate() (auto-restore on exit 1); release lock; update ledger; print summary. Batch-cap at config.promotion.batchCapMax. Add `"dream:apply": "node scripts/dream.js --apply"` to package.json. Create .claude/skills/dream-apply/SKILL.md for `/dream-apply` — state it targets the vault `memory/memory.md` store, is human-invoked only (never scheduled), snapshots first, and auto-restores on recall regression. Do NOT add --apply to any plist.
  </action>
  <acceptance_criteria>
    - `grep -n "\-\-apply" scripts/dream.js` matches and dispatches to snapshot→apply→eval gate
    - `grep -n "dream:apply" package.json` matches
    - `.claude/skills/dream-apply/SKILL.md` exists; `grep -n "human-invoked\|never scheduled\|human-only" .claude/skills/dream-apply/SKILL.md` matches
    - `grep -rn "dream.js --apply" config/scheduling.json ~/Library/LaunchAgents/com.secondbrain.dream.plist` returns NOTHING (apply is never scheduled)
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('./src/dream.js')" && grep -q "dream:apply" package.json</automated>
  </verify>
  <done>`/dream-apply` runs the full snapshot→apply→eval-gate sequence, is human-only, and never appears in any schedule.</done>
</task>

</tasks>

<verification>
- `npx jest test/dream-apply.test.js` green; `npm run lint` clean
- apply → eval-fail → snapshot-restore round-trip passes on the frozen seed vault
- `--apply` absent from every plist/schedule
</verification>

<success_criteria>
The only memory-mutating path snapshots first, supersedes-not-deletes, and auto-restores on recall regression — human-invoked, never scheduled.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.8-phases/34-promotion-integrity-lifecycle/34-07-SUMMARY.md`.
</output>
