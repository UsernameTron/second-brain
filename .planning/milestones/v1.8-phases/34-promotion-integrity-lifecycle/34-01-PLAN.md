---
phase: 34-promotion-integrity-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memory-proposals.js
  - src/promote-memories.js
  - test/memory-proposals.test.js
  - test/promote-memories.test.js
autonomous: true
requirements: [PROMOTE-PARSE-01, PROMOTE-VAULT-01, PROMOTE-ID-01]
must_haves:
  truths:
    - "A checkbox with whitespace variants (`- [ X] accept`, `- [X ] accept`, `-  [x]  accept`) is parsed as a real accept, not silently skipped"
    - "A batch where candidates carry unparseable checkbox marks and nothing is eligible fails loudly (non-zero / explicit error), never reports `promoted 0` as success"
    - "Both promote-memories.js and dream.js (later) read checkbox state through ONE shared function — no second parser implementation exists"
    - "memory-proposals.js resolves VAULT_ROOT at call time; both modules assert they agree on the resolved root at promotion start"
    - "Proposal IDs are collision-safe across proposal files and archive (no two live corpora mint the same mem-YYYYMMDD-NNN)"
  artifacts:
    - path: "src/memory-proposals.js"
      provides: "Shared parseCheckboxState helper, call-time VAULT_ROOT, collision-safe generateCandidateId"
      contains: "function parseCheckboxState"
    - path: "src/promote-memories.js"
      provides: "parseCandidateSections delegates checkbox state to the shared helper"
      contains: "parseCheckboxState"
  key_links:
    - from: "src/promote-memories.js"
      to: "src/memory-proposals.js"
      via: "require + call parseCheckboxState"
      pattern: "parseCheckboxState"
    - from: "src/memory-proposals.js VAULT_ROOT()"
      to: "src/promote-memories.js VAULT_ROOT()"
      via: "agreement assertion at promotion start"
      pattern: "VAULT_ROOT"
---

<objective>
Close the three P1/P2 promotion-gate backlog defects and lay the shared-parser foundation the dream work depends on. Today the accept matcher `/^- \[x\] accept$/im` (src/promote-memories.js:113) silently drops any whitespace variant, `VAULT_ROOT` binds at module load in memory-proposals.js:18 (forking the corpus across roots), and `generateCandidateId` scans only the current file (colliding IDs across forked corpora).

Purpose: no silent promotion failures, no forked corpora, no colliding provenance IDs — and one canonical checkbox parser both gates share.
Output: shared `parseCheckboxState` helper in memory-proposals.js, PARSE-01 whitespace tolerance + loud near-miss failure inside it, PROMOTE-VAULT-01 call-time root + agreement assert, PROMOTE-ID-01 collision-safe IDs.
</objective>

<constraints>
**OPERATOR HARD CONSTRAINT (verbatim, non-negotiable):** the /dream-apply changeset parser must share the exactly-one-checkbox parsing logic with the promotion gate — extract it from src/promote-memories.js:104-152 into a shared helper in src/memory-proposals.js and have both gates call it; no second implementation (two gate parsers will drift). The PROMOTE-PARSE-01 whitespace-tolerance fix lands inside that shared helper so both gates inherit it.

- Task 1 (extraction) is a BEHAVIOR-PRESERVING refactor: existing `test/promote-memories.test.js` must stay green with zero assertion changes before Task 2 adds new behavior.
- No `console.log` in production code (ESLint gate). Use `process.stderr.write` / `console.error` with the existing eslint-disable diagnostic pattern for loud failures.
</constraints>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.8-phases/34-promotion-integrity-lifecycle/34-CONTEXT.md
@.planning/milestones/v1.7-REQUIREMENTS.md

<interfaces>
Current inline parse (src/promote-memories.js:104-152) — extraction source:
```javascript
const acceptChecked = /^- \[x\] accept$/im.test(rawSection);
const rejectChecked = /^- \[x\] reject$/im.test(rawSection);
const editChecked = /^- \[x\] edit-then-accept$/im.test(rawSection);
const deferChecked = /^- \[x\] defer$/im.test(rawSection);
const checkedCount = [acceptChecked, rejectChecked, editChecked, deferChecked].filter(Boolean).length;
const ambiguous = checkedCount > 1;
// checkboxStatus: 'accepted' | 'rejected' | 'edit-then-accept' | 'deferred' | null
```

memory-proposals.js call-time root pattern to MATCH (already used in promote-memories.js):
```javascript
const VAULT_ROOT = () => process.env.VAULT_ROOT || path.join(process.env.HOME, 'Claude Cowork');
```
memory-proposals.js CURRENTLY (bug, line 18): `const VAULT_ROOT = process.env.VAULT_ROOT || path.join(process.env.HOME, 'Claude Cowork');`

generateCandidateId (memory-proposals.js:49-69) scans only PROPOSALS_FILE() for today's max NNN.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared parseCheckboxState helper (behavior-preserving)</name>
  <read_first>
    - src/promote-memories.js:104-152 (parseCandidateSections, the extraction source)
    - src/memory-proposals.js:1-70 (module shape, where the helper lands, existing exports)
    - test/promote-memories.test.js (the tests that must stay green)
  </read_first>
  <action>
    Add to src/memory-proposals.js a pure exported function `parseCheckboxState(sectionText)` returning `{ status, ambiguous, checkedCount, nearMissCount }` where status is one of `'accepted' | 'rejected' | 'edit-then-accept' | 'deferred' | null`. For THIS task use the EXACT current regexes so behavior is identical: `/^- \[x\] accept$/im`, `/^- \[x\] reject$/im`, `/^- \[x\] edit-then-accept$/im`, `/^- \[x\] defer$/im`; `checkedCount` = number matched; `ambiguous` = checkedCount > 1; status = the single matched label mapped to accepted/rejected/edit-then-accept/deferred, else null; set `nearMissCount: 0` for now (Task 2 populates it). Export it in module.exports.
    Then in src/promote-memories.js, `require` parseCheckboxState from ./memory-proposals and replace the inline lines 113-126 with a call: `const { status: checkboxStatus, ambiguous } = parseCheckboxState(rawSection);`. Do not change any other logic in parseCandidateSections. Do not change any test file in this task.
  </action>
  <acceptance_criteria>
    - `grep -n "function parseCheckboxState" src/memory-proposals.js` returns a match
    - `grep -n "parseCheckboxState" src/promote-memories.js` returns a match (the call site)
    - the module.exports block in src/memory-proposals.js references parseCheckboxState
    - `npx jest test/promote-memories.test.js` exits 0 with NO edits to that test file (behavior preserved)
  </acceptance_criteria>
  <verify>
    <automated>npx jest test/promote-memories.test.js test/memory-proposals.test.js</automated>
  </verify>
  <done>Shared parseCheckboxState lives in memory-proposals.js, promote-memories.js calls it, all existing tests green with no test edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PROMOTE-PARSE-01 — whitespace tolerance + loud near-miss inside the shared helper</name>
  <read_first>
    - src/memory-proposals.js (the parseCheckboxState added in Task 1)
    - .planning/milestones/v1.7-REQUIREMENTS.md:79 (PROMOTE-PARSE-01 definition and observed failure)
    - src/promote-memories.js (the run summary / where "promoted 0" is reported, to wire the loud failure)
  </read_first>
  <behavior>
    - `- [ X] accept`, `- [X ] accept`, `-  [x]  accept`, `- [x] accept ` (trailing space) all parse as status `accepted`
    - `- [x] accept` and `- [X] accept` still parse as accepted (no regression)
    - A section with `- [y] accept` (garbage in brackets) contributes to `nearMissCount` and yields status null
    - A section with an empty `- [ ] accept` box is NOT a near-miss (it is a legitimately-unreviewed line)
    - When a whole batch has zero parsed statuses AND nearMissCount > 0, the promotion run surfaces a loud non-zero / explicit-error outcome, not a silent `promoted 0` success
  </behavior>
  <action>
    In parseCheckboxState, replace the four exact-match regexes with whitespace-tolerant ones of the form `/^-\s*\[\s*[xX]\s*\]\s*accept\s*$/im` (and reject / edit-then-accept / defer). Compute `nearMissCount`: count checkbox lines matching `/^-\s*\[([^\]]*)\]\s*(accept|reject|edit-then-accept|defer)\b/gim` whose bracket capture, after trimming, is non-empty AND is not a tolerated x-form (`x`/`X`) — i.e. marks like `[y]`, `[xx]`, `[x!]`. An empty/whitespace bracket is NOT a near-miss. Return nearMissCount on the result.
    In src/promote-memories.js, aggregate nearMissCount across candidates during the run. When total promoted/eligible statuses == 0 AND aggregate nearMissCount > 0, emit a loud diagnostic via `console.error(JSON.stringify({ action: 'PROMOTE', decision: 'ABORT', reason: 'unparseable-checkboxes', nearMissCount }))` (with the existing `// eslint-disable-next-line no-console -- diagnostic` comment) and cause the run to exit non-zero / return an error result distinguishable from "nothing eligible". Add tests in test/memory-proposals.test.js for each behavior bullet above.
  </action>
  <acceptance_criteria>
    - A whitespace-tolerant checkbox regex of the form `\[\s*[xX]\s*\]` exists in src/memory-proposals.js (confirm by reading the file)
    - `grep -n "nearMissCount" src/memory-proposals.js` and `grep -n "nearMissCount" src/promote-memories.js` both match
    - `grep -n "unparseable-checkboxes" src/promote-memories.js` matches
    - `npx jest test/memory-proposals.test.js` exits 0 and includes new whitespace-variant + near-miss assertions
  </acceptance_criteria>
  <verify>
    <automated>npx jest test/memory-proposals.test.js test/promote-memories.test.js && npm run lint</automated>
  </verify>
  <done>Whitespace-variant boxes promote correctly; a batch of unparseable marks aborts loudly instead of reporting a green `promoted 0`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PROMOTE-VAULT-01 call-time root + agreement assert, PROMOTE-ID-01 collision-safe IDs</name>
  <read_first>
    - src/memory-proposals.js:18 (module-load VAULT_ROOT bug), :49-69 (generateCandidateId), :20-25 (path helpers that consume VAULT_ROOT)
    - src/promote-memories.js (the call-time VAULT_ROOT() pattern to match, and promotion entry point where the agreement assert belongs)
    - .planning/milestones/v1.7-REQUIREMENTS.md:81-83 (PROMOTE-VAULT-01, PROMOTE-ID-01 definitions)
  </read_first>
  <action>
    PROMOTE-VAULT-01: change src/memory-proposals.js:18 from `const VAULT_ROOT = process.env.VAULT_ROOT || ...` to a call-time arrow `const VAULT_ROOT = () => process.env.VAULT_ROOT || path.join(process.env.HOME, 'Claude Cowork');` and update every internal consumer (PROPOSALS_DIR, PROPOSALS_FILE, PENDING_FILE, LOCK_FILE, MEMORY_FILE, ARCHIVE_DIR) to call `VAULT_ROOT()`. Add an exported `resolvedVaultRoot()` returning the current value, and at promotion start in promote-memories.js assert both modules agree: if `require('./memory-proposals').resolvedVaultRoot() !== promoteMemoriesResolvedRoot()` throw a loud Error naming both paths.
    PROMOTE-ID-01: make generateCandidateId collision-safe. Scan not just PROPOSALS_FILE() but also the archive dir (ARCHIVE_DIR / proposal-archive) for today's max NNN, taking the global max across all files, so an archive-then-restage cycle cannot re-mint a used NNN. (Per-root discriminator is an acceptable alternative per CONTEXT Claude's Discretion — pick the archive-aware max scan for the smaller diff.)
    Add tests: memory-proposals resolves a per-call VAULT_ROOT override; generateCandidateId does not reuse an NNN already present in an archive fixture.
  </action>
  <acceptance_criteria>
    - `grep -n "VAULT_ROOT = () =>" src/memory-proposals.js` matches (call-time arrow)
    - `grep -c "VAULT_ROOT()" src/memory-proposals.js` >= 6 (all consumers call it)
    - `grep -n "resolvedVaultRoot" src/memory-proposals.js src/promote-memories.js` matches in both files
    - `npx jest test/memory-proposals.test.js` exits 0 with new VAULT_ROOT-override and ID-collision assertions
  </acceptance_criteria>
  <verify>
    <automated>npx jest test/memory-proposals.test.js test/promote-memories.test.js && npm run lint</automated>
  </verify>
  <done>Setting VAULT_ROOT after require routes to the new root; mismatched roots throw at promotion start; IDs are archive-aware unique.</done>
</task>

</tasks>

<verification>
- `npx jest test/memory-proposals.test.js test/promote-memories.test.js` green
- `npm run lint` clean (0 no-console warnings)
- One shared parseCheckboxState; no duplicate checkbox regex remains inline in promote-memories.js
</verification>

<success_criteria>
Whitespace-variant checkboxes promote; unparseable-mark batches abort loudly; VAULT_ROOT is call-time and asserted-agreed; candidate IDs are collision-safe; the shared parser is the single source both gates will use.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.8-phases/34-promotion-integrity-lifecycle/34-01-SUMMARY.md`.
</output>
