---
phase: 34-promotion-integrity-lifecycle
plan: 06
type: execute
wave: 4
depends_on: ["34-05"]
files_modified:
  - src/dream.js
  - scripts/dream.js
  - state/dream-ledger.json
  - config/scheduling.json
  - .claude/skills/dream-propose/SKILL.md
  - test/dream.test.js
  - package.json
autonomous: true
requirements: [DREAM-CONSOLIDATION-01]
must_haves:
  truths:
    - "MISSED PATTERNS extraction proposes ADDs into the existing memory-proposals.md gate, deduped by content_hash AND cosine ≥ patternDedupCosine (0.86), capped at maxPatternAdds"
    - "MERGE/STALE ops are written to proposals/dream-changeset-YYYY-MM.md in the exact design-doc format, with `- [ ] accept/reject/defer` boxes parsed by the SHARED parseCheckboxState"
    - "`/dream-propose` refuses to run while an unresolved changeset exists"
    - "`node scripts/dream.js --propose` runs detection → writes changeset + stages pattern ADDs → updates state/dream-ledger.json, applying NOTHING to memory.md"
    - "A launchd plist runs --propose only, monthly day 1 07:15; apply is never scheduled"
  artifacts:
    - path: "scripts/dream.js"
      provides: "--propose / --dry-run entrypoint"
      contains: "--propose"
    - path: "src/dream.js"
      provides: "detectPatterns + writeChangeset (shared parser) + refuse-on-unresolved"
      contains: "patternDedupCosine"
    - path: ".claude/skills/dream-propose/SKILL.md"
      provides: "/dream-propose skill targeting the vault memory.md store"
  key_links:
    - from: "src/dream.js writeChangeset / applier"
      to: "src/memory-proposals.js parseCheckboxState"
      via: "shared checkbox parser (no second impl)"
      pattern: "parseCheckboxState"
    - from: "scripts/dream.js --propose"
      to: "proposals/dream-changeset-YYYY-MM.md + memory-proposals.md"
      via: "changeset writer + pattern ADD staging"
      pattern: "dream-changeset"
---

<objective>
Complete the dream PROPOSE path: MISSED PATTERNS extraction (rides the existing promote gate), the reviewable MERGE/STALE changeset (parsed by the shared checkbox helper), refuse-on-unresolved-changeset, the `scripts/dream.js --propose` entrypoint, the ledger, the monthly plist, and the `/dream-propose` skill. Full detail: DREAM-CONSOLIDATION-DESIGN.md.

Purpose: a monthly, human-invocable propose pass that stages consolidation for review and applies nothing.
Output: `detectPatterns` + `writeChangeset` in src/dream.js, `scripts/dream.js --propose`, ledger, plist, `/dream-propose` skill.
</objective>

<constraints>
- **Shared parser (OPERATOR HARD CONSTRAINT):** the changeset checkbox parsing MUST call `parseCheckboxState` from src/memory-proposals.js (Plan 34-01). No second checkbox parser implementation in src/dream.js — two gate parsers will drift.
- Naming: commands are `/dream-propose` (and `/dream-apply` in Plan 07), NOT `/dream` — that trigger belongs to the existing `.claude/skills/dream-memory-consolidation/` skill which targets Claude Code MEMORY.md, a DIFFERENT store. The SKILL.md description must state it targets the vault `memory/memory.md`.
- PATTERNS ADDs ride the UNTOUCHED `/promote-memories` gate via memory-proposals.js writeCandidate — zero new apply mechanics for that op.
- propose REFUSES while `proposals/dream-changeset-YYYY-MM.md` exists unresolved (no pile-up).
- `maxLLMCalls` (40) is a hard circuit breaker inside the propose loop.
- Transcripts and standups are NOT read (daily-sweep already mines transcripts forward). Inputs: memory.md + embeddings, state/session-log.md + state/decisions.md windowed by sessionLogWindow.
- --propose applies NOTHING to memory.md.
</constraints>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.8-phases/34-promotion-integrity-lifecycle/34-CONTEXT.md
@.planning/research/DREAM-CONSOLIDATION-DESIGN.md

<interfaces>
Changeset format (copy verbatim from DREAM-CONSOLIDATION-DESIGN.md:76-108) — frontmatter `type: dream-changeset / generated / run / snapshot / total-ops`; per-op `## dream-YYYY-MM-DD-NNN · MERGE|STALE`, three boxes `- [ ] accept/reject/defer`, `sources::`, `similarity::`, `golden-hash::`, `rationale::`, inline `merged-entry::` (MERGE) or `target:: / reason:: / action::` (STALE).
`golden-hash:: YES` when any source/target hash appears in eval/golden-recall.json, else NO.
src/memory-proposals.js: `parseCheckboxState(sectionText)` (Plan 34-01), `writeCandidate(candidate)` (pattern ADD staging).
src/dream.js: detectMergePairs/authorMerge/detectStale (Plan 34-05).
Plist precedent (com.secondbrain.today.plist): absolute node path /opt/homebrew/opt/node@22/bin/node, WorkingDirectory, EnvironmentVariables PATH+VAULT_ROOT, StandardOut/ErrorPath logs. Use StartCalendarInterval {Day:1, Hour:7, Minute:15}, ProgramArguments running scripts/dream.js --propose.
Ledger shape: state/dream-ledger.json `{"lastRun": "...", "runs": [{date, opsProposed, opsAccepted}]}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MISSED PATTERNS extraction + anti-re-proposal dedup</name>
  <read_first>
    - .planning/research/DREAM-CONSOLIDATION-DESIGN.md:61-64 (PATTERNS op spec)
    - src/memory-proposals.js (writeCandidate, isDuplicate, computeHash)
    - src/semantic-index.js (embedding a batch for cosine dedup), state/session-log.md + state/decisions.md (input corpus shape)
  </read_first>
  <behavior>
    - detectPatterns reads state/session-log.md + state/decisions.md windowed by config.dream.sessionLogWindow, ≤2 chunked Sonnet calls (maxTokens 4096), confidence ≥ 0.75
    - a candidate whose content_hash matches memory/archive/open proposals is dropped
    - a candidate whose embedding cosine vs any live entry ≥ patternDedupCosine (0.86) is dropped
    - survivors (≤ maxPatternAdds) are staged via memory-proposals.writeCandidate with source-ref pointing at session-log headings
  </behavior>
  <action>
    In src/dream.js add `async function detectPatterns(config)`: read the two state files, window by sessionLogWindow days, chunk into ≤2 createSonnetClient() calls asking for "patterns spanning ≥2 sessions, not one-offs, confidence ≥0.75"; dedup survivors by (a) content_hash via existing isDuplicate/computeHash and (b) one Voyage batch cosine vs live embeddings, dropping ≥ config.dream.patternDedupCosine; cap at config.dream.maxPatternAdds; stage each via `writeCandidate({content, category, sourceRef, confidence, extractionTrigger:'dream'})`. Count these against the maxLLMCalls breaker. Add tests with mocked Sonnet + mocked embeddings: a duplicate-hash candidate dropped; a high-cosine candidate dropped; a novel one staged.
  </action>
  <acceptance_criteria>
    - `grep -n "function detectPatterns" src/dream.js` matches
    - `grep -n "patternDedupCosine" src/dream.js` and `grep -n "writeCandidate" src/dream.js` match
    - `grep -n "maxPatternAdds" src/dream.js` matches (cap enforced)
    - `npx jest test/dream.test.js` exits 0 with dedup-drop assertions
  </acceptance_criteria>
  <verify>
    <automated>npx jest test/dream.test.js && npm run lint</automated>
  </verify>
  <done>Pattern ADDs are extracted, double-deduped, capped, and staged into the existing gate — no new apply mechanics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Changeset writer (shared parser) + refuse-on-unresolved</name>
  <read_first>
    - .planning/research/DREAM-CONSOLIDATION-DESIGN.md:66-110 (changeset format verbatim + refuse rule)
    - src/memory-proposals.js parseCheckboxState (the shared parser to call), lock/frontmatter helpers
    - eval/golden-recall.json (for the golden-hash:: YES/NO stamp)
  </read_first>
  <behavior>
    - writeChangeset renders MERGE/STALE ops to proposals/dream-changeset-YYYY-MM.md in the exact design format with three `- [ ]` boxes each
    - golden-hash:: YES stamped when a source/target hash is in eval/golden-recall.json, else NO
    - a reader of the changeset resolves checkbox state via the SHARED parseCheckboxState (single multi-box op → ambiguous → skipped, same as promote gate)
    - propose refuses (throws/returns error, writes nothing new) when an unresolved dream-changeset-*.md already exists
  </behavior>
  <action>
    In src/dream.js add `writeChangeset(mergeOps, staleOps, meta)` producing proposals/dream-changeset-YYYY-MM.md with the verbatim frontmatter (type: dream-changeset, generated, run, snapshot, total-ops) and per-op blocks copied from the design doc; stamp golden-hash by checking each hash against eval/golden-recall.json. Add `hasUnresolvedChangeset()` scanning proposals/ for a dream-changeset with any op lacking a resolved (single-checked, non-defer) box — read box state via the shared `parseCheckboxState`. propose must call hasUnresolvedChangeset() first and refuse if true. Add a changeset-parse function that also routes through parseCheckboxState (this is what Plan 07 apply consumes). Add tests: a multi-box op parses as ambiguous→skipped via the shared parser; propose refuses when an unresolved changeset is present; golden-hash YES stamped for a golden hash.
  </action>
  <acceptance_criteria>
    - `grep -n "dream-changeset" src/dream.js` and `grep -n "parseCheckboxState" src/dream.js` both match (shared parser used, no local checkbox regex)
    - `grep -nE "\\[.?x.?\\]|\\[ \\]" src/dream.js` shows NO hand-rolled checkbox regex for op state (confirm by reading — box parsing delegates to parseCheckboxState)
    - `grep -n "golden-hash" src/dream.js` matches
    - `npx jest test/dream.test.js -t "unresolved"` and `-t "ambiguous"` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npx jest test/dream.test.js && npm run lint</automated>
  </verify>
  <done>The reviewable changeset is written in the locked format, parsed only through the shared checkbox helper, and propose refuses on pile-up.</done>
</task>

<task type="auto">
  <name>Task 3: scripts/dream.js --propose entrypoint + ledger + monthly plist + /dream-propose skill</name>
  <read_first>
    - src/dream.js (detectMergePairs/authorMerge/detectStale/detectPatterns/writeChangeset/hasUnresolvedChangeset)
    - ~/Library/LaunchAgents/com.secondbrain.today.plist (plist conventions to mirror) — or config/scheduling.json for the committed plist source
    - .claude/skills/dream-memory-consolidation/SKILL.md (the colliding skill — read to differentiate the description)
  </read_first>
  <action>
    Create scripts/dream.js parsing `--propose | --dry-run` (the `--apply` branch is Plan 07 — stub it to a clear "not-yet-implemented" for now or leave the dispatch ready). --propose flow: load config.dream; refuse if hasUnresolvedChangeset(); readMemory + readAllEmbeddings; detectMergePairs → authorMerge (drop bad-quote); detectStale; detectPatterns (stages ADDs); writeChangeset(mergeOps, staleOps, meta); update state/dream-ledger.json {lastRun, runs[{date, opsProposed, opsAccepted:0}]}; print a summary; enforce the maxLLMCalls breaker. --dry-run does everything except writing the changeset/ledger/ADDs. Add `"dream:propose": "node scripts/dream.js --propose"` to package.json. Commit the monthly plist as config/scheduling source and/or ~/Library/LaunchAgents/com.secondbrain.dream.plist: Label com.secondbrain.dream, ProgramArguments node + scripts/dream.js --propose, WorkingDirectory the repo, EnvironmentVariables PATH+VAULT_ROOT, StartCalendarInterval {Day:1, Hour:7, Minute:15}, log paths, RunAtLoad false. Create .claude/skills/dream-propose/SKILL.md describing `/dream-propose` — MUST state it targets the vault `memory/memory.md` store (not Claude Code MEMORY.md) to disambiguate from the existing dream-memory-consolidation skill.
  </action>
  <acceptance_criteria>
    - `node scripts/dream.js --dry-run` exits 0 against the live vault and writes nothing (verify no new proposals/dream-changeset file after a dry run)
    - `grep -n "hasUnresolvedChangeset" scripts/dream.js` matches (refuse wired)
    - `grep -n "com.secondbrain.dream" config/scheduling.json` OR the committed plist file exists with `<integer>15</integer>` minute and `--propose`
    - `.claude/skills/dream-propose/SKILL.md` exists and `grep -n "memory/memory.md" .claude/skills/dream-propose/SKILL.md` matches (store disambiguation)
  </acceptance_criteria>
  <verify>
    <automated>node scripts/dream.js --dry-run</automated>
  </verify>
  <done>--propose runs end-to-end (changeset + staged ADDs + ledger), --dry-run is side-effect-free, a monthly propose-only plist and the disambiguated /dream-propose skill exist.</done>
</task>

</tasks>

<verification>
- `npx jest test/dream.test.js` green; `npm run lint` clean
- `node scripts/dream.js --dry-run` side-effect-free; propose refuses on an existing unresolved changeset
- plist runs --propose only (apply never scheduled)
</verification>

<success_criteria>
A monthly, human-invocable propose pass stages MERGE/STALE to a shared-parser changeset and pattern ADDs to the existing gate, refuses on pile-up, and applies nothing.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.8-phases/34-promotion-integrity-lifecycle/34-06-SUMMARY.md`.
</output>
