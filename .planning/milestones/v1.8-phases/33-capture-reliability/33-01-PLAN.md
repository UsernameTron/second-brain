---
phase: 33-capture-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/com.secondbrain.daily-sweep.plist
autonomous: true
requirements: [CAP-LAUNCHD-01]
must_haves:
  truths:
    - "launchctl kickstart of com.secondbrain.daily-sweep exits 0 (not 78) and writes a non-empty log"
    - "The active launchd registration runs /opt/homebrew/opt/node@22/bin/node (not bare node)"
    - "config/com.secondbrain.daily-sweep.plist exists as the versioned source of truth (dream.plist pattern)"
  artifacts:
    - path: "config/com.secondbrain.daily-sweep.plist"
      provides: "Versioned fixed launchd plist"
      contains: "/opt/homebrew/opt/node@22/bin/node"
  key_links:
    - from: "~/Library/LaunchAgents/com.secondbrain.daily-sweep.plist"
      to: "scripts/daily-sweep.js"
      via: "ProgramArguments with full node@22 path + PATH env"
      pattern: "opt/node@22/bin/node"
---

<objective>
Root-cause the exit-78 (EX_CONFIG) daily-sweep failure, version the fixed plist into `config/`, rebootstrap the launchd registration, and observe one real launchd-context fire that exits 0.

Purpose: The nightly 23:45 sweep is the capture engine. It has fired 2× and exit-78'd both times with a 0-byte log — nothing was captured. Everything else in this phase (evidence, resilience) is theater if the job can't actually run. Prove the scheduler fires FIRST.
Output: `config/com.secondbrain.daily-sweep.plist` (new), a rebootstrapped + verified launchd job, and a documented same-day real fire.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.8-phases/33-capture-reliability/33-CONTEXT.md
@config/com.secondbrain.dream.plist

<diagnosis_already_confirmed>
Verified live 2026-07-21 (executor must re-confirm, not assume):
- Active registration (`launchctl print gui/$UID/com.secondbrain.daily-sweep`) runs BARE `node` with
  `default environment PATH => /usr/bin:/bin:/usr/sbin:/sbin` (no node@22 on PATH) → `node` not found →
  spawn fails → `last exit code = 78: EX_CONFIG`, 0-byte log.
- The ON-DISK installed plist (`~/Library/LaunchAgents/com.secondbrain.daily-sweep.plist`) is ALREADY the
  fixed version (full node@22 path, WorkingDirectory, EnvironmentVariables PATH, log paths, Hour 23 Minute 45)
  — but it was never rebootstrapped over the stale registration, and there is no `config/` copy.
Root cause = stale launchd registration. Fix = version the fixed plist into config/ + bootout + bootstrap.
</diagnosis_already_confirmed>

<locked_decisions>
- Do NOT add LLM_PROVIDER to this plist. Daily-sweep stays LOCAL-FIRST; the Anthropic pin is dream-only
  (per 260721-ljn). classifier-health (plans 02/03) is the sweep's resilience story, not a provider pin.
- No log rotation this phase (YAGNI). Keep the single existing log path.
- Observation = `launchctl kickstart` for a same-day REAL launchd-context fire (no overnight block).
  Tonight's scheduled 23:45 fire is the CONFIRMING observation → recorded as a human-verify item, not a task.
</locked_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm exit-78 root cause and version the fixed plist into config/</name>
  <files>config/com.secondbrain.daily-sweep.plist</files>
  <read_first>
    - config/com.secondbrain.dream.plist (the versioned-plist pattern to mirror: Label, ProgramArguments with full node path, WorkingDirectory, EnvironmentVariables PATH, StartCalendarInterval, log paths, RunAtLoad false)
    - The installed plist: run `/usr/bin/plutil -p ~/Library/LaunchAgents/com.secondbrain.daily-sweep.plist`
  </read_first>
  <action>
    1. Confirm the diagnosis (do not assume). Run and capture:
       - `launchctl print gui/$(id -u)/com.secondbrain.daily-sweep 2>&1 | grep -A4 'arguments\|last exit\|default environment'`
         Expect: `arguments = { node ... }` (BARE node), a PATH without `opt/node@22`, and `last exit code = 78: EX_CONFIG`.
       - `log show --predicate 'eventMessage CONTAINS "com.secondbrain.daily-sweep"' --last 3d --style compact 2>&1 | tail -20`
         (spawn/config errors, if any). This confirms stale-registration vs a deeper plist defect.
       - Record the one-line root cause in the SUMMARY ("stale registration: active job runs bare `node`, on-disk plist is fixed").
    2. Version the plist. Preferred (laziest correct): copy the already-fixed installed file:
       `cp ~/Library/LaunchAgents/com.secondbrain.daily-sweep.plist config/com.secondbrain.daily-sweep.plist`
       If that file is unreadable, author `config/com.secondbrain.daily-sweep.plist` verbatim as below.
    3. The file MUST be exactly this (no LLM_PROVIDER, no VAULT_ROOT — daily-sweep.js defaults VAULT_ROOT to ~/Claude Cowork and loads .env at entry for API keys):

    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
    <plist version="1.0">
    <dict>
      <key>Label</key>
      <string>com.secondbrain.daily-sweep</string>
      <key>ProgramArguments</key>
      <array>
        <string>/opt/homebrew/opt/node@22/bin/node</string>
        <string>/Users/cpconnor/projects/second-brain/scripts/daily-sweep.js</string>
      </array>
      <key>WorkingDirectory</key>
      <string>/Users/cpconnor/projects/second-brain</string>
      <key>EnvironmentVariables</key>
      <dict>
        <key>PATH</key>
        <string>/opt/homebrew/opt/node@22/bin:/usr/local/bin:/usr/bin:/bin</string>
      </dict>
      <key>StartCalendarInterval</key>
      <dict>
        <key>Hour</key>
        <integer>23</integer>
        <key>Minute</key>
        <integer>45</integer>
      </dict>
      <key>StandardOutPath</key>
      <string>/Users/cpconnor/Library/Logs/com.secondbrain.daily-sweep.log</string>
      <key>StandardErrorPath</key>
      <string>/Users/cpconnor/Library/Logs/com.secondbrain.daily-sweep.log</string>
      <key>RunAtLoad</key>
      <false/>
    </dict>
    </plist>
    ```
    4. Lint it: `plutil -lint config/com.secondbrain.daily-sweep.plist` must print "OK".
  </action>
  <verify>
    <automated>plutil -lint config/com.secondbrain.daily-sweep.plist && grep -q "opt/node@22/bin/node" config/com.secondbrain.daily-sweep.plist && grep -q "<integer>45</integer>" config/com.secondbrain.daily-sweep.plist && ! grep -q "LLM_PROVIDER" config/com.secondbrain.daily-sweep.plist && echo PLIST_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f config/com.secondbrain.daily-sweep.plist` succeeds
    - `plutil -lint config/com.secondbrain.daily-sweep.plist` prints "OK"
    - `grep "/opt/homebrew/opt/node@22/bin/node" config/com.secondbrain.daily-sweep.plist` matches (full node path, not bare `node`)
    - `grep "WorkingDirectory" config/com.secondbrain.daily-sweep.plist` matches
    - `grep -c "com.secondbrain.daily-sweep.log" config/com.secondbrain.daily-sweep.plist` returns 2 (StandardOut + StandardError)
    - `grep "LLM_PROVIDER" config/com.secondbrain.daily-sweep.plist` returns NOTHING (exit 1) — local-first, per locked decision
    - SUMMARY records the confirmed root cause (stale registration)
  </acceptance_criteria>
  <done>config/com.secondbrain.daily-sweep.plist exists, lints clean, matches the fixed content, carries no LLM_PROVIDER; root cause confirmed and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Rebootstrap the launchd job and observe one real fire (exit 0)</name>
  <files>config/com.secondbrain.daily-sweep.plist (installed to ~/Library/LaunchAgents/; launchctl-only, no other repo files)</files>
  <read_first>
    - config/com.secondbrain.daily-sweep.plist (the file from Task 1)
    - scripts/daily-sweep.js (what the job runs — confirms it loads .env at require.main so launchd has API keys)
  </read_first>
  <action>
    Install the versioned plist over the stale registration, then observe a real launchd-context fire.
    Run in order (the `|| true` guards a not-currently-loaded job):

    1. `cp config/com.secondbrain.daily-sweep.plist ~/Library/LaunchAgents/com.secondbrain.daily-sweep.plist`
    2. `launchctl bootout gui/$(id -u)/com.secondbrain.daily-sweep 2>/dev/null || true`
    3. `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.secondbrain.daily-sweep.plist`
    4. Verify the ACTIVE registration is now the fixed one:
       `launchctl print gui/$(id -u)/com.secondbrain.daily-sweep | grep -A3 arguments`
       MUST now show `/opt/homebrew/opt/node@22/bin/node` (not bare `node`).
    5. Truncate the log so the observation is unambiguous: `: > ~/Library/Logs/com.secondbrain.daily-sweep.log`
    6. Kickstart a REAL fire (runs the actual sweep — stages real proposals; this is the intended "observe one real fire"):
       `launchctl kickstart -k gui/$(id -u)/com.secondbrain.daily-sweep`
    7. The sweep is async and may take 1-3 min (LLM calls). Poll until it finishes, bounded (do NOT foreground-sleep):
       `for i in $(seq 1 60); do launchctl print gui/$(id -u)/com.secondbrain.daily-sweep | grep -q "last exit code = 0" && break; done`
       Then read the exit code: `launchctl print gui/$(id -u)/com.secondbrain.daily-sweep | grep "last exit code"`
       Expect `last exit code = 0`. If still 78, the fix failed — capture the log and STOP (do not paper over it).
    8. Confirm the log is non-empty: `test -s ~/Library/Logs/com.secondbrain.daily-sweep.log` and tail it for the
       `[daily-sweep] Sweep complete` line.
  </action>
  <verify>
    <automated>launchctl print gui/$(id -u)/com.secondbrain.daily-sweep | grep -q "opt/node@22/bin/node" && launchctl print gui/$(id -u)/com.secondbrain.daily-sweep | grep -q "last exit code = 0" && test -s ~/Library/Logs/com.secondbrain.daily-sweep.log && echo FIRE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `launchctl print gui/$(id -u)/com.secondbrain.daily-sweep | grep arguments -A3` shows `/opt/homebrew/opt/node@22/bin/node` (stale bare-`node` gone)
    - `launchctl print gui/$(id -u)/com.secondbrain.daily-sweep | grep "last exit code"` shows `= 0` (NOT 78)
    - `~/Library/Logs/com.secondbrain.daily-sweep.log` is non-empty and contains `[daily-sweep] Sweep complete`
    - SUMMARY notes the same-day fire timestamp
  </acceptance_criteria>
  <done>The rebootstrapped launchd job fired in launchd context, exited 0, and wrote a non-empty log — the scheduler is proven live.</done>
</task>

</tasks>

<verification>
Phase-level checks for this plan:
- `plutil -lint config/com.secondbrain.daily-sweep.plist` → OK
- Active registration runs full node@22 path; kickstart exit code 0; log non-empty

HUMAN-VERIFY (deferred to VERIFICATION.md — NOT a blocking task, per "no overnight block"):
- Confirm tonight's scheduled 23:45 fire ran in launchd context. After plans 02/03/04 ship to the main tree,
  the 23:45 fire should: exit 0, write `state/daily-sweep-last-run.json`, and make `/today` show the sweep line.
  Verify tomorrow morning via `launchctl print ... | grep "last exit code"` and `cat state/daily-sweep-last-run.json`.

Note on Jest counts: this plan adds no tests. If later plans change test totals, the pre-push docs-sync gate
handles README/CLAUDE.md count updates — do NOT hand-edit those counts here.
</verification>

<success_criteria>
- config/com.secondbrain.daily-sweep.plist versioned (fixed content, no LLM_PROVIDER)
- Stale registration replaced; a real kickstart fire exits 0 with a non-empty log
- 23:45 confirming fire recorded as a human-verify item
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.8-phases/33-capture-reliability/33-01-SUMMARY.md`
</output>
