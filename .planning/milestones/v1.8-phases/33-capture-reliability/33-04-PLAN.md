---
phase: 33-capture-reliability
plan: 04
type: execute
wave: 2
depends_on: [33-02]
files_modified:
  - scripts/daily-sweep.js
  - src/today/sweep-status.js
  - src/today-command.js
  - src/today/briefing-renderer.js
  - test/today/sweep-status.test.js
  - test/daily-sweep-last-run.test.js
autonomous: true
requirements: [CAP-EVIDENCE-01]
must_haves:
  truths:
    - "A non-dry-run sweep writes state/daily-sweep-last-run.json (atomic) with ts, staged count, durationMs, degraded"
    - "A --dry-run sweep writes nothing"
    - "/today Compounding shows 'sweep ran HH:MM, staged N' when the last run was <26h ago"
    - "/today shows 'sweep STALE (last ran YYYY-MM-DD)' when >26h, and 'sweep NEVER RAN' when the state file is missing/corrupt — never throwing"
  artifacts:
    - path: "src/today/sweep-status.js"
      provides: "computeSweepLine — reads last-run state, returns the ran/STALE/NEVER RAN line"
      exports: ["computeSweepLine"]
    - path: "scripts/daily-sweep.js"
      provides: "atomic last-run write at sweep end"
      contains: "daily-sweep-last-run.json"
    - path: "state/daily-sweep-last-run.json"
      provides: "fire evidence (gitignored, written by the sweep)"
  key_links:
    - from: "scripts/daily-sweep.js"
      to: "src/utils/classifier-health.js"
      via: "isDegraded() -> degraded flag in last-run.json"
      pattern: "isDegraded"
    - from: "src/today-command.js"
      to: "src/today/sweep-status.js"
      via: "computeSweepLine() passed to renderBriefing as data.sweep"
      pattern: "computeSweepLine"
    - from: "src/today/briefing-renderer.js"
      to: "the ## Compounding section"
      via: "renders the sweep line even when the trend body is null"
      pattern: "## Compounding"
---

<objective>
Persist proof-of-fire and surface it: write `state/daily-sweep-last-run.json` atomically at sweep end, and add a sweep-status line to the `/today` Compounding section — `sweep ran HH:MM, staged N` / `sweep STALE (last ran <date>)` / `sweep NEVER RAN`.

Purpose: A scheduler that fires silently is indistinguishable from one that doesn't. This makes "did the capture engine run last night?" answerable at a glance in the daily briefing, and gives the phase's human-verify step something concrete to read.
Output: the atomic last-run writer, a pure sweep-status line builder, and its wiring into /today's renderer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.8-phases/33-capture-reliability/33-CONTEXT.md
@scripts/daily-sweep.js
@src/today-command.js
@src/today/briefing-renderer.js
@src/utils/voyage-health.js

<interfaces>
- daily-sweep.js main() (lines 211-285) builds `results = { extraction, retry, archive, transcriptSweep, inboxSweep }`.
  - extraction: an array (non-dry-run) → `results.extraction.length` candidates.
  - transcriptSweep: `{ swept, extracted }`. inboxSweep: `{ processed, extracted }`.
  - `dryRun` flag (line 40). Existing state paths use env overrides (e.g. LEDGER_PATH_OVERRIDE, line 45) and
    write to repo-root `state/` (gitignored — .gitignore line 4). Atomic write idiom = voyage-health _writeHealth
    (tmp + renameSync).
- today-command.js (lines 262-308): builds `compoundingBody` then calls `renderBriefing({ ..., compounding: compoundingBody, ... })` (line 296-308). Lazy requires per Pattern 12; all these sections are non-fatal ("briefing-is-the-product").
- briefing-renderer.js renderBriefing (line 274): destructures data (line 275) incl. `compounding`. The Compounding
  block (line 332-334) is:
    `...(compoundingBody !== null ? ['## Compounding', '', compoundingBody, ''] : []),`
  where `compoundingBody = compounding || null` (line 289). The trend body is suppressed under 7 rows — but the
  sweep line must ALWAYS show, so the section must render when EITHER the trend body OR the sweep line is present.
- classifier-health (plan 02) exports `isDegraded()`.
</interfaces>

<locked_decisions>
- Exact line states: `sweep ran HH:MM, staged N` / `sweep STALE (last ran <date>)` / `sweep NEVER RAN`.
- STALE threshold: last run > 26h ago. A CONSTANT (`STALE_MS = 26*60*60*1000`), not config.
- Renders inside the existing Compounding section (Phase 30/31 renderer seam), adjacent to compounding-trend.
- Fail-open: missing/corrupt/unparseable state → `sweep NEVER RAN`; computeSweepLine NEVER throws; /today NEVER
  throws because of it (wrap the call, briefing-is-the-product).
- state/daily-sweep-last-run.json lives in repo `state/` (gitignored, dream-ledger.json precedent), written
  ATOMICALLY at sweep end with ts + staged count + duration + degraded flag. Dry-run writes nothing.
- JSON shape and HH:MM wording are Claude's discretion; keep the three line states verbatim.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write state/daily-sweep-last-run.json atomically at sweep end</name>
  <files>scripts/daily-sweep.js, test/daily-sweep-last-run.test.js</files>
  <read_first>
    - scripts/daily-sweep.js (main() 211-294; the LEDGER_PATH_OVERRIDE env pattern at line 45; the require.main guard at 287)
    - src/utils/classifier-health.js (isDegraded — for the degraded flag)
    - src/utils/voyage-health.js (_writeHealth atomic tmp+rename idiom to mirror)
  </read_first>
  <behavior>
    Add tests (extend test/daily-sweep*.test.js or a new test/daily-sweep-last-run.test.js) using a
    LAST_RUN override env pointed at an mkdtemp file:
    - a non-dry-run main() writes the state file with keys: ts (ISO string), staged (number), durationMs (number), degraded (boolean)
    - staged equals extraction candidates + transcriptSweep.extracted + inboxSweep.extracted for a stubbed results set
    - `--dry-run` (dryRun true) writes NOTHING (assert file absent)
    - the write is atomic (no `.tmp` left behind)
    - degraded reflects classifier-health.isDegraded() (stub/force it true → flag true)
    - a write failure (e.g. unwritable dir) does NOT throw out of main() (fail-open; log to stderr is fine)
  </behavior>
  <action>
    In scripts/daily-sweep.js:
    1. Add `const LAST_RUN_PATH = process.env.DAILY_SWEEP_LAST_RUN_PATH_OVERRIDE || path.join(__dirname, '..', 'state', 'daily-sweep-last-run.json');`
       near the other state-path consts (~line 45).
    2. At the TOP of main() capture `const startedAt = Date.now();`.
    3. At the END of main(), BEFORE `return results`, and ONLY when `!dryRun`, write the state file atomically
       (mirror voyage-health _writeHealth: mkdir recursive, write `${LAST_RUN_PATH}.tmp`, renameSync to LAST_RUN_PATH).
       Payload:
       ```
       const staged =
         (Array.isArray(results.extraction) ? results.extraction.length : 0) +
         ((results.transcriptSweep && results.transcriptSweep.extracted) || 0) +
         ((results.inboxSweep && results.inboxSweep.extracted) || 0);
       let degraded = false;
       try { degraded = require('../src/utils/classifier-health').isDegraded(); } catch (_) { /* fail-open */ }
       const lastRun = { ts: new Date().toISOString(), staged, durationMs: Date.now() - startedAt, degraded };
       ```
       Wrap the write in try/catch that logs to stderr and swallows — a failed evidence write must not fail the sweep
       (its exit code drives the launchd observation).
    No console.log in src/; scripts/ already uses console.error for logging — match that.
  </action>
  <verify>
    <automated>npx jest test/daily-sweep-last-run.test.js --silent 2>&1 | tail -12 || npx jest -t "last-run" --silent 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "daily-sweep-last-run.json" scripts/daily-sweep.js` and `grep -q "renameSync" scripts/daily-sweep.js`
    - `grep -q "DAILY_SWEEP_LAST_RUN_PATH_OVERRIDE" scripts/daily-sweep.js` (test isolation hook)
    - `grep -q "classifier-health" scripts/daily-sweep.js` (degraded flag sourced from the tracker)
    - a non-dry-run run writes JSON containing keys `ts`,`staged`,`durationMs`,`degraded`; `--dry-run` writes nothing
    - the write is atomic (no leftover `.tmp`) and a write error does not throw out of main()
    - test suite for the last-run write passes
  </acceptance_criteria>
  <done>Every real sweep drops an atomic state/daily-sweep-last-run.json with ts/staged/durationMs/degraded; dry-run stays side-effect-free; a write failure never breaks the sweep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: computeSweepLine + render it inside /today's Compounding section</name>
  <files>src/today/sweep-status.js, src/today-command.js, src/today/briefing-renderer.js, test/today/sweep-status.test.js</files>
  <read_first>
    - src/today/briefing-renderer.js (lines 274-335 — renderBriefing destructure + Compounding block)
    - src/today-command.js (lines 262-308 — compounding build + renderBriefing call)
    - src/today/compounding-trend.js (adjacent module; sweep-status sits beside it)
  </read_first>
  <behavior>
    Write test/today/sweep-status.test.js FIRST, using DAILY_SWEEP_LAST_RUN_PATH_OVERRIDE to point at temp fixtures:
    - state ts 5 minutes ago, staged 7 → returns `sweep ran HH:MM, staged 7` (HH:MM matches the ts's local time)
    - state ts 30h ago → returns `sweep STALE (last ran YYYY-MM-DD)` where the date is the ts's date
    - state file missing → returns `sweep NEVER RAN`
    - state file corrupt/garbage → returns `sweep NEVER RAN` (fail-open, no throw)
    - state ts exactly <26h boundary stays "ran"; >26h flips to STALE (exercise the constant)
    Also add a briefing-renderer assertion: with `compounding:null, sweep:'sweep NEVER RAN'`, output contains a
    `## Compounding` heading AND the line `sweep NEVER RAN`; with a compounding body AND a sweep line, both appear.
  </behavior>
  <action>
    1. Create src/today/sweep-status.js exporting `computeSweepLine(now = new Date())`:
       - Resolve the state path: `process.env.DAILY_SWEEP_LAST_RUN_PATH_OVERRIDE || path.join(__dirname, '..', '..', 'state', 'daily-sweep-last-run.json')`
         (src/today/ → ../../state; same file plan 04 task 1 writes).
       - `const STALE_MS = 26 * 60 * 60 * 1000;`
       - Read+parse in try/catch; on any error or missing `ts` → return `'sweep NEVER RAN'`.
       - `const age = now.getTime() - new Date(state.ts).getTime();` if NaN → `'sweep NEVER RAN'`.
       - if `age > STALE_MS` → `` `sweep STALE (last ran ${String(state.ts).slice(0,10)})` ``.
       - else → `` `sweep ran ${new Date(state.ts).toTimeString().slice(0,5)}, staged ${Number.isFinite(state.staged) ? state.staged : 0}` ``.
       - NEVER throws. No console.log.
    2. Wire into src/today-command.js near the compounding block (~line 278): 
       ```
       let sweepLine = 'sweep NEVER RAN';
       try { sweepLine = require('./today/sweep-status').computeSweepLine(); } catch (_) { /* fail-open, briefing-is-the-product */ }
       ```
       and add `sweep: sweepLine,` to the renderBriefing({...}) call (beside `compounding: compoundingBody,`).
    3. In src/today/briefing-renderer.js: add `sweep` to the renderBriefing destructure (line 275). Replace the
       Compounding block (line 332-334) so the section renders when EITHER body or sweep is present:
       ```
       ...((compoundingBody !== null || sweep) ? [
         '## Compounding',
         '',
         ...(compoundingBody !== null ? [compoundingBody, ''] : []),
         ...(sweep ? [sweep, ''] : []),
       ] : []),
       ```
       (Because computeSweepLine always returns a non-empty string, the Compounding section now always renders with
       at least the sweep line — intended: capture status should be visible every day.)
    ponytail: HH:MM uses machine-local time via toTimeString(); no timezone lib — add tz alignment only if a test demands it.
  </action>
  <verify>
    <automated>npx jest test/today/sweep-status.test.js test/today/briefing-renderer.test.js --silent 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/today/sweep-status.js`; `node -e "const {computeSweepLine}=require('./src/today/sweep-status'); if(typeof computeSweepLine!=='function')throw 0; console.log(computeSweepLine())"` prints one of the three states (NEVER RAN in a clean checkout) and does not throw
    - `grep -q "sweep NEVER RAN" src/today/sweep-status.js`, `grep -q "sweep STALE" src/today/sweep-status.js`, `grep -q "sweep ran" src/today/sweep-status.js`
    - `grep -q "26 \* 60 \* 60 \* 1000" src/today/sweep-status.js` (26h constant)
    - `grep -q "computeSweepLine" src/today-command.js` and `grep -q "sweep:" src/today-command.js`
    - `grep -q "sweep" src/today/briefing-renderer.js` and the Compounding block renders on `compounding:null` when `sweep` is set (proven by the renderer test)
    - `npx jest test/today/sweep-status.test.js` passes all five line-state cases; briefing-renderer test green
    - `npx eslint src/today/sweep-status.js src/today-command.js src/today/briefing-renderer.js` reports 0 errors
  </acceptance_criteria>
  <done>computeSweepLine returns the correct ran/STALE/NEVER-RAN line (fail-open), and /today renders it inside a ## Compounding section that appears even when the trend body is suppressed.</done>
</task>

</tasks>

<verification>
- `npx jest test/today/sweep-status.test.js test/today/briefing-renderer.test.js` green (all line states + render-with-null-trend)
- last-run write suite green; `--dry-run` writes nothing
- `npx eslint` clean on all touched src files
- Manual smoke (optional): `node scripts/daily-sweep.js` then `cat state/daily-sweep-last-run.json`; a dry `/today` shows the sweep line

Note on Jest counts: this plan ADDS test files; totals rise. Do NOT hand-edit README.md / CLAUDE.md counts —
the pre-push docs-sync gate reconciles them.
</verification>

<success_criteria>
- state/daily-sweep-last-run.json written atomically at real sweep end (ts/staged/durationMs/degraded); dry-run clean
- /today Compounding shows the correct sweep line for all three states; fail-open to NEVER RAN; never throws
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.8-phases/33-capture-reliability/33-04-SUMMARY.md`
</output>
