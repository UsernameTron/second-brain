---
phase: 33-capture-reliability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config/pipeline.json
  - config/schema/pipeline.schema.json
  - src/utils/classifier-health.js
  - test/utils/classifier-health.test.js
autonomous: true
requirements: [CAP-CLASSIFIER-01]
must_haves:
  truths:
    - "classifier-health records HTTP/parse/timeout failures and flips to degraded after 3 consecutive"
    - "recordSuccess resets the failure counter and closes the degraded window"
    - "A per-night Haiku call counter increments and is comparable against a cap; it resets on date rollover"
    - "config/pipeline.json carries classifier.haikuNightlyCap (default 50), schema-validated"
  artifacts:
    - path: "src/utils/classifier-health.js"
      provides: "Cross-invocation classifier health tracker + per-night Haiku cap"
      exports: ["recordFailure", "recordSuccess", "isDegraded", "getDegradedReason", "recordHaikuCall", "isHaikuCapReached"]
    - path: "config/pipeline.json"
      provides: "haikuNightlyCap config key"
      contains: "haikuNightlyCap"
    - path: "config/schema/pipeline.schema.json"
      provides: "haikuNightlyCap schema constraint"
      contains: "haikuNightlyCap"
  key_links:
    - from: "src/utils/classifier-health.js"
      to: "~/.cache/second-brain/classifier-health.json"
      via: "atomic tmp+rename persistence, CACHE_DIR_OVERRIDE honored"
      pattern: "classifier-health.json"
---

<objective>
Create `src/utils/classifier-health.js` — a voyage-health clone that tracks local-classifier failures (HTTP/parse/timeout), flips to degraded mode after 3 consecutive, and counts per-night Haiku fallback calls against a configurable cap. Add the `classifier.haikuNightlyCap` config key (+ AJV schema).

Purpose: This is the state layer for the sweep's resilience story. Today's live lesson (260721-ljn): an unpinned local model wedged loaded-but-stuck makes every classify call burn the 60s timeout. The tracker must count those timeouts so repeated wedges flip to degraded FAST, and cap the Haiku fallback so an unattended night can't rack up unbounded API calls.
Output: the health module, its config key + schema, and a unit suite mirroring the voyage-health testing style.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.8-phases/33-capture-reliability/33-CONTEXT.md
@src/utils/voyage-health.js
@config/pipeline.json
@config/schema/pipeline.schema.json

<interfaces>
Clone this API surface from src/utils/voyage-health.js (mirror the persistence + atomic-write shape exactly):

```javascript
// voyage-health.js — the template
function getSemanticCacheDir()  // CACHE_DIR_OVERRIDE || ~/.cache/second-brain
function getHealthPath()        // getSemanticCacheDir() + '/voyage-health.json'
function readHealth()           // ENOENT/parse -> DEFAULT_HEALTH (never throws)
function _writeHealth(state)    // mkdir 0700, tmp + renameSync, mode 0600 (atomic)
function recordFailure(code, degradedMinutes = 15)  // ++consecutive; at >=3 sets degraded_until
function recordSuccess()        // consecutive = 0; degraded_until = null
function isDegraded()           // degraded_until in the future?
function getDegradedReason()    // human string for banners
const DEGRADED_FAILURE_THRESHOLD = 3
```

classifier-health MUST live in its OWN file at ~/.cache/second-brain/classifier-health.json (NOT reuse
voyage-health.json). Import getSemanticCacheDir from voyage-health OR re-implement the same
CACHE_DIR_OVERRIDE-honoring resolver — your discretion; do not diverge from the atomic-write idiom.
</interfaces>

<locked_decisions>
- Failure codes counted: 'http' (non-2xx / shape error), 'parse' (JSON parse failure), 'timeout'
  (PR #83's 60s local timeout / AbortError counts as a failure).
- Same recovery/backoff semantics as voyage-health: 3 consecutive failures → degraded window (default 15 min);
  recordSuccess resets.
- Per-night Haiku cap: default 50, config key `classifier.haikuNightlyCap` in config/pipeline.json (+ schema).
- JSON shape of the state file, and how the Haiku counter is keyed by night, are Claude's discretion —
  keep it in classifier-health.json, keep the atomic-write idiom.
</locked_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add classifier.haikuNightlyCap config key + AJV schema constraint</name>
  <files>config/pipeline.json, config/schema/pipeline.schema.json</files>
  <read_first>
    - config/pipeline.json (the `classifier` block, lines 2-13)
    - config/schema/pipeline.schema.json (the `classifier` block, lines 18-84 — note `additionalProperties: false`, so a new key MUST be added to `properties` or validation fails)
  </read_first>
  <action>
    1. config/pipeline.json — add `"haikuNightlyCap": 50` inside the existing `classifier` object (alongside
       stage1ConfidenceThreshold etc.). Keep it a sibling of `llm`, not inside it.
    2. config/schema/pipeline.schema.json — inside `properties.classifier.properties`, add:
       `"haikuNightlyCap": { "type": "integer", "minimum": 1, "maximum": 1000 }`
       Do NOT add it to `classifier.required` (keep it optional so existing .local.json overlays / test
       fixtures without it still validate; the code defaults to 50 when absent).
    3. Confirm the merged config still validates (validate:true path):
       `node -e "require('./src/pipeline-infra').loadPipelineConfig(); console.log('CONFIG_OK')"`
    Because `additionalProperties:false` is set on `classifier`, forgetting step 2 makes loadPipelineConfig
    throw — that is the built-in tripwire.
  </action>
  <verify>
    <automated>node -e "const c=require('./src/pipeline-infra').loadPipelineConfig(); if(c.classifier.haikuNightlyCap!==50) throw new Error('cap not 50'); console.log('CONFIG_OK')" && grep -q "haikuNightlyCap" config/schema/pipeline.schema.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep haikuNightlyCap config/pipeline.json` shows the key with value 50
    - `grep -A1 '"haikuNightlyCap"' config/schema/pipeline.schema.json` shows `"type": "integer"` with `minimum`/`maximum`
    - `haikuNightlyCap` is NOT listed in the schema's `classifier.required` array
    - `node -e "require('./src/pipeline-infra').loadPipelineConfig()"` exits 0 (schema validates after overlay merge)
  </acceptance_criteria>
  <done>The cap key exists in pipeline.json (=50) and is schema-constrained as an optional integer; loadPipelineConfig validates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/utils/classifier-health.js (voyage-health clone + per-night Haiku cap)</name>
  <files>src/utils/classifier-health.js, test/utils/classifier-health.test.js</files>
  <read_first>
    - src/utils/voyage-health.js (clone the entire structure: cache-dir resolver, readHealth defaults, atomic _writeHealth, recordFailure/recordSuccess/isDegraded/getDegradedReason, DEGRADED_FAILURE_THRESHOLD)
    - .planning/milestones/v1.8-phases/33-capture-reliability/33-CONTEXT.md (classifier-health.js decision block)
  </read_first>
  <behavior>
    Write these tests FIRST (test/utils/classifier-health.test.js), all isolated via `process.env.CACHE_DIR_OVERRIDE`
    set to an mkdtemp dir in beforeEach and cleaned in afterEach (mirror how tests isolate the cache; this module
    is pure fs, hits no live endpoint, so NO CI/UAT guard is needed):
    - readHealth() on a missing file returns defaults (consecutive_failures 0, degraded_until null) and does not throw
    - recordFailure('timeout') once → isDegraded() is false; recordFailure ×3 (any of 'http'/'parse'/'timeout')
      → isDegraded() is true and getDegradedReason() mentions the last code
    - recordSuccess() after failures → consecutive_failures back to 0 and isDegraded() false
    - a corrupt/garbage classifier-health.json → readHealth() returns defaults (fail-open, no throw)
    - recordHaikuCall() increments a per-night counter; after N calls, isHaikuCapReached(N) is true and
      isHaikuCapReached(N+1) is false
    - Haiku counter is keyed by calendar date: simulate a date rollover (inject "now" or the date key) and
      assert the count resets to 0 for the new day (so last night's fallbacks don't cap tonight)
    - the state file is written atomically (assert no leftover `.tmp` file after a write)
  </behavior>
  <action>
    Create src/utils/classifier-health.js cloning voyage-health.js, persisting to
    `getSemanticCacheDir() + '/classifier-health.json'` (honor CACHE_DIR_OVERRIDE; reuse getSemanticCacheDir
    from voyage-health or re-implement identically). Keep the atomic tmp+rename, mode 0600 / dir 0700 idiom.

    Export at minimum:
    - `recordFailure(code)` — code ∈ {'http','parse','timeout'}; ++consecutive_failures; at >=3 set degraded_until = now + 15 min
    - `recordSuccess()` — reset consecutive_failures to 0, degraded_until to null
    - `isDegraded()` — degraded_until in the future
    - `getDegradedReason()` — short human string incl. last failure code (banner/log use)
    - `recordHaikuCall()` — increment today's Haiku fallback count (keyed by local YYYY-MM-DD; reset when the date differs from the stored one)
    - `isHaikuCapReached(cap)` — today's Haiku count >= cap
    - `DEGRADED_FAILURE_THRESHOLD` (= 3), and getHealthPath/getSemanticCacheDir for test parity

    Store both concerns in one classifier-health.json, e.g.
    `{ consecutive_failures, last_failure, last_failure_code, degraded_until, haiku_date, haiku_calls }`
    (exact shape is your discretion). Every read is fail-open (ENOENT/parse → defaults, never throw) — the sweep
    must never crash because a cache file is missing or garbage.

    No `console.log` in the module (ESLint no-console gate). This module does NOT classify or call any LLM — it
    is pure state; the classify wiring is plan 03.
  </action>
  <verify>
    <automated>npx jest test/utils/classifier-health.test.js --silent 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/utils/classifier-health.js` succeeds
    - `node -e "const h=require('./src/utils/classifier-health'); ['recordFailure','recordSuccess','isDegraded','getDegradedReason','recordHaikuCall','isHaikuCapReached'].forEach(f=>{if(typeof h[f]!=='function')throw new Error('missing '+f)}); console.log('EXPORTS_OK')"` prints EXPORTS_OK
    - `grep -q "classifier-health.json" src/utils/classifier-health.js` and `grep -q "CACHE_DIR_OVERRIDE" src/utils/classifier-health.js` (or imports getSemanticCacheDir which provides it)
    - `grep -q "renameSync" src/utils/classifier-health.js` (atomic write preserved)
    - `npx jest test/utils/classifier-health.test.js` passes (all behavior cases green)
    - `npx eslint src/utils/classifier-health.js` reports 0 errors
  </acceptance_criteria>
  <done>classifier-health.js mirrors voyage-health, adds a date-keyed Haiku cap counter, fails open on all reads, and its unit suite is green.</done>
</task>

</tasks>

<verification>
- `node -e "require('./src/pipeline-infra').loadPipelineConfig()"` validates (cap key schema-legal)
- `npx jest test/utils/classifier-health.test.js` green; `npx eslint src/utils/classifier-health.js` clean
- Module hits no live endpoint (pure fs) → intentionally NOT CI-guarded

Note on Jest counts: this plan ADDS a test file, so total test counts will rise. Do NOT hand-edit the
test-count/coverage numbers in README.md / CLAUDE.md — the pre-push docs-sync gate reconciles them.
</verification>

<success_criteria>
- classifier.haikuNightlyCap (=50) present + schema-valid
- src/utils/classifier-health.js exports the full API, persists atomically to ~/.cache/second-brain/classifier-health.json, fails open, and the Haiku counter resets per night
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.8-phases/33-capture-reliability/33-02-SUMMARY.md`
</output>
